---
name: project-t3-4-reliability-gates-scope
description: T3.4 reliability-gates scope + Miguel's 4 fork resolutions (2026-07-09) = NFR-4/5/13; integrity-only, feature-gated+out-of-process injection, full NFR-13 field-set, allow_external reason-gate seam (policy-bypass DB parity stays v1.1)
type: reference
---

T3.4 = **"Reliability gates NFR-4/NFR-5/NFR-13"** (impl-plan:90; STATUS one-liner said NFR-4/5 only — drift, reconcile UP to impl-plan). Depends on T3.1 (done). Started 2026-07-09.

**Already built (do NOT rebuild):** atomic write pipeline `unblock-sync/src/atomic.rs` (temp+flush+sync_all+rename+parent-dir fsync, T2.4); SIGTERM-mid-write NFR-5 half (T3.2/PR#395); `RELIABILITY_TARGET="unblock.reliability"` const (engine logging.rs:13); partial interleaving (`linearizable.rs`, `shutdown_drain_barrier.rs`).

**Out of scope (v1.1, confirmed):** `ReliabilityAuditRecord`/`audit.rs` (unblock-health lib.rs:33 commented v1.1); full Healthy/Drifted/Recoverable/Unsafe taxonomy; the full policy-gate + `close_policy.rs` bypass machinery (`unblock-policy/src/gates.rs` marked v1.1: `PolicyDocument{allow_bypass}`, `ClosePolicy{require_close_reason}`, `GateOutcome{bypassed}`).

**Miguel's 4 fork resolutions (2026-07-09), to be minted as a D-id (likely D30) with the D-range bump checklist (CLAUDE.md, ci-cd §2.1, doc_lint.rs):**
1. **NFR-13 scope = STANDARDIZE ALL sites.** Build a structured reliability-event helper carrying `operation/path/result/reason` and emit at EVERY guard site: external-path use, conflict-marker rejection, and force-override. Fixes the doc-vs-code drift (engine logging.rs:3-4 promises INFO events the code lacks; conflict.rs has 0 tracing). Add `tracing-test` dev-dep + the named test `tracing_capture_reliability` (in unblock-sync, where emissions live). NOT the minimal single-site upgrade.
2. **Emergency-override written reason = OPTION 1 (v1 seam).** Reason-gate `allow_external` in sync `ExportOptions`/`ImportOptions` (override without a reason = error at the sync boundary) + emit the NFR-13 force-override/external-path INFO with the reason. NOTE: `allow_external` is UNREACHABLE in v1 (engine forces false; MCP `sync` doesn't expose it; CLI has no export/import cmd) → this is a FORWARD SEAM, no MCP `CONTRACT_VERSION` bump. The beads "written reason" is the POLICY bypass (`--bypass-policy`/`--bypass-reason` persisted to DB) = the v1.1 `gates.rs` surface — stays v1.1 (NOT dropped, already stipulated). `close_reason` DB column already exists (T1.2).
3. **CI rate-limit/stress partition = T3.4 owns ONLY the NFR-5 stress/interleaving half, INTEGRITY-only** (no corruption / no partial-write / linearizable) at a MODEST corpus. NFR-18 single-serve rate-limit assertions + NFR-1/NFR-2 perf bench-gate/scale + 250k corpus → **T3.5**. (ci-cd:44/123 defer the shared `rate-limit` job to T3.4/T3.5.)
4. **Fault-injection mechanism = feature-gated seam + out-of-process.** A `testkit`-style feature-gated fault-injection seam in `atomic.rs` (deterministic per-stage: write/flush/sync_all/rename/parent-dir fsync) for export AND import, PLUS an out-of-process test (SIGKILL / ENOSPC on small tmpfs) for the real "killed export" case. The 3 existing `atomic_failure.rs` tests only reject at PREFLIGHT (before any temp) — insufficient for NFR-4.

**The 4 NFR-5 gates T3.4 must prove:** failure-replay (corrupted-workspace fixtures asserting the LITE doctor posture — `classify_file_state` + `integrity_check`, NOT the v1.1 audit posture); export/import failure-injection (= the NFR-4 mechanism, both directions); long-lived single-workspace stress (integrity throughout); interleaved concurrent command-family integrity (create/update/close vs export/import vs read — `linearizable.rs` only covers mutation interleaving today).

**Spec-first drifts to reconcile regardless:** STATUS:86 → NFR-4/5/13; beads field vocab (source/phase/outcome on br::reliability) is v1.1 classification, NOT the v1 PRD field set (operation/path/result/reason on unblock.reliability) — PRD wins; engine logging.rs:3-4 doc-vs-code; spine §569 "L7 boundary" vs emissions actually in L3 sync.

Lifecycle: Understand ✅ → Decide ✅ → Spec/Plan ✅ (mints D30) → design Review ✅ (PASS_WITH_CHANGES: B1 = NFR-13 "standardize ALL" gap — 2 omitted atomic.rs emitters + a required WARN arm — + 9 should-fixes folded) → Implement ✅ → Verify ✅ (PASS_WITH_CHANGES: mutation 6/8 CAUGHT; 2 survivors = reason-gate predicate + SSOT DEBUG arm → folded mutation-proven; + bd-path operation=import_bd fix) → Track ✅. **DONE + MERGED: [PR #399](https://github.com/websublime/unblock/pull/399) rebase-merged into main at `669c2e2`; STATUS T3.4 flipped ☑ via `docs(status): mark T3.4 done` (main now `5b47228`). Both gates PASSED, 11/11 CI-equiv green (1136 workspace + 91 sync fault-injection). M3 remaining: T3.5 (perf NFR-1/2 + NFR-18 rate-limit + 250k), T3.6 (release/dist), T3.7 (README).** Cleanup learning: a rebase-merge makes `git branch -d <feature>` fail (needs `-D`), and isolated-worktree agents leave `worktree-*` branches that accumulate — force-delete them after the task.

As-built (all faithful to D30): SSOT macro trio `reliability_guard!`/`reliability_warn!`/`reliability_detail!` in `unblock-sync/src/reliability.rs`; `RELIABILITY_TARGET` hoisted to L0 `unblock-error` (engine re-exports, sync imports); reason-gate `SyncError::ExternalOverrideWithoutReason`→`ErrorCode::PathTraversal` (no new code, forward-unreachable, no contract bump); fault-injection `#[cfg(feature="fault-injection")]` `AtomicU8` seam EXPORT-side; tests failure_replay.rs/atomic_failure.rs (+ the export_kill Cargo bin-target)/stress_longlived.rs/interleaved_families.rs/reliability_tracing.rs. **tracing-capture REJECTED** on transitive budget (duplicates `predicates`) → dep-free hand-rolled `tracing_subscriber::Layer`+`field::Visit` fallback. Note the buildability Verify lens worktree failed to create (corrupted output) — cross-covered by the other 3 lenses + the Track re-probe. See [[project-unblock-rust-rewrite]], [[project-t3-3-health-lite-scope]].
