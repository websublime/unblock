---
name: project-t3-5-perf-budgets-scope
description: T3.5 perf budgets (NFR-1/2) scope + Miguel's 4 fork resolutions (2026-07-13) — hybrid perf-gate, storage-direct 250k corpus, MINT RateLimited (contract v1.3→v1.4 bump), round-trip record-only
type: reference
---

T3.5 = **Performance budgets NFR-1/NFR-2** (M3 GA gate), ready (deps T3.1 + T3.4.2 ☑), on `main @ 37afaad` (post serve→mcp rename + `.write.lock` D31 — no rebase needed). **Five deliverables:** (1) NFR-1 bench-gate `criterion` baselines on storage/engine (NO storage/engine benches exist yet — only model/policy/error/render, the async_tokio criterion feature is declared-but-unused); (2) NFR-2 250k scale corpus in CI; (3) NFR-18 rate-limit half — enforce the **landed-but-DEAD** `Quotas.max_concurrent_requests` (default 64, `unblock-mcp/src/options.rs:66`; `tools/mod.rs:81` wrongly punts it as "an rmcp-transport concern"); (4) PRD §14 round-trip latency record; (5) CI wiring (bench-gate/scale/rate-limit DEFERRED→live in `ci.yml`).

**Miguel's 4 fork resolutions (2026-07-13 Decide phase):**
- **F-1 perf-gate SHAPE = HYBRID**: hard per-PR gate = generous absolute-ms budgets on a pinned ≥2-vCPU runner (storage-testkit precedent); relative-10% delta = advisory/nightly report-only. Mirrors T0.8's core-independent-hard-assert + diagnostic split (absolute-ms alone flakes with core count). Reword NFR-1 + §14.1 spec-first + re-baseline the numbers on libsql.
- **F-2 250k corpus = storage-direct validated non-minted, chunked ~1k/tx** — EXPLICITLY sanctioned (hard-rule "never simplify") vs the O(N²) engine mint (`ids.rs` calls `issue_count()` per create; separately tested → NOT a simplification). Assert NFR-1 READ budgets + `integrity_check` clean at 250k over a FILE-DB with live `.write.lock`. Job per-PR w/ timeout + `#[ignore]` soak variant. K-process contention-at-250k is OUT (that's NFR-3, proven by T0.8 + T3.4.1).
- **F-3 rate-limit over-limit error = MINT a new `RateLimited`/`TooManyRequests` ErrorCode** (Miguel chose the honest option over reusing DatabaseLocked) → **CONTRACT_HASH + CONTRACT_VERSION bump v1.3→v1.4**, re-bless the 2 discovery goldens + `exit_code_table.rs`, ErrorCode 35→36 variants, retryable. Heavier/cross-cutting at GA but faithful.
- **F-4 round-trip latency = RECORD-ONLY** (impl-plan says "record"): measure p50/p99 over a real spawned `unblock mcp`, publish v1 baseline, hard target → v1.1. Reconcile §14.1 "TBD on M2".

**Orchestrator defaults (non-Miguel forks, proceed unless objected):** F-5 mechanism = `Arc<Semaphore>` chokepoint at the MCP handler (covers tools+resources+future 8th tool), `try_acquire`→reject in-band; F-6 = ONE **D34** (rate-limit contract-bump as an explicit clause within it); F-7 = wire the EXISTING policy/render benches into the same gate mechanism (don't demote to informational).

**Key drifts to reconcile spec-first:** NFR-2 "single-MCP-server topology" wording DOWN to D31 child-per-client (PRD:180 / ci-cd:42 / roadmap:58 — the `roadmap:173` note already carries D31); `tools/mod.rs:81` + `unblock-mcp.md:43` comment correction (field is unenforced); NFR-1 `create<1ms` applies to the storage insert path, NOT the O(N) engine mint; name `crates/{unblock-storage,unblock-engine}/tests/scale.rs`; import bench gap (NFR-1 lists import 10k<1s but engine bench plan omits it).

**Spec/Plan DONE (2026-07-13, Workflow `wf_21a4e7a2-5c2`, 4/4 ok):** the SPEC-MANIFEST.md (44-edit D34 cascade) is at `<session-scratchpad>/t3.5-spec/SPEC-MANIFEST.md`. Key resolutions the manifest made: **`ErrorCode::RateLimited`** (serialized `RATE_LIMITED`), **exit 2** (FORCED — the fixed 0-8 exit taxonomy + the `{1..8}` coverage golden forbid a 9th bucket; DatabaseLocked's retryable-busy sibling), retryable=true, hint_shape=None; **4 goldens re-bless** (NOT 3 — the coordinator caught a D33 ripple: `init_agents__agents_managed_block.snap` renders `capabilities().error_codes`): `contract_suite__capabilities.snap` + `contract_suite__schema_bundle.snap` (bytes VERIFIED to move) + `exit_code_table__golden_exit_code_table.snap` + `init_agents__agents_managed_block.snap`; `CONTRACT_HASH` recompute-at-Implement; D34 row appends after `PRD:116` (D30 is the physical tail, NOT sorted).

**Miguel's 2 Spec-phase OQ resolutions (2026-07-13):** **OQ-1 → (A)** NFR-1 hard per-PR ceiling = a GENEROUS ~5× multiple over the current numbers, PROVISIONAL + Implement-calibrated on the pinned ≥2-vCPU runner (current PRD numbers stay as the reference/advisory baseline; T0.8 PROVISIONAL_RATIO_CEILING precedent); relative-10% delta = advisory/nightly. **OQ-3 → (b)** the D34 spec-first cascade lands as its OWN standalone PR (**PR-0**), merged BEFORE the code PRs — purest spec-first (PROCESS §2). Note: a v1.4-prose-vs-v1.3-code window persists PR-0→P2 regardless (harmless pre-1.0). Routed to design Review (not Miguel): OQ-2 (ratify exit-2/None/name), OQ-4 (`unblock-sync.md:69` reword vs grep-scope), OQ-5 (re-verify rmcp `call_tool` macro-suppression at P2 Implement).

**3-PR structure (was 2):** **PR-0** `t3.5-spec-d34` = the full 44-edit D34 cascade (all v1.4 prose), design-Reviewed + merged first. **P1** `t3.5-perf-scale` (off main after PR-0) = benches storage.rs/engine.rs + shared `testkit::seed_corpus` + tests/scale.rs + gate-enforcement + F-7 policy/render wiring + round-trip instrument + CI bench-gate/scale jobs; non-contract. **P2** `t3.5-rate-limit` (off main after P1) = the RateLimited variant + v1.4 CONTRACT_VERSION/HASH + 4-golden re-bless + Arc<Semaphore> chokepoint + tests/rate_limit.rs + CI rate-limit job; contract-touching → Verify adds `/security-review`.

**Lifecycle position (2026-07-13):** Understand ✅ → Decide ✅ (4 forks) → Spec/Plan ✅ (manifest + 2 OQ) → design Review ✅ **PASS_WITH_CHANGES** (`wf_8f5ce23e`, 7 must-fixes folded into `MANIFEST-AMENDMENTS.md`: grep-KEEP enumeration; OQ-1(A) two-tier NFR-1; OQ-3(b) 3-PR; round-trip relocated to unblock-cli per NFR-15; tool-in-band/resource-ErrorData qualification (MF-5); impl-plan:79 35→36 (MF-6); ci.yml out of PR-0 (MF-7); OQ-2 ratified, OQ-4 reword sync.md:69) → **PR-0 applied + OPEN = [PR #408](https://github.com/websublime/unblock/pull/408)** (branch `t3.5-spec-d34` @ `fe91ddd`, 16 files doc-only, doc-lint 19/6 clean, greps satisfy KEEP sets; the D34-row in-band phrase also MF-5-qualified in the main session).

**PR-0 (#408) MERGED** → main `98734c9` (synced + cleaned). **P1 DONE → [PR #409](https://github.com/websublime/unblock/pull/409) OPEN, awaiting Miguel merge.** Branch `t3.5-perf-scale`, 9 commits, NON-CONTRACT. Built: `unblock-storage/benches/storage.rs` + `unblock-engine/benches/engine.rs` + `testkit::seed_corpus` (chunked 250k) + `{storage,engine}/tests/scale.rs` + `xtask bench-gate` (reads criterion estimates.json → fails on ceiling breach; **non-vacuous** — proven on empty dir + injected 2000ms slow path) + F-7 policy/render wiring + `unblock-cli/tests/mcp_round_trip.rs` (record-only p50≈10.3ms/p99≈13.3ms, in cli L7 not engine) + CI bench-gate/scale jobs. Verify ≥3 = **PASS_WITH_CHANGES** (`wf_688b48b3`); folded MF-2 (engine-testkit clippy leg), MF-1 (advisory leg deferred to T3.5.1 + false-claim fix), SF-A (blind-window explicit), SF-B (stale-criterion purge). Orchestrator CI-equiv probe fully green (fmt/clippy×3/test-workspace/testkit/layering/doc-lint/scale-250k×2/benches+bench-gate+non-vacuity).

**⭐ N+1 FINDING (Miguel 2026-07-14 = fix PRE-GA):** the perf gate exposed `query.rs::collect_hydrated` = 1 id-query + N×get_issue(3q) → list/ready ~3-6× the NFR-1 tier-i budget (ready 1k 29ms/10k 284ms). Miguel chose Option 1 (fix before GA, dedicated task, NOT v1.4). **Minted T3.5.1** (STATUS + impl-plan): batch-fetch relations per id-set (WHERE issue_id IN(...)) → ~4 queries; re-tighten the tier-ii ceilings (widened at P1 to current cost) toward tier-i; **ALSO lands the deferred NFR-1 tier-i advisory relative-10% leg** (Miguel deferred it here so its committed baseline reflects the FIXED read cost). T3.5.1 depends on P1, must land before T3.6.

**P1 CI FLAKE (2026-07-14, resolved):** #409's first CI failed on a PRE-EXISTING T3.2 flake `c2_mid_write_sigterm_never_leaves_a_partial_batch` (`shutdown_failure_injection.rs`) — asserted exit==143 exactly, but a mid-write SIGTERM against an UNREAD stdout pipe legitimately exits 1 (serve loop errors on the blocked write during cancel); passed 3/3 local (macOS) + all prior CI, P1 only aggravated it via CI load (its `mcp_round_trip` test also spawns `unblock mcp` in the same workspace job). Miguel chose: relax the C2 exit assertion to {143,1} keeping the atomicity invariant strict (commit `7048fb7`) + isolate the round-trip (`#[ignore]` it out of the workspace job + a dedicated bench-gate `--ignored --nocapture` leg so the p50/p99 baseline is CI-visible, fixing a Verify NIT). Re-pushed; CI re-running. (Pattern: [[feedback-macos-probe-masks-linux-ci-path-confinement]] — macOS local masks Linux-CI timing/signal flakes.)

**P2 DONE → [PR #410](https://github.com/websublime/unblock/pull/410) OPEN, awaiting Miguel merge.** Branch `t3.5-rate-limit`, 8 commits, off main `6e5b72b`. Minted `ErrorCode::RateLimited` (RATE_LIMITED, exit 2, retryable, hint None, END of exit-2 group after AlreadyInitialized; ALL 35→36) → contract v1.3→v1.4, CONTRACT_HASH recomputed `eca90bb7…` (from the failing hash test), 4 goldens re-blessed (capabilities/schema_bundle/exit_code_table/init_agents[SF-5]); Arc<Semaphore> chokepoint (hand-written call_tool suppresses the rmcp macro — verified tool_handler.rs:44; in-band CallToolResult for tools, out-of-band ErrorData for resources per MF-5; permit RAII-dropped on all paths; deadlock-free above engine Semaphore(1)); tests/rate_limit.rs (non-vacuous N/N+1 + SF-6 assumption-pin + the SF-1 read_resource out-of-band test); CI rate-limit job. **Verify ≥3 + security lens = PASS_WITH_CHANGES, 0 must-fix, SECURITY SOUND** (no bypass/permit-leak/blast-radius escape; `wf_2a9a867f`). 3 SFs + 1 NIT folded (commit `cb6327f`): SF-1 read_resource coverage, SF-2 doc comment, SF-3 clamp Semaphore ≤MAX_PERMITS (0 preserved for the SF-6 pin), NIT timeout on the barrier. Orchestrator CI-equiv probe fully green (incl. insta --check). `git grep v1.3` = only options.rs History comment + frozen-history prose. CI on #410 being watched.

**T3.5.1 (2026-07-14, in progress):** PR-0+P1+P2 all MERGED → main `b6eb5c8`. **T3.5.1 design done** (`wf_632060b9`, PASS_WITH_CHANGES, no Miguel product fork; DESIGN-BRIEF at `<session-scratchpad>/DESIGN-BRIEF.md`). Design: private `hydrate_ids(conn, &ids)` = 4 batch queries (id-query + `WHERE id/issue_id IN (?1..?K)` for issues/labels/deps) CHUNKED at `HYDRATE_ID_CHUNK=900` (SQLite param floor 999; scale.rs pages at 1000); reconstruct by mapping over the ORDERED id vec (never IN-row order), reuse issue_from_row/dependency_from_row verbatim, filter_map skip-absent + unwrap_or_default empty, early-return empty set. **5 callers** (list/search/**stale** via collect_hydrated + ready + blocked — the plan's "4" undercounted). **R1 CRITICAL:** the AC was UNTESTABLE — the corpus seeds ZERO labels/deps + uniform priority + tests use `.contains()`, so a mis-group/reorder passes green → the Implement MUST add determinism/chunk-boundary/skip-absent/ordering tests with real labels/deps + distinct priorities. OQ resolved by orchestrator (no Miguel round): OQ-1 = OQ-1(A) generous ~5× ceiling w/ CI-slowdown headroom (Implement-calibrated); OQ-2 = FOLD get_issues into hydrate_ids (never-simplify); G-B1 = name `stale`. Re-tighten the 4 read families in bench_gate.rs CEILINGS_MS + PRD:185 (SF-2 both sites); advisory relative-10% leg lands here (fixed-path baseline JSON + xtask serde_json comparator + report-only nightly leg in fuzz-smoke.yml). Pure L2 refactor — Storage sigs + spine untouched, NO D-id/contract bump. **T3.5.1 DONE → [PR #411](https://github.com/websublime/unblock/pull/411) OPEN, CI running, awaiting Miguel merge.** Branch `t3.5.1-batch-hydration`, 6 commits, off main `b6eb5c8`. hydrate_ids (4 batch queries chunked 900) replaced the 1+3N loop on all 5 read paths + get_issues fold; ceilings re-tightened (list/ready 1k 100→50, 10k 1000→500, SF-2 both sites); advisory relative-10% leg landed (bench-baseline.json + xtask bench-compare + fuzz-smoke.yml perf-advisory nightly, report-only); the R1 determinism/chunk-boundary/skip-absent tests added (non-vacuity proven). **Verify ≥3 (incl. dedicated byte-preservation + integrity/security lens): PASS_WITH_CHANGES, 0 MUST-FIX** (byte-preservation PASS all 7 axes; no injection; ceilings sound). NOTE: the first Verify Workflow hit the session limit (byte-preservation lens + coordinator errored); recovered the 3 done lenses from journal + re-ran byte-preservation as a solo synchronous code-reviewer (PASS) + synthesized the verdict manually. SF-1 folded (`72d12ec`: documented + pinned the get_issues lookup-set/dedup narrowing — unreachable in prod). SF-2 (pre-existing open_in_memory heavy-parallel robustness, NOT caused by this PR) → tracked follow-up task `task_dc4e5a10`. Also hit the ccc/CocoIndex spawn injection on the first Implement attempt (0 work) — re-launched with a guard, see [[reference-cocoindex-spawn-injection-hijacks-subagents]]. On merge: STATUS **T3.5 ☑** (already flipped in-branch, the 3 code PRs merged) + **T3.5.1 ☑**. **T3.5 COMPLETE at that point.**

**(historical) NEXT after #410+#409 merge:** **P2** `t3.5-rate-limit` off main = RateLimited variant (35→36, exit 2, retryable, hint None, END of exit-2 group after AlreadyInitialized) + v1.4 CONTRACT_VERSION + CONTRACT_HASH recompute-at-Implement + **4-golden re-bless** (capabilities/schema_bundle/exit_code_table/init_agents) + count asserts (SF-1: rename `all_array_has_35_unique_variants`→36 + module-doc) + Arc<Semaphore> chokepoint (SF-6: LOUD rmcp macro-suppression pin, replicate call_tool body exactly, --locked) + tests/rate_limit.rs + CI rate-limit job → Verify ≥3 +`/security-review` → Track (`git grep "unblock.mcp.v1.3"` zero-hit only AFTER P2). **T3.5.1** (PRE-GA, independent of P2). Manifest/amendments/review at `<session-scratchpad>/t3.5-{spec,review,p1-verify}/`.
