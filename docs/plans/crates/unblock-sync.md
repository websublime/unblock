# unblock-sync — File-level Plan

- **Status:** DRAFT (conforms to `docs/plans/01-design-spine.md` + `docs/PRD.md` PRD APPROVED v1.1 + `docs/plans/00-roadmap.md`). APIs here MUST NOT drift from the spine; any change amends the spine first.
- **One-line purpose:** Light JSONL export/import — atomic temp+fsync+rename write, path-confinement preflight, conflict-marker + malformed-JSON rejection (zero DB writes on reject), tombstone-non-resurrection, and the one-shot best-effort `bd`→unblock import. **No git, no merge, no 3-way reconciliation** (D5/D13/NFR-6).
- **Layer:** L3 (`sync` | `health`).
- **Depends on:** `unblock-storage` (L2), `unblock-model` (L0), `unblock-error` (L0). Acyclic per spine layering `model|error → policy → storage → sync|health → config → engine → render → mcp|cli`. **No dependency** on policy/config/engine/render/mcp/cli (would be cyclic). Transitively re-exports nothing from libsql (NFR-15: no backend type crosses this boundary — sync consumes only the `Storage` trait + model types).

> **Scope discipline (D5).** This crate is *shrunk* relative to the original `temp/beads_rust-main/src/sync/` (`mod.rs` ~325k, `path.rs` ~58k, `history.rs`, `witness.rs`). We deliberately **drop**: git-merge / 3-way merge, 4-phase collision detection, distributed locks, witness/history snapshots, base-snapshot temp files, data-loss-guard heuristics (empty-db-over-nonempty refusal beyond a simple opt-in flag), and the `.manifest.json`/`metadata.json` machinery. We **keep** behaviour fidelity for: atomic write (temp in same dir → `flush` → `sync_all` → atomic rename; remove temp + leave original intact on error), path-confinement preflight, conflict-marker scan (`<<<<<<<`/`=======`/`>>>>>>>`), per-line JSONL validation before any DB mutation, and tombstone-non-resurrection.

---

## Public API summary (what other crates import)

The **only** consumer is `unblock-engine` (L5), via `Session::export_jsonl` / `import_jsonl` / `import_bd` (spine §4.1). The engine holds the write permit (D14) and calls these; sync itself acquires no semaphore and owns no concurrency policy.

### v1 (LOCKED) — surfaced from `lib.rs`

```rust
// ---- reports (CF-A: DEFINED in unblock-model §1.10 with the full derive set; sync re-exports, never redefines) ----
pub use unblock_model::{ExportReport, ImportReport}; // shapes per spine §1.10/§4.1; sync produces these

// ---- options ----
#[derive(Debug, Clone, Default)]
pub struct ImportOptions {
    pub dry_run: bool,                 // validate + plan, mutate nothing (FR-8 AC, MCP sync.import dry_run)
    pub allow_external: bool,          // opt-in to write/read outside .unblock/ (NFR-7); default false
    pub on_collision: CollisionPolicy, // id already present: Skip (default) | Error | OverwriteIfNewer (content_hash/updated_at)
}
#[derive(Debug, Clone, Copy, PartialEq, Eq, Default)]
pub enum CollisionPolicy { #[default] Skip, Error, OverwriteIfNewer }

#[derive(Debug, Clone)]
pub struct ExportOptions { pub allow_external: bool /* default false */ }

// ---- the two free functions sync exposes; engine passes its Arc<dyn Storage> + resolved confine root ----
pub async fn export_jsonl(
    storage: &dyn Storage, path: &Path, confine_root: &Path, opts: &ExportOptions,
) -> Result<ExportReport, SyncError>;

pub async fn import_jsonl(
    storage: &dyn Storage, path: &Path, confine_root: &Path, actor: &str, opts: &ImportOptions,
) -> Result<ImportReport, SyncError>;

pub async fn import_bd(
    storage: &dyn Storage, path: &Path, confine_root: &Path, actor: &str,
) -> Result<ImportReport, SyncError>;   // FR-26/D16; bd-export jsonl already produced upstream

// ---- preflight + scanning primitives (also used by unblock-health lite + fuzz) ----
pub fn validate_sync_path(path: &Path, confine_root: &Path, allow_external: bool) -> Result<PathBuf, SyncError>; // returns canonicalized confined path
pub fn scan_conflict_markers(path: &Path) -> Result<Vec<ConflictMarker>, SyncError>;
pub fn ensure_no_conflict_markers(path: &Path) -> Result<(), SyncError>; // -> ErrorCode::ConflictMarkers
pub struct ConflictMarker { pub line: usize, pub marker_type: ConflictMarkerType, pub branch: Option<String> }
pub enum ConflictMarkerType { Start, Separator, End }

// ---- error ----
pub enum SyncError { /* §error below; impl code()->ErrorCode */ }
```

`confine_root` is the absolute `.unblock/` dir; the engine/config resolves it. Sync never discovers the workspace itself (that is L4 `unblock-config`'s job) — passing it in keeps sync acyclic and pure. Concretely (F-4): the engine passes `self.unblock_dir` (`Session`'s `pub(crate) unblock_dir: PathBuf` — the discovered, already-canonicalized `.unblock/` dir), NOT `workspace_dir` (its parent).

**Name-collision (RESOLVED, F-3):** `unblock_sync::ImportOptions` is DISTINCT from the engine's public `unblock_engine::ImportOptions { dry_run }` (spine §4.1). The engine call site refers to `unblock_sync::ImportOptions` FULLY-QUALIFIED (or aliased `as SyncImportOptions`) and MAPS `{dry_run}` → `{dry_run, allow_external: false, on_collision: CollisionPolicy::Skip}`. The MCP surface is NOT widened — `on_collision` stays internal (FORK-2); `Error`/`OverwriteIfNewer` are internal opt-ins with NO MCP exposure and NO `CONTRACT_VERSION` bump.

**Import apply is ATOMIC (MF-1/D23 clause (5)):** the production (default `Skip`) path is all-or-nothing — a classify-then-atomic-create shape that (1) preflights + validates (zero DB writes on any failure), (2) classifies EVERY incoming record via read-only `get_issue` probes (tombstone-guard → `sync_equals`-skip → exists-skip → collect the new-id subset), then (3) applies the new-id subset through ONE `Storage::create_issues` transaction (the T2.3 atomic primitive, rollback-on-any → zero rows). NO per-record `create_issue` loop; NO new storage trait method (`create_issues` is ALSO the import atomic-apply primitive). The classify probes and the `create_issues` tx run under the SAME engine D14 write permit (single-serve), so no concurrent writer can insert a classified-new id between probe and tx. `imported` is set only after the tx COMMITS. A dangling `depends_on_id` edge (intra-batch or against the existing DB) is ACCEPTED and committed — `dependencies.depends_on_id` has **no** foreign key (`crates/unblock-storage/src/libsql/schema.rs:14,115`), intentionally faithful to beads' `external:` refs — so it does **not** trigger the whole-batch rollback; the k-of-n atomic guarantee is exercised instead by a colliding id / backend error on record k (in-tx failure via the single `create_issues` `with_immediate_tx`). Still do NOT hand-roll edge insertion — reuse the storage path. `OverwriteIfNewer` (which needs `update`) is an INTERNAL, non-production opt-in — `#[cfg(test)]`-gated (SH-2) so no future caller trusts it — OUTSIDE the FR-8 atomic guarantee. Faithful to the original single-`with_write_transaction` import (`mod.rs:4493`).

### v1.1 (LOCKED) — additive

- `append_interaction(path, &Interaction)` for FR-22 flight recorder (append-only `interactions.jsonl`, capture-only Tier-1 attribution). New file `src/audit.rs`. Roadmap §2 lists FR-22 touching `sync`.
- Export gains comment lines (FR-6 organization) — no API shape change, only model hydration carries `comments`.

### v1.2 (PROPOSED) — additive seams only

- Reconciliation seams *if any* for the libsql remote/replica path (roadmap §3 "`unblock-sync` (reconciliation seams if any)"). Likely just a `SyncEqualsReport` helper exposing `model::sync_equals` decisions for sync-conflict diagnostics; **no merge logic** enters this crate (D5 stays in force). May add `SyncError::SyncConflict` usage. Kept behind no feature unless remote pulls it.

### v1.4 (PROPOSED) — additive

- Compaction round-trip support (roadmap §5 "Compaction interacts with JSONL round-trip fidelity (D12)"): export/import must remain lossless for compacted issues; adds proptest coverage + a `compact`-aware normalize path. No new public fn — extends `roundtrip` proptests and `normalize`.

> v2+ (`◐` DB-only option) is direction-only; not planned at file level here.

---

## FILE BREAKDOWN

| Path | Responsibility | Key items (reference spine §) | Version | Tests |
|---|---|---|---|---|
| `Cargo.toml` | Crate manifest. deps: `unblock-model`, `unblock-error`, `unblock-storage`, `tokio` (fs/io via the async export write path through engine permit — but file I/O is sync inside `spawn_blocking`), `serde`/`serde_json`, `chrono`, `snafu`, `tracing`, `dunce` (cross-platform canonicalize, as in original `path.rs`). dev-deps: `proptest`, `insta`, `tempfile`, `tokio` test macro. `#![forbid(unsafe_code)]`, clippy pedantic, `missing_docs=warn` (spine §0). | — | v1 | n/a |
| `src/lib.rs` | Crate root + module wiring + re-exports of the public API above. Crate-level docs stating the D5 light-scope and the "no git / no merge" invariant (NFR-6). `#![forbid(unsafe_code)]`, `#![warn(missing_docs)]`. | sync-owned re-exports: `export_jsonl`/`import_jsonl`/`import_bd`/`validate_sync_path`/`scan_conflict_markers`/`ensure_no_conflict_markers`/`SyncError`/`ImportOptions`/`ExportOptions`/`CollisionPolicy`/`ConflictMarker(Type)`. Model re-exports (CF-A): `pub use unblock_model::{ExportReport, ImportReport}`. | v1 | doctest: a confined export→import round-trip on a small in-memory `Storage` fake. |
| `src/error.rs` | `#[derive(Debug, Snafu)] pub enum SyncError` with context selectors; `impl SyncError { pub fn code(&self) -> ErrorCode }` mapping every variant to the exit-6/8 codes. Backend errors absorbed via `#[snafu(transparent)]` over `StorageError` (never re-exposes libsql). | Variants → `ErrorCode` (spine §2.2/§2.3): `ConflictMarkers`→`ConflictMarkers`(6); malformed line→`JsonlParseError`(6); symlink/`.git`/`..`/bad-ext→`PathTraversal`(6); id already present under `Error` policy→`ImportCollision`(6); prefix mismatch (bd/import)→`PrefixMismatch`(6); semantic conflict (v1.2)→`SyncConflict`(6); fs read/write/rename/fsync→`IoError`(8); serde encode→`JsonError`(8); storage passthrough→`StorageError.code()`. `is_retryable` follows `ErrorCode::is_retryable`. | v1 | unit: each variant → expected `ErrorCode` + `exit_code()`; snafu `Display` non-empty; transparent storage wrap preserves code. |
| `src/path.rs` | Path-confinement preflight (NFR-7). Reject ORDER (each a defence layer, faithful port of `temp/beads_rust-main/src/sync/path.rs`): (1) `.git` FIRST — ALWAYS rejected even under `allow_external` (`has_git_component` on the raw path AND each canonicalized existing ancestor) → `GitComponent`; (2) lexical `..` normalize (pop on `ParentDir`; escape → `ParentEscape`; `CurDir` dropped; purely lexical); (3) symlink-escape of an existing ancestor → `SymlinkEscape` (runs regardless of `allow_external`) — NOTE: the original checks LEAF-only for external paths (SH-1); T2.4 always passes `allow_external:false` (latent), so the ancestor check is a deliberate TIGHTENING, not a faithful-claim on the original external path; (4) containment `starts_with(confine_root)` → `OutsideConfineRoot` UNLESS `allow_external` (the ONLY relaxed check); (5) non-existent target → canonicalize PARENT (`dunce::canonicalize`), confirm confined, validate ext+name, rejoin; (6) existing target → reject non-regular-file (`NonRegularFile`), reject escaping symlink, then ext/name; (7) ext/name allowlist → `DisallowedExtension`/`DisallowedName`. **Invariant (NFR-8):** `allow_external=true` relaxes ONLY step 4 — steps 1/2/3 + regular-file NEVER relaxed. Distilled from original `path.rs` (drops manifest/metadata exact-names beyond `issues.jsonl`). | `validate_sync_path(path, confine_root, allow_external) -> Result<PathBuf>`; `validate_temp_path(temp, final, confine_root)`; `has_git_component`; `normalize_lexically`; `pub enum PathReject { GitComponent, ParentEscape, SymlinkEscape, DisallowedName, DisallowedExtension, OutsideConfineRoot, NonRegularFile, TempCollision }`; const `ALLOWED_EXTENSIONS = ["jsonl"]` (+ pid-scoped `*.jsonl.<pid>.tmp` temp pattern); const `ALLOWED_EXACT_NAMES = ["issues.jsonl"]`. `allow_external=true` relaxes the confine-root prefix check but **never** the `.git`/`..`/symlink-escape checks (NFR-8: force never bypasses safety). | v1 | unit: confined path ok; `../escape` rejected (`PathTraversal`); `.git/x` rejected; symlinked-ancestor-escape rejected (labeled a deliberate tightening, SH-1); `.txt` ext rejected; new (non-existent) file in confined dir ok; `allow_external` still rejects `.git`/`..`/symlink-escape. proptest: random relative paths under root never escape after normalize (no normalized path starts outside `confine_root`). |
| `src/conflict.rs` | Conflict-marker scanner (FR-8/NFR-8). Streams lines (2 MiB `BufReader`), detects `<<<<<<<`/`=======`/`>>>>>>>` line-prefixes, records line number (1-based) + branch tail. `ensure_no_conflict_markers` errors with a ≤5-marker preview. Reads via a metadata-validated fd (size guard — RESOLVED, see open Q1). **Ingestion caps (FORK-3, NFR-18):** `const MAX_IMPORT_FILE_BYTES: u64 = 2 * 1024 * 1024 * 1024` (2 GiB) enforced at preflight via fd-metadata (`std::fs::metadata(path).len()`) BEFORE any read; `const MAX_LINE_BYTES: usize = 4 * 1024 * 1024` (4 MiB) enforced per-line by a **bounded** read (`reader.take(MAX_LINE_BYTES + 1).read_until(b'\n', &mut buf)` — NOT `.lines()`, which buffers the whole line first → OOM before the check, MF-3) that aborts without materializing the full line. File-size breach → `SyncError::FileTooLarge` (`IoError`, exit-8); line-length breach → `SyncError::LineTooLong` (`JsonlParseError`, exit-6). **Overridable-later WITHOUT a v1 signature break:** the caps are `pub const`s the engine MAY later override via an additive `ImportOptions` field (e.g. `max_file_bytes: Option<u64>`) — v1 ships the consts; the seam is additive (NFR-18). | `ConflictMarker`, `ConflictMarkerType{Start,Separator,End}`, `scan_conflict_markers`, `ensure_no_conflict_markers`, `detect_conflict_marker(line) -> Option<(ConflictMarkerType, Option<String>)>`, consts `CONFLICT_START/SEPARATOR/END`, `MAX_IMPORT_FILE_BYTES`, `MAX_LINE_BYTES`. Mirrors original `mod.rs:1387-1480` behaviour. | v1 | unit: clean file → `[]`; each marker kind detected at right line/branch; mixed markers ordered; CRLF lines; `ensure_*` returns `ConflictMarkers` with preview; empty file ok; `FileTooLarge`/`LineTooLong` at the caps (a (cap+1)-byte single line → `LineTooLong`, bounded peak alloc). fuzz target consumes this (see fuzz). |
| `src/jsonl.rs` | Line-oriented JSONL read/parse + per-line validation (FR-7/FR-8). Parse each non-empty trimmed line as `model::Issue` (serde); `content_hash` recomputed on load (`#[serde(skip)]`, spine §1.8/CR-3); run `IssueValidator::validate`; reject duplicate ids within the file; collect per-line failures **before** any DB mutation. The reader uses the SAME bounded per-line read as `conflict.rs` (`take(MAX_LINE_BYTES+1).read_until(b'\n', …)`, MF-3) + the fd-metadata file-size guard. Also the deterministic **serializer** for export: one `Issue` per line, stable field order via serde, trailing `\n` written by `atomic.rs` (not here), byte-deterministic for fixed DB state (FR-7 AC / NFR-4). **Timestamp canonicalization (CF-TS/D-OQ-B):** `serialize_issue_line` builds `serde_json::to_value(issue)?` then rewrites EVERY present `DateTime<Utc>` field to `unblock_model::fmt_ts_secs(parsed.with_timezone(&Utc))` (MF-5 — `parse_from_rfc3339` returns `FixedOffset`) via a RECURSIVE `canonicalize_ts_in_value(&mut Value)` over the whole `Value` tree (MF-6 — covers the 7 top-level ts + nested `dependencies[].created_at` which has NO `skip_serializing_if`, `relations.rs:24-25`, + `comments[].created_at`); do NOT rely on `skip_serializing_if` gating for nested fields. Serialize error → `JsonEncode`. | `serialize_issue_line(&Issue) -> Result<String, SyncError>`; `parse_issue_line(line, line_no) -> Result<Issue, SyncError>`; `validate_records(path) -> Result<JsonlValidationSummary, SyncError>`; `JsonlValidationSummary { record_count, failures: Vec<(usize,String)>, ids: Vec<String>, records: Vec<Issue> }` (records carried to avoid a 2nd parse pass); `normalize(&mut Issue)` (canonicalize before hash/compare; recompute `content_hash`; `updated_at = updated_at.max(created_at)`; `compaction_level None==0`, spine §1.8); `canonicalize_ts_in_value(&mut Value)`. | v1 | unit: valid line round-trips; malformed JSON → `JsonlParseError` w/ line no; blank lines skipped; duplicate id in file → failure; invalid (title>500 / priority 5) → `ValidationFailed`; export line is byte-stable across runs; sub-second `dependencies[].created_at` → exported line is `…Z` (no `.`, MF-6); (cap+1)-byte single line → `LineTooLong`. proptest: `parse(serialize(issue))` `sync_equals` (NOT `==`) for arbitrary valid `Issue` with second-truncated timestamps (round-trip incl. all Option/relation fields, spine §1.6). |
| `src/atomic.rs` | Atomic write primitive (FR-7/NFR-4). Create pid-scoped temp in the **same dir** as the target (path-validated), write all lines, `flush`, `File::sync_all` (fsync the FILE), then atomic `rename` over the target. On any error: remove temp (RAII guard), leave original untouched. Unix: set restrictive perms (0600) on temp before write. All blocking fs ops run inside ONE `tokio::task::spawn_blocking`; `confine_root`/`final_path` are cloned to owned `PathBuf` BEFORE the closure (MF-11(b) — `&Path` is not `'static`). Temp name = `final_path.file_name()` + push `".{pid}.tmp"` (→ `issues.jsonl.<pid>.tmp`), NOT `with_extension` (SH-4 — ext-replacement foot-gun). **Parent-dir fsync (DRIFT-1, NFR-4/5 — faithful port of `util::durable_rename`):** AFTER the `rename`, OPEN the target's PARENT DIRECTORY and `File::sync_all` it (fsync the dir) so the rename's directory-entry update is itself crash-durable. Sequence: `temp write → flush → sync_all(file) → rename → sync_all(parent dir)`. Omitting the parent-dir fsync leaves the rename non-durable under power-loss. REQUIRED, not optional. (unix; no-op+trace elsewhere.) | `pub async fn write_atomic(final_path, confine_root, lines: impl Iterator<Item=String> + Send + 'static) -> Result<usize, SyncError>`; `temp_path_for(final_path, attempt) -> PathBuf` (`file_name()`+`.{pid}.tmp`, collision retry like original `mod.rs:203`); `set_restrictive_perms` (cfg unix / no-op else); `sync_directory(parent) -> io::Result<()>` (cfg unix `File::open(parent)?.sync_all()`, else trace+skip). | v1 | unit: write creates target with content; killed-before-rename (inject error after temp write) leaves original intact + removes temp (NFR-4 failure-injection); temp lands in same dir with the `issues.jsonl.<pid>.tmp` byte-form (SH-4); rename is atomic (no partial); perms 0600 on unix; parent-dir fsync happens after rename. (Full SIGTERM-mid-write e2e lives in engine M3, NFR-5 — this is the unit-level guarantee.) |
| `src/export.rs` | Orchestrates export: preflight path (`path::validate_sync_path`), pull the FULL corpus via `Storage::list_issues(&ListFilters { include_closed: true, include_deferred: true, include_tombstone: true, ..Default::default() })` (FORK-1/D23 — `include_tombstone` is the new spine §1.10 field; RESOLVED open Q3), then EXCLUDE `ephemeral` rows and `-wisp-` ids IN-CRATE (storage has no ephemeral filter on `list_issues`), order `id ASC` (deterministic bytes), serialize each via `jsonl::serialize_issue_line` (which canonicalizes each `DateTime<Utc>` via `unblock_model::fmt_ts_secs`, CF-TS/D-OQ-B — NOT `serde_json::to_string(&issue)` directly, which leaks chrono sub-second+offset), hand to `atomic::write_atomic`, return `ExportReport{written, path: canonical}`. `tracing` on `unblock.reliability` (INFO on external-path use, NFR-13). v1 exports ALL tombstones (no retention config; compaction is v1.4). | `export_jsonl(storage, path, confine_root, opts) -> Result<ExportReport, SyncError>` (the public fn). | v1 | unit (with `Storage` fake): N issues → file has N lines, report.written==N; ephemeral/`-wisp-` excluded; external path without `allow_external` → `PathTraversal`; 0-issue DB exports empty file cleanly. insta: snapshot of a fixed 3-issue export (deterministic bytes, incl. a sub-second NESTED `dependencies[].created_at` fixture → `…Z`, MF-8; NFR-14). |
| `src/import.rs` | Orchestrates import preflight→classify→atomic-apply (FR-8 order is normative, ZERO DB writes before the tx): (1) `path::validate_sync_path`; (2) `conflict::ensure_no_conflict_markers`; (3) `jsonl::validate_records` (normalize→recompute hash→`IssueValidator::validate`→in-file dup-id; ALL failures collected → abort with ZERO DB writes); (4) if `dry_run`, classify every record via read-only `get_issue` probes, build the planned `ImportReport` (would-import = new-id subset size; skips explained), STOP — mutate nothing; (5) **apply = classify-then-atomic-create (MF-1/D23 clause (5), engine holds the D14 permit across the whole call — MF-4):** (5a) classify (READ-ONLY) each record via `existing = storage.get_issue(&incoming.id).await?` (RETURNS tombstoned rows) → decide per the `Skip` matrix: tombstone-resurrection (`existing.is_tombstone() && !incoming.is_tombstone()`) → skip (`"tombstone protection"`, spine §1.8); `existing.is_some()` + `sync_equals` → skip (`"identical"`, FR-26); `existing.is_some()` + differ (under `Skip`) → skip (`"exists (skip policy)"`, surfaced — never silently overwritten); `existing.is_none()` → collect into `create_subset`; (5b) if `create_subset` non-empty → ONE `storage.create_issues(&create_subset, actor).await?` tx (rollback-on-ANY → ZERO rows; `?` propagates as `SyncError::Storage`); NO per-record `create_issue` loop; (5c) `imported = create_subset.len()` set ONLY after commit. `Error` policy on an existing id → `ImportCollision`. `OverwriteIfNewer` is `#[cfg(test)]`-gated (SH-2), non-production, outside the atomic path. | `import_jsonl(storage, path, confine_root, actor, opts) -> Result<ImportReport, SyncError>`; `enum ApplyDecision { Import(Box<Issue>), Skip{reason: &'static str}, Collision }` (SF-7 — the landed code boxes `Issue`, `import.rs:60`); `classify(existing: Option<Issue>, incoming: Issue, policy) -> Result<ApplyDecision, SyncError>`; `is_tombstone_resurrection(existing, incoming) -> bool` (uses `model` tombstone helpers); extract `pub(crate) apply_records(storage, records: Vec<Issue>, dropped: Vec<String>, actor, opts) -> Result<ImportReport, SyncError>` (D24/F5) doing steps 5a-5c (classify + atomic `create_issues` one-tx). **`apply_records` count plumbing (MF-2, option (a) — the buildable seam):** `apply_records` builds+returns the `ImportReport` with `imported`/`skipped`/`dropped_fields` set and `dependencies: 0, comments: 0`; EACH CALLER then finalizes the two relation counts on the returned report — `import_jsonl` leaves them `0` (never tallies relations), `import_bd` sets them to its POST-repair, POST-dedup tallies (faithful to bd's `record_imported_relation_counts`, `temp/beads_rust-main/src/sync/mod.rs:4611-4614` — counted on the normalized record). So the seam carries NO deps/comments params and each caller owns the finalize. **dry_run/opts (SF-5):** the `dry_run` early-return stays in `import_jsonl` (the opts-bearing generic path, `import.rs:166`) — `apply_records` is the classify+atomic-create tail only; `import_bd` (NO opts) synthesizes `ImportOptions{dry_run:false, allow_external:false, on_collision:Skip}` at its `apply_records` call site. **ImportReport ripple (MF-3, F1 field addition — `ImportReport` has NO `Default`, `results.rs:54`):** adding `dependencies`/`comments` is a hard compile break at every 3-field literal (no `..Default::default()` escape) — Implement updates the enumerated sites `crates/unblock-sync/src/import.rs:167`+`:180` (fold both into `apply_records`, defaulting the two counts to `0`), `crates/unblock-model/src/results.rs:157-161`, `crates/unblock-mcp/src/tools/output.rs:57-61`, + the new `bd_import.rs` constructor, AND re-blesses the JsonSchema golden `crates/unblock-model/tests/snapshots/schema_snapshots__import_report.snap` (currently 3 properties/3 required → WILL diff). No `CONTRACT_VERSION` bump AT T2.5 (then, the DTO rode in the un-bundled mcp `SyncOutput`). **Forward note (T2.6/D25):** the bundle now carries the per-tool output schemas (`sync` output = `SyncOutput`), so any FUTURE `ImportReport`/`ExportReport` field change moves `CONTRACT_HASH` and forces a bump. BOTH `import_jsonl` (after `validate_records`, passing `dropped: vec![]`) and `import_bd` (after per-line `map_bd_record`, passing its `dropped_fields`) call `apply_records`. | v1 | unit (Storage fake): clean file imports all via ONE `create_issues`; conflict-marker file → `ConflictMarkers`, **0 writes** asserted; malformed line → `JsonlParseError`, 0 writes; symlink-escape → refused at preflight, 0 writes; re-import is idempotent (`sync_equals` skip), imported==0 second run; tombstoned id + non-tombstone line → skipped, DB unchanged; `dry_run` mutates nothing but reports plan (ZERO create/update calls); `CollisionPolicy::Error` on existing id → `ImportCollision`. **contract (real libsql, MF-1/MF-9):** k-of-n mid-batch failure (colliding id / backend error on record k — via the single `create_issues` `with_immediate_tx`; a dangling `depends_on_id` edge is NOT a trigger since it has no FK, schema.rs:14/115) leaves ZERO rows (re-SELECT count unchanged), non-vacuous. proptest: export-then-import is identity at the DB level (`sync_equals`, spine §1.8); import is idempotent under repetition. |
| `src/bd_import.rs` | **T2.5 BUILDS this file** (no longer a stub) + wires the engine `Session::import_bd` (D24/F5). FR-26/D16 one-shot best-effort `bd`→unblock import. Input = a `bd sync --flush-only`-produced `issues.jsonl` that the **user** generates with their existing bd install; **this crate runs NO external command** (NFR-6/D13) [DRIFT-2 fix]. The struct/serde field map is **IDENTITY** — unblock's `Issue`/`Dependency`/`Comment`/enums are a byte-faithful field-for-field port of bd's, so a bd-export line deserializes directly into `unblock_model::Issue` with zero renames. **The real work is the 7-step bd import-normalize repair pass** (a faithful port of `temp/beads_rust-main/src/sync/mod.rs:3813-3918` — the 7 repairs unblock's shared `jsonl::normalize()` lacks): dep-type legacy-underscore repair — a **GENERAL** rule (bd `mod.rs:3822-3832`): for any `Custom` `dep_type`, `replace('_','-')` and adopt iff it parses to a known (non-`Custom`) `DependencyType` variant — repairing `parent_child`/`conditional_blocks`/`waits_for`/`discovered_from`/`replies_to`/`relates_to`/`caused_by` (every legacy underscore form, since `DependencyType::parse_lowercased`, `crates/unblock-model/src/enums/dependency.rs:83-96`, recognizes only kebab forms); `parent_child`→`ParentChild` is the canonical EXAMPLE, not the whole rule; dependency dedup keep-latest per `(issue_id, depends_on_id, dep_type)`; terminal-status text aliases `done`/`complete`/`completed`/`finished`/`resolved`→`Closed`; `-wisp-` id→`ephemeral=true`; `closed_at`=`updated_at` when terminal & none, cleared when non-terminal; `external_ref` blank/whitespace→`None` else trimmed — applied INSIDE this file **in bd's SOURCE ORDER** (status-alias BEFORE the `closed_at` set/clear repair, which tests `is_terminal()` — SF-2) BEFORE the `content_hash` recompute. **Hash-recompute composition (SF-4):** after the 7 repairs, `bd_import` calls the SHARED `jsonl::normalize()` (`crates/unblock-sync/src/jsonl.rs:97` — labels sort/dedup + `updated_at>=created_at` clamp + `content_hash` recompute) so the recompute INCLUDES the shared steps bd folds into the same `normalize_issue` pass and the recomputed hash equals bd's stored hash byte-for-byte (FR-26 idempotency + cross-tool dedup — spine §1.8 NOTE); it does NOT recompute standalone. The shared `jsonl::normalize()` itself stays UNCHANGED. `dropped_fields` = unknown top-level keys, via `map_bd_record(serde_json::Value)` diffing raw JSON keys against the known `Issue`/`Dependency`/`Comment` key set BEFORE `from_value::<Issue>` (serde silently discards unknowns — neither struct sets `deny_unknown_fields`). **bd ids are preserved verbatim** — no remap/reject on the default path (`content_hash` excludes `id` so preserving is idempotency-safe); `SyncError::PrefixMismatch` is a RESERVED v1 seam (defined, not exercised); prefix remap is DEFERRED to v1.1. **FR-8 preflight on the bd `Value`-per-line path (MF-4 — closes FR-26's "reuses the FR-7/FR-8 import path" guarantee):** because `import_bd` reads records via `map_bd_record(Value)` (NOT `validate_records`), it does NOT inherit `import_jsonl`'s inline FR-8 preflight by calling `apply_records` (which is classify+tx only, steps 5a-5c) — so it MUST re-run every FR-8 guard on its own path BEFORE the `apply_records` classify+tx: (1) path confinement (`path::validate_sync_path`), (2) conflict-marker rejection (`conflict::ensure_no_conflict_markers`), (3) the bounded read (`MAX_LINE_BYTES`/`MAX_IMPORT_FILE_BYTES`, D23), (4) `IssueValidator::validate` on each mapped `Issue`, (5) in-file dup-id — ALL failures collected → abort with ZERO DB writes, faithful to the generic path. **Prefer a SHARED preflight/confinement helper both paths call — `preflight_source(path, confine_root, allow_external) -> Result<CanonicalPath, SyncError>`** (path confinement + conflict markers + the bounded fd-metadata size guard), so FR-8 cannot be bypassed by construction; `import_jsonl` keeps its per-line `validate_records` (parse+`IssueValidator`+dup-id) and `import_bd` runs the equivalent per-line (`IssueValidator::validate` on the mapped `Issue` + in-file dup-id) after `map_bd_record`. Funnels into the shared `import.rs` `apply_records` helper (reusing the tombstone-guard-first / `sync_equals`-Skip / one-atomic-tx guarantees, D23/D24-F5); finalizes `dropped_fields` + the POST-repair/POST-dedup deps/comments counts on the returned report (MF-2). | `import_bd(storage, path, confine_root, actor) -> Result<ImportReport, SyncError>` (Skip-only, NO opts; synthesizes `ImportOptions{dry_run:false, allow_external:false, on_collision:Skip}` at the `apply_records` call site — SF-5); `preflight_source(path, confine_root, allow_external) -> Result<..., SyncError>` (shared FR-8 confinement+conflict-marker+bounded-read helper, MF-4); `pub(crate) map_bd_record(serde_json::Value) -> Result<(Issue, Vec<String> /*dropped*/), SyncError>` (**crate-internal, NOT re-exported** — `import_bd` is the only public bd entry; the sole in-crate caller feeds a `from_str`-bounded `Value`, so the unbounded `from_value` recursion surface is not exposed, V1/V4); the 7 repair fns (dep-type, dep-dedup, status-alias, wisp, closed_at set/clear, external_ref); `KNOWN_ISSUE_KEYS`/`KNOWN_DEP_KEYS`/`KNOWN_COMMENT_KEYS` key sets for the drop diff. **KNOWN_*_KEYS derivation + drift guard (SF-6):** the key sets DERIVE from the serde-visible field set of `Issue`/`Dependency`/`Comment` — INCLUDING the 3 relation-container keys `labels`/`dependencies`/`comments`, MINUS the `#[serde(skip)]` `content_hash` (no struct sets `deny_unknown_fields`, so a genuinely unknown top-level key is the only `dropped_field`). Add a guard test asserting the hand-listed sets equal the serde field set so a future `Issue`/`Dependency`/`Comment` field cannot surface as a spurious `dropped_field`. | v1 | unit: a synthesized byte-faithful `bd_export.jsonl` fixture maps to the expected `Issue` set; **every one of the 7 repairs exercised (non-vacuity, SF-1) via explicit per-repair POST-import field assertions, each of which must FAIL under a single-repair mutation:** status `done`→`Closed`; a NON-`parent_child` underscore dep (e.g. `waits_for`)→its kebab variant (`waits-for`); dup-deps→N−1; `-wisp-` id→`ephemeral=true`; terminal `closed_at:None`→`updated_at`; non-terminal→`closed_at:None`; `external_ref` blank→`None` AND (SF-3) a whitespace-PADDED non-blank value→trimmed (BOTH `external_ref` sub-branches, `mod.rs:3903-3904`). Idempotency-alone (round-trip Skip) does NOT satisfy the gate — `content_hash` is `#[serde(skip)]`, so a repair skipped-in-both runs is still idempotent. Also: dropped/unmapped (unknown top-level) fields reported; idempotent on rerun (dedup by `content_hash`=bd's); deps/comments counts reported. insta: snapshot of the mapping report (counts + dropped-field list) over the synthesized fixture. |
| `src/audit.rs` | **[v1.1]** FR-22 flight recorder: append-only `interactions.jsonl` writer; one JSON line per interaction with capture-only Tier-1 attribution (`agent_name`/`harness`/`model`); never enforced; append uses `OpenOptions::append` + per-line flush (not the atomic-rewrite path — append-only). Path-confined like export. | `append_interaction(path, confine_root, &Interaction) -> Result<(), SyncError>`; `Interaction { ts, actor, tool, action, attribution, .. }` (or re-uses a model type if one is added). | v1.1 | unit: append adds exactly one line; concurrent appends don't interleave a partial line; path-confined; malformed dir refused. |
| `tests/contract.rs` | Crate-level integration: drives the full public surface against the real `unblock-storage` libsql impl (in-memory/temp file) — the export/import contract suite tie-in (NFR-16). round-trip, idempotency, reject-with-zero-writes, tombstone, dry-run, external-path. | uses `export_jsonl`/`import_jsonl`/`import_bd`. | v1 | integration: end-to-end export→import identity on real storage; reject paths leave DB + file untouched; `import_bd` over a **synthesized byte-faithful `bd_export.jsonl` fixture** (D24/F4 — hand-authored to bd's serializer semantics, NO dev-dep on `temp/beads_rust-main`) that exercises ALL 7 repairs + dropped (unknown-top-level) keys + deps/comments counts: assert ids preserved, deps/comments counts + dropped list reported, rerun idempotent (dedup by `content_hash`=bd's), and **every repair exercised (non-vacuity, SF-1)** via explicit per-repair POST-import field assertions (status→`Closed`; a NON-`parent_child` underscore dep→its kebab variant; dup-deps→N−1; `-wisp-`→`ephemeral`; terminal `closed_at:None`→`updated_at`; non-terminal→`None`; `external_ref` blank→`None` AND — SF-3 — whitespace-padded→trimmed, BOTH sub-branches), each failing under a single-repair mutation (idempotency-alone does NOT satisfy the gate — `content_hash` is `#[serde(skip)]`) (FR-26 AC dogfood-shaped). |
| `tests/atomic_failure.rs` | NFR-4 failure-injection integration: simulate write failure / process-kill before rename via an injected fault point; assert original file integrity + temp cleanup. | — | v1 | integration: pre-rename failure leaves original byte-identical; orphan temp removed; partial temp never visible as target. |
| `tests/roundtrip_proptest.rs` | Property suite: arbitrary `Vec<Issue>` → export → import → `sync_equals` identity; double-import idempotency; serialize/parse line round-trip for the full field set (spine §1.6) incl. compaction fields. | proptest strategies for `Issue` (shared with model where possible). | v1 (extended v1.4 for compaction) | proptest (256+ cases): DB-level identity; idempotency; no-panic on adversarial-but-valid inputs. |

> `unblock-fuzz` (separate workspace member, not owned here) adds a `cargo-fuzz` target over `conflict::scan_conflict_markers` + `jsonl::parse_issue_line` (NFR-16 "fuzz over the ingestion surface"). This plan only notes the seam; the targets live in the fuzz crate.

---

## Crate-level test & bench plan

- **Unit (per file):** as tabled. Every error variant asserted to map to the exit-6/8 `ErrorCode` (spine §2.3 golden table). `Storage` interactions use a lightweight in-crate fake `#[cfg(test)] struct FakeStorage` that impls the FULL object-safe `#[async_trait] Storage` (`unimplemented!()` for methods sync never calls), `Send + Sync` (SH-5); its `list_issues` HONOURS `include_tombstone`/`include_closed`, `get_issue` RETURNS tombstoned rows, and `create_issues` records the batch (else the round-trip/resurrection tests are vacuous). (No reusable exported double exists — `NoopStorage` is `#[cfg(test)]` inside `trait_def`.)
- **proptest (NFR-16):** (1) line serialize/parse round-trip over arbitrary valid `Issue`; (2) export→import DB identity via `sync_equals`; (3) import idempotency; (4) path normalization never escapes `confine_root`. v1.4 extends (1)/(2) to compacted issues (D12 fidelity).
- **insta (NFR-14):** deterministic export bytes for a fixed issue set; bd-import mapping report over the **synthesized byte-faithful `bd_export.jsonl` fixture** (D24/F4 — hand-authored, exercising all 7 repairs incl. a NON-`parent_child` underscore dep and BOTH `external_ref` sub-branches (blank→`None` AND whitespace-padded→trimmed, SF-3) + dropped unknown-top-level keys + deps/comments counts; NO dev-dep on `temp/beads_rust-main`); conflict-marker preview string. CI snapshot-check gate.
- **Integration contract (NFR-16):** `tests/contract.rs` runs the same scenarios against the **real libsql** storage to prove the trait contract, not just the fake.
- **Failure-injection (NFR-4/NFR-5):** `tests/atomic_failure.rs` — pre-rename fault leaves original intact; the SIGTERM-mid-write *whole-process* test is owned by `unblock-engine` M3 (this crate provides the unit-level atomic guarantee it relies on).
- **fuzz (NFR-16):** ingestion-surface targets registered in `unblock-fuzz` over `scan_conflict_markers` + `parse_issue_line`.
- **Bench:** no `criterion` owned here in v1 — NFR-1 export/import budgets (export 10k <500ms, import 10k <1s) are measured at the **engine** level (`Session::export_jsonl`/`import_jsonl`) since that is the user-facing op and includes storage. If profiling shows sync-internal serialize/parse dominates, add `benches/jsonl.rs` (serialize/parse 10k lines) in v1.1 — flagged as an open question.

---

## Open questions specific to this crate

1. **Input-size guard — RESOLVED (FORK-3).** 2 GiB file (fd-metadata) / 4 MiB line (bounded `read_until`, MF-3). Overridable-later via an additive `ImportOptions` field — no v1 break. (conflict.rs/jsonl.rs rows.)
2. **CollisionPolicy default + surface — RESOLVED (FORK-2).** Internal default `Skip`; MCP NOT widened; no `CONTRACT_VERSION` bump. `Error`/`OverwriteIfNewer` internal opt-ins (`OverwriteIfNewer` `#[cfg(test)]`-gated, SH-2).
3. **Export scope — RESOLVED (FORK-1/D23).** All non-ephemeral incl. closed + tombstones (via `include_tombstone`), exclude ephemeral/`-wisp-`, order `id ASC`, timestamps canonicalized.
4. **bd field map authority — RESOLVED at T2.5 (D24).** `bd_import.rs`/`import_bd` built: identity struct/serde map + the 7-repair bd import-normalize pass + `Value` key-diff `dropped_fields` + preserve-ids + the `apply_records` reuse seam. Synthesized byte-faithful fixture (no dev-dep on `temp/beads_rust-main`).
5. **v1.2 reconciliation seam — DEFERRED (no v1 action).** Reconciliation stays OUT of sync (D5).
6. **`spawn_blocking` — RESOLVED.** Sync OWNS its `spawn_blocking` (thin engine adapter); the DB apply stays async via `Storage`.
