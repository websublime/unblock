# unblock-fuzz — File-level Plan

**Source of truth:** PRD APPROVED v1.1 (this plan conforms to PRD §8.1 and `01-design-spine.md`; on any cross-crate type/signature disagreement, the spine wins).

**One-line purpose:** Unpublished workspace member hosting `cargo-fuzz` (libFuzzer) targets and shared harness scaffolding that exercise the untrusted-input ingestion surfaces — model content-hash/sync-equality, JSONL/`bd` import & atomic export round-trips, TOML config parsing, and storage query/import invariants — asserting "never panic / never corrupt / never escape" (NFR-16, NFR-7/8, NFR-3).

**Layer:** aux (`—` in PRD §8.1; unpublished, `publish = false`). Not part of the acyclic library DAG; it is a leaf *consumer* that sits **above** everything it tests.

**Depends-on (must stay acyclic — fuzz is a sink, nothing depends on it):**
> L0-edge convention (PRD §8.1): both L0 crates (`model`, `error`) are listed as **direct** v1 edges of `unblock-fuzz` — this row follows the "always list L0 when used" convention, matching the PRD §8.1 fuzz row (`, model, error` at v1; `config` at v1.1).
- `unblock-model` (Issue/Dependency/Comment/Event, `compute_content_hash`, `sync_equals`, `IssueValidator`, open enums) — L0, **v1**.
- `unblock-error` (`ErrorCode`, `StructuredError`, per-crate error `code()` views) — L0, **v1**.
- `unblock-storage` (`Storage` trait + libsql local impl, `ListFilters`, `IssuePatch`, `DeletePlan`) — L2.
- `unblock-sync` (JSONL export/import, path-confinement, conflict-marker scan, bd import) — L3.
- `unblock-config` (layered TOML resolution) — added at **v1.1** when the config target lands.
- Dev/runtime deps: `libfuzzer-sys`, `arbitrary` (derive), `tokio` (current-thread runtime to drive async `Storage`), `tempfile`, `serde`/`serde_json`, `chrono`, `proptest` (only for the regression-replay unit tests under `tests/`).

> **Acyclic guarantee:** `unblock-fuzz` is referenced by **no** other crate. It depends only on already-built layers (model/error/storage/sync/config). It is excluded from the default `cargo build`/`cargo test` of the workspace via `[workspace] default-members` (the fuzz crate is a member but not a default member), so the libFuzzer toolchain requirement never leaks into normal CI.

> **Reshaping from `beads_rust/fuzz` (grounding source):**
> - `config_yaml` target → **`config_toml`** (D10: TOML, not the unmaintained serde_yaml fork). The YAML attack surface is gone; the TOML parse + layered-precedence surface replaces it. Lands **v1.1** with `unblock-config` full layering (v1 ships only a config *subset*; a minimal `config_toml` smoke target lands in v1).
> - `merge_issue` target (three-way merge, `ConflictResolution`, `MergeReport`) → **DROPPED** (D5: heavy git-merge coordination removed; `unblock-sync` is light export/import only). Its *valuable* invariants (determinism, validator-on-output, tombstone-never-resurrected, content_hash freshness) are **folded into** `content_hash` and `jsonl_roundtrip` targets.
> - `markdown_import` target → **DEFERRED** to whenever bulk-markdown create (FR-1a "bulk markdown import") gets its own parser surface; tracked as an open question (markdown parser location is not yet in the spine). Not in v1/v1.1 unless the parser lands.
> - `content_hash`, `jsonl_import`, `sync_cycle`, `search_issues` carry forward, retargeted to the async `Storage` trait + snafu errors + `sync_equals`/`compute_content_hash` from `unblock-model`.

---

## Public API summary

`unblock-fuzz` exports **no library API consumed by other crates** (it is a sink). Its "public surface" is:
1. The set of fuzz **binaries** (`[[bin]]` targets under `fuzz_targets/`) runnable via `cargo +nightly fuzz run <name>`.
2. An **internal** (crate-private) shared harness module (`src/lib.rs` + `src/*.rs`) re-used by every target and by the `tests/` regression suite. These items are `pub` only so the `fuzz_targets/*.rs` binaries (separate crates under cargo-fuzz) can import them, but they are **not** a workspace-stable API.

| Version | Harness items exposed to targets |
|---|---|
| **v1** | `ByteCursor`, `CursorExt` (enum sampling), `tokio_block_on`, `FuzzWorkspace` (tempdir + `.unblock/` + outside-sentinel), `open_local_storage`, `assert_storage_invariants`, `assert_import_validation`, `assert_outside_untouched`, `assert_nonempty_structured_error`, `arbitrary_issue`, `arbitrary_issues`. |
| **v1.1** | + `ConfigCorpus` helpers (TOML layer builders), `assert_config_precedence`. |
| **v1.2** | + `RemoteStorageHarness` (wiremock-backed) behind the `remote` feature, `assert_sync_idempotent`. |
| **v1.3** | + `large_corpus` generator (scale ingestion fuzz), `assert_compaction_roundtrip`. |

---

## FILE BREAKDOWN

### Crate-config & scaffolding

| Path | Responsibility | Key items | Version | Tests |
|---|---|---|---|---|
| `fuzz/Cargo.toml` | The unpublished fuzz package manifest. `publish = false`, `[package.metadata] cargo-fuzz = true`. One `[[bin]]` per target (`test=false, doc=false, bench=false`). Path deps on model/error/storage/sync (+config at v1.1). Features: `remote` (forwards to `unblock-storage/remote`, v1.2). | `[dependencies]`, `[[bin]]` blocks, `[features] remote = ["unblock-storage/remote"]` | v1 (targets added per version) | n/a (config) |
| `fuzz/README.md` | How to run each target, install `cargo-fuzz`, seed corpus location, what each target proves (trace to NFR-16/7/8/3), and how `tests/` replays crash artifacts. | — | v1 | n/a |
| `fuzz/rust-toolchain.toml` *(or doc note)* | Pin the nightly used **only** for libFuzzer (sanitizer codegen needs nightly); the library crates stay on stable `1.96.0` (NFR-12). A documented, isolated nightly use. | toolchain channel pin | v1 | n/a |
| `fuzz/.gitignore` | Ignore `target/`, `artifacts/`, generated `coverage/`; **keep** `corpus/` (committed seed corpora) and crash artifacts referenced by regression tests. | — | v1 | n/a |

### Shared harness library (`fuzz/src/`)

| Path | Responsibility | Key items | Version | Tests |
|---|---|---|---|---|
| `fuzz/src/lib.rs` | Harness crate root. `#![forbid(unsafe_code)]`, `#![warn(missing_docs)]` (NFR-9). Re-export the harness modules; expose `tokio_block_on(fut)` (a `current_thread` runtime to drive async `Storage` from the sync libFuzzer entrypoint). | `pub use cursor::*; pub use workspace::*; pub use invariants::*; pub use arbitraries::*;` `pub fn tokio_block_on<F: Future>(f: F) -> F::Output` | v1 | unit: `tokio_block_on` returns value; nested-future smoke |
| `fuzz/src/cursor.rs` | Deterministic byte-driven sampler (port of original `common.rs`), retargeted to unblock model enums. Drives structured choices from raw `&[u8]`. | `pub struct ByteCursor<'a>`; `next_byte/next_u16/next_bool/usize/bytes/text/optional_text`; trait `CursorExt { fn prefix(&mut self)->&'static str; fn status(&mut self)->Status; fn issue_type(&mut self)->IssueType; fn dep_type(&mut self)->DependencyType }` (uses model open-enum `Custom` tails) | v1 | unit: empty input → all-zero deterministic; `status()`/`issue_type()`/`dep_type()` cover every variant incl. `Custom`; same bytes → same sequence |
| `fuzz/src/arbitraries.rs` | `arbitrary::Arbitrary` impls / builders producing **structurally-valid-ish** `Issue`/`Dependency`/`Comment` so targets explore the typed surface (complement to raw-bytes targets). Clamps priority 0..=4, non-empty title, unique sorted labels, dep keys deduped — mirrors original `normalize_issue`. | `pub fn arbitrary_issue(c:&mut ByteCursor)->Issue`; `arbitrary_issues(c,n)`; `normalize_issue(Issue)->Issue` (id sanitize, title non-empty, priority clamp, status-field coherence, relation dedup) | v1 | unit: `normalize_issue` output passes `IssueValidator::validate`; idempotent (`normalize(normalize(x))==normalize(x)`); priority always in range |
| `fuzz/src/workspace.rs` | Temp `.unblock/` workspace builder + the **outside-sentinel** directory used to prove path-confinement (NFR-7). Unix symlink-escape setup; `assert_outside_untouched`. Opens a **local** libsql `Storage` via `unblock-storage` (default build, no remote). | `pub struct FuzzWorkspace { unblock_dir, outside_dir, outside_sentinel,.. }`; `pub fn new(c:&mut ByteCursor)->Result<Self>`; `pub async fn open_local_storage(&self)->Result<Arc<dyn Storage>>`; `pub fn assert_outside_untouched(&self)->Result<()>`; cfg(unix) symlink helpers | v1 | unit: `new` creates `.unblock/`; sentinel readable; `assert_outside_untouched` passes on untouched, fails on tampered |
| `fuzz/src/invariants.rs` | Reusable post-condition assertions shared by every storage/sync target. Ported & retargeted from original `assert_storage_invariants`/`assert_import_validation` to the async `Storage` trait, `sync_equals`, and `StructuredError`/`ErrorCode` boundary. | `pub async fn assert_storage_invariants(&dyn Storage)->Result<()>` (every dep/label/comment references a live id; no dup edges; tombstones not resurrected); `pub async fn assert_import_validation(&dyn Storage)`; `pub fn assert_nonempty_structured_error(&StructuredError)` (code maps to a valid `ErrorCode`/exit code, message non-empty, `retryable == code.is_retryable()`); `pub fn assert_jsonl_parseable(path)` | v1 | unit: invariants pass on a hand-seeded good store; each assertion fails on a deliberately-broken fixture |

### Fuzz targets (`fuzz/fuzz_targets/`)

| Path | Responsibility | Key items / invariants asserted | Version | Fuzz target + seed corpus |
|---|---|---|---|---|
| `fuzz/fuzz_targets/content_hash.rs` | Drive an `Issue` from bytes; assert `unblock_model::compute_content_hash` is **total, deterministic, format-stable, and field-scoped** per spine §1.8. Absorbs the *pure* invariants of the dropped `merge_issue` target. | `fuzz_target!(|data|)`; build issue via `arbitrary_issue`; skip if `IssueValidator::validate` errs; assert: 64-char lowercase-hex shape; deterministic (twice equal); **compact/pretty/reordered/CRLF JSON round-trip** preserves hash; unknown JSON fields ignored; empty-`Option` ≡ `None` for hashed fields; **ignored fields** (id, timestamps, tombstone/compaction fields, `estimated_minutes`, relations) do **not** change hash; meaningful title/desc change **does** change hash; `sync_equals` reflexive & consistent with hash on hashed-field equality | **v1** | `corpus/content_hash/` seeds: minimal issue, all-Custom-enum issue, unicode/NUL title, max-length fields |
| `fuzz/fuzz_targets/jsonl_roundtrip.rs` | The FR-7/FR-8 safe-import contract. Seed a sentinel store, write fuzz bytes as `.unblock/issues.jsonl`, run `unblock-sync` import; on `Ok` assert storage invariants + import validation; on `Err` assert **zero mutation** of the pre-existing sentinel snapshot and a non-empty `StructuredError`. Then reopen DB and re-check invariants (durability). | `fuzz_target!` over `tokio_block_on`; `FuzzWorkspace`; seed sentinel parent/child (+label/dep/comment); `before = snapshot`; `import_jsonl(path, ImportOptions{dry_run sampled})`; match Ok/Err → `assert_storage_invariants` / `before==after`; reopen-and-recheck; tombstone-never-resurrected (a non-tombstone line for a tombstoned id is rejected/skipped, not applied) | **v1** | `corpus/jsonl_roundtrip/` seeds: valid line, malformed JSON, git conflict markers (`<<<<<<<`/`=======`/`>>>>>>>`), CRLF, duplicate line, tombstone-resurrection attempt, prefix-mismatch line |
| `fuzz/fuzz_targets/sync_cycle.rs` | Full export→mutate-jsonl→preflight→import→re-export cycle over a fuzz-seeded local store, including **path-confinement** (NFR-7): reject `.git/`, `..`, external abs paths, symlink escape; reject conflict markers (NFR-8); atomic export never corrupts existing file on injected error (NFR-4). Asserts outside-sentinel untouched throughout. | `fuzz_target!` over `tokio_block_on`; seed store; `export_jsonl` (atomic temp+fsync+rename); `mutate_jsonl` (8 mutation modes incl. conflict markers, truncation, garbage append, CRLF, dup-line); `preflight`/conflict-scan via sync; reject git/external/symlink paths (assert refusal + path not created); forced re-export; `assert_outside_untouched`; reopen DB invariants | **v1** | `corpus/sync_cycle/` seeds: carry forward selected original corpus inputs (incl. `force_flag_combinations`), plus symlink-escape & nested-jsonl-path cases |
| `fuzz/fuzz_targets/bd_import.rs` | FR-26/D16 one-shot `bd`→unblock import path (`import_bd` → JSONL → import) is **idempotent** (dedup by `content_hash`) and **best-effort** (unmapped fields reported, never panic). Re-running import twice yields identical store; reported `dropped_fields` is stable. | `fuzz_target!` over `tokio_block_on`; generate a `bd`-shaped JSONL from bytes; `import_bd` once → snapshot; `import_bd` again → assert store equal (idempotent via `content_hash`); assert `ImportReport.dropped_fields` deterministic; never resurrect tombstone | **v1** | `corpus/bd_import/` seeds: real dogfood `bd-export` sample (sanitized), unmapped-field line, duplicate-`external_ref` line |
| `fuzz/fuzz_targets/config_toml.rs` | Parse fuzz bytes as `.unblock/config.toml` through `unblock-config`; assert parse is total (Ok or non-empty `StructuredError` mapping to `ConfigParseError`/`ConfigError`, never panic), and that layered precedence (CLI > env `UNBLOCK_*` > project TOML > defaults) is well-formed when the parse succeeds. **v1 = minimal smoke** (parse never panics); **v1.1 = full precedence assertions** when `unblock-config` full layering lands. | `fuzz_target!`; write bytes to temp `config.toml`; `unblock_config::load(...)` → assert Ok or `assert_nonempty_structured_error`; (v1.1) build env/CLI overlays from bytes, `assert_config_precedence` | **v1** (smoke) → **v1.1** (full) | `corpus/config_toml/` seeds: valid config, partial config, unknown keys, invalid types, oversized values |
| `fuzz/fuzz_targets/query_filters.rs` | Storage query surface (FR-4) under fuzzed `ListFilters` + fuzzed seeded corpus: `list/ready/blocked/search/count/stale` never panic, return consistent subsets, and `search`/`count` respect caps; results are a subset of seeded ids; `ready` excludes blocked/deferred/closed. (Port of original `search_issues`, broadened to the consolidated query surface.) | `fuzz_target!` over `tokio_block_on`; thread-local seeded `FuzzWorkspace`+store (built once); fuzz `ListFilters` (status/type/assignee/labels_all/any/priority range/text/limit/offset) + a search query; assert each query Ok & subset & `ready`-exclusion & `count` buckets sum-consistent | **v1** | `corpus/query_filters/` seeds: empty filter, all-status filter, label AND+OR combos, unicode search query |
| `fuzz/fuzz_targets/claim_race.rs` | FR-2 atomic-claim correctness fuzz: from one seeded issue, spawn N fuzz-ordered concurrent `claim` futures on the same `Storage`; assert **exactly one winner**, all losers get `ErrorCode::AlreadyClaimed`, final state is `in_progress` with the winner's assignee, and **no hot-spin** sentinel (bounded wall-time per case). Lightweight in-process complement to the M0 contention lab (NFR-3). | `fuzz_target!` over a multi-thread `tokio` runtime; seed issue; derive N + interleave from bytes; `join_all` claims; assert one `Ok`, rest `AlreadyClaimed`; assert single mutation (no lost/duplicate `in_progress`) | **v1.1** (after engine/claim hardened in M1; v1 covers claim via storage contract suite, fuzz adds it once stable) | `corpus/claim_race/` seeds: 2-agent, 8-agent, 64-agent interleavings |
| `fuzz/fuzz_targets/remote_sync.rs` | (Behind `remote` feature, v1.2) Fuzz the libsql remote/replica sync path with a `wiremock`-backed server (NFR-16): malformed/partial/duplicated server responses never panic, offline-first queue-and-reconcile holds, jittered backoff (`backon`/`tokio-retry`) never hot-spins, credentials never read from `config.toml`. | `#[cfg(feature="remote")] fuzz_target!` over `tokio_block_on`; `RemoteStorageHarness` (wiremock); fuzz response bodies/latency/status; assert reconciliation idempotent (`assert_sync_idempotent`), bounded retry count, no panic | **v1.2** | `corpus/remote_sync/` seeds: 200/409-conflict/5xx/truncated-body responses |
| `fuzz/fuzz_targets/scale_ingest.rs` | (v1.3) Large-corpus ingestion fuzz: import a fuzz-sized batch (up to the 1M-scale shape) and exercise compaction/archival round-trip (D12) — assert `compaction_level` round-trips through JSONL and that compacted issues still satisfy `sync_equals` round-trip fidelity. | `fuzz_target!` over `tokio_block_on`; `large_corpus(bytes)`; bulk import; compact; export+reimport; `assert_compaction_roundtrip` (`compaction_level None≡0`); invariants hold at scale | **v1.3** | `corpus/scale_ingest/` seeds: small batch, batch with compacted issues |

### Regression / replay tests (`fuzz/tests/`)

| Path | Responsibility | Key items | Version | Tests |
|---|---|---|---|---|
| `fuzz/tests/regression.rs` | Deterministic **non-nightly** replay of saved crash artifacts + curated corpus through each target's `run_*_case` core (factored out of the `fuzz_target!` macro so it is callable from a normal `#[test]`). Runs in standard `cargo test` (stable) so the workspace CI guards against regressions **without** the libFuzzer toolchain. | `#[test] fn replays_content_hash_artifacts()` … one per target; iterate `corpus/<target>/` + `artifacts/<target>/`; call the extracted `run_case(bytes)` and assert `Ok` | v1 (grows per target as targets land) | the tests themselves; plus a `proptest!` smoke per target generating random inputs to `run_case` for extra coverage on stable |

> To make `tests/regression.rs` possible, each `fuzz_targets/<t>.rs` keeps its body as a thin `fuzz_target!(|d| assert!(run_case(d).is_ok()))` wrapper over a `run_case` exported from `fuzz/src/<t>_core.rs` (or co-located `pub fn` in `src/`). This is the single most important structural rule of this crate: **all logic lives in `src/`, the `fuzz_targets/*.rs` are 5-line wrappers**, so corpus replay is testable on stable Rust.

---

## Crate-level test & bench plan

- **Fuzz runs (manual / scheduled, nightly toolchain):** each `[[bin]]` is runnable via `cargo +nightly fuzz run <name> -- -max_total_time=…`. CI runs a **short timed smoke** (e.g. 60s/target) on a scheduled (nightly) job only — never on the stable PR gate (keeps NFR-12 stable-Rust default path clean).
- **Regression replay (stable, every PR):** `fuzz/tests/regression.rs` replays committed `corpus/` + `artifacts/` through the extracted `run_case` functions under plain `cargo test`. This is the hard gate; new crashes get their artifact committed here.
- **proptest smoke (stable):** thin `proptest!` wrappers over each `run_case` give randomized coverage on the stable PR gate without libFuzzer.
- **No criterion benches** in this crate — performance benchmarking belongs to the crates under test (`unblock-storage`/`unblock-engine` own NFR-1 criterion budgets; the NFR-3 contention lab lives in `unblock-storage`'s M0 gate). `unblock-fuzz` is correctness/robustness only. (v1.3 may add a `large_corpus` generator reused by the storage scale harness, but the bench itself stays in storage.)
- **Coverage:** `cargo fuzz coverage <target>` documented in README for periodic corpus-quality review; not gated.
- **Workspace integration:** crate is a `[workspace]` member but **excluded from `default-members`** so `cargo build`/`cargo test` at the root never require nightly/libFuzzer; `forbid(unsafe_code)` + clippy pedantic still apply (NFR-9).

---

## Open questions specific to this crate

1. **Async-in-libFuzzer model.** libFuzzer's entry is sync; `Storage` is `async_trait`. Plan uses a per-case `current_thread` `tokio` runtime via `tokio_block_on`. Confirm this is acceptable (vs. a shared lazily-initialized runtime) for the storage/sync targets, and that `claim_race` may use a `multi_thread` runtime — needs a ruling on runtime reuse vs. fresh-per-case (corpus determinism prefers fresh; throughput prefers shared).
2. **`config_toml` v1 vs v1.1 scope.** v1 ships only a config *subset* (no DB/user layers). Should the v1 `config_toml` target be a parse-only smoke, deferring all precedence assertions to v1.1 (current plan), or should even the subset precedence (CLI > env > project > defaults) be fuzzed in v1?
3. **`markdown_import` fate.** FR-1a mentions "bulk markdown import" but the spine defines no markdown parser surface. Where does that parser live (engine? a model helper?), and does it warrant a fuzz target at all, or is JSONL the only untrusted bulk-ingest path in v1? (Currently deferred/undecided.)
4. **`bd_import` corpus sourcing.** FR-26 idempotency fuzz wants a real `bd-export` sample. Can a sanitized dogfood `bd` export be committed as a seed corpus, or must it be synthesized to avoid leaking internal issue text?
5. **Dropped `merge_issue` invariants — fully covered?** Confirm that folding three-way-merge's *kept* invariants (determinism, validator-on-output, tombstone-no-resurrect, content_hash freshness) into `content_hash` + `jsonl_roundtrip` + `bd_import` leaves no untested behaviour now that 3-way merge is gone (D5). If `unblock-sync` retains *any* reconciliation (v1.2 remote), a dedicated reconciliation target may be needed earlier than v1.2.
6. **Nightly pin location.** Should the fuzz nightly live in `fuzz/rust-toolchain.toml` (scoped) or be invoked ad-hoc via `cargo +nightly` in CI only, to avoid any chance of the workspace picking up nightly? (Plan favors scoped `fuzz/rust-toolchain.toml`, but it must not shadow the root stable `1.96.0` pin.)
