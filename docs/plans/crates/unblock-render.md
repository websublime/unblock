# unblock-render — File-level Plan

- **Status:** DRAFT — for review. Conforms to `docs/plans/01-design-spine.md` (AUTHORITATIVE) + `docs/PRD.md` (APPROVED) + `docs/plans/00-roadmap.md`. Source of truth: **PRD APPROVED v1.1** (G-19).
- **Date:** 2026-06-19
- **One-line purpose:** Output/format layer — turn engine/domain result types into byte-stable serialized output (`json` / `robot` / `plain` / `csv` / `markdown`; `toon` feature-gated v1.1) behind a single `Renderer` trait. **Reduced under D7** (no rich terminal stack: no `rich_rust`/`crossterm`/`indicatif`).
- **Layer:** L6.
- **Depends on:** `unblock-model` (L0), `unblock-error` (L0) **only** — per PRD §8.1 / spine §0 layering. Plus leaf crates: `serde`/`serde_json` (json/robot), `csv` (RFC-4180 escaping), `chrono` (timestamp formatting). **No** dependency on `engine`/`storage`/`mcp`/`cli` (would be a back-edge — forbidden by NFR-15). The MCP and CLI crates (L7) depend **on** render, not the reverse.

> **Critical scope note (D7 / spine §"render").** MCP is PRIMARY (D2) and rendering is "the MCP client's job" (D7). `unblock-mcp` returns the structured per-tool outputs (spine §5.3, D25) as JSON/`JsonSchema` directly — it does **not** route through this crate's human formats. `unblock-render` therefore exists mainly for **`unblock-cli`** lifecycle/ops output (mcp/migrate/doctor/version diagnostics) and for the small set of human-facing/CSV/markdown exports that are not the MCP structured surface. The render crate is deliberately thin in v1; the original's ~3.2k-LOC `format/` + ~2.3k-LOC `output/` rich stack collapses to a small trait + a handful of format backends. **This crate never writes to a file** (atomic JSONL export is `unblock-sync`'s job, FR-7/NFR-4) and never touches stdout/stderr directly — it returns `String`/bytes; the caller (cli) owns the stdout/stderr split (NFR-14).

---

## 1. Public API summary (what other crates import)

### v1
- `Renderer` trait — an **object-safe** dispatch with one method per renderable kind: `issue` / `issues` / `counts` / `dep_tree` / `cycles` / `structured_error` / `diagnostics`, each `&self` + a typed reference + `&RenderOptions` → `Result<RenderOutput, RenderError>`, plus `format() -> OutputFormat` (see §3 `src/renderer.rs`). `fn renderer_for(fmt, opts) -> Box<dyn Renderer>` is the factory.
- `OutputFormat` — **RE-EXPORTED** from `unblock-model` (`pub use unblock_model::OutputFormat`), **not defined here** (G-7 / CF-J, spine §1.10). Variants `Json | Robot | Plain | Csv | Markdown` (`Toon` variant `#[cfg(feature = "toon")]`, behaviourally v1.1) and its full derive set (`Debug, Clone, Serialize, Deserialize, JsonSchema, PartialEq, Eq`) are owned by the model crate. Render adds the **parse/precedence helpers** locally (`FromStr`/`Display`, env-string parse mapping `UNBLOCK_OUTPUT_FORMAT`, D10) — implemented as inherent/extension helpers in `src/format.rs`, not a second enum.
- `RenderOptions` — color off (D7: always no-color/plain), max-width, CSV field selection, pretty-json toggle, timestamp format. **No TTY detection** (caller decides; render is pure).
- `RenderOutput` — `{ stdout: String, content_type: ContentType }` (pure value; caller routes to stdout, diagnostics to stderr — NFR-14). v1 has no stderr payload (errors are `Err`).
- `RenderError` (snafu, per-crate, D4) + inherent `fn code(&self) -> unblock_error::ErrorCode` **+ `impl unblock_error::CodedError for RenderError`** (D27/AF-4, T3.1 — additive; delegates to the inherent `code()`) so the uniform `(&err).into()` L7 bridge covers it. **4 variants** (T3.1 adds the 4th, `UnknownFormat { name: String } → ValidationFailed`).
- `sanitize::{sanitize_inline, sanitize_text}` — terminal-control escaping for untrusted strings (ported from original `format/text.rs`; NFR-18 untrusted-input hygiene even for human output).
- `pick_format(cli, env, config_default) -> OutputFormat` — pure precedence resolver (CLI > env `UNBLOCK_OUTPUT_FORMAT` > config default > `Json` for cli-lifecycle). Mirrors FR-13 precedence shape but is a pure function (config values passed in; render does not read env/files itself — keeps it pure & testable).

> **Type-ownership note (spine §1.10 / §6.1).** `RenderOptions`, `RenderOutput`, `ContentType`, the `Renderer` trait, `RenderError`, and `pick_format` are **render-private, L6-internal** types — they are **NOT** spine §1.10 cross-crate contract types and carry **no** §1.10 derive obligation (no `JsonSchema`/`Serialize` required on them). Only the **re-exported §1.10 DTOs** (`OutputFormat`, `CountBucket`, `GraphEdge`, `DepTree`, `CloseOutcome`, `ImportReport`, `ExportReport`, `DiagnosticReport`, `DiagnosticFinding`, `DiagnosticKind`) and `StructuredError` (spine §2.4) are bound by §6.1 exact-shapes and the §1.10 derive policy. The render contract suite (§6.1 of this plan) binds only those re-exported DTOs' parse-back, never the private types.

### v1.1
- `toon` feature lights up `OutputFormat::Toon` + the `ToonRenderer` backend (D12, roadmap §2 "TOON output").
- Render support for the new v1.1 domain surface that the CLI/MCP may need to display: `Comment`, `EpicStatus`, label lists, `GateResult`, scheduler/coordination report value types (csv/markdown/plain views). Additive only.

### v1.4
- Large-result formatting helpers (streaming/chunked CSV + markdown writer over an iterator, not a `Vec`) for the 1M-issue / batch surface (roadmap §5, `unblock-render` ◐). Additive: `render_stream_csv(iter, …) -> impl` writer-based API alongside the v1 `Vec`-based one.

> **No v1.2 participation** (roadmap §9: render is blank for v1.2 — remote/replica sync touches storage/config/health, not formatting).

---

## 2. Crate-level conformance invariants

1. `#![forbid(unsafe_code)]`, `#![warn(missing_docs)]`, clippy pedantic (workspace lints) — spine §0.
2. Depends on **model + error only** (+ pure leaf format libs). No back-edge to L5/L7 (NFR-15; CI layering check).
3. **No I/O, no global state, no stdout/stderr writes** — every entry point returns `Result<RenderOutput, RenderError>` or `Result<String, RenderError>`. Caller owns the stdout/stderr split (NFR-14) and any file write (NFR-4 is sync's, not render's).
4. **Byte-deterministic** for a fixed input + fixed `RenderOptions` (FR-7 export determinism spirit; NFR-14 snapshot-stable). No `HashMap` iteration-order leaks (use the caller's `Vec` order / `BTreeMap` where a map is unavoidable). **All** `DateTime<Utc>` values are formatted through `fmt_ts`, which delegates to the single source-of-truth `unblock_model::fmt_ts_secs` (= `to_rfc3339_opts(SecondsFormat::Secs, true)`; lifted to L0 at T2.4/FORK-4 so render + sync share it) — UTC, second precision, `Z`. **No backend may call `to_rfc3339()` directly** (it emits sub-seconds / offset and breaks both determinism and the T2.4 export-byte coherence — see §5 item 5).
5. **Untrusted-string hygiene (NFR-18; spine §2.4:503).** Every human/text format (`plain`/`markdown`/`csv`) routes user-controlled strings through `sanitize::*` before embedding: `Issue` fields (title, labels, assignee, owner, description, notes, …), the open-enum `Status`/`IssueType`/`DependencyType` `as_str()` labels (incl. their `Custom` arms — untrusted), embedded issue IDs, **and** — for `structured_error` — **every `StructuredError.context` VALUE**. The `message`/`hint` fields arrive **pre-sanitized** from the L0 §2.4 chokepoint and are NOT re-sanitized; **context values do NOT** (the chokepoint leaves them raw — JSON-safe only), so plain/markdown/csv MUST pass each context value's string form through `sanitize_inline` (single-line) before embedding. The ONLY values left un-sanitized are machine-generated and control-free by construction: timestamps (via `fmt_ts`), `priority` (bare int `.0`), numeric `Option<i32>` columns, and bool columns. `json`/`robot` rely on `serde_json` escaping (no extra sanitize); `csv` additionally applies RFC-4180 quoting + the formula-injection guard. **Contract+snapshot test obligation:** a `StructuredError` carrying an ESC/BEL byte in a `context` value, rendered as plain/markdown/csv, has that byte **escaped** (`\u{1b}`) in the output — never raw.
6. `Robot` == compact single-line JSON to stdout (machine-readable, diagnostics elsewhere); `Json` == pretty JSON. Both reuse the same `serde_json` path; robot is just `to_writer` (no pretty).
7. TOON behind `#[cfg(feature = "toon")]`, default-off; default build has **zero** TOON transitive surface (NFR-10).

---

## 3. FILE BREAKDOWN

Legend for **Version**: `v1` introduced in v1; `v1.1` / `v1.4` introduced or changed in that release.

| Path | Responsibility | Key items (reference spine where noted) | Version | Tests |
|---|---|---|---|---|
| `Cargo.toml` | Crate manifest. `[dependencies]`: `unblock-model`, `unblock-error`, `serde`, `serde_json`, `chrono`, `snafu` (the per-crate error-enum convention, CLAUDE.md "Errors: snafu, per-crate"; the locked `RenderError` is `#[derive(Snafu)]`) — **`csv` is NOT a runtime dep** (the CSV write path is **hand-rolled**, decision #2: a verbatim port of the original `escape_field` + `writeln!` over an in-memory `String`). `[dev-dependencies]`: `insta`, `proptest`, `criterion`, **`csv`** (the contract suite re-parses emitted rows with `csv::Reader` to assert RFC-4180 well-formedness + cell count). `[features] default=[]`, `toon=["unblock-model/toon"]` — **must propagate the model marker feature** (`unblock-model/toon`, `crates/unblock-model/src/output.rs:26`) so the re-exported `OutputFormat::Toon` variant is visible under `--features toon`. The `dep:toon` encoder-crate token is **NOT** added in v1 (the encoder lands in v1.1, see §5 item 4); the v1 line is exactly `toon = ["unblock-model/toon"]`. Feature name pinned **`toon`** per G-24b / spine §1.10. `[[bench]] name="render" harness=false`. Workspace lints inherited. | feature table; `forbid(unsafe_code)` | v1 (toon encoder added v1.1) | n/a (built by all suites) |
| `src/lib.rs` | Crate root: lint attrs, module decls, re-exports (the §1 public API). Crate-level docs with a usage doctest. | `pub use unblock_model::OutputFormat; pub use renderer::*; pub use options::*; pub use format::*; pub use error::*; pub use sanitize::{sanitize_inline, sanitize_text};` (`format::*` re-exports the parse/precedence helpers, **not** an `OutputFormat` enum — that comes from model, G-7) | v1 | doctest: render a 1-issue list as `Json` and assert it parses back; doctest compiles & runs |
| `src/error.rs` | Per-crate snafu error enum (D4) + `ErrorCode` mapping. **4 variants** (T3.1/D27/AF-4 adds the 4th `UnknownFormat`; the CSV write path stays infallible — decision #2 dropped the `csv` runtime dep, so there is still no `Csv`/`IoError` path). `#[derive(Debug, Snafu)] #[snafu(visibility(pub(crate)))] pub enum RenderError { Serialize{source: serde_json::Error}, UnsupportedFormat{format: OutputFormat}, FieldUnknown{field: String}, UnknownFormat{name: String} }`; the enum + variants + `code()` are `pub` (cli/mcp match on them), the context selectors are `pub(crate)`. **`UnknownFormat { name: String }` (T3.1/D27/AF-4)** carries the RAW offending format name from `parse_format` — DISTINCT from `UnsupportedFormat` (a KNOWN format that cannot render a particular result kind); `#[snafu(display("unknown output format: {name}"))]`, placed after `FieldUnknown`. `impl RenderError { pub fn code(&self) -> unblock_error::ErrorCode }` (Serialize→`JsonError`; UnsupportedFormat/FieldUnknown/**UnknownFormat**→`ValidationFailed`). **`impl unblock_error::CodedError for RenderError`** (T3.1/D27/AF-4 — additive, delegates to the inherent `code()`) so the uniform L7 `(&err).into()` bridge covers it; render already depends on `unblock-error` and `RenderError: Snafu` yields `Error + Display` (the blanket-`From` bounds). **`code()`/module-doc/doctest updates:** the module-doc "exactly **three** variants" → "exactly **four** variants" (the CSV-infallible prose stays true), and the type doctest ADDS one line asserting `RenderError::UnknownFormat{name:"xml".into()}.code() == ValidationFailed`. No `ToonEncode` variant in v1 (no encoder; added in v1.1 alongside the encoder, mapping to `JsonError`). No new `ErrorCode`; spine §2.3 exit table + §6.5 UNCHANGED. | v1 (4th variant + `impl CodedError` at T3.1/D27) | unit: each variant → correct `ErrorCode` → correct exit code (8/4/4/4, cross-check spine §2.3 table); golden mapping snapshot; a doctest covers `UnknownFormat`; the blanket `StructuredError: From<&RenderError>` compiles |
| `src/format.rs` | `OutputFormat` parsing/precedence **helpers** (the enum itself is re-exported from `unblock-model`, G-7) + the single timestamp helper. | `pub use unblock_model::OutputFormat;` then `Display`/`FromStr` (case-insensitive) + `parse_format(&str) -> Result<OutputFormat, RenderError>`; `parse_env_value(&str)`; `pick_format(cli, env, cfg_default)` precedence resolver (CLI > env `UNBLOCK_OUTPUT_FORMAT` > config > default). Default for cli-lifecycle = `Json`. **`parse_format` unknown-name arm (T3.1/D27/AF-4):** the `_ =>` arm returns `RenderError::UnknownFormat { name: value.trim().to_string() }` — carrying the RAW (trimmed) name the caller passed, REVERTING the current mislabeled placeholder `UnsupportedFormat{format: OutputFormat::Json}` that LOST the name (`UnsupportedFormat` returns to its true kind-not-supported meaning). Update the `parse_format` doc + its `# Errors` line to `Returns [RenderError::UnknownFormat]`. `parse_env_value`/`pick_format`/`FormatName: FromStr` are UNCHANGED — they route through `parse_format`, and `parse_env_value` maps `.ok()` so an unknown env value still falls through. **ZERO `parse_format` call-sites outside `unblock-render` (grep-confirmed — mcp never calls it); the T3.1 cli `-o` value_parser is the only consumer**, so the 4th-variant blast radius is confined to this crate. **`fmt_ts(dt: DateTime<Utc>) -> String`** now DELEGATES to `unblock_model::fmt_ts_secs(dt)` (FORK-4/D-OQ-B/D23 — the canonical formatter is LIFTED into `unblock-model` L0 as the single source of truth; render keeps the `fmt_ts` name as a thin wrapper so all backends and their snapshots are byte-unchanged, and the `use chrono::{…SecondsFormat…}` import drops `SecondsFormat` if unused locally). Render no longer owns the `to_rfc3339_opts` call site. Still the ONLY timestamp path any backend may use (§5 item 5; A.OQ-B). The `#[cfg(feature="toon")] Toon` variant is gated in the model definition; render only lights up its backend. | v1 (Toon variant cfg-gated; behaviour v1.1) | unit: round-trip `Display`↔`FromStr` for all variants; `parse_env_value` aliases (`text`→`Plain`, `plain`→`Plain`); precedence matrix (CLI overrides env overrides cfg overrides default) — proptest over the 4 layers; unknown string → error; `fmt_ts` second-precision/UTC/`Z` snapshot (byte-identical to the model delegate) |
| `src/options.rs` | Render configuration value type (pure). | `struct RenderOptions { pretty_json: bool, max_width: Option<usize>, csv_fields: Option<Vec<String>>, timestamp_secs_only: bool }`; `Default`; builder-style setters. **No color/TTY field (D7 — always plain).** `struct RenderOutput { stdout: String, content_type: ContentType }`; `enum ContentType { Json, Text, Csv, Markdown, Toon }`. | v1 | unit: defaults are plain/no-color; builders mutate expected field |
| `src/sanitize.rs` | Terminal-control escaping for untrusted strings (port of original `format/text.rs::sanitize_terminal_*`, NFR-18). **Hand-off from T0.4 / `unblock-error`:** `unblock-error` ships ONLY the `\n`/`\t`-**preserving** `sanitize_message` (= original `sanitize_terminal_text`) for the structured-error `message` field. The **stricter inline variant** that ALSO escapes `\n`/`\t` (`sanitize_inline` here = original `format/text.rs::sanitize_terminal_inline`, lines 40–42) is the render crate's responsibility for single-line display fields (title/labels/assignee on one line) — it must be **ported here** so it does not vanish in the render port. The two sanitizers are intentionally split across the two crates by layout-control policy; do NOT collapse them. | `pub fn sanitize_inline(&str) -> Cow<str>` (escape all C0/C1/ESC/OSC **including `\n`/`\t`**; port of original `sanitize_terminal_inline`); `pub use unblock_error::sanitize_message as sanitize_text;` (the layout-preserving variant is **re-exported, not copied** — one definition only, per CLAUDE.md "defined once, re-exported"); `Cow`-borrow fast path when unchanged. | v1 | unit: ANSI/ESC/BEL/DEL escaped to `\u{..}`; plain ASCII borrows (no alloc); `\n`/`\t` preserved by `text`, escaped by `inline`; a context value containing `\x1b`/`\x07` is escaped (not raw); proptest: output never contains a raw control byte except allowed-layout; fuzz target (see §4) |
| `src/renderer.rs` | The `Renderer` trait + format dispatch. The crate's contract. | `trait Renderer { fn format(&self) -> OutputFormat; fn issue(&self, &Issue, &RenderOptions) -> Result<RenderOutput, RenderError>; fn issues(&self, &[Issue], …); fn counts(&self, &[CountBucket], …); fn dep_tree(&self, &DepTree, …); fn cycles(&self, &[Vec<String>], …); fn structured_error(&self, &StructuredError, …); fn diagnostics(&self, &DiagnosticReport, …) }` — all input types from spine §1/§3/§2.4. `fn renderer_for(fmt: OutputFormat, opts: RenderOptions) -> Box<dyn Renderer>` factory. `structured_error` on `plain`/`markdown`/`csv` MUST route every `StructuredError.context` value's string form through `sanitize::sanitize_inline` before embedding (spine §2.4:503); `message`/`hint` are already pre-sanitized at L0 and pass through verbatim. | v1 | unit: factory returns a renderer whose `.format()` matches; trait-object dispatch compiles; per-method "renders without error" smoke for each (Issue/Vec/counts/tree/cycles/error/diag) × each format |
| `src/backend/mod.rs` | Backend submodule wiring. | `mod json; mod plain; mod csv_fmt; mod markdown; #[cfg(feature="toon")] mod toon;` | v1 (toon arm v1.1) | n/a |
| `src/backend/json.rs` | JSON + Robot backend (one impl, two modes). | `struct JsonRenderer { robot: bool, opts }` impl `Renderer`. Robot = `serde_json::to_string` (compact); Json = `to_string_pretty`. **Always-valid-JSON even on error** (FR-11): `structured_error` serializes `StructuredError` (spine §2.4). | v1 | insta snapshot: each result kind → pretty JSON + compact robot JSON; unit: `structured_error` produces valid JSON (parse-back); proptest: any `Issue` round-trips serialize→parse without panic; determinism: same input → identical bytes (no map-order flake) |
| `src/backend/plain.rs` | Plain text (human, no color — D7). Line-oriented issue/list/tree/count/error views. | `struct PlainRenderer { opts }`; helpers `format_issue_line`, `format_issue_long`, status/priority/type labels (ported from original `format/text.rs`, **stripped of color/icons-as-ANSI**; ascii labels only). ALL user strings — incl. `Status`/`IssueType` `as_str()` labels (incl. `Custom`), `DependencyType.as_str()` on dep-tree/cycles edges, and embedded issue IDs — via `sanitize_inline`; multi-line description/notes via `sanitize_text`; **every `StructuredError.context` value via `sanitize_inline`**. Timestamps via `fmt_ts`. | v1 | insta snapshot: issue line, long view, empty list, count table, dep-tree indent, cycle path, error block; unit: width truncation respects `max_width`; a context value containing `\x1b`/`\x07` is escaped (not raw) in the rendered error block; sanitize applied (inject ESC into title → escaped in output) |
| `src/backend/csv_fmt.rs` | CSV backend (RFC-4180). **Hand-rolled** port of original `format/csv.rs` (field selection + escaping; decision #2 — no `csv` runtime dep). | `struct CsvRenderer { fields: Vec<&'static str>, opts }`; `escape_field` ported **VERBATIM** (manual double-quote wrapping + quote-doubling + the formula-injection guard: a cell starting with `= + - @ \t \r \n` → `"'<escaped>"`); rows written via `writeln!` into an in-memory `String` (infallible — no `csv` crate, no `IoError`). `DEFAULT_FIELDS` (8, original order): `id, title, status, priority, issue_type, assignee, created_at, updated_at`. `ALL_FIELDS` (15, original order, faithful port — decision #3): `id, title, description, status, priority, issue_type, assignee, owner, created_at, updated_at, closed_at, due_at, defer_until, notes, external_ref`. `get_field_value` per original: `status`/`issue_type` `as_str()` routed through `sanitize_inline` then `escape_field` (untrusted `Custom` arms, MF-2); `priority` = bare int `priority.0.to_string()` (NOT `P{n}`); timestamps via `fmt_ts` (`Option` → empty cell when `None`); `id` + user-string columns sanitized before escaping. Unknown `--fields` → `RenderError::FieldUnknown` (typed error, NOT the original's silent filter). Only `issues`/`issue` produce CSV; other kinds → `RenderError::UnsupportedFormat`. | v1 | insta snapshot: default-fields CSV, **all-fields (15-col) column-pin** (HARD AC), bare-int priority pinned; unit: comma/quote/newline/formula-injection escaping; a context value is moot for CSV error kinds (CSV errs `UnsupportedFormat` on `structured_error`) — but a `Custom` status carrying ESC is escaped in the cell; unknown `--fields` → `FieldUnknown`; empty list → header-only; proptest: every emitted row re-parses with `csv::Reader` (dev-dep) to the same cell count |
| `src/backend/markdown.rs` | Markdown backend — **new views built here + the ported `escape_markdown`**. The original `format/markdown.rs` is only `render_markdown`/`strip_markdown`/`escape_markdown` for description CONTENT; it has **no** issue-detail / GFM-table / dep-tree / error / diagnostics renderers to copy, so those views are authored here (drop syntax highlighting, D7). | `struct MarkdownRenderer { opts }`; `escape_markdown` ported with the exact char set (`\` `` ` `` `* _ { } [ ] ( ) # + - . ! \| ~ >`); issue→detail section, issues→GFM table, dep tree→nested list, cycles→list, structured_error→block, diagnostics→table. Sanitize + markdown-escape user strings (incl. `Status`/`IssueType`/`DependencyType` labels and embedded IDs) AND **every `StructuredError.context` value**. Timestamps via `fmt_ts`. | v1 | insta snapshot: issue detail md, list table md, tree md, error block; unit: pipe/backtick/bracket escaping in titles; a context value containing `\x1b`/`\x07` is escaped (not raw) in the rendered error block; empty list → "no issues" note |
| `src/backend/toon.rs` | TOON (token-optimized) backend — **feature-gated** (D12, roadmap v1.1). | In v1 this file is **`#[cfg(feature="toon")]`-empty**: the whole module is gated, and under `--features toon` it provides only a `ToonRenderer { opts }` whose `Renderer` impl errs `UnsupportedFormat` for every kind (no encoder). **No `toon`/`toon_rust` crate dependency is added in v1** (A.OQ-C); the encoder, its `cargo-deny` audit, and the real impl land in v1.1. The `toon` feature in v1 only propagates `unblock-model/toon` so the re-exported `OutputFormat::Toon` variant + `ContentType::Toon` exist. | **v1.1** (file present, `#[cfg]`-gated in v1) | feature-off build has no `toon` symbol; `--features toon` compiles and the placeholder errs `UnsupportedFormat`; v1.1 adds list + counts TOON snapshots |
| `src/stream.rs` | Large-result streaming writers (CSV/markdown over an iterator + `io::Write`, not a `Vec`). | `pub fn render_stream_csv<W, I>(w: &mut W, rows: I, fields, opts)`; `render_stream_markdown_table(...)`. For 1M-issue/batch surface (roadmap §5). Additive; v1 `Vec` API stays. | **v1.4** | unit: streamed CSV byte-identical to the `Vec`-based `CsvRenderer` for the same input (equivalence test); large-input no-OOM smoke (10k rows into a sink); determinism |
| `tests/contract.rs` | **Render contract suite** — every `OutputFormat` × every result kind renders deterministically and (for json/robot/csv) re-parses. The crate's NFR-16-style contract gate. | parametrized over `OutputFormat::all()`; asserts: no panic, byte-stable across 2 runs, json/robot parse back to equal value, csv re-parses. | v1 (extended v1.1 for new kinds, v1.4 for stream) | the suite itself |
| `tests/snapshots/` (insta) | `insta` golden files for plain/markdown/csv/json/robot/toon outputs (CI `--check` gate, NFR-14/NFR-16). | one `.snap` per (format × representative fixture) | v1 (+toon v1.1) | `cargo insta test` |
| `tests/determinism.rs` | Byte-determinism + no-map-order-flake across all formats and repeated renders. | renders the same fixture N times, asserts identical bytes; shuffles input `Vec` order where order is defined and asserts stable sort where the spine defines one (ready hybrid sort lives in engine/policy, not here — render preserves caller order). | v1 | proptest |
| `tests/sanitize_fuzz_seed.rs` | Seed corpus + property bridge for the fuzz target (control-char escaping never panics / never emits raw control). | proptest over arbitrary strings → `sanitize_*` invariant. | v1 | proptest |
| `benches/render.rs` | `criterion` benches for the hot render paths (NFR-1 budget visibility for the cli/diagnostics path). | bench: render 1k / 10k issues as json, robot, csv, plain, markdown. Async not needed (render is sync/pure). | v1 (extended v1.4 with stream bench) | criterion baseline + 10% regression gate |

> **Fuzz target** (lives in the workspace `unblock-fuzz` crate, not here — PRD §8.1): `fuzz_targets/render_sanitize.rs` over `sanitize::{sanitize_inline, sanitize_text}` + `fuzz_targets/render_csv_escape.rs` over CSV field escaping. Listed here for traceability; the file lives in `unblock-fuzz` (NFR-16). Introduced v1.

---

## 4. Crate-level test & bench plan

- **Unit tests** — colocated `#[cfg(test)]` per backend (escaping edge cases, format parsing, error→code mapping).
- **Contract suite** (`tests/contract.rs`) — the render analogue of the Storage contract suite (NFR-16): for each `OutputFormat` × each renderable kind, assert (a) no panic, (b) byte-determinism across two runs, (c) parse-back equivalence for the structured formats (json/robot via serde, csv via `csv::Reader`). This is the gate that catches a format regressing.
- **insta snapshots** (`tests/snapshots/`) — golden output for plain/markdown/csv/json/robot (+toon when the feature is on); CI runs `insta --check` (NFR-14). Snapshots are the human-output stability gate.
- **proptest** — (1) `sanitize_*` never emits a raw control char (except allowed `\n`/`\t` in `text`) and never panics on arbitrary input; (2) `OutputFormat` `Display`↔`FromStr` round-trip; (3) any `Issue` json-renders→parses→equal; (4) `pick_format` precedence holds for all 4-layer combinations.
- **fuzz** — `cargo-fuzz` targets in `unblock-fuzz` over sanitize + CSV escaping (untrusted-input boundary, NFR-18).
- **criterion** (`benches/render.rs`) — 1k/10k issue render across formats; baseline + 10% regression gate (NFR-1). v1.4 adds the streaming-writer bench (1M-row sink).
- **Feature-matrix CI** — build & test both `--no-default-features` (default, no toon) and `--features toon` (v1.1) to keep the TOON surface off the default build (NFR-10) and prove the cfg-gated file compiles both ways.

---

## 5. Open questions specific to this crate

1. **~~Does the CLI actually need `plain`/`markdown`/`csv` in v1?~~ CLOSED (Miguel, T2.1 Decide).** v1 ships **all five backends** (`json`/`robot`/`plain`/`csv`/`markdown`) **plus** `sanitize` **plus** `pick_format`; the `toon` backend file is present but `#[cfg]`-empty (v1.1). **Deferring any backend is a FORBIDDEN simplification** (Hard rule "never simplify"). The binding acceptance criterion is **impl-plan line 58**: "json/robot/plain/csv/markdown behind a trait … (TOON feature-gated stub = v1.1)". No backend slips.
2. **~~Where does `DiagnosticReport` live and what is its shape?~~ RESOLVED (spine §1.10, CF-B).** `DiagnosticKind` (Stats/Info/Where/Version/Lint/Changelog/Orphans) and `DiagnosticReport { kind, findings: Vec<DiagnosticFinding> }` plus `DiagnosticFinding { label, detail }` are now defined normatively in `unblock-model` (spine §1.10). Render imports them directly — no back-edge.
3. **~~Where do the render-visible display/result DTOs live — model or engine/storage?~~ RESOLVED (spine §1.10, CF-A/CF-C — the single most important cross-crate decision for this crate).** All display/result DTOs render must format now live in `unblock-model`: `CountBucket` (+`CountGroupBy`), `DepTree` (+`GraphEdge`), `CloseOutcome`, `ExportReport`, `ImportReport`, `DiagnosticReport`. `unblock-storage`/`unblock-engine` **re-export** them; they do not define them. Render therefore stays **model + error only** with no spine conflict and no dependency-list widening.
4. **~~TOON crate sourcing?~~ DEFERRED to v1.1 (Miguel, T2.1 Decide).** T2.1 adds **only** the feature plumbing: the `toon = ["unblock-model/toon"]` feature (§3 Cargo.toml row), the `#[cfg(feature = "toon")]`-empty file `src/backend/toon.rs` (present, compiles under both feature states, contains no encoder), and the `ContentType::Toon` variant. **No `toon`/`toon_rust` crate dependency is added in v1** — the encoder, its `cargo-deny` audit, and the `ToonRenderer` impl land in v1.1. The v1 feature-matrix CI (`--no-default-features` AND `--features toon`) must both compile with the cfg-empty file.
5. **~~Timestamp format canonicalization?~~ CLOSED (Miguel, T2.1 Decide).** Render pins **ONE** timestamp helper: `fn fmt_ts(dt: DateTime<Utc>) -> String { dt.to_rfc3339_opts(SecondsFormat::Secs, /*use_z=*/ true) }` — **RFC-3339, UTC, `SecondsFormat::Secs`** (no sub-second; `Z` suffix). It lives in `src/format.rs` and is the **only** path any backend uses to render a `DateTime<Utc>` (csv/plain/markdown). This is an **intentional deviation from the original bytes** (the original CSV used `to_rfc3339()` with sub-second + offset, and the original text long-view used `%Y-%m-%d`) — Verify must NOT flag the resulting snapshot bytes as a regression vs the original. **Cross-task dependency (RECORDED): T2.4 `unblock-sync` JSONL export bytes MUST use the identical RFC-3339/`SecondsFormat::Secs`/UTC form** so human/csv views match export bytes — divergence is a T2.4 bug to reconcile, not a render change. **UPDATED (T2.4/FORK-4=B/D23):** the helper is LIFTED into `unblock-model::time::fmt_ts_secs` (L0) so `unblock-sync` (which cannot depend on render, L6) shares it; `fmt_ts` becomes a thin delegate. Byte form unchanged; snapshots stable.

---

## 6. Cross-crate dependencies assumed (summary)

- **Display-DTOs in `unblock-model` (CONFIRMED by spine §1.10):** `CountBucket` (+`CountGroupBy`), `DepTree`/`GraphEdge`, `CloseOutcome`, `DiagnosticReport`/`DiagnosticKind`/`DiagnosticFinding`, `ExportReport`/`ImportReport`, and `OutputFormat` are all **defined in `unblock-model` (spine §1.10)** and importable from there (plus `unblock-error` for `StructuredError`, spine §2.4). `unblock-storage` (spine §3.1) and `unblock-engine` (spine §4.1) **re-export** these — they no longer define them. Render keeps its **model + error only** dependency with no spine conflict (CF-A/CF-B/CF-C/CF-J resolved; Open Questions §5.2/§5.3 closed).
- **Derive contract (G-1 / spine §1.10 derive-policy):** every §1.10 DTO render parses back (json/robot contract suite, §2 invariant 6, §4) derives the **full set** — `Debug, Clone, Serialize, Deserialize, JsonSchema` (+`PartialEq, Eq` for the parse-back equality assertions). This is normatively guaranteed by spine §1.10; render's `tests/contract.rs` serialize→parse→`assert_eq` round-trip therefore compiles. `ExportReport.path: PathBuf` is `JsonSchema`-valid (string) per the same policy. Render itself adds **no** derives to these types (it owns none of them).
