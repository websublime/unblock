# unblock-engine — File-level Plan

- **Status:** DRAFT — for review. Conforms to `docs/plans/01-design-spine.md` §4 (authoritative) + `docs/plans/00-roadmap.md` + `docs/PRD.md` (PRD APPROVED v1.1). Where this file and the spine disagree, the spine wins; amend the spine first.
- **Date:** 2026-06-19

## Purpose / Layer / Depends-on

- **One-line purpose:** The single mutation home (FR-9) — a `Session` that **consumes a `WorkspaceContext` built by `unblock-config`** (CF-D: config performs `.unblock/` discovery, opens/migrates libsql, and builds the `Arc<dyn Storage>`; the engine does **not** construct storage itself) and composes storage + policy (+ optional sync/health) over one lifecycle (`open → import? → mutate → export? → recover`), serializing in-process writes through a tokio `Semaphore(1)` (D14) while reads bypass it (FR-10), with cooperative shutdown/logging (FR-17).
- **Layer:** L5 (`engine`). Acyclic invariant: `model|error → policy → storage → sync|health → config → engine → render → mcp|cli` (NFR-15).
- **Depends-on (PRD §8.1, exactly):** `unblock-config` (L4 — builds the `WorkspaceContext` incl. the `Arc<dyn Storage>` the engine consumes, CF-D), `unblock-sync` (L3), `unblock-storage` (L2 — used only via the `Storage` trait handed in through the context), `unblock-policy` (L1), `unblock-health` (L3), plus `unblock-model` + `unblock-error` (L0 — source of the relocated report/diagnostic/filter contract types, CF-A/CF-B/CF-C). External: `tokio` (`sync::Semaphore`), `async-trait` (consumed via `Storage`), `chrono`, `snafu`, `tracing`, `serde`/`schemars` (only for the report/outcome types that flow to L7). *(**No** `signal-hook`: OS signal-handler install lives in `unblock-cli` (L7) — OQ-4 RESOLVED; the engine only reads the cooperative shutdown flag.)* **No** libsql/backend type ever crosses this boundary (CF rule 2); **no** git crate / `Command::new("git")` (NFR-6).
- **Consumers:** `unblock-mcp` (L7) and `unblock-cli` (L7) are thin adapters; both call only the `Session` surface so behaviour cannot drift (FR-9, conformance rule 4).

## Public API summary (what L7 imports), per version

**v1** — `Session`; `SessionConfig`; the read methods (`get/list/ready/blocked/search/count/stale/dependency_tree/detect_cycles/diagnostics`); the mutation methods (`create/update/delete/claim/defer/undefer/add_dep/remove_dep/close_with_suggestions`); the dependency-graph read (`dependency_graph`); interchange (`export_jsonl/import_jsonl/import_bd`); the lifecycle ops `doctor`/`recover` and `shutdown` (OQ-2 RESOLVED — `doctor`/`recover` are public, spine §4.1); engine-owned `ImportOptions`; `EngineError` (+ `code()`); the `is_shutdown_requested` shutdown-flag surface (the `install_shutdown` handler-install API relocates to `unblock-cli`, OQ-4); `init_tracing`. The outcome/report/diagnostic types (`CloseOutcome`, `ImportReport`, `ExportReport`, `DiagnosticKind`, `DiagnosticReport`, `DiagnosticFinding`) are **re-exported from `unblock-model` §1.10** (CF-A/CF-B — relocated so `unblock-render` (model+error only) can format them); the engine does not define them locally. `DiagnosticFinding` travels inside `DiagnosticReport`, so the engine re-exports it explicitly (G-10) so consumers importing it via engine resolve from one place. Re-exports of the contract types L7 needs — also model-owned (CF-A/CF-C) — (`ListFilters`, `CountGroupBy`/`CountBucket`, `DepTree`/`GraphEdge`) plus storage-owned (`IssuePatch`, `DeletePlan`/`DeleteMode`) so adapters import them from one place.

**v1.1** — additive: `add_comment`/`list_comments`, `rename_label`/`list_all_labels`, `epic_status`; `scheduler`/`coordination_status` (delegating to `unblock-policy` contracts); workflow-gate enforcement inside mutating transitions (`gate_check`); saved-query resolve/run; audit flight-recorder hook (`interactions.jsonl`) wired through mutate; full health taxonomy report surface. All gated behind additive `contract_version` bumps (no breaking changes to v1 signatures).

**v1.2** — additive: write-topology under libsql remote/replica sync (D14 follow-up); sync-state surfaced in `DiagnosticReport`/health; engine resolves remote credentials via config (env/keychain, never `config.toml`).

**v1.3** — additive: claim TTL/heartbeat + active-coordination orchestration; compaction/archival orchestration; tracing perf spans for the scale gate.

---

## FILE BREAKDOWN

> Layout: a small, single-responsibility module set. `src/session/` holds the `Session` split by concern (lifecycle / read / write / interchange) sharing one struct via `impl Session` blocks. Tests are split between in-crate `#[cfg(test)]` unit tests and `tests/` integration + property suites driven by an in-memory/temp libsql backend (real `unblock-storage` libsql impl, NOT a mock — the engine's contract is "identical behaviour through one path").

### Crate manifest & root

| Path | Responsibility | Key items (spine refs) | Version | Tests |
|---|---|---|---|---|
| `Cargo.toml` | Crate manifest; deps per PRD §8.1; `[lints]` workspace inherit (`unsafe_code=forbid`, `missing_docs=warn`, clippy pedantic); features `sync`/`health` on by default, `remote` passthrough off (D15); dev-deps `proptest`, `insta`, `tokio` test macros, `tempfile`. | feature flags: `default=["sync","health"]`, `remote` (forwards to `unblock-storage/remote` only, D15/NFR-10). | v1 (extend feature list v1.2 for `remote`) | n/a (built by every test target) |
| `src/lib.rs` | Crate root: `#![forbid(unsafe_code)]`, `#![warn(missing_docs)]`, module wiring, curated re-exports (public API summary above), crate-level rustdoc with the lifecycle + Semaphore contract (D14, spine §4.2) and a runnable doctest of `open → ready → claim → close → shutdown`. | `pub use` of `Session`, `SessionConfig`, `ImportOptions`, `EngineError`, shutdown + tracing surface; `pub use unblock_model::{CloseOutcome, ImportReport, ExportReport, DiagnosticKind, DiagnosticReport, DiagnosticFinding, ListFilters, CountGroupBy, CountBucket, DepTree, GraphEdge}` (CF-A/CF-B/CF-C, defined in model, spine §1.10; `DiagnosticFinding` re-exported per G-10) and the storage-owned contract types (`IssuePatch`, `DeletePlan`/`DeleteMode`) re-exported. | v1 (re-export list grows v1.1) | Doctest: full happy-path round-trip against a temp workspace (also serves FR-9 smoke). Unit: `re_exports_are_public` compile check. |

### Error

| Path | Responsibility | Key items | Version | Tests |
|---|---|---|---|---|
| `src/error.rs` | The engine's composed error — the union surfaced to L7 (spine §2, §4.1). `#[derive(Snafu)]` `EngineError` with `#[snafu(transparent)]`/`source` wrapping of `ConfigError`, `StorageError`, `SyncError`, `HealthError`, `PolicyError`, `ModelError`; plus engine-local variants (`WorkspaceNotOpen`, `ShutdownInProgress`, `ImportPreflightFailed`, `WritePermitPoisoned`). Implements `fn code(&self) -> ErrorCode` delegating to each `source`'s `code()` (spine §2.1) so the boundary builds `StructuredError` uniformly. **No backend type leaks** (rule 2). | `pub enum EngineError`; `impl EngineError { pub fn code(&self) -> unblock_error::ErrorCode; pub fn hint(&self)->Option<String>; pub fn retryable(&self)->bool; }`; `pub type Result<T> = core::result::Result<T, EngineError>`. | v1 (variants added additively v1.1: `GateBlocked`, `SavedQueryNotFound`; v1.2: `SyncTopologyConflict`) | Unit: every variant → expected `ErrorCode` (table-driven, mirrors spine §2.3); `transparent` source codes pass through unchanged; `retryable()` matches `code().is_retryable()`. insta snapshot of the `(variant → code → exit_code)` map (golden, FR-11). |

### Session — shared struct & lifecycle

| Path | Responsibility | Key items | Version | Tests |
|---|---|---|---|---|
| `src/session/mod.rs` | Declares the `Session` struct (the single shared state) + `SessionConfig`; re-exports the per-concern `impl` modules. Holds `storage: Arc<dyn Storage>` (**received from the `WorkspaceContext`, not built here** — CF-D), `write_permit: Arc<tokio::sync::Semaphore>` (1 permit, D14, spine §4.2), `policy: Arc<dyn …>` (policy contracts), `config: ResolvedConfig` (from the `WorkspaceContext`), `actor: String` (from the context), `jsonl_export: bool`, `workspace_dir: PathBuf` (from the context), `shutdown: Arc<AtomicBool>` (FR-17 flag), optional `health`/`sync` handles. `Session: Send + Sync + 'static` (shared across MCP tasks). | `pub struct Session { … }` (fields private); `#[derive(Debug, Clone)] pub struct SessionConfig { pub jsonl_export: bool, pub import_on_open: Option<ImportOptions>, pub remote: Option<RemoteConfig /*v1.2*/> }` (engine-policy knobs only; `workspace_dir`/`actor`/resolved config now arrive via `WorkspaceContext`, CF-D, spine §4.1). | v1 (fields added additively in later versions; `remote` field reserved seam v1.2) | Unit: `Session` is `Send + Sync` (`fn assert_send_sync<T: Send+Sync>(){}`); `SessionConfig` round-trips `Debug`/`Clone`. |
| `src/session/lifecycle.rs` | `impl Session` open/recover/shutdown. `open()`: **consume the `WorkspaceContext` built by `unblock-config`** (CF-D — config already did `.unblock/` discovery, opened/migrated libsql, and built the `Arc<dyn Storage>`; the engine takes `ctx.storage`/`ctx.workspace_dir`/`ctx.actor`/`ctx.config` and does **not** construct storage or run migrations itself); if `cfg.import_on_open` set, run the import seam (FR-26/dogfood) under the write permit; build the Semaphore; install nothing global (shutdown install is caller's job, see shutdown.rs). `shutdown()`: flip flag, drain in-flight permit, flush + close libsql cleanly (FR-17 AC). `doctor()`/`recover()`: v1 = libsql `integrity_check` passthrough via health-lite (`doctor` returns a `DiagnosticReport` for cli `doctor`, FR-16-lite; `recover` performs the lite repair); v1.1 full taxonomy + `.unblock/.recovery/` evidence. Both are public (OQ-2 RESOLVED). | `pub async fn open(ctx: WorkspaceContext, cfg: SessionConfig) -> Result<Self>` (spine §4.1; consumes the **storage-bearing** `WorkspaceContext { storage: Arc<dyn Storage> /* NON-OPTIONAL */, workspace_dir, actor, config }`, DEFINED in and re-exported from `unblock-config`, G-5/G-9; the resolve-only no-storage `ResolvedContext` from `open_workspace` is **not** what `Session::open` consumes); `pub async fn shutdown(&self) -> Result<()>`; `pub async fn doctor(&self) -> Result<DiagnosticReport>` (OQ-2 RESOLVED — v1 FR-16-lite surface for cli `doctor`); `pub async fn recover(&self) -> Result<RecoveryReport>` (OQ-2 RESOLVED — public). | v1 (`recover`/`doctor` lite); v1.1 (full recovery taxonomy + evidence dir); v1.2 (config resolves remote replica before building the context) | Integration (`tests/lifecycle.rs`): open with a fresh-workspace context succeeds (config did migrate); reopen is idempotent; open with `import_on_open` populates store; shutdown closes DB with no WAL leftover (failure-injection assertion, NFR-5); double-shutdown is a no-op. Unit: open with an empty/un-discovered context surfaces `ConfigError` from the context build (raised by `unblock-config`, not the engine). |

### Session — reads (no write permit, FR-10)

| Path | Responsibility | Key items | Version | Tests |
|---|---|---|---|---|
| `src/session/read.rs` | `impl Session` read methods — **never acquire the write permit** (FR-10, rule 4). Each delegates to `Storage`, mapping `StorageError → EngineError` transparently. `ready()` applies the hybrid sort via `unblock-policy` (priority + readiness ranking) after `storage.ready_issues`. `diagnostics()` dispatches `DiagnosticKind` over pure-DB storage calls (`closed_since`/`orphan_candidates`/`integrity_check`/counts) — **no git** (FR-15/NFR-6). | `pub async fn get/list/ready/blocked/search/count/stale/dependency_tree/detect_cycles`; `pub async fn dependency_graph(&self, roots: &[String]) -> Result<DepTree>` (backs the dep `graph` action, spine §3.2/§4.1, G-23c); `pub async fn diagnostics(&self, kind: DiagnosticKind) -> Result<DiagnosticReport>` (spine §4.1). | v1 | Integration (`tests/reads.rs`): `ready` excludes blocked/deferred/closed and reflects an edge change immediately (FR-4/FR-5 AC); `ready` hybrid sort = policy order (golden insta on a fixed corpus); reads succeed **while a write holds the permit** (FR-10 AC — spawn a write that parks mid-tx, assert a concurrent `get` returns); `dependency_graph(roots)` returns the edge set backing the dep `graph` action (G-23c). Unit: `diagnostics` dispatch covers every `DiagnosticKind`. |
| `src/diagnostics.rs` | The engine-side dispatch over the `DiagnosticKind` taxonomy → `DiagnosticReport` (shape consumed by L7 `ToolOutput::Diagnostics`, spine §5.3). **The `DiagnosticKind`/`DiagnosticReport`/`DiagnosticFinding` types are defined in `unblock-model` §1.10 (CF-B) and re-exported, not defined here** — so render (model+error only) can format them. `kind`: `Stats|Info|Where|Version|Lint|Changelog{since}|Orphans` (mirrors MCP `DiagnosticsInput`, spine §5.2). Pure-DB derivation only. | `pub use unblock_model::{DiagnosticKind, DiagnosticReport, DiagnosticFinding}`; engine-local dispatch fn mapping each `DiagnosticKind` to a `DiagnosticReport` via storage calls. | v1 (Lint/Changelog/Orphans landed); v1.1/v1.2 add sync + full-health variants (in model) | Unit: each kind serializes stable; `Changelog` honours `since`; `Orphans` derives from `external_ref` commit pattern (no git read — assert no `Command`/git symbol via the workspace static gate). insta snapshot per kind. |

### Session — writes (acquire the single permit, D14)

| Path | Responsibility | Key items | Version | Tests |
|---|---|---|---|---|
| `src/session/write.rs` | `impl Session` mutation methods. Each: check shutdown flag → `acquire()` the single permit for the **entire** storage transaction → call `Storage` mutate (rows + audit `Event` written transactionally by storage) → release on drop. `close_with_suggestions()` closes then queries newly-unblocked issues via policy/storage (returns `CloseOutcome`, FR-11). Permit acquisition is uncancel-safe: a dropped future before commit releases the permit and leaves committed-or-rolled-back (D14, spine §4.2, NFR-5). | `pub async fn create/update/delete/claim/defer/undefer/add_dep/remove_dep`; `pub async fn close_with_suggestions(&self, id, reason) -> Result<CloseOutcome>` (spine §4.1). | v1 (v1.1: gate-check hook before status transitions; audit hook after commit; v1.3: claim TTL/heartbeat) | Property (`tests/linearizable.rs`, proptest): interleaved mutations through the engine are linearizable; same op via two call sites yields identical state (FR-9 AC). Integration: `claim` race — N tasks claim same id, exactly one wins, losers get `AlreadyClaimed` (FR-2 AC); `delete` DryRun mutates nothing; reparent cycle rejected (FR-1b/FR-5); `close --suggest-next` returns newly-unblocked (FR-11). Failure-injection: drop a write future mid-tx → no partial state, permit reusable (NFR-5). |
| `src/permit.rs` | The write-permit guard abstraction over `tokio::sync::Semaphore` — encapsulates D14 / spine §4.2 so write.rs never juggles raw `OwnedSemaphorePermit`. RAII guard that holds the permit across the tx and asserts shutdown-aware acquisition. | `pub(crate) struct WriteGuard(OwnedSemaphorePermit)`; `impl Session { pub(crate) async fn acquire_write(&self) -> Result<WriteGuard> }` (errors `ShutdownInProgress` if flag set). | v1 (v1.2: extended for remote write-back topology, D14 follow-up) | Unit: single permit → second `acquire_write` parks until first drops; acquiring after shutdown flag set → `ShutdownInProgress`; guard drop releases (loom-style or tokio paused-time interleave test). Property: at most one guard alive at any time (no over-issue). |

### Session — interchange (delegates to unblock-sync)

| Path | Responsibility | Key items | Version | Tests |
|---|---|---|---|---|
| `src/session/interchange.rs` | `impl Session` JSONL/bd interchange (FR-7/8/26). `export_jsonl`: delegate to `unblock-sync` atomic write (temp+fsync+rename, NFR-4); read snapshot of current DB state. `import_jsonl`: delegate preflight (conflict-marker + malformed-JSON rejection, path confinement) to `unblock-sync` **before** any DB mutation; apply under the write permit; never resurrect tombstones (model invariant). `import_bd`: D16 one-shot, idempotent via `content_hash`; reports dropped/unmapped fields. | `pub async fn export_jsonl(&self, path) -> Result<ExportReport>`; `pub async fn import_jsonl(&self, path, opts: ImportOptions) -> Result<ImportReport>`; `pub async fn import_bd(&self, path) -> Result<ImportReport>` (spine §4.1). | v1 | Integration (`tests/interchange.rs`): export byte-deterministic for fixed DB (NFR-4 AC, insta on file bytes); killed export leaves original intact (failure-injection); import rejects conflict marker with zero DB writes (FR-8 AC); symlink-escape path refused at preflight (NFR-7); tombstone not resurrected (model invariant); `import_bd` rerun idempotent by `content_hash` (FR-26 AC). Property: export→import round-trip is identity under `sync_equals` (spine §1.8). |
| `src/report.rs` | **`CloseOutcome`, `ImportReport`, `ExportReport` are defined in `unblock-model` (spine §1.10, CF-A) and re-exported here** — relocated so render (model+error only) can format them. This module owns only the engine-local `ImportOptions` input and re-exports the model report/outcome types. All model types are `Serialize + JsonSchema` (feed `ToolOutput`, spine §5.3). | `pub use unblock_model::{CloseOutcome, ImportReport, ExportReport}`; `#[derive(Debug,Clone,Default)] pub struct ImportOptions { pub dry_run: bool }` (engine-owned). | v1 (v1.1: `gate_results` added to `CloseOutcome` in model where a transition was gated) | Unit: serde round-trip; JsonSchema generation stable (insta on schema). |

### Cross-cutting: shutdown & logging seams (FR-17, NFR-13/14)

| Path | Responsibility | Key items | Version | Tests |
|---|---|---|---|---|
| `src/shutdown.rs` | Cooperative shutdown **flag-read** surface (FR-17). **OQ-4 RESOLVED:** the OS signal-handler install (SIGINT/SIGTERM/SIGHUP → atomic flag, `signal_hook`) lives in **`unblock-cli`** (L7) — a library must not hijack process-global signals; the engine only **reads** the flag at mutation checkpoints (write.rs). The cli wires the `Arc<AtomicBool>` it sets into the `Session`. | `pub fn is_shutdown_requested() -> bool` (reads the cli-installed flag). *(The `install_shutdown`/`shutdown_exit_code` handler API lives in `unblock-cli/src/shutdown.rs`, relocated at T3.2.)* | v1 | Unit: `is_shutdown_requested()` reflects the wired flag (unset by default). Integration (cli/engine, NFR-5): SIGTERM mid-write commits-or-rolls-back cleanly, DB closes with no WAL corruption (failure-injection, run from the `unblock-cli` harness). |
| `src/logging.rs` | `tracing` init helper on the `unblock.reliability` target (NFR-13): INFO for guard activations / external-path use / conflict-marker rejection / force overrides; DEBUG per-file/per-issue. Structured-stdout / diagnostics-stderr discipline is the renderer's job (NFR-14) — engine only emits spans/events. | `pub fn init_tracing(opts: TracingOptions)`; `pub struct TracingOptions { pub level: tracing::Level, … }`; internal span constructors for write/import/export guards. | v1 (v1.3: perf spans for the 1M scale gate, NFR-13) | Unit: events route to the `unblock.reliability` target (capture via `tracing-test`/a test subscriber); guard activation emits at INFO; per-issue at DEBUG. |

### v1.1 additive files (reserved seams)

| Path | Responsibility | Key items | Version | Tests |
|---|---|---|---|---|
| `src/session/organization.rs` | FR-6 surface through the engine: comments add/list, label rename/list-all, epic rollups + auto-close-eligibility. Mutations take the write permit; reads bypass. | `pub async fn add_comment/list_comments`; `pub async fn rename_label/list_all_labels`; `pub async fn epic_status(&self, id) -> Result<EpicStatus>`. | v1.1 | Integration: threaded comment add/list; label rename cascades; epic rollup `eligible_for_close` matches closed/total children. |
| `src/session/coordination.rs` | FR-18 diagnostics through the engine: `scheduler` (ranked, explainable `unblock.scheduler.v1`) + `coordination_status` (`unblock.coordination.v1`, read-only stale-claim diagnosis). Pure-DB-derived; delegates ranking to `unblock-policy` contracts. Reads only (no permit). | `pub async fn scheduler(&self, …) -> Result<SchedulerReport>`; `pub async fn coordination_status(&self) -> Result<CoordinationReport>`. | v1.1 (v1.3: active reclaim, scheduler v2) | Integration: scheduler order deterministic + explainable (golden); coordination flags stale `in_progress` claims; both versioned contract snapshots. |
| `src/session/gates.rs` | FR-19 workflow gates: enforce `.unblock/policy.toml` transition gates (ci_green/min_reviewers/security_sign_off) inside mutating status transitions; block until required gates pass; record `GateResult`s on the outcome. Delegates gate decisions to `unblock-policy`. | `pub(crate) async fn gate_check(&self, from, to, issue) -> Result<Vec<GateResult>>`; integrated into `write.rs` transitions. | v1.1 | Integration: a transition blocked by a failing gate → `GateBlocked` with gate evidence; passing gates allow + record results. |
| `src/session/saved_queries.rs` | FR-21 saved queries: resolve a named filter set, run it via the read path. | `pub async fn run_saved_query(&self, name) -> Result<Vec<Issue>>`; `pub async fn list_saved_queries(&self) -> Result<Vec<SavedQuery>>`. | v1.1 | Unit/integration: named query resolves to filters; run matches an equivalent `list`. |
| `src/audit.rs` | FR-22 flight recorder: append-only `interactions.jsonl` with capture-only Tier-1 attribution (agent_name/harness/model). Hooked after each committed mutation; never enforced; atomic append. | `pub(crate) fn record_interaction(&self, …)`; wired into `write.rs` post-commit. | v1.1 | Integration: each mutation appends one line; attribution captured verbatim; append is atomic (failure-injection: partial line never written). |

### v1.2 / v1.3 additive files

| Path | Responsibility | Key items | Version | Tests |
|---|---|---|---|---|
| `src/session/sync_topology.rs` | D14 follow-up under libsql remote/replica: write-topology when writes can originate from synced serve processes (local single-writer still holds; remote reconciliation rules); resolve remote credentials from config (env/keychain, never `config.toml`, NFR-18). | extends `acquire_write`/`open` for the `remote` feature; `RemoteConfig` resolution. | v1.2 (feature `remote`) | Integration (`wiremock`, remote contract suite): write-back through replica; offline-first queue-and-reconcile; credential never read from `config.toml`. |
| `src/compaction.rs` | D12/v1.3 archival orchestration: activate the model's compaction fields as a real archival path for very large stores; restore-from-snapshot. | `pub async fn compact(&self, …) -> Result<CompactionReport>`; `pub async fn restore_snapshot(&self, …)`. | v1.3 | Property: compaction preserves JSONL round-trip fidelity (D12) — round-trip identity under `sync_equals` extends to compacted issues. |

### Test-only files

| Path | Responsibility | Version |
|---|---|---|
| `tests/common/mod.rs` | Shared harness: spin a `Session` over a tempdir libsql workspace (real storage impl, not a mock); corpus builders (N issues, dep graphs, deferred set); a "park mid-tx" write helper for the read-during-write and cancel-safety tests. | v1 |
| `tests/lifecycle.rs` | open/migrate/reopen/import-on-open/shutdown (see lifecycle.rs row). | v1 |
| `tests/reads.rs` | read semantics + FR-10 read-during-write + ready hybrid sort. | v1 |
| `tests/linearizable.rs` | proptest FR-9 linearizability + dual-callsite identity; claim-race FR-2. | v1 |
| `tests/interchange.rs` | export/import/import_bd ACs + round-trip property. | v1 |
| `tests/error_contract.rs` | `EngineError → ErrorCode → exit_code` golden (insta), FR-11. | v1 |

---

## Crate-level test & bench plan

- **Contract / behaviour suite (FR-9, rule 4):** the engine is the convergence point — every test drives the **real** `unblock-storage` libsql impl over a temp workspace (no engine-level mock of `Storage`; a mock would let behaviour drift, defeating FR-9). The dual-callsite identity test asserts two independent call sites produce identical state — the proxy for "MCP and CLI cannot drift."
- **Property (`proptest`, NFR-16):** (a) interleaved-mutation linearizability (FR-9); (b) write-permit single-occupancy invariant (D14); (c) export→import round-trip identity under `sync_equals` (spine §1.8); v1.3 extends (c) to compacted issues (D12).
- **Concurrency / cancel-safety (NFR-5, D14, spine §4.2):** tokio paused-time / interleave tests for read-during-write (FR-10) and drop-mid-tx (no partial state, permit reusable). The N-writer claim race (FR-2) and the cross-crate contention/SIGTERM-mid-write failure injection run in the M0 contention lab + the cli harness; the engine owns the unit-level permit and cancel-safety assertions.
- **Snapshots (`insta`, NFR-14):** `EngineError`→code→exit-code map; each `DiagnosticReport` kind; report/outcome JsonSchema; ready hybrid-sort order on a fixed corpus.
- **Bench (`criterion`, NFR-1):** engine-level overhead vs raw storage for `create`, `ready` (1k/10k), `list` (1k/10k), `claim` (permit acquisition cost), `export` (10k) — the engine must add negligible overhead over the storage budgets; 10% CI regression gate. v1.3 promotes the 1M `ready`/`list` corpus from manual to a (possibly nightly) gate (NFR-2).
- **No-git static gate (NFR-6):** assert no `Command::new("git")`/git crate symbol anywhere in the crate (CI workspace check; diagnostics derive purely from DB).

## Open questions specific to this crate

0. **Workspace-open ownership.** *(RESOLVED — CF-D, spine §4 intro + §4.1.)* `unblock-config` owns `.unblock/` discovery, opens/migrates libsql, and **builds the `Arc<dyn Storage>`**, exposing `WorkspaceContext { storage, workspace_dir, actor, config }`. The engine **consumes** the context — `Session::open(ctx: WorkspaceContext, cfg: SessionConfig)` — and never constructs storage or runs migrations itself. This keeps the acyclic layering intact (engine sees storage only through the trait object) and removes the prior ambiguity over who opens the DB.

1. **Policy handle shape.** Is `unblock-policy` consumed as a concrete type or a `dyn` trait object in `Session`? The spine shows `policy` as an opaque field. A trait object keeps the v2+ "pluggable policy" seam open but costs a vtable on the hot `ready` path — decide before T1.2. (Lean: concrete in v1, trait-object only if a second policy impl is demanded.)
2. **`recover()`/`doctor()` visibility in v1.** *(RESOLVED — OQ-2, spine §4.1.)* The cli `doctor` command (L7) needs a public entrypoint, so v1 exposes **both** `pub async fn doctor(&self) -> Result<DiagnosticReport>` (FR-16-lite surface, `integrity_check`-backed) and `pub async fn recover(&self) -> Result<RecoveryReport>` (lite repair) on the public lifecycle surface; the rich v1.1 taxonomy + `.unblock/.recovery/` evidence dir is additive and does not change these signatures.
3. **Import-on-open vs explicit import.** `SessionConfig::import_on_open` (dogfood FR-26 convenience) vs requiring callers to call `import_bd` after `open`. The spine lists an "optional import seam" in `open`. Confirm the dogfood path uses `import_on_open` (one call) rather than a two-step open+import (avoids a window where the store is empty). 
4. **Shutdown install ownership. RESOLVED (OQ-4, with Miguel — T0.1+T0.2):** L7 (`unblock-cli`) owns signal-handler installation (`signal-hook` is a **cli** dep, unix-only); the engine never installs process-global handlers (a library hijacking signals is an anti-pattern) and only **reads** `is_shutdown_requested()`. Matches D13 ("the MCP stdio server is a foreground process launched by the client"). The `install_shutdown`/`shutdown_exit_code` handler API thus lives in `unblock-cli/src/shutdown.rs` (relocated at T3.2); the engine keeps only the flag-read surface.
5. **Audit hook timing (v1.1).** `interactions.jsonl` append after commit means a crash between commit and append loses the audit line; appending inside the tx couples audit durability to the DB but contradicts "append-only JSONL." Decide capture-only semantics tolerance (FR-22 says capture-only/never-enforced, so post-commit best-effort is likely acceptable — confirm).
