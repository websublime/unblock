# unblock — Build STATUS (retired pointer stub)

**Task tracking has moved to unblock (MCP).** This project now dogfoods its own tracker: the system of
record for what is done / in progress / to do is **unblock over MCP** (server wired in `.mcp.json` +
`AGENTS.md`), not this file.

- **Git-backed record:** `.unblock/issues.jsonl` — the D5 committed JSONL export (the local `unblock.db` stays
  gitignored). This is the shared, versioned task state; re-export and commit it in the same commit as the work.
- **Wiring, tool surface & capabilities:** `AGENTS.md` (the managed MCP block, generated by `unblock agents`);
  read `unblock://capabilities` at runtime for the full tool/resource/prompt surface.
- **Still-authoritative planning docs:** [`implementation-plan.md`](implementation-plan.md) (task DAG +
  acceptance criteria) and [`00-roadmap.md`](00-roadmap.md) (when each version ships).

> **History.** Through the **v1.0.0 GA** this file WAS the durable, git-backed system of record — the full
> M0–M3 v1 task registry (Phase 0 planning through every T-id, with per-task review/verify notes). That
> registry is preserved in this file's **git history** (`git log -p -- docs/plans/STATUS.md`); it was
> superseded by the unblock tracker, not deleted.
