# unblock — Build STATUS (task registry)

Durable, git-backed system-of-record for **what is done / in progress / to do**. It is the live status
overlay over [`implementation-plan.md`](implementation-plan.md) (the task DAG + acceptance criteria) and
the [roadmap](00-roadmap.md). The harness Task tools (TaskCreate/Update) are the **per-session execution
layer**; THIS file is the **cross-session source of truth** (it survives compaction and restarts).

**Legend:** ☑ done · ◐ in-progress · ⊘ blocked · ☐ todo.  **Ready** = every "Depends on" is ☑.

**Rules**
- Update a row the moment a task changes state; keep this file in the same commit as the work.
- A task is ☑ only when it meets its PRD acceptance criteria **and** passed review (no close-without-review).
- When any decision (D-id), FR tier, or command surface changes, run the decision-change checklist (see
  `ci-cd-and-distribution.md` §2.1 doc-lint + spine = single source of truth).
- The spine (`01-design-spine.md`) wins on any cross-crate interface disagreement.

---

## Phase 0 — Planning & QA  *(DONE)*

| Item | Status |
|---|---|
| Discovery (3-agent + coordinator) → dossier | ☑ |
| PRD v0.1 → 3-lens review → v0.2 → **APPROVED v1.1** | ☑ |
| Per-file plans: spine + 12 crate docs (~190 files) | ☑ |
| 5 spine amendments (CF-A..CF-E) | ☑ |
| CI/CD + distribution — D17 (dist/axoupdater) | ☑ |
| Gap/drift QA: 24 findings applied + verified → **GO** | ☑ |
| Tracking decision: Markdown `STATUS.md` (this file) | ☑ |

## Pre-T0 — before crate creation

| id | Item | Depends on | Status |
|---|---|---|---|
| P.1 | This `STATUS.md` registry | — | ☑ |
| P.2 | Root `CLAUDE.md` (global rules / arch / idiomatic Rust) | — | ☑ |
| P.3 | Roadmap v1.2/v1.3 review — **deferred to v1 GA** (just-in-time; not a pre-T0 gate) | — | — |

## M0 — Foundation  *(gate: Storage contract suite green + contention lab proves no hot-spin)*

| id | Task | Depends on | Status | Notes |
|---|---|---|---|---|
| T0.1 | Workspace scaffold deps (`Cargo.toml`: libsql/clap/axoupdater; backoff→backon; reqwest dropped; deny→forbid) | P.2 | ☑ | **MERGED with T0.2** ([PR #366](https://github.com/websublime/unblock/pull/366)); 3-lens design Review = PASS-with-changes, applied |
| T0.2 | Create 12 `unblock-*` crates + `xtask` layering check | T0.1 | ☑ | **MERGED with T0.1** ([PR #366](https://github.com/websublime/unblock/pull/366)); AC green: build 1.96 + remote/no-default variants + storage/mcp network-free; per-crate `CLAUDE.md` stubs done; **design Review + Verify gates both PASS** (layering rejects an injected back-edge — non-vacuous) |
| T0.3 | `unblock-model` (types, hash, sync-eq, validation) | T0.2 | ☑ | **MERGED** ([PR #368](https://github.com/websublime/unblock/pull/368)); **design Review + Verify gates both PASS_WITH_CHANGES, applied**. Spec-first PART 1 (spine §1.8 frozen 17-field hash padding tail Q4=KEEP; §1.1–§1.4 hand-rolled enum serde, no `untagged`) + crate impl (Issue + open enums, `content_hash` bd-byte-parity, `sync_equals`, tombstone, full `IssueValidator`→aggregate `ValidationFailed{fields}`, `src/id.rs`, 12 §1.10 DTOs). Verify fix: `is_expired_tombstone` panic-free (`checked_add_signed`) + panic-safety proptest. Ingestion cargo-fuzz targets **deferred to T0.7** (see that row); proptest covers panic-safety now. |
| T0.4 | `unblock-error` (snafu taxonomy, exit codes) | T0.2 | ☑ | **MERGED** ([PR #367](https://github.com/websublime/unblock/pull/367)); **design Review + Verify gates both PASS_WITH_CHANGES, applied**. Spec-first PART 1 (spine §2 D-E1 `FieldError`/`ValidationFailed`, 35-variant reconcile, OQ-1/2/4/5 + model Q2 resolved) + crate impl (35 `ErrorCode`, `StructuredError`, `ExitCode`, `CodedError`, `ModelError`). Security hardenings: (1) sanitize `hint` at the L0 chokepoint (with_hint/from_coded/From), spine §2.4 covers message+hint+context-render note; (2) bound `find_similar_ids` input (`MAX_SUGGESTION_INPUT_CHARS=256`) + two-row Levenshtein. `unblock-error` fuzz target (`fuzz_sanitize`) **deferred to T0.7** (see that row); proptest covers the invariants now. |
| T0.5 | `unblock-storage`: `Storage` trait | T0.3, T0.4 | ☑ | **MERGED** ([PR #369](https://github.com/websublime/unblock/pull/369)); **design Review + Verify gates both PASS_WITH_CHANGES, applied**. Spec-first PART 1 (spine §3.1 IssuePatch Option-B full field set + `Default`; spine §3.1 `StorageError` made concrete incl. `Migration{from:i32,to:i32,reason:String}`; spine §2.1 per-crate enums implement `CodedError`; crate plan: `dependency_graph` added to trait + deps.rs, T0.5↔T0.6 manifest/dep boundary, CF-E seams commented, ErrorCode map pinned `Backend`→`DatabaseError`/`Migration`→`SchemaMismatch`/`IntegrityFailed`→`DatabaseError`) + crate impl (4 backend-free files: `lib.rs`/`trait_def.rs` (full §3.2 method set incl. `dependency_graph`, CF-E commented)/`filters.rs` (`DeletePlan`/`DeleteMode`/`IssuePatch`)/`error.rs` (`StorageError`+`CodedError`+`BackendOpaque`); object-safety + `NoopStorage` + golden ErrorCode + retryable + context-holder + sanitization tests; manifest trimmed, network-free). **5 non-blocking doc-precision follow-ups → T0.6 spec-first**: `update_issue` no-op semantics, `search_issues` field set, `blocked_issues` order (DESC-beads vs ASC fork), tombstone delegation to the model helper, `retryable()` coupling. |
| T0.6 | `unblock-storage`: libsql impl (WAL, busy_timeout) | T0.5 | ☑ | **MERGED** ([PR #370](https://github.com/websublime/unblock/pull/370)); **design Review (2 iterations: FAIL→reconcile→PASS) + Verify (PASS_WITH_CHANGES) both applied**. Spine §3.2.1 source-verified method-semantics + EventType oracle; §3.3 (busy_timeout=5000 native, BEGIN IMMEDIATE, wal_autocheckpoint=0+manual, OQ-5 = write conn + separate read conn / shared-cache `:memory:`). Backend: `LibsqlStorage` (two-conn), 38-col schema (CASCADE; `depends_on_id` no-FK; `idx_issues_ready`), `CURRENT_SCHEMA_VERSION=1` forward migrations, CRUD + **atomic assignee-only claim** (no race; same-actor no-op), queries (`instr`+id search; **3-pass live blocked** = direct 3-type ∪ epic-rollup ∪ **transitive parent→children**), deps (petgraph + DFS cycle-path), events, pure-DB diagnostics; `From<libsql::Error>` busy/locked map. 58 tests, network-free (RK-4), no panic in lib paths. Verify fixes: `:memory:` WAL-pragma flake (WAL file-path-only; 32-task stress test, 0 flakes/21+ runs) + transitive 3rd blocked pass (Miguel). **Deferred:** T0.7 contract suite + fuzz targets; T0.8 contention lab (**must use a FILE DB** — `:memory:` shared-cache can't WAL). *(Reconciled at T0.8, spec-first: **NFR-1/NFR-2 perf budgets are T3.5**, not T0.8 — the contention lab asserts only non-spin + correctness, no throughput/latency, per `implementation-plan.md` T0.8 AC + the locked-scope `benches/storage.rs` at T3.5. The earlier "+ NFR-1/2 perf" framing on the T0.8 line was drift.)* |
| T0.7 | Storage contract test suite (NFR-16) | T0.6 | ☑ | **MERGED** ([PR #371](https://github.com/websublime/unblock/pull/371)); **design Review (PASS_WITH_CHANGES) + Verify (PASS, no must-fix) gates both applied**. (A) NFR-16 backend-independent contract suite in `unblock-storage::testkit` (generic over a storage factory; one `contract_*` case per trait method + cross-cutting invariants + 2 gated `StorageTestkit`-seam cases) run against `open_in_memory` + temp-file `open_local` in `tests/contract.rs`; `behaviour.rs` kept 100% intact. (B) 8 cargo-fuzz targets wired in the `unblock-fuzz` MEMBER crate as stable `run_<t>_case` cores + a nested nightly libFuzzer package (`fuzz/`, `exclude`d from the workspace): model+error `{content_hash, issue_ingest, parse_id, enum_deserialize, sanitize}` (**incl. T0.4's deferred `fuzz_sanitize`** + T0.3's deferred model ingestion targets) + storage `{query_filters, cycle_detect, id_alloc}`; `tests/regression.rs` replays the committed corpus + a `proptest!` smoke per target on stable. **Notes:** root `[workspace] exclude = ["crates/unblock-fuzz/fuzz"]` + `tempfile` added to `[workspace.dependencies]`; nested fuzz package pins `nightly-2024-10-31` (scoped `fuzz/rust-toolchain.toml`) — _note: this pin was DOA (it predated edition 2024 + let-chains) and was repaired post-T1.3 to `nightly-2026-04-01`, see the fuzz-smoke repair note in T0.9_; gated `StorageTestkit` seams (`testkit_insert_raw_edge` + `testkit_child_high_water`) added (impl in-module in `libsql/testkit.rs`, no crate-root visibility widening); member crate **drops the stub `unblock-sync` dep** (the JSONL/`bd`/sync targets are post-T0.7); spec-first: `unblock-fuzz.md` reconciled (OQ-1/OQ-6 resolved, structural rule + landed targets), `count_issues` trait doc corrected for the Label exception, model `EXTERNAL_REF_MAX_CHARS`/`LABEL_MAX_LEN` promoted to `pub`. **Remaining M0:** T0.8 contention lab (M0 gate; FILE DB) + T0.9 CI scaffolding (CI test job **must** run `--features testkit` so the NFR-16 suite executes). |
| T0.8 | **Contention lab (RK-1 / NFR-3 — M0 GATE)** | T0.6 | ☑ | **MERGED** ([PR #372](https://github.com/websublime/unblock/pull/372)); **design Review (PASS_WITH_CHANGES) + Verify (PASS, `metric_sound=true`, no must-fix) both applied**. **RK-1 RESOLVED — no rusqlite pivot.** **Topology:** `tests/contention_lab.rs` (`#![cfg(feature = "testkit")]`) drives `K` independent `LibsqlStorage` instances (adaptive to `available_parallelism()`, floor 2; hard-fail < 2 vCPU) on **one shared temp FILE DB** (`open_local`, not `:memory:` — shared-cache is non-WAL), so cross-instance writers contend on the real WAL write lock. **Metric:** baseline-relative **CPU-per-write ratio** via the `cpu-time` crate (whole-process CPU, all tokio threads, unsafe-free — no raw libc) — **sequential** own-file baseline (honest single-write cost) vs **parallel** shared-file contended, `R = contended ÷ baseline`. Gate asserts **R ≤ 3.0 (PROVISIONAL, calibration-pending; perf budgets = T3.5; recalibrated `5.0 → 3.0` post-T1.3 — see below)**. **Measured on a multi-core dev machine: `R ≈ 1.0–1.2`** (honest blocking — sleep-based, never spins; the band BOUNDS the independent run-to-run spread observed across the implementer's 3× flake check AND the Verify gate's runs — not a single point), well under the ceiling. **Busy-retry witness** (mandatory, deterministic): a zero-timeout probe (libsql has no busy-handler callback; native timeout blocks silently) → witness > 0 contended / == 0 baseline (else INCONCLUSIVE, never a silent pass). **Correctness:** claim storm → exactly-one durable winner (re-SELECT; losers allowlist `AlreadyClaimed|DatabaseLocked`); disjoint-create reconciliation (no `IdCollision`; allowlist `DatabaseLocked`); update storm (allowlist `DatabaseLocked`); any other variant = corruption. **WAL-bound:** passive `wal_checkpoint(PASSIVE)` every **50** committed mutations on the held write conn (never TRUNCATE in the hot path — resolves spine §3.3 "tuned at T0.8") bounds the `-wal` under a 64 MiB ceiling; **negative control** (`#[ignore]`, checkpoint OFF) breaches it at ~220 MiB. **Forced-spin control** (`#[ignore]`, `busy_timeout=0` → tight non-yielding spin via gated `open_local_with_busy_timeout`) measured **`R ≈ 27`** on this 14-core box — proves the metric detects a real hot-spin (non-vacuous). Clean `integrity_check()`; full diagnostic block printed every run. **No throughput/latency asserted (perf = T3.5).** **Recalibration (post-T1.3, Miguel-approved; commit `fix(test): recalibrate contention gate for low-core CI`):** a CI dispatch on a 4-vCPU GitHub runner exposed that `R` **scales with core count** — a real forced hot-spin gives `R ≈ 28` on a 14-core dev box but only **`R ≈ 4.42` on a 4-vCPU runner** (baseline 550µs/write, contended 2433µs/write, ~3.56 cores burning, busy_retries 547100 vs baseline 0). At the old `5.0` ceiling the gate was **near-vacuous on low-core runners** (a real spin at `R ≈ 4.42` would have *passed* `R ≤ 5.0`) and the forced-spin control **flaked** around the 5 threshold. Fix: **(1)** lowered `PROVISIONAL_RATIO_CEILING` `5.0 → 3.0` (honest `~1.0–1.2` keeps ~2.5× margin; a real spin at `R ≈ 4.42` is now CAUGHT, `4.42 > 3.0`, on the weakest runner). **(2)** Hardened the forced-spin control to a **core-independent hot-spin signature** as its PRIMARY proof (no longer just `R > ceiling`): a **busy-retry witness** (contended `busy_retries > 10_000`, observed 547k/2.45M, while baseline `== 0`) + a **CPU-burn witness** (contended `cpu/wall > 1.8` cores burning, observed ~3.56 on 4 vCPU / 10.5 on 14-core, while baseline `≈ 1`) — both hold on **any `>= 2`-core runner**; `R > 3.0` is kept as a corroborating secondary check. **(3)** The control now also measures + prints the **honest (native-blocking) contended R** side by side (asserts `≤ 3.0`) so each CI run shows `honest R ≈ 1.x < ceiling 3.0 < spin R` on the actual runner. WAL controls / `WAL_CEILING_BYTES` / correctness asserts / fuzz config UNCHANGED. **Manifest:** root + storage `Cargo.toml` add dev-dep `cpu-time` (Cargo.lock pinned `1.0.0`); `mod.rs`/`libsql/testkit.rs`/`src/testkit.rs` add 3 `AtomicU64`/`AtomicBool` counters + checkpoint cadence + busy-witness toggle + gated `StorageTestkit` accessors (`testkit_busy_retry_count`/`testkit_checkpoint_count`/`testkit_mutation_count`/`testkit_set_checkpoint_interval`/`testkit_set_busy_witness`) + the gated forced-spin constructor — **no crate-root visibility widening**; new `mod.rs` unit test proves the passive checkpoint bounds the WAL sidecar. **Fallback if it ever genuinely fails (real contention + high R / lost write / non-empty integrity): rusqlite behind the `Storage` trait, re-open D14/D15.** |
| T0.9 | CI scaffolding (ci.yml, deny.toml, .cargo/config, **doc-lint**) | T0.2 | ☑ | **MERGED** ([PR #373](https://github.com/websublime/unblock/pull/373)); **design Review (PASS_WITH_CHANGES) + Verify (PASS_WITH_CHANGES) + a follow-up focused Verify (PASS, `fix_sound=true`) all applied — CLOSES M0.** **As-built job set (11 M0 jobs, ci-cd §2):** `.github/workflows/ci.yml` = `fmt` / `clippy` (workspace + targeted `-p unblock-storage --features testkit`) / `test` (`--workspace --locked`, the always-on set) / `storage-testkit` (`--features testkit --test contract` NFR-16 + `--test contention_lab` the M0 gate, with an explicit **≥ 2 vCPU** `nproc` precondition step) / `snapshots` (`insta test --check`) / `layering` (`cargo xtask check-layering`) / `audit` (`rustsec/audit-check`) / `deny` (`EmbarkStudios/cargo-deny-action check`) / `toolchain` (`cargo build --workspace --locked`, NFR-12) / **`doc-lint`** (`cargo xtask doc-lint`). Plus `.github/workflows/fuzz-smoke.yml` (`schedule: cron 0 4 * * *` + `workflow_dispatch`): a nightly-`2026-04-01` (= rustc 1.96.0-nightly) libFuzzer matrix over the **8** fuzz targets (`-max_total_time=60`) + a **separate stable-1.96** `contention-controls` job running the 2 `#[ignore]`d controls (`-- --ignored`) — nightly never leaks onto the stable storage build. **Repair note (post-T1.3):** this fuzz-smoke leg was effectively **DOA since T0.7/T0.9** (the cron was unwatched) — three chained latent bugs: (1) the former `nightly-2024-10-31` pin (cargo 1.84) predated edition 2024 (>= 1.85), so the `unblock-*` tree could not even parse; (2) the nested `fuzz/Cargo.toml` lacked an empty `[workspace]` table, so `cargo fuzz` failed building the manifest directly; (3) the tree uses let-chains (>= 1.88), so the pin had to be >= the workspace's stable 1.96 target, not merely >= edition-2024. Repaired by re-pinning to `nightly-2026-04-01` (= rustc 1.96.0-nightly) across `fuzz-smoke.yml` + `fuzz/rust-toolchain.toml` + `fuzz/Cargo.toml`, and adding the empty `[workspace]` table; the contention-controls leg's WAL-negative control was separately decoupled from core count (it was flaky on a 4-vCPU runner — its volume scaled with `available_parallelism`). The action SHA-pins are unchanged. **Every `uses:` SHA-pinned** to a 40-char commit + `# vX.Y.Z`: checkout `08c6903…` v5.0.0, rust-cache `c193711…` v2.9.1, dtolnay/rust-toolchain `29eef33…` (channel via `toolchain:` input), cargo-deny-action `bb137d7…` v2.0.20, audit-check `69366f3…` v2.0.0. **DEFERRED ledger** (listed at the top of `ci.yml`): `bench-gate`→T3.5, `scale`→T3.5, `no-network`→T3.1/T3.6, `rate-limit`→T3.5 (each needs an artefact that doesn't exist until its task). **`doc-lint` (NEW `xtask/src/doc_lint.rs`, 6 classes a–f over the fixed 19-file corpus, `regex`-driven — already in the lock via tracing-subscriber, +0 transitive):** GREEN (`doc-lint OK: 19 docs, 6 classes clean`); 6 planted-violation unit tests (one/class) + 7 guard tests + a `tests/doc_lint_corpus.rs` integration test pinning the real corpus GREEN **and** asserting the existence-guard FAILs on a truncated corpus (non-vacuity, mirroring T0.2's back-edge proof); a live-corpus injection probe fired a+c+d+e simultaneously then reverted. **Targeted features, NOT `--all-features`** (ci-cd §2.2): `cargo tree -e features --all-features` resolves the libsql `remote` TLS stack (reqwest/hyper/rustls), so `--all-features` is banned from the M0 gate (D15/NFR-10); `-p unblock-storage --features testkit` verified TLS/network-free. **`deny.toml`:** all four checks pass (`advisories ok, bans ok, licenses ok, sources ok`); license allowlist (MIT/Apache-2.0[+LLVM-exc]/BSD-2/BSD-3/ISC/Zlib/**Unicode-3.0**/**CDLA-Permissive-2.0**, confidence 0.93) — every entry load-bearing per `cargo deny list`, covers the unconditional axoupdater/aws-lc/`ring` TLS stack; **MPL-2.0 and OpenSSL removed** (Verify MUST-FIX — used by ZERO crates, so listing them only pre-cleared a forbidden/unneeded class; their absence keeps `licenses ok`). `[bans].deny` = `git2`/`gix`/`libgit2-sys` (NFR-6) **proven non-vacuous** (temporarily adding `git2` made `cargo deny check bans` FAIL with `error[banned]` for git2 + transitive libgit2-sys; reverted, lock cleaned). **`cargo audit`:** flagged **RUSTSEC-2026-0185** (`quinn-proto 0.11.14`, high, lock-resident but UNREACHABLE by any feature path — behind reqwest QUIC) → resolved by `cargo update -p quinn-proto` to 0.11.15 (semver-compatible, no code change); audit now clean. **Cargo.lock delta = +regex edge +quinn-proto bump.** **`.cargo/config.toml` + `rust-toolchain.toml` already present (T0.2)** — confirmed, not re-created. **Reconciliations (spec-first):** ci-cd §2 (per-job `Lands` column + DEFERRED ledger + new §2.2 targeted-features) + impl-plan T0.9 + spine §6 (conformance rules promoted to addressable `### 6.1`..`§6.7`) + ~30 doc cross-ref qualifications (bare `§N` → `spine §N`/`PRD §N`) that the doc-lint surfaced. **Verify gate = PASS_WITH_CHANGES, all applied:** (1) **storage flake ROOT-FIXED** — the `:memory:` parallel-open flake (`open_in_memory_parallel_first_write_stress`, blamed at T0.6 on the WAL pragma) was diagnosed by a 200-round repro harness to its real cause: concurrent `sqlite3_open_v2` on `cache=shared` URIs races `SQLite`'s **process-global shared-cache registry** and surfaces `SQLITE_MISUSE` ("bad parameter or other API misuse") — captured verbatim at "round 7 task 25". Fixed at source in `libsql/mod.rs` by serializing the in-memory open (build + connect + pragmas) behind a process-global `memory_open_lock` (`OnceLock<tokio::Mutex>`); scoped to in-memory only (`open_local`/production file path untouched). **PROOF: 3× the 200-round repro = 600 parallel-open scenarios, 0 flakes**; the permanent stress test is the regression guard; no band-aid (no retry/sleep/ignore/task-count change). (2) **deny MPL-2.0 + OpenSSL removed** (above). (3) **doc-lint nits:** class-(e) carry-forward now **breaks on an intervening other-doc barrier** (`unblock-*` / `*.md`), so a far qualifier cannot mask a dangling self-ref — this exposed 10 more genuinely-ambiguous bare `§N` (now qualified) + 2 new barrier unit tests; `e.g.` dropped from the rejection guard (latent false-negative); qualifier case-convention documented in lock-step with `short_name_to_file`. (4) ci.yml header count corrected (10 in-file jobs + fuzz-smoke). **Gate is now deterministic.** |

## M1 — Engine + core domain  *(gate: CRUD/ready/dep linearizable via engine)*

| id | Task | Depends on | Status | Notes |
|---|---|---|---|---|
| T1.1 | `unblock-policy` (ready-sort, gating, cache-key) | M0 | ☑ | **DONE — [PR #374](https://github.com/websublime/unblock/pull/374) merged** (rebased onto `main`: spec-first `d1dfe78` + impl `e33cd51` + Verify-hardening `9f26665`). **Spec-first** (design Review PASS_WITH_CHANGES, genuine ≥3-agent): ready-hybrid comparator drift fixed → source-faithful **bucket hybrid** `bucket(priority.0≤1) ASC, created_at ASC, id ASC` (`sort_ready_hybrid` sqlite.rs:10444/10515; the plan had mis-cited the `list`-default `sort_list_default`:3464) + **spine §4.1** normative pin + storage **OQ-3** resolved; **v1 `PolicyError` = single `Internal` seam** (Miguel — no v1 primitive is fallible; the inheritance helper is infallible; gate/evidence variants → v1.1) + Q1/Q2 resolved. **Impl**: `ready` (comparator + `is_ready`/`is_blocked` gating on `affects_ready_work` non-terminal-source edges + `READY_GATING_TYPES`), `cache_key` (disjoint `ready:`/`blocked:` ns + canonical length-prefixed **injective** `filters_fingerprint`), `contract` (schema-first `ContractEnvelope` + `contract_versions` + `POLICY_CONTRACT_VERSION`), `error` (`Internal` seam via `CodedError`), minimal infallible `inheritance`; `proptest_support` behind off-by-default `proptest-support` feature (testkit precedent). **Verify gate PASS_WITH_CHANGES** (4 lenses PASS, 1 minor doc must-fix): folded injectivity proptest + wire-format golden + inheritance order doc/proptest + deterministic ts fallback. **89 tests green** + clippy pedantic + layering NFR-15 + doc-lint (run in main session — subagent cargo builds are sandbox-denied). **Unblocks T1.2.** |
| T1.3a | `unblock-config` (v1-MINIMAL: `WorkspaceContext`/`ResolvedContext`/`ConfigError` + open facades, defaulted `ResolvedConfig` + config-owned `ConfigPaths`; `.unblock/` discovery + path resolution + libsql open-then-migrate) | T1.1 | ☑ | **DONE — [PR #375](https://github.com/websublime/unblock/pull/375) merged** (rebase-merged onto `main`). Carved out per CF-D — engine consumes config's `WorkspaceContext`; config OWNS path resolution (`ConfigPaths`) from T1.3a; config is L4 and cannot depend on engine L5 (build dep edge is engine→config). Full layered resolver = T1.3. **Spec-first** (design Review PASS_WITH_CHANGES, ≥3-agent): pin `WorkspaceContext`/`ResolvedContext`/`ResolvedConfig`/`ConfigPaths`/`ConfigError` (spine §4 CF-D + §2.1); restore `ConfigPaths` (config owns paths); `config→model` direct dep (Option A, Miguel — **PRD §8.1 + layering matrix amended**, ratified on merge); DAG re-sequence (config-min before engine). **Impl**: `open_workspace` (no DB) / `open_with_storage` (open_local→migrate→`Arc<dyn Storage>`) + `.unblock/` upward discovery + actor `UNBLOCK_ACTOR`→`$USER`→`"unblock"` + `ConfigError`(impl `CodedError`, forwards `source.code()`). **Verify gate PASS_WITH_CHANGES** (`ready_to_pr`, Security PASS): folded `exit_code()` asserts + facade-negative + reopen-idempotency tests + crate-plan dep-row fix. **23 tests** + clippy pedantic + layering NFR-15 + doc-lint green (verified in main session — subagent cargo sandbox-denied). Deferred → T1.3: full layered resolver, `&CliOverrides`, insta golden, 3 security seams. **Unblocks T1.2.** |
| T1.2 | `unblock-engine` (session + write Semaphore, D14) | T1.3a | ☑ | **DONE — [PR #376](https://github.com/websublime/unblock/pull/376) merged** (rebase-merged onto `main`). **Spec-first reconciliation landed**: plan↔spine drifts resolved before implementation. **OQ-1 RESOLVED (Miguel) — NO policy handle in v1**: `unblock-policy` ships only free fns (`cmp_ready` etc.), so `Session` has no `policy` field and `ready()` calls `unblock_policy::cmp_ready` directly (dyn-pluggable seam → v2 additive); spine §4.1 struct comment + plan `mod.rs` row fixed. **`recover()` → `DiagnosticReport`** (spine §4.1 verbatim): the phantom `RecoveryReport` (defined in no crate) dropped from the plan. **sync/health seam** pinned: `export/import/import_bd` (sync, T2.4) + **`doctor()`/`recover()`** (health, T3.3) land SIGNATURES now, bodies return typed `EngineError::FeatureNotWired{feature}` (never faked, never inline L3 logic). **`doctor()`/`recover()` are SEAM-DEFERRED to T3.3 (health), NOT v1-lite-now** (design-Review must-fix, Miguel): the signatures land returning `Result<DiagnosticReport>` (spine §4.1 unchanged) but the v1 bodies return `EngineError::FeatureNotWired{feature:"health"}` — an integrity report needs a `DiagnosticKind` variant for integrity/doctor that the landed enum lacks (`Stats|Info|Where|Version|Lint|Changelog|Orphans`, `results.rs:76-91`); the integrity `DiagnosticKind` + `DoctorReport`→`DiagnosticReport` mapping are a T3.3 design item (health owns it), **no model change at T1.2**. **Forward-note (D29, 2026-07-08 — supersedes the pre-D29 framing above; frozen history unchanged):** per D29 (T3.3 HEALTH-LITE) `recover()` moves to v1.1 (F1 — stays `FeatureNotWired{feature:"health"}` through v1) and `doctor()` is wired LITE at T3.3 REUSING `DiagnosticKind::Info` (F2 — NO new variant, NO spine §1.10/`CONTRACT_HASH` change), so no integrity `DiagnosticKind` variant is ever minted. The 7-kind read path `diagnostics(kind)` STAYS BUILD-now (kind is the input, never an integrity placeholder). **`EngineError` v1 = 3-source** (`StorageError`/`PolicyError`/`ModelError` transparent-wrap, `CodedError` impl) + engine-local (`WorkspaceNotOpen`/`ShutdownInProgress`/`WritePermitPoisoned`/`FeatureNotWired`); **`ConfigError` NOT wrapped** (CF-D: caller builds `WorkspaceContext` via `unblock_config::open_with_storage` and handles `ConfigError` before `Session::open`; engine never calls config facades); `Sync`/`Health` sources additive at T2.4/T3.3. **`SessionConfig` = spine bool** `{jsonl_export, import_on_open, remote}` (richer on-open opts / RemoteConfig are additive seams). **`ImportOptions` collision** (engine spine-owned `{dry_run}` vs sync-internal) resolved at T2.4 (map at call site). **T1.2 BUILD-now**: write-permit core (Semaphore(1)+WriteGuard+acquire_write, D14) · all reads (Storage delegation; `ready` via `cmp_ready`) · `diagnostics(kind)` (7-kind read path, kind is the input) · all mutations under permit · `Session::open(WorkspaceContext, SessionConfig)` · `EngineError` · shutdown flag-read + `init_tracing` · model-DTO re-exports · FR-9 linearizability + FR-10 read-during-write + cancel-safety over REAL libsql (`open_in_memory`/temp workspace, no mock). **SEAM-deferred**: export/import/import_bd (sync, T2.4) + `doctor()`/`recover()` (health, T3.3 — signatures land, bodies return `FeatureNotWired{feature:"health"}`). Doc-lint green. **Verify gate PASS_WITH_CHANGES** (ready_to_pr=FALSE → fixes folded, this PR): **(must-fix A)** `update` now LOADS the current issue, merges every patch field into a candidate `Issue`, and runs the FULL `IssueValidator::validate` under the permit (storage `update_issue` is validation-free, so a blank-title/NUL/over-length-or-whitespace `external_ref` update would otherwise persist unvalidated → now `EngineError::Model`, DB unchanged); **(must-fix B)** added a GENUINELY CONCURRENT FR-9 linearizability test (multi-thread runtime + in-flight `tokio::spawn`/`FuturesUnordered` over the shared real-libsql DB) asserting clean `integrity_check`, no lost writes, and outcomes consistent with SOME serial order (single durable claim winner; all creates present; last-writer update state) — non-vacuous (fails if the permit were removed); the existing single-threaded identity test kept. **`close_reason` PERSISTED** (Miguel decision): `IssuePatch.close_reason: Option<Option<String>>` added (spine §3.1, no migration — `close_reason TEXT DEFAULT ''` already exists); `update_issue` persists it; `close_with_suggestions(id, Some(reason))` now writes it (round-trip `get_issue(id).close_reason == Some(reason)`), removing the tracing-only gap. **should-fixes**: cancel-safety second half (parked-write drop → permit reusable + no partial state), read-method tests (`search` cap / `count` group-by / `stale`), `insta` per-kind `DiagnosticReport` goldens (golden-pin v1 shapes so T2.7 can't drift), `init_tracing` doc caveat (stays engine-owned — opt-in/idempotent/override-safe, unlike the cli OS signal handler). **Both gates PASS (Verify must-fixes folded + independently re-verified); [PR #376](https://github.com/websublime/unblock/pull/376) merged — ratified the spine §3.1 + `unblock-storage` `close_reason` cross-crate change. Unblocks T1.4/T1.5/T1.6 (+ T1.3 full config).** |
| T1.3 | `unblock-config` (v1 FULL: layered TOML/env/CLI precedence resolver, **additive over T1.3a** — no public-type/signature change) | T1.3a | ☑ | **DONE — [PR #377](https://github.com/websublime/unblock/pull/377) merged** (rebase-merged onto `main`). Both gates PASS. Full lifecycle as spawned Workflows in isolated worktrees (Understand→Decide→Spec→design-Review→Implement→Verify→Track). **Spec-first** (2 commits, design Review = PASS_WITH_CHANGES; 5 must-fixes + 6 should-fixes folded): **Miguel's 4 forks** — FORK-1 facade **OVERLOAD** (`&Path` stay + additive `open_*_with_cli(&CliOverrides)`, no swap); FORK-2 **`_unblock` monorepo alias** (amends **D8** — decision-change checklist applied: PRD D8/§12.5, spine §4, crate plan; zero stale `.unblock`-only framing); FORK-3 **symlink canonicalize+confine** (NFR-18, not reject); FORK-4 **actor precedence** `--actor`>`UNBLOCK_ACTOR`>`config.toml [actor]`>`$USER`>`"unblock"`. **MF-1**: `validate_actor` single-home in **`unblock-model`** (L0, additive — ≤200 chars `chars().count()` + NUL/control reject; config Seam A routes through it). **MF-3**: `backend`+`deletions_retention_days` parsed/classified but **not projected** to `ResolvedConfig` (reserved; `backend` `"libsql"`-only). **Q2** no `metadata.json`; **D2** no `DiscoveryFailed` (reuse `WorkspaceNotFound`); **MF-4** variant id `Parse`; **MF-5** `into_resolved()` infallible. **Impl**: `keys.rs`/`schema.rs`/`env.rs`/`cli.rs`/`merge.rs` (highest-first fold, proptest-pinned) + `config.rs` `WorkspaceConfig::resolve`/`validate`/`into_resolved` + `paths.rs` Seam-B confinement (reject absolute/`..`/sep + post-join `starts_with(canonical unblock_dir)`, filenames **and** `--db`) + `discovery.rs` `.unblock`/`_unblock` + Seam-C canonicalize (`dunce`, already in lock) + `context.rs` rewire + overloads + `error.rs` additive `Parse`/`Io`/`InvalidValue` + warn-only unknown-key + `auth_token` hard-deny (NFR-18). **Verify gate = PASS** (`ready_to_pr`, 4 adversarial incl. security/NFR-18 + coordinator; zero must-fix — the lone security MUST downgraded: FORK-3 spec explicitly accepts the canonicalize-then-open TOCTOU, attack needs in-workspace write access): frozen surface byte-identical; seams A/B/C + `auth_token` deny + merge engine + layering all confirmed; C1-C5 folded. **252 tests** + clippy pedantic + layering NFR-15 + doc-lint + insta(4) + cargo-deny green (re-verified in main session). Deviations accepted: discovery returns the `.unblock`/`_unblock` dir (workspace_dir=parent), `ProjectConfig`/`resolve` made pub, `ConfigPaths::derive`→`resolve`, `dunce` (lock-resident, justified for Windows UNC). **Out-of-scope flag → RESOLVED ([PR #379](https://github.com/websublime/unblock/pull/379), under the T1.1 epic):** the rare `unblock-policy` proptest (`never_includes_tombstoned`) was a **test/generator artifact, not a production bug** — the over-broad `arb_ancestor_chain` generated duplicate ancestor ids (impossible in storage; unique PK) and the property's `find`-by-id lookup was ambiguous on them; the generator now enforces **unique ids** + a duplicate-id unit test pins the position-based tombstone-safety (production logic byte-unchanged). **Unblocks T1.4/T1.5/T1.6** (full config now landed). |
| T1.4 | Issue lifecycle FR-1a/1b/1c, FR-2, FR-3 | T1.2 | ☑ | **DONE — [PR #380](https://github.com/websublime/unblock/pull/380) merged (rebase) into main at `8c4c9d7`; CI 10/10 green (initial `fmt`-only failure fixed in `style(test)`). Both gates PASS.** AC-proof + test-completeness task (the engine mutation surface was already built at T1.2; T1.4 adds ZERO new `Session` methods). **Spec-first (design Review PASS_WITH_CHANGES):** D1 (impl-plan: engine `update` is **single-id**; the multi-id loop is the MCP `issue` adapter at T2.3, spine §4.1), D2 (`write.rs` doc-label FR-1c→FR-1b), Q1 (bulk-markdown = thin MCP/CLI parser looping `Session::create` at T2.3/T3.1 + a T2.3 AC clause — Miguel), Q2 (`reopen` == `update(status)` mapped by MCP T2.3 — Miguel), Q3 (open-enum `IssueType`/`Status` + strict `Priority` 0..=4 satisfies FR-1a "invalid enum" — Miguel). **Tests:** FR-1c never-run `DeleteMode::Cascade`/`Hard` now covered at **storage** (behaviour + NFR-16 contract cases over a dotted-id `{target}.%` corpus: per-affected `Deleted`-event guard, terminal-child `deleted_by`-without-event, dot-boundary decoy `ub-10`, bounded blast radius, Hard≠Cascade child rows survive) **and engine** (`delete()` Cascade/Hard/Tombstone + DryRun-reports-non-empty-plan); reparent-via-`update{parent}` cycle + self guards (exit 5) through the patch path; reparent move/detach/no-op; `updated_at`/no-op through the engine; max-populated create round-trip (incl. parent + deps). **Production fix (Verify iter-1 surfaced; Miguel-decided as a bug):** a real reparent was SILENT (no `updated_at`, no event) — contradicting FR-1b "reparent… updated_at advances" + inconsistent with add/remove_dependency; `apply_reparent` now advances `updated_at` + emits `DependencyRemoved`(old)+`DependencyAdded`(new) transactionally with actor propagation; no-op stays silent; `content_hash` unperturbed (timestamps/relations excluded). **FR-1c "recoverable" recovery path → new v1 task T1.7** (`Session::restore`); the Gap-2 test documents the current tombstone-retained-but-not-patchable behavior. **Gates:** design Review PASS_WITH_CHANGES (folded); Verify 2 iterations (iter-1 FAIL on the reparent drift → fix → iter-2 PASS_WITH_CHANGES, folds applied; never-run cascade/hard confirmed non-vacuously covered; no security/data-integrity hole). Green via cargo exit-0: clippy pedantic, `--workspace` test, storage `--features testkit` (behaviour+contract), engine writes. |
| T1.5 | Querying FR-4 | T1.2 | ☑ | **DONE — [PR #381](https://github.com/websublime/unblock/pull/381) merged (rebase) into main at `76d87c7`; CI 10/10 green. Both gates PASS.** Querying FR-4 via the engine; the read surface (list/ready/blocked/search/count/stale) was built at T1.2, so T1.5 is AC-proving + ONE production change. **Spec-first (design Review PASS_WITH_CHANGES):** OQ-A (one engine determinism test for the NFR-14 ordering half — Miguel), **OQ-B (`blocked()` composes the `list` narrowing facets — Miguel; minted as `D18` with the full decision-change checklist)**, OQ-C (`count()` default = list-parity — Miguel, confirmed). D1 (spine §3.2.1: pin `list`/`count`/`stale` ORDER BY for NFR-14), D2 (PRD FR-4 footnote: "full-text" = `instr()` substring in v1, FTS5 → v1.3). **Production change (D18):** `blocked_issues` now composes the narrowing facets (status-OR/`issue_type`-OR/inclusive `priority` range/`assignee`/`labels_all` AND/`labels_any` OR/`text_contains` title-only) via a new `compose_facets` extracted so `build_filter_where` stays **byte-identical** for its 4 callers (list/search/count/stale); `blocked`'s baseline stays **deferred-inclusive** (`include_closed`/`include_deferred` are no-ops on blocked). **Tests:** filters-compose intersection (flip-each non-vacuity, the headline AC), label AND≠OR, priority-range direction-pinned (P0=CRITICAL=lowest excluded), blocked-composes + the REQUIRED deferred-preservation guard (a `status=Deferred` blocked row, proving blocked did NOT inherit list's `NOT IN ('deferred')`) + cross-backend contract leg (NFR-16), `count` all 5 group-by + None/total + Label double-count + `COALESCE` empty-assignee key, count=list-parity, `ready` uncapped >50, `text_contains` title-only≠search + ESCAPE guard, determinism (id-ASC tiebreak). **Gates:** design Review PASS_WITH_CHANGES (caught the deferred-inclusive regression trap — blocked must NOT reuse `build_filter_where`); Verify PASS_WITH_CHANGES (folded: completed the D18 fan-out across ci-cd/CLAUDE.md/doc_lint D-range). Green via cargo exit-0: fmt, clippy pedantic, `--workspace` test (602), storage `--features testkit` (contract+behaviour), engine reads, doc-lint. |
| T1.6 | Dependencies & graph FR-5 (petgraph) | T1.2 | ☑ | **DONE — [PR #382](https://github.com/websublime/unblock/pull/382) merged (rebase) into main at `d3ea629` (impl); both gates PASS.** AC-proving + test-completeness task (the 6 Storage dep ops + 5/6 Session forwards were built at T0.6/T1.2); the spec-first pass reconciles the drifts/fixes Understand surfaced before Implement. **Miguel decisions (Q1–Q4 → minted D19):** **Q1/D2** — `add_dependency` rejects a gating cycle with the **REAL ordered cycle path naming every node** (`a -> b -> c -> a`) via a private `find_cycle_path` DFS over the petgraph built in `would_cycle_in_tx`, NOT the synthetic `a -> … -> a` placeholder (the crate plan already mandated `find_cycle_path`/the actual path; PRD FR-5 AC tightened). **Q2/D3** — `detect_cycles` returns **ordered traversal witnesses** (`[start,…,start]`, self-loop `[node,node]`), faithful to the original, NOT alphabetically-sorted SCC node sets. **Q3/D1** — add the missing `Session::list_dependencies(&self, id) -> Result<Vec<Dependency>, EngineError>` read forward NOW (no write permit, full name, 1:1 with the storage method) — fixes the spine §4.1 self-inconsistency that blocked the T2.3 dep `list` action (mcp→engine→storage only). **Q4 → NEW D19** — `detect_cycles` gains `blocking_only: bool` (true = the 4 gating types = `affects_ready_work`, current behaviour; false = ALL dep types, integrity/lint view), faithful to original `detect_blocking_cycles(true)`/`detect_all_cycles(false)`; the dep `cycles` MCP action surfaces it (default TRUE). **D4 (correctness FIX, not a fork)** — `would_cycle_in_tx`/`detect_cycles` insert `parent-child` edges REVERSED (parent→child, matching unblock's own parent→child blocked propagation + original `check_cycle`:2440/`load_dependency_cycle_graph`:11379), the other three gating types forward — spine §3.2.1 normative pin. **D5 (info)** — `load_all_edges` `unwrap_or(Blocks)` is dead (infallible parse → `Custom`); optional cleanup at Implement. **Spine edits:** §3.2 `detect_cycles(blocking_only)` + ordered-witness comment; §3.2.1 NEW dep-ops semantics block (source-cited); §4.1 `list_dependencies` forward + `detect_cycles(blocking_only)`; §5.2 `Cycles{blocking_only}` (default TRUE) + §5.3 `Cycles` witness-meaning note. **PRD:** D19 minted (§4); FR-5 AC tightened. **Crate plans:** storage deps row (real path + ordered witness + D4 orientation), engine read.rs forward, mcp dep tool, fuzz. **impl-plan T1.6** folds the reconciliations. **D-id range D1..D18→D1..D19** bumped (ci-cd §2.1, doc_lint.rs comment, CLAUDE.md; zero stale `D1..D18` hits). **IMPLEMENTED.** Single shared `build_gating_graph` orientation home (parent-child reversed once, used by `would_cycle_in_tx` + `detect_cycles`); `would_cycle_in_tx` returns the real ordered path (`Option<Vec<String>>`) feeding `add_dependency` + `apply_reparent` from the SAME graph. **GATE-MUST-4 deviation (Miguel-ratify):** the PROSPECTIVE edge is oriented by its own type (parent-child reversed), NOT literal-forward as the original `check_cycle` — the original misses pure parent-child reparent cycles (its `test_get_ready_issues_recursive_parent_cycle` adds 3 pc edges closing a cycle, all succeed), but unblock rejects them since T1.4; documented fidelity improvement (spine §3.2.1 + deps.rs), Verify-gate re-derived **provably sound + complete**. Tests: M-E1 `ready_reflects_remove_dep_immediately` (the unproven remove-edge AC half), M-E2 named cycle path through engine, M-E3 list_dependencies, M-E4 tree/cycles forwards, M-E5 error codes; M-S1 ≥3-node interior-node path, M-S3 mixed/pure/conditional/waits-for gating cycles (gate D4, RED on forward-all), `detect_cycles_blocking_only_param`, witness shape, non-empty-roots graph, deterministic tree, non-gating no-cycle; strengthened the 2 weak cycle assertions. **Design Review** PASS_WITH_CHANGES (3 lenses, all folded); **Verify** PASS_WITH_CHANGES `ready_to_pr=true` (4 lenses: D4-correctness/test-non-vacuity/code-quality/security-integrity + coordinator). Clean rebuild exit-0: fmt, clippy pedantic (workspace + storage `--features testkit`), `--workspace` test (617), storage testkit contract+behaviour (45), doc-lint (19 docs), layering. |
| T1.7 | Issue restore/undelete (FR-1c "recoverable") | T1.4 | ☑ | **DONE — [PR #383](https://github.com/websublime/unblock/pull/383) merged (rebase) into main at `4d1f472`; both gates PASS. Spawned from T1.4 Gap-2 (Miguel decision); mints D20.** Dedicated `Session::restore(id)` (write permit) → new `Storage::restore_issue(id, actor)` un-tombstones a SOFT-deleted issue; MCP `issue` `restore` action wired at T2.3. **The 4 Miguel decisions (D20), baked into spine §1.8/§3/§3.2.1/§4.1/§5 + PRD §4 D20/FR-1c/NFR-8 + crate plans:** **(1) target status best-effort via `closed_at`** (`closed_at.is_some() ? Closed : Open` — pre-delete status not preserved; self-satisfies the issues-table CHECK on both branches); **(2) audit = a single `Event(Restored)`** (NEVER `StatusChanged`/`Reopened` — a dedicated path; spine §3.2.1 EventType-table **carve-out** is the test oracle); **(3) `issue_type` UNTOUCHED + `original_type` cleared → `None`** — **CORRECTS** the old "restore `original_type`→`issue_type`" wording (that write is a no-op locally and CORRUPTS imported rows; `into_tombstone` never mutates `issue_type`); **(4) scalar/non-cascading in v1** (no `--cascade`/ancestor guard; cascade-restore = v1.1 seam needing a delete-batch identity). **Resolutions (S1–S10):** S1 one TOCTOU-safe tx (load-in-tx); S2 missing/hard-deleted → `IssueNotFound` (bounds "recoverable" to soft deletes; **no new ErrorCode** — frozen unblock-error golden/exit-code/retryable set); S3 non-tombstone → idempotent no-op `Ok(issue)` (no event, no `updated_at`); S4 closed_at CHECK-driven (Open→NULL, Closed→KEEP — correctness-critical, a miss → rollback → silent failure); S5 recompute `content_hash` (includes status; was-Closed restore legitimately differs); S6 re-read via `get_issue` for relation hydration; S7 restored issue re-enters the graph with surviving edges (may be immediately blocked — TRAP B); S8 DISJOINT from the import no-resurrection invariant (NFR-8 now import-scoped); S9 TTL reserved/unenforced in v1 (`deletions_retention_days` = v1.1 seam); S10 model adds pure inverse `Issue::restore_from_tombstone` (no clock) + a round-trip proptest. **D-id range D1..D19→D1..D20** bumped (ci-cd §2.1, doc_lint.rs comment, CLAUDE.md; doc-lint definition set is PRD-§4-data-driven, NO regex/logic change; zero stale `D1..D19` hits). **Impl** (commit `389d4bc`): model `restore_from_tombstone` (pure inverse, no clock) + storage `restore_issue` (libsql `crud` + `NoopStorage` + test `ParkedStorage`) + engine `Session::restore` + tests (model round-trip proptest + 5 units; storage behaviour: was-Closed/CHECK, deps-survive, hash+`updated_at`, hard-deleted; NFR-16 contract in-memory+temp-file; engine: recover/no-op/hard-deleted; **Gap-2 test updated** to the positive restore path). **Gates (≥3-agent adversarial):** design Review **PASS_WITH_CHANGES** (0 must-fix; 5 should-fixes folded — roadmap v1+v1.1 rows, `update_issue` guard SSOT + retargeted cross-refs, `delete-guard`→`update tombstone-patch guard`, restore re-read tx/`None` clause) + Verify **PASS_WITH_CHANGES** (0 must-fix; lenses **mutation-tested** the green suite — Restored→StatusChanged / drop-`closed_at` / rewrite-`issue_type` all caught; 2 should-fixes folded incl. a **mutation-proven non-vacuous** no-op guard `updated_at`/`content_hash` `==`). Final main-tree verify green: fmt, clippy×2, `--workspace` test (630), storage `--features testkit` (82, contract in-memory+temp-file), doc-lint (19), layering. **M1 complete** (T1.1–T1.7 all ☑) — closes the FR-1c "recoverable" AC; unblocks M2 (T2.1 `unblock-render`). |
| T1.8 | Engine id allocator (root mint + child alloc + slug; under the D14 permit) FR-1a | T1.2 | ☑ | **DONE — [PR #386](https://github.com/websublime/unblock/pull/386) merged (rebase) into main at `5e3cd45`; both gates PASS.** design Review (FAIL → folded `next_child_number` trait method + faithful slug prefix-budget → re-gate PASS) + Verify (PASS_WITH_CHANGES, `ready_to_pr`; suite independently green — 728 workspace + 82 storage-testkit + 101 engine + 179 model; port byte-faithful, mint atomicity mutation-proven non-vacuous, id-input safe). **Mints D21.** Late-surfaced by T2.2 (MCP `issue.create` had no way to mint an id). Adds the engine **minting** create path `Session::create_issue(NewIssue) -> Issue` (spine §4.1) that mints the id **under the write permit**: root `ub-<hash>` (faithful port of `temp/beads_rust-main/src/util/id.rs` — SHA-256 length-prefixed seed → first-8-bytes `u64` → base36-lowercase, **adaptive length** `3..8` via the birthday-problem heuristic `P<0.25`, nonce `0..10`→grow-length→12-char/`…{nonce}` saturated fallback), slug variant `ub-<slug>-<hash>` (`normalize_slug` lowercase-alnum + single hyphens cap 48, then `normalize_slug_for_prefix` budget-fits `<prefix>-<slug>` within `MAX_ID_PREFIX_LEN`=64, hash always appended, empty/budget-drop→hash-only), hierarchical child `parent.N` via the **`Storage::next_child_number` trait method** (libsql body built at T0.6 as `pub(super)`; **T1.8 promotes it onto the trait** so the engine's `Arc<dyn Storage>` can call it). Mint **probes** existence via `storage.get_issue(id).await?.is_some()` (the existence probe reuses `get_issue` — there is **no** `Storage::exists`); probe→insert + child-counter read→bump are ATOMIC under the SAME permit (no `parent.N` race — **WHY it's engine, not L7**; FR-9 single mutation home). **`Session::create(&Issue)` STAYS** for the import/internal path (preserves ids — FR-26; never mints). **Placement (D21):** pure candidate-compute (hash/seed/adaptive-length/slug-normalize) in `unblock-model` `id.rs` (I/O-free, reuses the parser home); stateful retry-loop + storage probe in the engine allocator. **Prefix CONFIG-DERIVED (default `ub`):** via an ADDITIVE `unblock-config` `id_prefix` key (`normalize_prefix`-normalized, faithful to `IdConfig::with_prefix`), read by the allocator from `ResolvedConfig` at mint time — NOT a constant; T1.8 Implement spans **model** (pure helpers incl. `normalize_slug_for_prefix`) + **config** (additive prefix key, no public-type break) + **storage** (promote `next_child_number` onto the `Storage` trait + `NoopStorage`/test-stub impls) + **engine** (allocator), consuming `Storage::next_child_number`. **Spec-first lands:** D21 (PRD §4) + spine §3.2 (`next_child_number` trait method) + §3.2.1 (`next_child_number`/create_issue notes) + spine §4.1 (`create_issue`/`NewIssue`, `create` annotated) + §5.2 `Create`/bulk-markdown mapping + FR-1a + impl-plan T1.8 + crate plans (engine/model/config/storage). **Gate FAIL folded:** MF-1 (`next_child_number` promoted to the `Storage` trait so the engine can call it), MF-2 (faithful `normalize_slug_for_prefix` prefix-budget truncation restored), SF-1 (`get_issue(id).await?.is_some()` — async+fallible probe), SF-2 (`compute_id_hash` takes the LAST `len` base36 chars), SF-3 (relaxed `id.rs` header note). **D-id range D1..D20→D1..D21** bumped (CLAUDE.md, ci-cd §2.1, doc_lint.rs comment; doc-lint D-set is PRD-§4-data-driven, NO regex/logic change; zero stale `D1..D20` hits). **Unblocks T2.2 + T3.1** (both consume `create_issue`). |

## M2 — MCP surface (primary)  *(gate: MCP client does ready→claim→close; bd import works)*

| id | Task | Depends on | Status | Notes |
|---|---|---|---|---|
| T2.1 | `unblock-render` (reduced) | M1 | ☑ | **DONE — [PR #385](https://github.com/websublime/unblock/pull/385) merged (rebase) into main at `02e0410`; CI 11/11 green; both gates PASS.** First M2 task; full spawned-Workflow lifecycle (Understand→Decide→Spec→design-Review→Implement→Verify→Track). **Miguel decisions:** (1) timestamp = single `fmt_ts` RFC-3339/Utc/`SecondsFormat::Secs`/`Z` helper (recorded **T2.4 cross-task dep** — see T2.4 row); (2) CSV escaping = **HAND-ROLLED byte-faithful** `escape_field`+`writeln!` (formula-injection guard kept) — `csv` crate dropped from runtime → **dev-dep only** (parse-back); (3) CSV `ALL_FIELDS` = original **15 curated** (not the 38-field expansion; json/robot carry full set). **Spec-first** (commit `5dfee01` `docs(plan)`, design Review PASS_WITH_CHANGES, 5 must-fix + 9 should-fix folded; **no spine change**): crate-plan reconciliations D1 (`toon=["unblock-model/toon"]` propagates the model marker so re-exported `OutputFormat::Toon` is visible), D5 (render-private types NOT spine §1.10 contract types; spine §6.1 binds only re-exported spine §1.10 DTOs), D6 (OQ-1 CLOSED = all 5 backends in v1; deferral = forbidden simplification, impl-plan line 58 is the AC), D7 (`StructuredError` **context VALUES** sanitized in plain/markdown/csv — spine §2.4:503), OQ-B (fmt_ts pin + T2.4 dep + intentional-deviation-from-original-bytes note), OQ-C (TOON deferred v1.1, feature plumbing + cfg-empty `backend/toon.rs` only). **Impl** (`e902944` feat + `5acbe13` test + `9975719` chore): object-safe `Renderer` trait + `renderer_for→Box<dyn Renderer>`; **5 backends** (json+robot one impl, plain, csv, markdown); `RenderError` = **3 locked variants** (Serialize→`JsonError`, UnsupportedFormat/FieldUnknown→`ValidationFailed`; one-error-one-code spine §6.5); `sanitize_inline` ported (escapes `\n`/`\t`, byte-equiv original) + `sanitize_text` = **re-export** `unblock_error::sanitize_message` (no divergent copy); `fmt_ts` sole timestamp path; `pick_format` precedence (CLI>env>cfg>Json). **NFR-18:** all untrusted strings sanitized incl. open-enum `Custom` labels, embedded IDs, `context` values; CSV formula-injection guard; FR-11 always-valid-JSON `structured_error`. **Deviations (sound, non-simplifying):** `snafu` added to deps (CLAUDE.md error convention, reconciled in plan); `OutputFormat` `Display`/`FromStr` via free-fns + sealed `OutputFormatExt` + `FormatName` (orphan rule — `OutputFormat` model-owned); `compaction_level: Some(0)` parse-back fixture (model serializes `None`→`0`, D12). **Gates:** design Review PASS_WITH_CHANGES (folded); **Verify PASS `ready_to_pr=true`, 0 must-fix** (qa-expert independent cargo on a ff-merged worktree, toolchain 1.96.0 — fmt/clippy×2(+`--features toon`)/test×2 **66/66**/no-default/`xtask check-layering`/insta+4 doctests all green; NFR-18 security lens: zero sanitization holes; D7 ESC tests non-vacuous). **Hand-off (NOT this PR):** `parse_format` unknown-name returns `UnsupportedFormat{format:Json}` with a misleading `Display` — fix touches the LOCKED 3-variant `RenderError` → reconcile with spine at **T2.2**. **Unblocks T2.2** (`unblock-mcp` rmcp serve). |
| T2.2 | `unblock-mcp`: rmcp stdio serve | T2.1, T1.8 | ☑ | **DONE — [PR #387](https://github.com/websublime/unblock/pull/387) merged (rebase) into main at `7da524f`; both gates PASS.** design Review PASS_WITH_CHANGES (rmcp 1.7 API confirmed buildable — 8/8 pinned claims survived falsification, incl. the hand-written `get_info` coexisting with the stacked `#[tool_handler]`/`#[prompt_handler]`) + Verify PASS_WITH_CHANGES (`ready_to_pr`; suite independently green — 42 crate / 770 workspace tests; **M2 lifecycle gate non-vacuous** over a real `serve_with_ct` in-memory duplex; conformance (spine §6) holds — 7 tools ≤8, zero backend type in the public API; NFR-18 untrusted-input boundary safe — quota preflight on every tool, always-valid-JSON; the `test-util` test seam does not leak the shipped public surface; S1/S2/N1/N2 folded — server identity `("unblock", version)` + instructions + fail-closed quota). **Depends on T1.8** (engine id allocator, D21).** Option A (full-crate scope, ratified by Miguel):** T2.2 lands the FULL `unblock-mcp` surface (server + 7 tools + 5 resources + 3 prompts + error mapping + quotas-preflight infra + capabilities/schema builders + the M2 lifecycle gate `query{ready}→claim→close`); T2.3 is the AC-proving sibling (taxonomy conformance closing PRD §12.2, schemars-drift gate, quota AC suite, bulk-markdown, cli_parity). **rmcp 1.7 API PINNED** from vendored source (OQ-1/OQ-2 resolved — `#[tool*]`/`#[prompt*]` macros, `Parameters<T>`, hand-written resources, in-band `structured_error`, `serve_with_ct` + `tokio-util`; no blocking unknowns). **restore taxonomy edit** applied (impl-plan §6 + crate plan `issue.rs` → 7 actions incl. `restore`, D20). **Create path (T1.8):** the `issue.create`/quick-create action maps `IssueInput::Create` → `NewIssue` and calls the MINTING `Session::create_issue` (engine mints the id; quick-create returns `.id`). Render `parse_format` fix HANDED to T3.1 (mcp never calls it). |
| T2.3 | MCP taxonomy conformance + AC-proving | T2.2 | ☑ | **DONE — [PR #388](https://github.com/websublime/unblock/pull/388) merged (rebase) into main at `1ee527e`; both gates PASS. Mints D22; EXPANDED by Miguel's F7 decision** from pure AC-proving into a real **create-surface field extension + the bulk-markdown feature** (NOT a simplification — a faithful scope grow). **D22 (PRD §4):** (1) bulk surface = a NEW `IssueInput::CreateBulk { markdown: String }` discriminator on the `issue` tool (inline content, NOT a path — tool count stays 7 ≤ 8, spine §6.6); (2) `parse_bulk_markdown` = a byte-faithful port of `temp/beads_rust-main/src/util/markdown_import.rs::parse_markdown_content` (`src/tools/bulk_markdown.rs`, pure); (3) **TRULY ATOMIC bulk create — Miguel-ratified option (b)** (R5 RESOLVED): all-or-nothing at TWO levels — (3a) pre-mutation parse+validate (ONE `code`+`hint`, ZERO writes on any malformed/unresolvable block; deviation from the original best-effort `continue` — PRD safe-import discipline wins, NFR-8) AND (3b) one-`BEGIN IMMEDIATE`-tx mint+insert via the NEW engine `Session::create_bulk(Vec<NewIssue>)` over the NEW storage `Storage::create_issues(&[Issue], actor)` — rollback-on-ANY-failure (mint exhaustion, out-of-band id/`external_ref` collision, any backend error on record #k after 1..k-1 staged) → ZERO issues persisted. **Required because the minting path is NON-IDEMPOTENT** (unlike id-preserving import): a partial-batch loop + re-run would produce DUPLICATES with no clean recovery, so the batch MUST be one atomic unit — never N independent `create_issue` calls (the R5 hole the consistency check found); (4) **2-phase intra-file dep/parent resolution** (F8 — title/`### ID` stand-in → minted ids) done IN MEMORY by the engine under ONE permit (the `get_issue` probe consulting committed state + the in-batch minted set), wired into the built `Issue`s before the one tx; output reuses `ToolOutput::Issues` (F3); (5) **create-surface field extension (F7):** `design`/`acceptance_criteria`/`assignee`/`agent_context` (the markdown-captured set per `apply_section_to_issue`) ADDED to `NewIssue` (engine) + `CreateInput` (mcp) + the `create_issue` mapping + spine §4.1/§5.2 — **`notes`/`owner` NOT added** (no markdown section sets them) — **NO model change** (`Issue` already carries all six); scalar create + bulk both full-fidelity; (6) **`max_batch` wired into `enforce_quota`** (F5 — bulk record cap pre-mutation); (7) **CONTRACT_VERSION bumps** (the `create_bulk` arm + 4 `Create` fields change the `issue` schema — the FR-12 gate fires by design); (8) **hash-coupled drift gate** (F6 — `SCHEMA_BUNDLE_HASH` pinned next to `CONTRACT_VERSION`; a schema change FORCES a bump, not a silent re-bless; non-vacuous both directions). **FOLDED multi-crate scope (Miguel-ratified — the atomic bulk primitive is part of THIS task, NOT a separate precursor):** storage `Storage::create_issues` (one-tx batch) + engine `Session::create_bulk` (mint-all-under-one-permit + in-memory dep resolution + one storage tx) + create-field extension + bulk parser + `max_batch` + the 4 AC suites + the fault-injection rollback test — staged as ONE T2.3. **Artifacts A0–A5** (impl-plan T2.3): **A0 the ATOMIC bulk primitive** — storage `create_issues` (one `BEGIN IMMEDIATE` tx via a `pub(super) insert_issue_in_tx` helper both paths share; `NoopStorage` + engine stubs implement it) + engine `Session::create_bulk`; A1 bulk-markdown feature + parser (parser UNCHANGED — pure; the adapter maps validated records → `Vec<NewIssue>` → `Session::create_bulk`, NOT a loop); A2 create-field extension; A3 hash-coupled `contract_version` gate (`tests/contract_suite.rs` — BUMP + re-bless 2 goldens at Implement); A4 NFR-18 quota AC suite (`tests/quotas.rs` — RecordingStorage spy + `connect_with_quotas` + `max_batch` cap + F9 path string-bound, real `../` at T2.4); A5 taxonomy-conformance golden (live `list_tools`≤8=7/resources=5/prompts=3 + builder-vs-router parity, closes PRD §12.2) + **F4** `tests/cli_parity.rs`→`tests/mcp_session_parity.rs` (MCP-vs-`Session`; no CLI until T3.1). **Fault-injection rollback test (storage + engine):** a mid-batch insert failure leaves ZERO rows (the all-or-nothing proof). **Spec-first lands:** D22 (PRD §4 — atomic design + R5-resolution rationale) + FR-1a/FR-12 AC + §12.2 closure + spine §3.2/§3.2.1 (`create_issues` one-tx) / §4.1 (`Session::create_bulk` + `NewIssue` +4 fields) / §5.1 (`create_bulk` 8th-action nit) / §5.2 (`CreateBulk` arm → `Session::create_bulk` + `Create` +4 fields) / §5.3 (`Issues` reuse note) / §6.6 + crate plans (`unblock-mcp.md` + `unblock-engine.md` + `unblock-storage.md`) + impl-plan T2.3. **R5 RESOLVED** — the spec now states TRUE atomicity (no partial commit); the "all-or-nothing/atomic" wording is now ACCURATE (no downgrade needed). **D-id range D1..D21→D1..D22** bumped (CLAUDE.md, ci-cd §2.1, doc_lint.rs comment; doc-lint D-set is PRD-§4-data-driven, NO regex/logic change; zero stale `D1..D21`). **Gates (adversarial, ≥3 lenses each):** design Review PASS_WITH_CHANGES — the dedicated **atomicity lens caught a real buildability gap** (same-parent siblings in one batch would collide `parent.N` via the committed-only `next_child_number` → guaranteed whole-batch rollback) → folded the **in-batch per-parent child counter + parent-before-child topological mint order** + the full reject set + the one-tx rule → **re-verify PASS**. Verify **PASS `ready_to_pr`** (0 must-fix): suite independently green — **834 workspace + 86 storage-testkit + 79 mcp**; **NO partial-commit path** (atomicity mutation-proven non-vacuous — an N-tx mutation makes the rollback + racer tests FAIL); byte-faithful port (grammar + full reject set, no thinning); conformance holds — **7 tools ≤ 8, zero backend type in the public API**, `CONTRACT_VERSION` bumped; NFR-18 bulk untrusted-input safe (doc bounded pre-parse, batch-capped before any mint, no parser DoS/panic, always-valid-JSON). 2 should-fixes folded (timing-independent test flake-hardening + bulk error-message truncation) — independently re-verified GREEN after a mid-verify background-agent crash (edits preserved as a patch + recovered). **CONTRACT_VERSION `unblock.mcp.v1`→`v1.1`** + hash-coupled `SCHEMA_BUNDLE_HASH`. Non-vacuous headliners: `out_of_band_racer_rolls_back_whole_batch`, `create_issues_rolls_back_whole_batch_on_mid_batch_collision`, `two_same_parent_children_mint_distinct_child_numbers`, the 6 reject-case tests, the hash-coupled drift gate, the `RecordingStorage` zero-Session-call quota proof. **Unblocks T2.4/T2.6/T2.7.** |
| T2.4 | `unblock-sync` (light JSONL export/import) | T2.2 | ☑ | **DONE — [PR #389](https://github.com/websublime/unblock/pull/389) merged (rebase) into main at `a65992a`; both gates PASS; CI 11/11 green.** Builds `unblock-sync` (L3) as a light JSONL export/import engine + wires engine `Session::{export_jsonl,import_jsonl}` bodies behind `#[cfg(feature="sync")]`; `import_bd` STAYS `FeatureNotWired{"sync"}` (T2.5). **Miguel's 4 forks (D23):** **FORK-1A** export includes tombstones via a NEW `ListFilters.include_tombstone: bool` (default false) — spine §1.10 contract + model `filters.rs` + storage `visibility_branch` (widest-visibility branch) + policy `filters_fingerprint` (INJECTIVE; wire-format golden re-blessed); export sets `include_tombstone=true`+`include_closed=true`; import anti-resurrection via `get_issue` (returns tombstoned rows). **FORK-2A** import collision DEFAULT = `Skip` (idempotent no-op via `sync_equals`/`content_hash`, FR-26, faithful to D5); `Error`/`OverwriteIfNewer` internal opt-ins on sync's `ImportOptions`; MCP `sync` surface NOT widened (spine §5.2 exposes only `dry_run`; NO `CONTRACT_VERSION` bump). **FORK-3A** ingestion caps `MAX_IMPORT_FILE_BYTES = 2 GiB` + `MAX_LINE_BYTES = 4 MiB` (preflight fd-metadata + bounded streaming read; overridable-later without a v1 break). **FORK-4B** the canonical RFC-3339 `SecondsFormat::Secs`+`Z` formatter is LIFTED into `unblock-model` (`fmt_ts_secs`, L0 single source of truth); render's `fmt_ts` rewires to call it, sync uses it on export (D-OQ-B render↔export byte-coherence). **MF-1/D23 clause (5) — import apply is ATOMIC:** classify via read-only `get_issue` probes then apply the new-id subset through ONE `Storage::create_issues` tx (rollback-on-any → zero rows) under the D14 write permit; never a per-record loop (reuses the T2.3 atomic primitive; faithful to the original single-tx import). **Drift/gap folded:** F-2 additive `#[cfg(feature="sync")] EngineError::Sync{source:SyncError}` (transparent, `CodedError` forwarding; spine §4.1 additive note); DRIFT-1 atomic write adds the parent-dir fsync after rename (NFR-4/5, faithful `util::durable_rename`); storage `create_issue` trait-doc fixed (does NOT dedup — idempotency lives in sync). **Invariants:** D5 (no git, no merge); NFR-4 atomic temp→flush→sync_all→rename→parent-dir-fsync; NFR-7/8 path confinement (`.git`/`..`/symlink-escape rejected; force never bypasses `.git`); tombstone-non-resurrection (existing Tombstone + incoming non-Tombstone → skip, DB unchanged, even under force). **Gates:** design Review (FAIL→MF-1 import-atomicity resolved by Miguel = A1 atomic `create_issues` tx→re-gate) + Verify (PASS_WITH_CHANGES, ready_to_pr; headline invariants — tombstone-guard-first + k-of-n rollback — mutation-proven non-vacuous by 3 lenses) each ≥3-agent adversarial (PROCESS §4). Post-merge CI hotfix `fix(test)` (`a65992a`): a macOS-`/tmp`-symlink-dependent MCP path-confinement test made deterministic (explicit `..`-traversal, rejected lexically). **Unblocks T2.5/T2.6/T2.7.** |
| T2.5 | `bd` one-shot import FR-26 (generic bd→unblock) | T2.4 | ☑ | **DONE — [PR #391](https://github.com/websublime/unblock/pull/391) merged (rebase) into main at `8c2ab04`; both gates PASS.** Mints **D24** (bd-import fidelity contract, FR-26/D16, 6 coupled forks, Miguel-ratified; D-range D1..D23→D1..D24): bd_import-local 7-repair pass (spine §1.8 NOTE; shared `normalize()` unchanged) / preserve ids verbatim (`PrefixMismatch` reserved, remap→v1.1) / extend `ImportReport{dependencies,comments}` (no `CONTRACT_VERSION` bump — DTO not in schema bundle) / synthesized byte-faithful fixture / `dropped_fields`=unknown-keys / `apply_records`+`preflight_source` reuse seam. **Builds** `unblock-sync/src/bd_import.rs` (7 bd repairs faithfully ported in bd SOURCE ORDER from `normalize_issue` — general underscore dep-type rule, dep dedup, terminal-status aliases→Closed before closed_at set/clear, wisp→ephemeral, external_ref blank/trim — composed with shared `jsonl::normalize()` so the recomputed `content_hash`==bd's stored hash → FR-26 cross-tool dedup + idempotent rerun) + `Session::import_bd` cfg-split wiring (D14 permit) + `ImportReport` extension + shared FR-8 preflight (`preflight_source`: confinement/conflict-markers/bounded-read/`IssueValidator`/dup-id, zero-writes on failure). **design-Review gate PASS_WITH_CHANGES → folded (amended spec commit):** MF-1 general dep-type underscore rule (not just `parent_child`); MF-2 `apply_records` count plumbing (caller-finalized); MF-3 `ImportReport` ripple (golden re-bless + 5 literal sites, no `Default`); MF-4 bd-path FR-8 preflight; SF-1..SF-9 (per-repair non-vacuity, source-order, both `external_ref` branches, hash composition, dry_run/opts, KNOWN_*_KEYS drift guard, `ApplyDecision::Import(Box<Issue>)`, doc_lint token, roadmap `(D16/D24)`). **Verify gate (≥3 + security NFR-18) PASS_WITH_CHANGES → folded:** deps/comments counts scoped to the APPLIED subset (over-reported on rerun — bd counts only Insert/Update, never Skip) + rerun (`0/0`)/mixed-import count assertions; mutation-proven non-vacuous; QA lens independently ran full CI (923 pass); no panic/DoS/backend-leak, atomic tx under D14. **Closes part of the M2 dogfood DoD ("a representative bd repo imports").** |
| T2.6 | Self-describing contracts FR-12 | T2.2, T2.3 | ☑ | **DONE — [PR #392](https://github.com/websublime/unblock/pull/392) merged into main at `540e4ee`; both gates PASS; CI 10/10 green. Mints D25 + a D24 forward-amendment note.** Recomposed scope (Understand drift-gap report, Miguel-ratified): the literal one-liner landed at T2.2/T2.3 and is machine-proven, so T2.6 = (1) the FR-12 AC-2 client-side drift e2e — live duplex `read_resource` over ALL 5 URIs + the unknown-URI branch, client-side parse into `Capabilities`/`SchemaBundle`, `contract_version` equality (F-1); (2) the `-32002` `resource_not_found` mapping fix it exposes (F-2); (3) **FORK-1B** SchemaBundle recomposed to 7 per-tool `{input,output}` schema pairs + the shared in-band error schema — outputs materialized as wire-identical per-tool untagged unions constructed at every success path (spine §5.4 "JsonSchema per tool I/O" becomes TRUE as written; also fixes the spine §5.3 sketch that missed the landed delete/dep-add/dep-remove shapes) + **FORK-2B** ONE pinned hash — `CONTRACT_HASH` (supersedes `SCHEMA_BUNDLE_HASH`) over the ordered two-document tuple `(capabilities(), schema_bundle())` (the capabilities-document descriptor copies + error map incl. `hint_shape` + all input/output schemas) — designed JOINTLY as one hash/gate design, non-vacuous BOTH directions; `CONTRACT_VERSION` `unblock.mcp.v1.1`→`unblock.mcp.v1.2` + `CONTRACT_HASH` re-pin (`d3cbd6c0…dc70`) + goldens re-blessed; REVERSES the D24 clause-3 forward rule (T2.5's no-bump ruling stays historically valid — explicit forward-amendment note on the PRD §4 D24 row, never a silent rewrite); (4) **FORK-3A** `unblock://issues/{id}` not-found folds `unblock_error::find_similar_ids` fuzzy hints over the FULL id corpus (cap 3, scan-failure surfaces), faithful to the original `issue_not_found_resource` (F-9); (5) **FORK-4B** `HintShape{None,StaticText,ContextualText,SimilarIds}` + `hint_shape`/`static_hint` const views in `unblock-error` + `hint_shape` on `ErrorCodeDescriptor` (spine §5.4 error-map line satisfied as written), grounded in a real hint-site survey — honest 3/1/1/30 map (F-8); (6) closures — F-5 non-vacuous ErrorCode quadruple cross-check (serde wire round-trip) + plan-row wording, F-6 the 3 prompt rendered-message snapshots over live `get_prompt` (golden-only, NOT version-coupled), F-10 the `diagnostics{kind:version}` `mcp_contract_version` test, F-11/F-12 hygiene. **D-id range D1..D24→D1..D25** (CLAUDE.md, ci-cd §2.1, doc_lint.rs comments — the doc-lint D-set is PRD-§4-data-driven, NO regex/logic change; the closing grep sweep is clean modulo its enumerated expected-hit sets — KEEP/EXTEND rows + the self-referential supersession mentions). **Gates (adversarial, ≥3 lenses each):** design Review PASS at round 3 (2 prior rounds converged — rust-engineer clean PASS; the residual was a self-referential grep off-by-one, fixed); Verify **PASS** — 4 lenses (code-review, qa-expert, rust-engineer, security/NFR-18), 0 must_fix, QA mutation matrix 9/9 RED (non-vacuity proven), wire-identity of the untagged unions `to_value`-verified, `#![forbid(unsafe_code)]` intact, no new network/git surface. **Accepted v1.1 optimization seam:** each `issues/{id}` miss hydrates the full id corpus via `Session::list` (read path has no quota preflight; bounded per-request — 256-char Levenshtein cap, distance 3, no reachable panic). **Two non-blocking follow-ups** (PR body): a `read_resource` call-site quota-absence doc-comment + a direct box-transparency unit test. **Open content-fork for later (deliberately NOT T2.6):** the 7 live rmcp-attr description strings diverge from their capabilities-document copies — taxonomy parity pins name/URI SETS only. |
| T2.7 | Diagnostics FR-15 (pure-DB) | T2.2 | ☑ | **DONE — [PR #393](https://github.com/websublime/unblock/pull/393) merged (rebase) into main at `5da7ab4`; both gates PASS; CI green. CLOSES M2** (T2.1–T2.7 all ☑; the M2 gate — MCP client does ready→claim→close + bd import — is met). Mints **D26** (five ratified OQ resolutions, PRD §4): OQ-1 changelog `since` threading (`Session::diagnostics(kind, since: Option<DateTime<Utc>>)`, bare arg + wire default — the D19 `detect_cycles` precedent; mcp adapter STOPS dropping `since`); OQ-2 keep THIN `id`=`title` `{label,detail}` findings (no grouped DTO); OQ-3 PORT bd's template-section lint (Bug/Task/Feature/Epic required `## …` sections, case-insensitive over `description`, template/terminal rows excluded, one finding per missing section — REPLACES the `blocked=<n>`-lite finding); OQ-4 status-agnostic `external_ref`-commit-pattern orphans + non-vacuity test; OQ-5 PORT bd's rich `StatsSummary` (per-status counts, blocked/ready, pinned, tombstone-excluded total, epics-eligible, avg lead time) MINUS the git-activity block (NFR-6). All enrichment rides generic `DiagnosticFinding{label,detail}` rows ⇒ **NO `CONTRACT_VERSION` bump** (v1.2 stays). **Design-Review RATIFIED the NARROW stats primitive (Miguel):** bd-faithful `stats` `epics_eligible_for_closure` needs ONLY the per-epic child rollup as a primitive — `pinned` is composed in-memory (`issue.pinned || status == Pinned`) and the per-status/tombstone tallies come from the EXISTING `count_issues` (`include_tombstone:true` for the tombstone bucket) — so the ONE additive pure-DB method is `Storage::epic_child_rollup() -> Vec<(String,(usize,usize))>` (per-epic `(child_total, child_closed_or_tombstone)`, `ORDER BY` epic id in SQL for NFR-14, bd's `get_epic_counts` ported 1:1; internal aggregate, NOT a wire DTO — no `CONTRACT_HASH` impact, no new `StatsRollup` type). **Tombstone framing corrected:** `total` ALREADY excludes tombstones today (no over-count); T2.7 ADDS the tombstone VISIBILITY/counter. Emission order PINNED for NFR-14 (stats + lint); `changelog` filters templates (SF-2). Dropping the counters is a forbidden simplification. **Spec-first + design-Review must-fixes landed:** D26 (PRD §4) + FR-15 AC + spine §4.1 `diagnostics(kind, since)` / §3.2.1 diagnostics bullet + `since` asymmetry / §5.3 advisory-findings note / §3.2 `epic_child_rollup` primitive note + crate plans (`unblock-engine.md`, `unblock-mcp.md`, `unblock-storage.md`) + impl-plan T2.7 + **D-id range D1..D25→D1..D26** (CLAUDE.md, ci-cd §2.1, doc_lint.rs comments — the doc-lint D-set is PRD-§4-data-driven, NO regex/logic change). **Implement (3 commits):** storage `epic_child_rollup` (bd `get_epic_counts` 1:1, static SQL, `ORDER BY` epic id) + `NoopStorage`/testkit/sync stubs + NFR-16 contract case + behaviour test [`f642a0b`]; engine `diagnostics(kind, since)` bd-faithful dispatch — `changelog` `closed_since(since)`+template-filter, template-section `lint` (REPLACES `blocked=<n>`), rich `stats` (per-status/tombstone/`total` via `count_issues(include_tombstone:true)`, `pinned` in-memory, `blocked` deduped union, `ready`, `epics_eligible` via `epic_child_rollup`+in-memory epic filter, `avg_lead_time` absent-when-None, PINNED order), `orphans` status-agnostic [`77db74a`]; mcp adapter threads `since` (was dropped) [`d2cc6a4`]. **Gates (each ≥3-agent adversarial, PROCESS §4):** design Review PASS_WITH_CHANGES (narrow `epic_child_rollup` ratified by Miguel + tombstone-visibility premise + NFR-14 order pins folded); Verify PASS_WITH_CHANGES → **mutation-testing caught 2 VACUOUS stats tests** (`epics_eligible` single-eligible-epic corpus; `blocked` dedup never asserted) → folded non-vacuous ([`4e83101`]: `stats_epics_eligible_excludes_ineligible_epics`, `stats_blocked_counts_overlap_once`, Task/Feature+Custom lint cases — **re-verify: mutations d/d2/e now RED**) + SF-1 default-visibility doc precision ([`6704ac4`]). **CI green** (963 workspace tests + storage contract/behaviour testkit + mcp 99 incl. `contract_hash` gate + insta + doc-lint + check-layering); **CONTRACT_HASH `d3cbd6c0…dc70` / `unblock.mcp.v1.2` UNCHANGED** (findings stay `{label,detail}`; no schema/capabilities/contract golden re-blessed); NFR-6 clean (no git/`Command` symbol on any diagnostics path); security (NFR-18) clean (static/parameterized SQL, no panic/backend-leak). **Deferred follow-up (NOT this PR):** the byte-frozen `since` doc-comments in `crates/unblock-mcp/src/tools/diagnostics.rs` keep their stale "dropped — T2.7" wording to hold `CONTRACT_HASH` — a re-word is a future `CONTRACT_VERSION`-bump task. |

## M3 — Reliability + ops + GA  *(gate: shutdown/failure-injection + perf budgets green)*

| id | Task | Depends on | Status | Notes |
|---|---|---|---|---|
| T3.1 | `unblock-cli` (lifecycle: serve/migrate/doctor/version/init/agents/update) | M2 | ☑ | **DONE — [PR #394](https://github.com/websublime/unblock/pull/394) merged (rebase) into main at `5b4d745`; both gates PASS. Mints D27.** Builds `unblock-cli` (L7) — the reduced `unblock` lifecycle/ops binary (D3 set: serve, migrate, doctor, version, init, agents, update); a thin adapter over the engine that calls `unblock_mcp::serve` for `serve`, `unblock-render` for output, and `unblock-error` for the 0–8 exit boundary. **D27 (PRD §4, seven clauses, reconciled spine-first vs the live code @ b384103):** AF-1 doctor-lite = `Session::diagnostics(Stats\|Lint\|Info)` + a NEW pure `Session::integrity_check()` (non-empty integrity → `DatabaseError` exit 2; Lint/orphans advisory; the full Healthy/Drifted/Recoverable/Unsafe taxonomy + `--repair` land at v1.1 over the wired `doctor()`, which itself wires the lite body at T3.3 — D29); AF-2 NEW `Session::migrate()` under the D14 write permit → engine-local `MigrateOutcome{from,to,applied}`, backed by a NEW `Storage::schema_version()` (PRAGMA user_version, 10 impls; a newer DB → transparent `SchemaMismatch` exit 2); AF-3 `init` scaffold = hand-written `config.toml` (normalized `id_prefix`) + a migrated empty `unblock.db` via the config facade ONLY (clobber guard → CLI-local `AlreadyInitialized` exit 2; no `.gitignore`/`metadata.json`/`issues.jsonl`); AF-4 `impl CodedError for RenderError` + the 4th variant `UnknownFormat{name}` + an explicit `McpServerError`→`InternalError` (exit 1) map. AD-1 no v1 command mints; AD-2 the four CLI-local reports render via mapping onto `DiagnosticReport`; AD-3 clap-`env` FR-13 CLI half (config owns all layering); AD-4 the LIVE 2-arg `unblock_mcp::serve(Arc<Session>, ServeOptions)` + FR-17 shutdown (signal-hook → `CancellationToken` + `AtomicBool` + 128+signo; Windows no-op; no unsafe); AD-5 `build.rs` pure `option_env!` metadata (no git/network/vergen). **Spec-first** (design Review PASS_WITH_CHANGES; 2 must-fix + 5 should-fix folded — the false `CloseOutcome` analogy → engine-local `ImportOptions`; impl-plan + STATUS T3.2/T3.3 re-scoped to the FR-17 reliability-GATE + full-health so the shutdown wiring + doctor-lite that land at T3.1 are not double-scoped; version env-precedence resolved for the no-workspace path). **Impl (8 atomic commits: seams → cli-src → cli-tests → serve-SIGTERM fix → non-vacuity folds):** storage `schema_version`, engine `integrity_check`/`migrate`/`MigrateOutcome`, render `UnknownFormat`+`CodedError`; the full cli crate (cli/dispatch/exit/output/logging/shutdown/commands×7/build.rs) + 40 unit tests + the integration matrix (the golden 0–8 exit contract, serve `initialize`+`ready→claim→close`+SIGTERM→143, migrate idempotent+schema report, doctor-lite corruption exit-2, init clobber+`--prefix`, the no-git static gate, cli/engine parity, update attestation via `wiremock`); plus a real `fix(cli)` (SIGTERM was hanging on the blocking stdin read → now a clean 143). **Verify gate PASS_WITH_CHANGES → re-Verify PASS (`ready_to_pr`):** mutation testing caught 2 vacuous-coverage must-fixes (the doctor AF-1 exit-2 flip + the `CliError::Mcp`→exit-1 mapping) → folded with mutation-proven non-vacuity (extracted pure `doctor_exit`/`build_doctor_report`; `test-util`-gated `McpServerError` constructors that do NOT leak the shipped surface). CI-equivalent green (fmt/clippy-pedantic/**1049 tests, 0 fail**/insta/doc-lint/layering + the `--no-default-features` feature-matrix). **Lands the deferred `no-network` gate** (T0.9 ledger) as `tests/no_git_gate.rs`. **Deferred follow-up:** a real tampered-download attestation test needs a dist-receipt fixture (TODO T3.6/GA). **Unblocks T3.2–T3.7.** |
| T3.2 | Cooperative-shutdown reliability gate FR-17/NFR-5 | T3.1 | ☑ | **DONE — [PR #395](https://github.com/websublime/unblock/pull/395) merged (rebase) into main at `1b689fd`; CI 10/10 green; both gates PASS** (design Review PASS_WITH_CHANGES + Verify PASS, 0 must-fix). Re-scoped (D27): the FR-17 shutdown WIRING (signals → `CancellationToken`+`AtomicBool` → `Session::shutdown()`, exit `128+signo`, second-signal escalation) LANDS in T3.1/AD-4; T3.2 is the adversarial NFR-5 failure-injection PROOF over that serve loop (SIGTERM mid-write commits-or-rolls-back, no WAL corruption) — it does NOT re-build the machinery. **Test-only (no shipped-code change):** deterministic anchors — C4 engine drain-to-commit barrier (`shutdown()` PARKS then commits `count==N`) + C5 SIGKILL abandoned-tx WAL-recovery (`count==0`, raw-libsql test-only `[[bin]]`) — plus invariant-only real-signal e2e (C1/C2/C3/C6 + C-doctor + `#[ignore]` C-neg/C2-soak). **Non-vacuity mutation-proven** (5 production mutations → target test RED, reverted). Miguel forks: FORK-1 layered (no shipped seam); FORK-2 SIGKILL C5 in-scope; FORK-3 default job + gated soak; FORK-4 doc tag `NFR-4/NFR-5`→`NFR-5`. Post-merge CI drift fix (unrelated to the test content): `crossbeam-epoch` 0.9.18→0.9.20 for RUSTSEC-2026-0204 (dev-only via criterion, not in the shipped binary). **Defect follow-up (2026-07-17) → T3.2.1 (mints D38):** the AC's `exit == 128+signo` + second-signal-escalation proofs were **PARTIAL** — every e2e case here completes the MCP `initialize` handshake first, so the pre-handshake `Err(ServerInitializeError::Cancelled)` path (which HANGS forever) was never exercised, and the 5-mutation non-vacuity matrix did not cover the `commands/mcp.rs` `Err` early-return. The C2 exit-1 relaxation (`6e5b72b`) papered over this defect with a factually wrong "blocked stdout pipe write" diagnosis (refuted at T3.2.1: `common::write_without_reading` background-drains stdout to EOF, so the pipe can never block). This row STAYS ☑ — T3.2's landed content is not reverted; the gap is closed by T3.2.1. |
| T3.2.1 | FR-17 signal-exit defect: a signal delivered BEFORE any client handshake never exits (hang) + a signalled `Err` path discards `128+signo`. Spawned from the T3.2 gate. Mints **D38**; **PRE-GA, GA-BLOCKING** | T3.2, T3.4.2 | ☑ | **DONE — [PR #423](https://github.com/websublime/unblock/pull/423) merged into main at `c94648a` (2026-07-17); both gates PASS. Branch `t3.2.1-fr17-signal-exit-hang`. Mints D38 (Miguel, 2026-07-17); amends D27/AD-4.** **Defect (orchestrator-verified, deterministic, 3/3 darwin — not inferred):** `unblock mcp` signalled before a client completes the MCP `initialize` handshake **NEVER EXITS**. Chain: `shutdown.rs` records `signalled = 143` + cancels the token → rmcp 1.7 `serve_server_with_ct` `select!`s the WHOLE handshake against `ct.cancelled()` → `Err(ServerInitializeError::Cancelled)` → `McpServerError::Transport` → `commands/mcp.rs` early-returns **past** the `signal_exit_code()` → `128+signo` guard (Ok-path-only) → exit 1 → the `#[tokio::main]` runtime drop blocks forever in `BlockingPool::shutdown` on the parked `tokio::io::stdin()` read (`sample` stack captured). A SECOND signal kills it with 143 → proves `signalled` WAS recorded then discarded, and that the escalation branch is **load-bearing** today — the SOLE exit on the pre-handshake path, which the C6 doc's "accepted gap" note (true as far as it went: the escalation LINE is e2e-indistinguishable) OMITTED. Same root cause as the CI flake in `c6_second_signal_escalation_never_hangs_and_keeps_a_valid_exit_code` (`got Some(1)`, run 29513091656) — post-handshake there is no parked stdin read, so it exits 1 in microseconds instead of hanging. **D38 (two ratified clauses):** **(1) the signal wins, on the cooperative-shutdown returns** — a recorded signal yields `128+signo` on the `run_mcp_server` return (Ok AND post-cancel Err) AND the `session.shutdown()` return; the teardown `Err` is a *consequence* of the cancellation → emitted by `commands/mcp.rs` as a human stderr diagnostic (NFR-14), never the exit code; a failure raised BEFORE the run loop starts (e.g. `Session::open`) is NOT a consequence of the cancellation and keeps its spine §2.3 0–8 code; with NO signal a genuine `Err` stays exit 1 rendered per FR-11 (never swallowed); both cooperative returns still reach `session.shutdown()`. **(2) no-hang on every return path** — including a **panic unwind** (`ManuallyDrop`; a `shutdown_background()` after `block_on` is dead code on an unwind). (1) alone is insufficient because the ratified mechanism returns the signal exit as a VALUE, so the signal path flows through the runtime drop too — a restore-blocking-drop mutation still hangs the pre-handshake case (4 tests RED, genuine 20s hangs). **CORRECTED at the Verify gate (2026-07-17): the earlier "the no-signal `Err` path hangs too" is FALSE and retracted** — measured 3x independently, that case does not hang and is never RED under that mutation (rmcp returns after a complete message without re-issuing `receive()`, so no stdin read is parked); the no-hang proof is carried by the **pre-handshake signal** case. **Mechanism RATIFIED by the design gate (2026-07-17), not open:** `main.rs` owns the runtime explicitly (`Builder::new_multi_thread().enable_all()`) + `rt.shutdown_background()` after `block_on` → `mcp::run` returns `Ok(Some(128+signo))` through `run_with` and the in-command first-signal `std::process::exit` is **REMOVED** (the rejected in-place guard fires only when a signal was recorded → re-encodes the defect's false premise, leaves (2) unmet). The **second-signal escalation** `process::exit` in `shutdown.rs` is NOT superseded — it stays NORMATIVE, demoted from load-bearing to a backstop. **Stale-corpus corrections (same PR):** the C2 rationale ("blocked stdout pipe write") is FALSE (`common::write_without_reading` background-drains stdout to EOF) → the `6e5b72b` C2 widening is **re-tightened to `assert_eq!(code, Some(143))`** (widening an assertion as the fix is the prohibited simplification); the C6 "accepted gap" framing is **EXTENDED/SHARPENED, not falsified** (the escalation LINE stays e2e-indistinguishable — true before and after; what was omitted is that the branch is the SOLE pre-handshake exit). C6's own `matches!(code, Some(143 | 130))` is a legitimate two-signal DELIVERY race (both `128+signo`) and is NOT tightened. **NO new `ErrorCode`, spine §2.3 exit table UNCHANGED, no `CONTRACT_HASH`/`CONTRACT_VERSION` bump** — `128+signo` is the D27/AD-4 signal convention, OUTSIDE the D35-stable 0–8 taxonomy, so this is a bugfix conforming behavior TO the ratified spec, not a semver event. D-range `D1..D37`→`D1..D38` at the 3 sites (`CLAUDE.md`, ci-cd §2.1(a), the `xtask/src/doc_lint.rs` comment incl. the tokenizer alternation). AC = impl-plan T3.2.1 (a deterministic pre-handshake-signal → exact `128+signo` test that FAILS pre-fix; a no-signal `Err` → exit 1 without hang; mutation-proven non-vacuity; full 11-job CI-equivalent probe). **GA-BLOCKING** — `unblock mcp` is the primary runtime command and child-per-client (D31) makes signalled teardown the NORMAL path; a hang strands a process holding the `.write.lock`. HOLDS the `v1.0.0` tag alongside T3.9. **PR #423 merged (`c94648a`, 2026-07-17) — flipped to ☑; both gates ≥3 passed, human-merged.** **Verify gate iteration 1 = FAIL → iteration 2 (2026-07-17), all findings resolved:** the new regression test was FLAKY (measured RED under CPU load: `left: None` = WIFSIGNALED — the fixed 300ms settle expired while the child was still opening/migrating the DB, so the signal beat `install()`) → replaced with a DETERMINISTIC in-band **pre-`initialize` ping barrier** (rmcp answers ping pre-handshake and loops back to `expect_next_message`, so the response proves: run loop up ⇒ `install()` done, stdin read parked, handshake incomplete) — 5/5 green under the same load that reproduced the flake. **Miguel ruling 1** → D38 clause **(1a)** labelling (`is_cancellation()`, ADDITIVE, no contract bump; cancellation → `tracing::debug!`, genuine → `error[CODE]` stderr). **Miguel ruling 2** → `shutdown::install()` moved ABOVE the workspace open (no more hard kill mid-`migrate()`). **Orchestrator rulings 3/4** → the **panic** path no longer hangs (`ManuallyDrop`) and the **unsignalled displaced teardown error** is no longer dropped by `run.and(teardown)`. **Spec corrected (must-fix 4/5):** the unmeasured "no-signal `Err` path hangs" causal claim is retracted in PRD/impl-plan/STATUS and AC(3) re-scoped to what it proves. **Non-vacuity re-proven:** gutting `emit_diagnostic` → 2 tests RED (it SURVIVED at iteration 1); gutting the debug arm → RED; reverting the demotion → 5 RED; removing the precedence guard → `left: Some(1) right: Some(143)` + 6 unit RED (orchestrator re-verified in the main tree); restoring the blocking drop → 4 tests RED with genuine 20s hangs. **Verify gate iteration 2 = PASS_WITH_CHANGES, `ready_to_pr`, 0 must-fix** (2 adversarial lenses + coordinator; the closure-audit lens reproduced every RED-turning mutation, trees restored clean; orchestrator independently re-ran the original pre-handshake hang probe → `143` 3/3 where it was `HANG` 3/3 pre-fix, and confirmed the routine-SIGTERM stderr is now quiet). **3 follow-ups filed (Miguel-ratified as out-of-scope for this GA-blocking PR, NOT blockers):** (a) pin the panic-path no-hang with a D30-style feature-gated panic-injection seam (`ManuallyDrop` is correct-by-construction + measured 4/4, but a bare-local mutation survives the suite — a regression-guard gap); (b) **D40 — RESOLVED, PR #425 merged (2026-07-20):** the deferred "decide the EOF-pre-handshake exit code" is DECIDED — a pre-`initialize` client disconnect with NO signal now exits **0**, not 1 (mints **D40**, PRD §4; ADDITIVE `is_pre_handshake_disconnect()` + `resolve_mcp_exit` teardown-delegation; no `ErrorCode`, exit table UNCHANGED, no contract bump; lands PRE-`v1.0.0`-tag on branch `d40-eof-exit-0` as a T3.2.1 follow-up, NOT a new T-row); (c) test the D38 scope boundary (a pre-run-loop open Err coinciding with a signal keeps its 0–8 code — guaranteed today only by the shape of `?`). Residual NIT (RESOLVED by D40): a post-signal `Transport{ConnectionClosed}` no longer prints `error[INTERNAL_ERROR]` — `diagnostic_route` now demotes `ConnectionClosed` on both the signalled and unsignalled paths. |
| T3.3 | `unblock-health` (LITE) FR-16 | T3.1 | ☑ | **DONE — [PR #398](https://github.com/websublime/unblock/pull/398) merged (rebase) into `main` at `af7a540`; both gates PASS. HEALTH-LITE (D29, Miguel 2026-07-08).** Both gates PASS: design Review PASS_WITH_CHANGES (2 must-fixes folded) + Verify PASS_WITH_CHANGES (mutation 5/5 RED, WAL deviation RATIFY, must-fixes + 4 should-fixes folded); full CI-equiv probe green (1124 passed / 5 ignored). D29 gained the ratified `TruncatedWal 0<len<32` addendum (F5-A — a 0-byte live WAL is valid under the persistent-open `Session`; the beads literal `len<32` would false-positive on every healthy workspace). Build the six v1 modules of the currently-empty `unblock-health` crate (`lib`/`error`/`level`/`paths`/`file_state`/`doctor`): libsql `integrity_check` passthrough + PURE `classify_file_state` (the 7-variant `FileAnomaly` subset) + storage-free `run_doctor(integrity_rows: &[String], paths) -> DoctorReport` (F3 — the engine calls `Session::integrity_check()` and passes the `Vec<String>` rows in; NO `unblock-storage` dep; `sync` is an unused edge kept for v1.1 `classify.rs`) + the stable 4-variant `HealthLevel` (v1 produces only Healthy/Recoverable/Unsafe; Drifted is v1.1). WIRE ONLY `Session::doctor()` off the `FeatureNotWired{health}` seam with the lite body, mapping `DoctorReport`→`DiagnosticReport` REUSING `DiagnosticKind::Info` (F2 — contract-neutral: NO new variant, NO spine §1.10 change, NO `CONTRACT_HASH` bump) + the additive `#[cfg(feature="health")] EngineError::Health{source:HealthError}` source (`HealthError::code()` maps to an EXISTING ErrorCode — IoError/DatabaseError/InternalError — NO new code). The cli `doctor` ROUTES through the now-wired `doctor()` (F4 — forward-refines D27/AF-1; keeps exit 2 only on detected corruption). Severity map (F5): `JsonlConflictMarkers`→Unsafe; `DatabaseMissing`/`DatabaseNotSqlite`/`TruncatedWal`/`SidecarMismatch`/`JournalSidecarPresent`/`OrphanedLockFile`→Recoverable (lock path `.unblock.lock`, D8; ported faithfully — defensive/vestigial under the D14 Semaphore). **STAY v1.1:** the ACTIVE 4-state taxonomy, `--repair`, the `Session::recover()` body (STAYS `FeatureNotWired`, F1), the `.unblock/.recovery/` evidence writer, `classify_workspace`, `audit.rs`, and the CF-E `Storage::diagnostic_probe(s)` seams. |
| T3.4 | Reliability gates NFR-4/5/13 | T3.1 | ☑ | **DONE — [PR #399](https://github.com/websublime/unblock/pull/399) merged (rebase) into main at `669c2e2`; both gates PASS. Mints D30 (four Miguel fork resolutions, 2026-07-09).** **(1) NFR-13 field standardization:** every `unblock.reliability` emission carries `operation`/`path`/`result`/`reason` via the `unblock-sync` SSOT macro trio `reliability_guard!` (INFO) / `reliability_warn!` (WARN) / `reliability_detail!` (DEBUG) — the WARN arm is REQUIRED so the live unix-perms warn (`atomic.rs:73`) and the non-unix parent-fsync-skip DEBUG (`atomic.rs:97`) route through the SSOT WITHOUT re-leveling (re-leveling either would be a forbidden silent simplification); the single `RELIABILITY_TARGET` const is hoisted to the L0 crate `unblock-error` (engine re-exports, sync imports — the `init_tracing` `EnvFilter` filter-target can never diverge from the emit-target); the named test `tracing_capture_reliability` (in `unblock-sync`, where the emissions live) drives the REACHABLE conflict-marker-rejection guard and asserts one INFO event carrying all four field VALUES — a dep-free hand-rolled `tracing_subscriber::Layer` + `field::Visit` FALLBACK (`tracing-capture` rejected on the transitive budget: it pulls `predicates 2.1.5`, a `multiple-versions` duplicate of the workspace `predicates 3.x`). **(2) Emergency-override-written-reason = a v1 FORWARD SEAM:** the reason gates `allow_external` on the sync-internal `ExportOptions`/`ImportOptions` (an override without a written reason = `SyncError::ExternalOverrideWithoutReason` → the existing `ErrorCode::PathTraversal`/exit-6, NO new code) + emits the reason on the force-override/external-path INFO; `allow_external` is UNREACHABLE in v1 (engine forces it `false` at `session/interchange.rs`, the MCP `sync` tool does not expose it, the CLI has no export/import command) → NO `CONTRACT_VERSION`/`CONTRACT_HASH` bump and NO public-spine `ImportOptions` change; the DB-persisted policy-bypass parity (`bypass_reason`) STAYS v1.1 (`crates/unblock-policy.md` `src/gates.rs`). **(3) CI split:** T3.4 owns ONLY the NFR-5 stress+interleaving half, INTEGRITY-only at a MODEST corpus (a new `stress-integrity` CI job + a targeted `-p unblock-sync --features fault-injection` clippy+test leg); the NFR-18 single-serve rate-limit + NFR-1/NFR-2 perf/scale/250k gates go to T3.5. **(4) fault-injection = a feature-gated per-stage seam** in `unblock-sync/src/atomic.rs` (write / flush / `sync_all` / rename / parent-dir fsync), EXPORT-side, process-global `AtomicU8` (NO `static mut` — `#![forbid(unsafe_code)]`), each call-site `#[cfg(feature = "fault-injection")]` so it compiles OUT of the shipped binary; import failure-injection = the DB-tx rollback→zero-rows proof + an out-of-process SIGKILL/ENOSPC case. **Four NFR-5 gates proven:** `failure_replay.rs` (LITE doctor posture — `unblock_health::classify_file_state` + `Session::integrity_check()`, NOT the v1.1 audit/full-taxonomy posture); `atomic_failure.rs` (the per-stage seam, 5 fault points, assertions SPLIT by rename ordering — Write/Flush/SyncAll/Rename PRE-rename leave the original byte-identical + zero orphan `*.tmp`; `ParentDirFsync` is POST-rename so the target already holds the new export) + the out-of-process `export_kill` `[[bin]]` SIGKILL-mid-write; `stress_longlived.rs` (a MODEST 10^3–10^4-op mixed run over ONE workspace, integrity-only, + an `#[ignore]`-gated ~20k soak); `interleaved_families.rs` (create/update/close vs export/import vs read families under an integrity invariant). **Both gates PASS:** design Review **PASS_WITH_CHANGES** (B1 must-fix = the NFR-13 "standardize ALL" completeness gap — two omitted `atomic.rs` emitters + the missing WARN arm — plus 9 should-fixes folded; doc-lint green) + Verify **PASS_WITH_CHANGES** (mutation matrix 6/8 CAUGHT at the gate; the 2 survivors — the reason-gate predicate + the SSOT DEBUG arm — folded as mutation-proven tests, alongside the bd-path `operation="import_bd"` vocab-fidelity fix; re-probe 11/11 CI-equiv green: 1136 workspace + 91 `unblock-sync` fault-injection tests). **Deviations:** `allocate_temp_file` extracted (behavior-preserving); `preflight_source` STAYS pure — the conflict-marker guard INFO is emitted only where `preflight_source` is called (the orchestrator bodies); `ImportOperation` is a `Copy` enum pinning the `"export"`/`"import"`/`"import_bd"` operation vocab. **Spec-first lands:** D30 (PRD §4) + the NFR-4/5/13 AC + the PRD §13 M3 row + the spine L7→L3 reliability reconcile + the ci-cd `stress-integrity`/`rate-limit` split + the crate plans (`unblock-sync.md`/`unblock-engine.md`/`unblock-error.md` — the `RELIABILITY_TARGET` L0 home/`unblock-health.md`) + the D1..D29→D1..D30 decision-change checklist. Depends on T3.1. |
| T3.4.1 | D-lock: cross-process advisory write lock (D14 amendment). Mints D31 | T3.4, T3.3 | ☑ | **DONE — [PR #405](https://github.com/websublime/unblock/pull/405) merged (rebase) into main at `db60139`; both gates PASS. Mints D31 (D14 forward-amendment, Miguel 2026-07-09).** Surfaced by Miguel's TUI-vs-serve question → an 8-agent analysis found the D14↔PRD §8.2 incoherence + the silently-dropped beads cross-process lock. **design Review PASS_WITH_CHANGES** (folded: the lock must be WHOLE-MUTATION scope, not tx-scoped — the engine mints `parent.N` from a pre-tx `next_child_number` read). **Verify FAIL → loop-back → re-Verify PASS:** mutation-testing caught 2 vacuous ACs (tests drove the storage primitive, never the production engine/migrate paths) + 2 unspecced implementer simplifications Miguel ruled toward fidelity — closed with an engine-level two-process test (dropping the engine acquire → 15 cross-process IdCollisions) + a `migrate()`-under-held-lock test, the ported MF4 re-entrancy guard, and the reverted MF2 (migrate now takes the lock UNCONDITIONALLY). Restore beads' cross-process advisory write lock as a NEW file `.unblock/.write.lock` (an L2 `unblock-storage` primitive — `std::fs::File::try_lock`, `#![forbid(unsafe_code)]` holds, NO fs2/fd-lock crate), reconciling D14 DOWN to PRD §8.2 (child-per-client = the SUPPORTED topology). Whole-mutation scope via a NEW `Storage::acquire_write_lock` guard the engine holds across the `next_child_number` allocation READ + the write tx (composes BELOW the retained `Semaphore(1)` D14, ABOVE the write-conn Mutex + `BEGIN IMMEDIATE`); `migrate` takes the SAME exclusive lock timeout=0 fail-fast for the whole command, unconditionally — before the version check (read-only `doctor` takes NO lock — a Miguel fork); timeout reuses `StorageError::DatabaseLocked` (NO new ErrorCode / NO `CONTRACT_HASH`/exit-table/golden churn); an additive `unblock-config` `write_lock_timeout_ms` key (default 30000, a Miguel fork over a bare constant) threaded to `open_local`. Design Y (faithful): `unblock-health`/F5/its snapshots + `file_state.rs` STAY UNCHANGED (the vestigial `.unblock.lock` detector stays never-written; the separate `.write.lock` is crash-released, never orphans). Contention lab gains a no-lock CONTROL leg (retains the WAL busy-witness + the M0 no-hot-spin proof) + a flock-contention witness + ASYMMETRIC mixes + a PROVISIONAL p99 fairness ceiling; a two-process `[[bin]]` test proves cross-process id-collision exclusion (AC-1: active `.write.lock` → distinct ids; the no-lock control REPRODUCES the IdCollision). Spec-first cascade landed FIRST. Depends on T3.4 + T3.3. |
| T3.4.2 | `serve`→`mcp` full rename (CLI subcommand + library fn/types + concept nouns). Mints D32 | T3.1, T3.4.1 | ☑ | **DONE — [PR #406](https://github.com/websublime/unblock/pull/406) merged (rebase) into main at `f56dba8`; both gates PASS. Mints D32.** Full spawned-Workflow lifecycle (Spec → design Review → Implement → Verify). **Verify FAIL (iter 1: caught a functional regression — `mcp.json`, the repo-root MCP launch config, still ran the removed `serve` subcommand) → PASS_WITH_CHANGES (iter 2); the final doc residuals were closed via an authoritative repo-wide `git grep -nw serve/Serve` re-attest** (the earlier `rg`-on-`*.rs` greps under-scoped, missing config/README/crate-plan surfaces). NO `CONTRACT_HASH`/`CONTRACT_VERSION` bump (`contract_suite__schema_bundle.snap` untouched; `contract_version` stays `unblock.mcp.v1.3`). 2 atomic bisectable-green commits (docs / code). **Unblocks T3.4.3** (AGENTS.md capabilities table, mints a new D-id). Rename the primary MCP subcommand `serve`→`mcp` (clap `Mcp(McpArgs)`; the former unblock serve command becomes `unblock mcp`) AND the library entry to `run_mcp_server(Arc<Session>, McpServerOptions)` (the `#[doc(hidden)]` test-util seams become `mcp_server_duplex_for_test`/`mcp_server_duplex_unclamped_for_test`; identifiers per spine §0.1 SSOT) AND rewrite the "serve process"/"single-serve"/"multi-serve"/"multiple serve"/"one serve per workspace" concept nouns → "MCP server" language across D14/D27-AD-4/D31/NFR-2/NFR-5/FR-17/FR-20/PRD §8.2 + spine + roadmap + ci-cd + impl-plan + plans-README + crate plans + CLAUDE.md (+per-crate) + `xtask/src/doc_lint.rs` (`CANONICAL_VERBS` serve→mcp, load-bearing/same-commit) + code + snapshots. The bare verb `serve` is RESERVED for the v2+ web app. `McpServerError` and the rmcp API (`serve_with_ct`, the cargo feature `server`) STAY. No `CONTRACT_HASH`/`CONTRACT_VERSION` bump (only Rust identifiers + prose change; serialized descriptor bytes unchanged — `contract_suite__schema_bundle.snap` NOT re-blessed). STATUS.md:106's unblock serve change-log token flips to `unblock mcp` (CI-forced doc-lint carve-out; the rest of the change-log stays frozen). D-range `D1..D31`→`D1..D32` at the 3 sites (`CLAUDE.md`, ci-cd §2.1(a), `xtask/src/doc_lint.rs`). AC = the zero-live-hits greps clean over the LIVE surface (modulo the enumerated self-referential expected-hit set) + full 11-job CI green (fmt/clippy/test/insta/doc-lint/layering); both gates over the whole PR; Claude opens the PR, human merges. |
| T3.4.3 | Enrich the `unblock agents` managed AGENTS.md block into a full capabilities table (render from `capabilities()`/`schema_bundle()`). Mints D33 | T3.4.2, T2.6 | ☑ | **DONE — [PR #407](https://github.com/websublime/unblock/pull/407) merged (rebase) into main at `7158a27`; both gates PASS. Mints D33 (two Miguel forks resolved this session: mechanism = Option C, and the FULL required+optional param surface).** **Gates:** design Review + a focused design-review DELTA (on the expanded required+optional / `$ref` extraction) both PASS_WITH_CHANGES (all folded); Verify **PASS** — the mutation matrix **M1–M10 all CAUGHT, zero survivors** (the two riskiest, M8 arm-root `$ref` + M9 property-level `$ref` on `delete.mode`, confirmed non-vacuous against the live schema). CHANGE 2 (D27/AF-3 STAYS: `agents` SEPARATE; init does NOT touch AGENTS.md). Rewrite `commands/agents.rs::managed_block()` into a THIN markdown renderer over the NEW pure typed `unblock_mcp::agents_digest() -> AgentsDigest` (Option C, D33 — a derived offline view over `capabilities()`/`schema_bundle()`; NO `serde_json` prod dep in the CLI): descriptor tables (7 tools/5 res/3 prompts) ← `capabilities()`; per-tool actions ← the oneOf discriminants of `schema_bundle().<tool>.input` (NOT description prose — captures `create_bulk`); each action's FULL parameter surface — required AND optional params — derived structurally from the `oneOf` arm (resolving an arm-root `$ref` one level, e.g. `issue create` → `title` + its optional fields); error-code map ← `capabilities().error_codes`; pointers to `unblock://schema`+`unblock://capabilities`. NO `CONTRACT_HASH`/`CONTRACT_VERSION` bump; `contract_suite__schema_bundle.snap` NOT re-blessed — only `init_agents__agents_managed_block.snap` + agents.rs assertions change. Mandatory drift-guard tests `managed_block_lists_every_tool_action` (asserts `create_bulk` for `issue`) and `managed_block_shows_create_title_param` (asserts `issue create` lists `title` as required, proving arm-root `$ref` resolution). D-range `D1..D32`→`D1..D33` at the 3 sites. AC = FR-14 enriched-block + idempotent-merge preserved + snapshot re-bless + CI green; both gates; Claude opens the PR, human merges. **Spec-first cascade + code shipped together in PR #407 (2 atomic commits: docs cascade / code).** |
| T3.5 | Performance / scale / rate-limit ratifications NFR-1/2/18. Mints D34 | T3.1, T3.4.2 | ☑ | **DONE — all three PRs merged (PR-0 spec cascade + P1 `t3.5-perf-scale` + P2 `t3.5-rate-limit`); each PR's gates pass. Base `b6eb5c8`.** Spawns the PRE-GA follow-up **T3.5.1** (read-path batch hydration — the P1 perf-gate finding). Mints D34 (Miguel, 2026-07-13): a HYBRID `criterion` perf gate — hard generous-absolute-ms budget per-PR on a pinned ≥2-vCPU runner + advisory/nightly 10% relative-delta report-only (F-1); a 250k `scale` corpus seeded storage-direct, validated but non-minted (batched `Storage::create_issues` + `IssueValidator::validate`, NOT the O(N²) engine mint — F-2); a MINTED `ErrorCode::RateLimited` (exit 2, retryable=true, hint_shape=None; taxonomy 35→36; `CONTRACT_VERSION` `unblock.mcp.v1.3`→`v1.4`; FOUR goldens re-blessed incl. the D33 `init_agents` ripple — F-3); a RECORD-ONLY `ready→claim→close` round-trip baseline instrumented in `unblock-cli`, NOT `unblock-engine` (an L5→L7 layering back-edge — F-4); the `Arc<Semaphore>` rate-limit chokepoint on `UnblockServer` gating the tool-router dispatch AND `read_resource` (F-5). **Ships as THREE genuinely-independent, separately-reviewable PRs** (the PROCESS.md sub-PR-split guidance): **PR-0** (doc-only, this cascade, off `main`); **P1** `t3.5-perf-scale` (off `main` after PR-0 — `benches/storage.rs` + `benches/engine.rs`, `seed_corpus`, both `tests/scale.rs`, the gate-enforcement mechanism + the F-7 policy/render wiring, the round-trip instrument, the `bench-gate` + `scale` CI jobs; non-contract-touching); **P2** `t3.5-rate-limit` (off `main` after P1 — the `RateLimited` variant, `CONTRACT_HASH` re-pin, the 4 re-blessed goldens, the `Arc<Semaphore>` chokepoint, `tests/rate_limit.rs`, the `rate-limit` CI job; contract-touching → Verify adds `/security-review`). Flip to ☑ only after all three PRs merge + each PR's gates pass. |
| T3.5.1 | Read-path batch hydration — fix the `collect_hydrated` N+1 (1+3N queries) so ALL FIVE read paths `list`/`ready`/`search`/`blocked`/`stale` (+ the `get_issues` sibling) re-tighten toward the NFR-1 tier-i budgets. Spawned from the T3.5-P1 perf-gate finding (Miguel 2026-07-14, **PRE-GA**) | T3.5 | ☑ | **DONE — [PR #411](https://github.com/websublime/unblock/pull/411) merged (rebase) into main at `0d1216e`; CI 14/14 green; both gates PASS — CLOSES T3.5 (all four PRs: PR-0 #408 / P1 #409 / P2 #410 / T3.5.1 #411 merged).** design Review (Understand+Spec folded) + Verify ≥3 (incl. a dedicated byte-preservation + integrity/security lens) both PASS_WITH_CHANGES, 0 must-fix: byte-preservation PASS on all 7 axes (order/label-sort/dep-sort/skip-absent/empty/chunk-accumulation/mapper-reuse; the label col→col-1 shift verified correct). Re-tightened read ceilings HELD on the CI runner with ~4.3–4.5× headroom (storage/engine list/ready 1k ~11ms ≤ 50, 10k ~115ms ≤ 500). SF-1 folded (documented + pinned the `get_issues` lookup-set/dedup narrowing — unreachable in prod); SF-2 (a PRE-EXISTING `open_in_memory` heavy-parallel-load robustness note, not caused by this PR) → a tracked follow-up task.** New `crud.rs::hydrate_ids` batch-fetches issue rows + labels + deps for the whole id-set via chunked `WHERE ... IN (...)` (chunk 900 < the 999 `SQLITE_MAX_VARIABLE_NUMBER` floor) → `1+3·⌈N/900⌉` (=4 for a ≤900 page) instead of `1+3N`, folding every chunk into ONE accumulator and reconstructing over the ORDERED id vec (byte-identical: order/label-sort/dep-sort/skip-absent/empty preserved; reuses `issue_from_row`+`dependency_from_row`). Rewires the five `collect_hydrated`/`ready`/`blocked` callers (G-B1: `stale` is the 5th) AND folds `crud.rs::get_issues` through the same helper (OQ-2 — the identical 1+3N loop; all callers pass unique ids). NEW determinism/chunk-boundary/skip-absent guards (proven non-vacuous). Re-tightened the PRD NFR-1 tier-ii read ceilings (list/ready 1k 100→50, 10k 1000→500; SF-2 in PRD + `bench_gate.rs`) from the fixed-path calibration (~5ms/1k, ~56ms/10k local). Landed the NFR-1 tier-i advisory relative-10% leg (committed `xtask/bench-baseline.json` + `cargo xtask bench-compare` + the `fuzz-smoke` `perf-advisory` report-only leg) — deferred here from T3.5-P1 so its baseline reflects the FIXED read cost. Pure L2 refactor — NO D-id, NO `CONTRACT_HASH`/`CONTRACT_VERSION` bump (`Storage` signatures + spine untouched). Must land before T3.6 (release). |
| T3.6 | Release pipeline + GA graduation (dist/axoupdater; mints D35) FR-25 | T3.1, T3.4.2 | ☑ | **DONE — 3-PR linear stack all merged: PR-0 ([#413](https://github.com/websublime/unblock/pull/413), mints D35 = GA 1.0.0/tag `v1.0.0` + NFR-17 mechanism reframe + `0.1.0`→`1.0.0`) + P1 ([#414](https://github.com/websublime/unblock/pull/414), `dist-workspace.toml` `[dist]` cargo-dist 0.32.0 + `dist=true` force-include + SHA-pinned `release.yml` + `verify-pins` xtask/job + `checksum="sha256"`) + P2 ([#415](https://github.com/websublime/unblock/pull/415), G1 `new_for("unblock-cli")`+`load_receipt()` self-update fix + `--dry-run` correctness fix + hermetic D7 tampered-reject test + `no-network`/`feature-matrix` confinement jobs). Each PR: design Review + Verify (PASS_WITH_CHANGES) both applied; supply-chain-verified (7 action SHAs gh-api-confirmed). NFR-17 reframed spec-first (client verify-before-swap = the dist installer SHA256 checksum; GitHub artifact attestations = publish-side provenance via `gh attestation verify`). App-name = `unblock-cli` (dist package name → the install receipt is `unblock-cli-receipt.json`; GA branding accepted, no rename). The 5 publish-step `GH_TOKEN`s use `secrets.WS_GH_TOKEN`. **Remaining HUMAN step (post-merge, not agent scope):** the `v1.0.0` tag cut + a real end-to-end `unblock update` smoke on ≥1 triple (exercises the dist installer SHA256 + attestation — the half D7 deliberately does not cover); needs a `WS_GH_TOKEN` secret with `contents: write`. Tracked v1.1 follow-up **ADDRESSED** ([PR #418](https://github.com/websublime/unblock/pull/418), branch `t3.6-harden-no-network-whitelist`): the `no-network` whitelist false-green vectors are closed — the self-update exemption now keys on the vetted `axoupdater` symbol inside a `#[cfg(feature = "self-update")]`-gated region (reusing `is_cfg_gated_region`) rather than a by-filename blanket, and the `is_cfg_gated_region` tripwire itself is hardened (H1 un-braced-item + comma-sibling armed-leak, H2 line-index keying, H3 string/char/comment-aware lexer, where-clause over-flag), applied IDENTICALLY to the T3.1 crate-scoped `no_git_gate.rs`. Corpus-green + non-vacuous (each closed vector RED-provable), the one intrinsic tuple-struct paren-field under-flag documented; `update.rs` byte-identical; no doc drift (spec-fidelity to the already-documented "whitelist the self-update axoupdater path" intent — no spine/PRD/ci-cd edit forced). The `--no-default-features` feature-matrix build remains the authoritative confinement gate (NFR-17). Closes the M3/GA build.** **Post-GA (D36, 2026-07-16):** v1.0.0-rc.1 (run 29486619269) failed only on `aarch64-pc-windows-msvc` (no upstream axoupdater standalone updater) → the target was DROPPED from the dist set (→ 5 triples, `install-updater=true` kept); the release is RE-CUT. See D36 + the 2026-07-16 change-log entry. |
| T3.7 | Product `README.md` (root) + `RELEASING.md` | T3.1, T3.4.2 | ☑ | **DONE — [PR #417](https://github.com/websublime/unblock/pull/417) merged** into `main` (rebase; commits `2143069` + banner `190f461`). GA product face authored as a spawned Workflow (Draft → Verify 4-lens adversarial gate + coordinator). Deliverable: root `README.md` (full GA doc — banner `docs/assets/unblock-readme-banner.png`, what/why, dist install, MCP-client wiring quickstart `init`→`agents`→`mcpServers`→`unblock mcp --dir`, the 7-tool/5-resource/3-prompt surface, lifecycle CLI + the 0–8 exit-code summary, self-update, architecture pointer, docs, license) + `RELEASING.md` (the `cargo xtask release` runbook — flow/version-compute/pre-flight/typed double-confirmation/partial-failure recovery/`--dry-run`, pointing to `ci-cd-and-distribution.md` §3/§4 as authoritative). The root `LICENSE-MIT`/`LICENSE-APACHE` (dual `MIT OR Apache-2.0`, holder `websublime`) already existed and are UNCHANGED — the README license section links both. **Verify gate = PASS_WITH_CHANGES** (3 lenses PASS; the fresh-user AC lens FAILed on 1 must-fix → folded): the `mcpServers` block gained an absolute-path fallback note (GUI clients like Claude Desktop don't inherit the shell `PATH` → spawn ENOENT) — closes the AC "a new user can install + wire it into an MCP client from the README alone"; 8 should-fixes folded (run-from-project-root + abs-path hint, post-install PATH/new-terminal note, pre-GA 404 caveat, build-from-source `publish=false` precision, tag-existence-guard ordering, typed-confirm cross-ref, `actions/attest` `v4.1.1` pin, 250k-claim CI pointer). No D-id, no `CONTRACT_HASH`/`CONTRACT_VERSION` bump; the new root files are outside the fixed 19-file doc-lint corpus (confirmed — `load_corpus` iterates the fixed list, never scans the tree). |
| T3.8 | Release helper DX polish — `cargo xtask release` visual presentation | T3.6, T3.7 | ☑ | **DONE — merged into main (`b44a3c9` feat + `6da92f6` docs); both gates PASS. Branch `t3.8-release-dx-polish` (post-GA DX polish; presentation-only over `xtask/src/release.rs`; no D-id, no `CONTRACT_HASH`/`CONTRACT_VERSION` bump).** **Strategy C (DECIDED):** hand-roll the visual DX over the EXISTING I/O seam (`orchestrate(env, input, out, dry_run, palette)` / `ask` / `preflight` / `show_plan` / `execute`) using **`anstyle`** as the styling primitive — already in the lock via clap, so **ZERO new crates** (`git diff Cargo.lock` = only two dep edges on the `xtask` package, no new `[[package]]`). **Rejected `cli-prompts`/`crossterm`/`dialoguer`/`inquire`/`cliclack`/`demand`/`indicatif` and `console`-as-a-normal-dep** — every interactive-prompt/terminal-takeover lib owns the terminal in raw mode with no custom-writer parameter, which would make the irreversible flow untestable and add fresh transitive surface. **The seam is preserved verbatim:** all output still flows through `out: &mut dyn Write`, all input through `input: &mut dyn BufRead`; color is an EXPLICIT `Palette{ color: bool }` decided ONCE in `run()` (`stdout().is_terminal()` + `NO_COLOR`/`CLICOLOR`/`CLICOLOR_FORCE` via a pure, unit-tested `resolve_color`) and THREADED through — never auto-detected from the sink. **Presentational wins:** styled header banner; pre-flight rendered as green pass / red fail check lines + a yellow warn for the un-verifiable `WS_GH_TOKEN` reminder; the Release plan as a boxed, aligned block (box widths computed on PLAIN visible text so borders stay aligned WITH color on) with the version diff + tag bolded; a loud white-on-red IRREVERSIBLE banner before the typed gate; colored success/abort lines; a **stderr-only, TTY-gated spinner** on the two slow blocking steps (`git fetch origin`, `cargo update --workspace`) via a std background thread (no new deps; clean start/stop+join guard + a `Drop` safety net) that degrades to a single static styled line off a TTY and NEVER writes through `out` (diagnostics→stderr, NFR-14). **Safety model UNCHANGED:** numbered menus stay numeric/typed; the typed-tag DOUBLE-confirmation stays FREE-TEXT (`ask_line`); `--dry-run` mutates nothing; the pre-flight order + the atomic push are untouched. **Tests:** color forced OFF through `drive()` keeps the ~15 seam-driven behavioral tests (the mutation-sequence `assert_eq!` on `FakeEnv::calls()` + the fail-path recovery-hint asserts) green + non-vacuous; the plan/real-run shape substring asserts (formerly `tag             : v1.1.0` etc.) were MIGRATED to **`insta`** snapshots (4 new: `plan_dry_run_pre_none`, `plan_dry_run_final_minor`, `real_run_full_stdout`, + a color-ON `plan_dry_run_final_minor_color_on` pinning the ANSI shape, color forced OFF vs ON via `drive`/`drive_colored`); a color-OFF invariant test asserts the captured `Vec<u8>` contains NO ESC (`0x1b`) byte; the spinner is exercised against an injected shared buffer (both the static + animated branches) plus a `Spinner` sink lifecycle test. `#![forbid(unsafe_code)]` holds (pure string formatting + std `IsTerminal` + std thread — no raw-mode, no unsafe). Spec-first: **ci-cd §3.3** gains the presentation note (TTY-styled via `anstyle`; color auto-off on non-TTY and `NO_COLOR`; stderr spinner; NFR-14). **Verify PASS (3 reviewers, 0 must-fix); 4 should-fixes folded:** (1) a FAILED slow step no longer prints the green `✓ … done` glyph — the outcome is threaded into `Spinner::stop(success)` / `with_progress` (returns the step `Result`) so a failure renders a red `✗ … failed` line in BOTH the animated + static branches; (2) the env→color parse is unit-tested through a pure `color_from_env(Option<&OsStr>×3, is_terminal)` helper that `detect_color` (stdout) + `detect_err_color` (stderr) delegate to; (3) a color-ON `execute()` snapshot (`real_run_full_stdout_color_on`) pins the `STYLE_LOUD` banner + `STYLE_OK` success ANSI; (4) stderr styling now keys on stderr's OWN `is_terminal()` via a SEPARATE `err_palette` (`detect_err_color`), so a redirected stderr (`2>log`) stays plain even when stdout is a TTY. Safety model + `out`/`input` seam UNCHANGED; `#![forbid(unsafe_code)]` holds. Depends on T3.6 + T3.7. **Merged into main (`b44a3c9`/`6da92f6`, 2026-07-17) — flipped to ☑.** |
| T3.9 | Comments pull-forward (FR-6 partial → v1). Implements **D37**; **HOLDS the `v1.0.0` GA tag** (D-A) | T3.6, T3.7 | ☑ | **DONE — [PR #422](https://github.com/websublime/unblock/pull/422) merged into main at `823487d` (2026-07-17); both gates PASS. Mints D37.** The vertical slice (model→storage→engine→render→mcp→sync) is IMPLEMENTED on `t3.9-comments`; contract bumped `unblock.mcp.v1.4`→`v1.5`, `CONTRACT_HASH` re-pinned `ddd02b7b…`, tool count 7→8 (RK-3 budget FULL). BOTH gates PASS (design Review 7→1→closed; Verify 3 must-fixes → all pinned by mutation + closed, +`/security-review` lens). Miguel-decided this session (2026-07-17): FORK 1 (add `StorageError::CommentNotFound`, map to `IssueNotFound`), FORK 2 (`validate_body` — `validate_comment` must NOT call it, both call a private `body_rules`), the redact-disclosure fold (`delete` text now discloses the audit trail retains the original body — a mask, not erasure — a hash-moving GA freeze), and the ratified fuller `claim` WIRE description as canonical (repaired a pre-existing rc.1 wire↔hash divergence).** *(Was: TODO — the D37 comment surface is GA-BLOCKING (comments ship IN v1.0.0; the human tag-push waits on this).** *(Task id = T3.9, NOT T3.8: T3.8 is the concurrent release-DX-polish task on branch `t3.8-release-dx-polish`, not yet on `main`.)* ONE coherent vertical slice (model→storage→engine→render→mcp→sync), ONE PR, whose atomic unit is the `unblock.mcp.v1.4`→`v1.5` contract bump. Scope: `Comment` +`updated_at`/`redacted_at` (Options, FORK-M1); `EventType` +`CommentEdited`/`CommentRedacted` (ALL_KNOWN [17]); NEW `CommentValidator` with TWO entry points (`validate_body` + the add-path superset `validate_comment`, which delegates to it — FORK 2); `sync_equals` comment comparator (compare `redacted_at`, ignore comment `updated_at`, FORK-M2 — the derived `PartialEq` at `sync_eq.rs:92` silently violates this the moment `updated_at` exists); storage schema ALTER (+2 nullable cols, NO migration) + NEW `libsql/comments.rs` (4 methods — `add` leaves `updated_at` NULL/MUST-1, soft-redact KEEP-row+mask, existence guard FORK-3, `issues.updated_at` bump FORK-S1) + read hydration on ALL 7 paths (`hydrate_ids`/`hydrate`) + the 6-col seed INSERT (`crud.rs:171-182` — MUST-1 does NOT reach it) + the `parse_event_type` arms (`mappers.rs:254-273`) + `StorageError::CommentNotFound{id: i64}`→`ErrorCode::IssueNotFound` (FORK 1/FORK-E1, no taxonomy churn) + the 4 methods across ALL **12** `impl Storage for` blocks (per-site stub idiom — NOT a blanket rule); engine 4 `Session` methods (mutations under permit+`.write.lock`, `list_comments` no permit; `session/organization.rs` does NOT exist — zero work); render plain/markdown comments section on `issue_long` + `safe_md_multiline` (csv comment-free, no CLI command, FORK-R1/R2); mcp NEW `tools/comment.rs` = the 8th tool (RK-3 FULL) + `CommentToolInput` (CD-1) + `CommentOutput`/`CommentList` (CD-2) + `resources/schema.rs` (the `comment` `SchemaBundle` field — declared LAST, hash-visible) + `agents_digest.rs:106` `[…;7]`→`[…;8]` (SILENT trap — the AGENTS.md table does NOT auto-gain) + `server.rs:175` handshake string + EDIT the 7→8 tool-count asserts (`contract_suite.rs:115`/`:237`, `src/resources/capabilities.rs:191`, `rate_limit.rs:52`) + the `CONTRACT_VERSION` asserts (`options.rs:135`, `public_api.rs:14`) + flip the literal `v1.4`→`v1.5` + re-pin `CONTRACT_HASH` + re-bless the 2 mcp goldens + the `init_agents` ripple (`list_tools` is NOT a golden — live asserts) + version-string/tool-count DOCS (README:130/:138, `unblock-mcp.md:45`/`:77`, `unblock-mcp/CLAUDE.md:3`, `docs/plans/README.md:96`/`:120`/`:187` — **no spine flip remains**); sync export emits `Issue.comments` over a NON-EMPTY live+redacted fixture (a re-bless against the comment-empty `Issue::default()` fixture is a NO-OP that certifies zero coverage) — `ImportReport.comments` is ALREADY DONE (T2.5/D24; do NOT touch `import.rs`); the model golden set is EXACTLY 4 (`comment`, `issue`, `close_outcome`, `epic_status`). `content_hash` UNCHANGED (FR-26 intact). Both gates ≥3 (+`/security-review` for MCP input). See impl-plan T3.9 (**AC corrected 2026-07-17 — it now names the real sites**) + spine §1.5–§5.6 + the D37 PRD §4 row — the bead description is NOT the spec. **Task DONE per PROCESS §6 (both gates + PR #422 merge) — flipped to ☑ 2026-07-17. The `v1.0.0` GA tag itself remains HELD (D-A): it is not cut until T3.2.1 also merges + the human tag-push.** |
| T3.10 | Workspace discovery adopts `CLAUDE_PROJECT_DIR` + bounds the walk-up + reports the bound dir at startup. Mints **D39**; **PRE-GA** (co-holds the `v1.0.0` GA tag, additive/non-semver) | T3.1 | ☑ | **DONE — [PR #424](https://github.com/websublime/unblock/pull/424) merged into main at `73973f2` (2026-07-17); both gates PASS. Mints D39.** The D39 discovery hardening was a PRE-GA quality item (additive, no contract bump) that co-holds the `v1.0.0` tag with T3.9/T3.2.1 but was ORTHOGONAL to both (discovery + startup only — no shared files). *(Task id = T3.10, the next free top-level M3 id — a peer of T3.9/T3.6, NOT a sub-task; T3.9 = comments, T3.8 = release-DX-polish.)* ONE PR (spec cascade + code). Scope: (1) **precedence** — plumb `CLAUDE_PROJECT_DIR` (+ `$HOME`/`%USERPROFILE%`) through the injectable `EnvSource` seam (`crates/unblock-config/src/env.rs`, NOT `std::env`) as a ROOT-probe tier BEFORE the walk-up (miss → fall through), giving `--db` > `--dir`/`UNBLOCK_DIR` > `CLAUDE_PROJECT_DIR` > guarded cwd walk-up; the two `&Path` facades + two `_with_cli` overloads stay SIGNATURE-STABLE (the `env` is threaded into the internal `discover_unblock_dir`); (2) **guard** — bound `walk_up_for_unblock_dir` (today UNBOUNDED to `/`) to a `$HOME` + filesystem-`.git` ceiling (a `Path::exists` probe — NOT a git op, NO git library; D13/NFR-6 hold, INCLUSIVE boundary), turning a repo-blind `cwd=$HOME` spawn into an honest `WorkspaceNotFound` (existing variant, no new `ErrorCode`); (3) **visibility** — a `WorkspaceSource` enum + `pub source` field on `WorkspaceContext`/`ResolvedContext` (spine struct block edited + every construction site + the engine `Session::open` destructure); `unblock mcp` reports `unblock: workspace bound to <dir> (via <source>)` on stderr at startup (a direct stderr emit, NOT `tracing::info!`); (4) **cleanup rider** — DELETE the dead `EnvOverrides.dir` (never consumed by discovery — `UNBLOCK_DIR` reaches `cli.dir` via clap `env`). **NO new `ErrorCode`, spine §2.3 exit table UNCHANGED, no `CONTRACT_HASH`/`CONTRACT_VERSION` bump.** D-range `D1..D38`→`D1..D39` at the 3 sites (`CLAUDE.md`, ci-cd §2.1(a), `xtask/src/doc_lint.rs`). AC = impl-plan T3.10 (MapEnv-injected precedence/guard tests, NEVER `ProcessEnv` — NFR-16 landmine; positive stderr-capture test; non-vacuity mutations). Both gates ≥3 (+`/security-review` — discovery is a path-confinement surface, NFR-18). Read `PRD §4` (D39) + spine §4 (the normative precedence + guard + `source` field, the SSOT) + `unblock-config.md` discovery/env/context rows before implementing — this bead is a checklist, not the spec. **Task DONE — both gates PASS + [PR #424](https://github.com/websublime/unblock/pull/424) merged; flipped to ☑ 2026-07-17.** Verify PASS: 3 non-vacuity mutations RED (guard / `CLAUDE_PROJECT_DIR` tier / `source` label), full CI-equivalent probe green. Non-blocking follow-ups deferred: the PRD §13 tag-holder cell full-train cleanup (D38+D39); `doctor`/`migrate` emit no startup line (FORK-4 scopes the line to `mcp`, the guard still closes the hazard). |

## Future versions — deferred *(not scheduled)*

Intentionally **not decomposed into tasks** until v1 is substantially complete. They are reviewed and locked
**just-in-time** (near v1 GA), using real v1 learnings — not speculatively up front. v1 already reserves their
seams (libsql `remote` feature off-by-default D15, backend-agnostic `Storage` trait, NFR-3 remote backoff), so the
v1 design is not at risk. See `00-roadmap.md`.

- **v1.1** (LOCKED scope, backlog): FR-6, FR-13 (full), FR-16 (full taxonomy), FR-18, FR-19, FR-21, FR-22, FR-23, TOON.
- **v1.2–v1.5 / v2+** (PROPOSED — direction only): see `00-roadmap.md`; each version is reviewed and locked just-in-time.
- **2026-07-07 — roadmap v1.2–v1.5 resequence + D28 (docs change, Miguel-ratified).** Mints **D28** (PRD §4: audience = mixed human+agent company teams over ONE shared logical issue store; humans served via MCP clients — D2/D3/FR-9 preserved; PRD §3 persona note updated). Roadmap re-shaped: **v1.2** shared state re-framed (libsql embedded replicas; writes-require-network offline stance; primary-serialized D14 extension; team onboarding/config split/sqld/actor attribution/no-ACL note; dist-enables-`remote`) · **v1.3** planning layer INSERTED (first-class milestones + slim goals + one `planning` MCP tool; two lock-time forks recorded) · **v1.4** = renumbered old v1.3 (scale/coordination/MCP richness) + the MCP streamable-HTTP transport moved in from v2+ · **v1.5** local UI ADDED (MCP client over the v1.4 HTTP transport; Astro static-only; proposed `unblock-ui` L7 crate) · **v2+** gains the Turso Sync backend-migration + fine-grained-ACL candidate rows (human-surface + transports rows moved out with pointers). All PROPOSED — nothing locked, no task decomposition (just-in-time rule above). Cross-doc: `CLAUDE.md` doc-map (roadmap row + D1..D28), ci-cd §2.1(a) D-range, `xtask/src/doc_lint.rs` range comment.
  - *Downstream v1.3→v1.4 renumber sweep — DONE in this PR (Miguel-sanctioned 2026-07-07):* the old "v1.3 = scale" footprint is reconciled across the docs corpus. PRD NFR-2 (1M / 10k-agent corpus) and PRD FR-4 (ranked FTS5 deferral) now read **v1.4**; spine §3.2.1 FTS5 → **v1.4**; docs/plans/README.md relabels its per-crate scale column to **v1.4 (PROPOSED)** (+ a note that the table predates the full v1.2–v1.5 shape — roadmap §3–§7) and fixes its "fenced to v1.2/v1.4" prose; every old-scale "v1.3" tier token in the crate plans (model/error/policy/storage/sync/health/config/engine/render/mcp/fuzz) → **v1.4**, and their pre-resequence roadmap cross-refs are corrected to the content-preserving new numbers (old scale §4 → roadmap §5, old crate-impact §7 → roadmap §9, old §6/§7 → roadmap §8/§9). The **new v1.3 = planning layer** meaning is left intact wherever it legitimately appears (roadmap §4, PRD §4 D28, the ci-cd tier vocabulary). Out of scope by instruction and left as-is: the STATUS task rows (P.3 "v1.2/v1.3 review", the T1.5 FTS5 note). doc-lint stays green.
- **2026-07-08 — T3.3 scoped to HEALTH-LITE + D29 mint (spec-first reconciliation, Miguel-ratified; branch `t3.3-health-lite`).** Mints **D29** (PRD §4): the five T3.3-lite sub-decisions — **F1** `Session::recover()` STAYS `FeatureNotWired` through v1 (its `--repair`/`.unblock/.recovery/` body is v1.1); **F2** doctor()-lite REUSES `DiagnosticKind::Info` — contract-neutral, NO new variant / NO spine §1.10 / NO `CONTRACT_HASH` bump; **F3** `run_doctor(integrity_rows: &[String], paths)` is storage-free + non-async (engine passes rows in; NO `unblock-storage` dep); **F4** the cli `doctor` ROUTES through the now-wired `Session::doctor()` (forward-refines D27/AF-1, the D24 forward-amendment precedent — NOT a reversal); **F5** the FileAnomaly severity map (`JsonlConflictMarkers`→Unsafe; missing/not-sqlite/truncated-wal/sidecar/journal/orphaned-lock→Recoverable; `Degraded`→`Drifted` rename; `.unblock.lock` D8; `OrphanedLockFile` ported faithfully). Reconciled DOWN to lite (spec-first, this PR): impl-plan T3.3 (LITE) + T3.1/AF-1 boundary, STATUS T3.3 (◐) + T3.1 note, PRD §5 FR-16 + §4 D27/AF-1 forward-note, spine §4.1 (doctor/recover + cli-doctor note) + the integrity_check/cli-render comments, `crates/unblock-health.md` (storage-free `run_doctor`, `integrity_ok = rows.is_empty()` bug-fix, OQ-2 severity-map ratified), `unblock-engine.md` (OQ-2 F2 reuse-Info), `unblock-cli.md` (Q3/F4), + doc-comments in `unblock-health`/`unblock-engine`/`unblock-cli` CLAUDE.md/src. Bug fixed: crate-plan `integrity_ok = rows == ["ok"]` → `rows.is_empty()` (storage normalizes the "ok" sentinel to an empty `Vec`). Decision-change checklist: D-id range `D1..D28` → `D1..D29` (`CLAUDE.md` doc-map, ci-cd §2.1(a), `xtask/src/doc_lint.rs` comments — PRD-§4-data-driven set, no lint logic change). **Implement + Verify DONE — both gates PASS; Verify added the ratified D29 F5-A `TruncatedWal 0<len<32` addendum; [PR #398](https://github.com/websublime/unblock/pull/398) merged (rebase) into `main` at `af7a540`.**
- **2026-07-08 — v1.5 pivot: local UI → local TUI; web dashboard → v2+ commercial PRO track (docs change, Miguel-ratified; supersedes the v1.5/web framing in the 2026-07-07 entry above).** `00-roadmap.md` §6 rewritten: the v1.5 human surface is now a **local TUI** (ratatui; an **MCP client over stdio** — spawns `unblock mcp` as a child, no loopback HTTP, no embedded web assets, no v1.4-transport dependency; a `tui` lifecycle command; **dependency graphs EXCLUDED** — they move to the PRO web). The proposed L7 crate is renamed `unblock-ui` → **`unblock-tui`** (no `ui` Cargo feature, no Node/npm build stage). **v1.4** (`00-roadmap.md` §5) streamable-HTTP transport keeps its slot but **loses its "UI enabler" justification** — re-framed honestly (thinner rationale: a serve for clients that cannot spawn a local stdio child) and re-evaluated at v1.4 lock. **v2+** (`00-roadmap.md` §7) gains a NEW **commercial web dashboard (PRO)** direction: a separate product (static SPA + client-minted read-only Turso token + `unblock-model`/`unblock-policy` compiled to WASM — FR-9 preserved, no second domain surface; where Astro + line-ui migrate; gated on a future go-to-market D-id). **PRD §4 D28** light touch: "local UI" → "local TUI" (principle unchanged — a TUI is an MCP client) + a minimal PRO-is-v2+ note. All PROPOSED — no FR added/re-tiered, no D-id minted now, no spine/crate-plan change, no v1.5 task decomposition. Cross-doc: `docs/plans/README.md` per-crate-table note (v1.5 local TUI).

- **2026-07-09 — MCP-conformance drift probe (Inspector/`serve`) + CD-1/CD-2 spine reconciliation (spec-first design-Review gate PASS 3-lens → spine §5.2a/§5.3 W1 MERGED in PR #400; paired impl code LANDED at `unblock.mcp.v1.3`, Verify gate PASS 4-lens incl. MCP-input security).** A live probe of the primary MCP surface against strict clients (the just-added Inspector + the MCP TS SDK / spec 2025-06-18) found five schema/protocol conformance drifts (CD-1..CD-5). This change lands the **spec-first spine reconciliation for the two spine-coupled drifts ONLY**: **CD-1** (`inputSchema` root omits `type:"object"` for the six tagged-enum tools → a strict TS-SDK client rejects the WHOLE `tools/list`) → new normative spine §5.2a + `#[schemars(extend("type" = "object"))]` on the six tagged-enum §5.2 inputs (`ClaimInput` struct already conforms); **CD-2** (`query`/`dep`/`issue` `structuredContent` serializes as a bare top-level array, not the object MCP requires) → the spine §5.3 object-wrap of the untagged list arms (`IssueList`/`CountList`/`DepList`/`CycleList`) + the reconciled §5.3 intro and §5.4 gate note. Both change schema-bundle bytes, so the paired code shipped JOINTLY as `CONTRACT_VERSION` `unblock.mcp.v1.2` → `unblock.mcp.v1.3` (the `extend` attrs, the wrapper structs, the strengthened `every_tool_schema_is_an_object` + a live `tools[*].inputSchema.type` gate + CD-2 structuredContent-is-object gates, the bump/re-pin/re-bless). Crate plan reconciled (crates/`unblock-mcp.md` F-5 history + new F-7). **Pure-impl siblings (separate follow-up PRs, no spine edit, no `CONTRACT_HASH` impact):** CD-3 empty `resources/list` + static-vs-template split → a `list_resources` override re-aligning to PRD §12.2; CD-4 `initialize` echoes any client `protocolVersion` verbatim → clamp to a supported version; CD-5 read-path `mimeType:"text"` → `application/json`. **No PRD D-id minted** (D-range stays D1..D30) — RESOLVED 2026-07-10 (Miguel): CD-1..CD-6 are spec-conformance fixes (MCP-spec-forced, not product decisions), so no PRD §4 D-id; the rationale is recorded at the right altitude (spine §5.2a/§5.3 + crate-plan F-7 + git), and the `v1.2`→`v1.3` bump is governed by the FR-12 drift gate + F-5 history, not a D-id. Full per-drift file/line/wire evidence lives in the session drift-gap report (git is the archive — no standalone report committed).

- **2026-07-10 — D-lock (D31): restore the cross-process advisory write lock (a D14 forward-amendment); T3.4.1 spec-first cascade (branch `t3.4.1-dlock`).** Mints **D31** (PRD §4): child-per-client stdio (multiple `serve` per workspace) is the SUPPORTED topology — the D14 "one serve process / multiple serve unsupported" clause RETIRED as stdio-unrealizable and reconciled DOWN to PRD §8.2 — and beads' silently-dropped cross-process `.write.lock` is RESTORED as a NEW file `.unblock/.write.lock` (an L2 `unblock-storage` primitive; `std::fs::File::try_lock`; `#![forbid(unsafe_code)]` holds; NO crate), acquired at the WHOLE-MUTATION scope (spanning the `next_child_number` allocation READ + the write tx) so two serves cannot mint the same `parent.N`; it COMPOSES BELOW the RETAINED in-process `Semaphore(1)` (D14). `migrate` takes the same exclusive lock timeout=0 fail-fast for the whole command, unconditionally (before the version check); read-only `doctor` takes no lock (Miguel fork). Timeout reuses `StorageError::DatabaseLocked`→`ErrorCode::DatabaseLocked` (NO new ErrorCode, NO `CONTRACT_HASH`/exit-table/golden churn). An ADDITIVE `unblock-config` `write_lock_timeout_ms` key (default 30000, a Miguel fork over a bare constant) is threaded DOWN to `open_local`. Design Y (faithful): `.unblock.lock`/`OrphanedLockFile`/F5 + `file_state.rs` + the health snapshots STAY VESTIGIAL/UNCHANGED (the separate `.write.lock` is crash-released, never orphans). Rejected: D-wal (relabel-not-fix) and B/C shared-daemon (v1.4 socket + NFR-18 surface). Decision-change checklist (ONE spec-first commit): the D31 PRD §4 row + the D14 amendment note + the PRD §8.2/NFR-3 reconcile + the `.unblock/` on-disk-artifacts table + spine §3.1/§3.2/§3.3/§4.2 + roadmap §6 + FR-9/FR-10 + crate plans (storage/engine/config/cli/mcp) + impl-plan (T3.4.1 row + T3.5 depends-on) + the D1..D30→D1..D31 range-string bump at EXACTLY 3 sites (`CLAUDE.md`, the ci-cd D-range bullet, the `xtask/src/doc_lint.rs` comment) — the frozen `D1..D29→D1..D30`/`D1..D30` history at impl-plan and STATUS is left untouched. **Implement in progress:** the code (lock primitive, the trait guard + all impls, the engine whole-mutation hold, the config key, migrate-exclusive, the contention-lab + witnesses, and the AC-1..AC-12 tests) lands after this spec-first cascade.

- **2026-07-13 — D32: rename the `serve` subcommand → `mcp`; reserve the bare verb `serve` for the v2+ web app (T3.4.2 spec-first cascade, branch `t3.4.2-serve-mcp-rename`).** Mints **D32** (PRD §4): the primary MCP subcommand becomes `unblock mcp` and the library entry `run_mcp_server(Arc<Session>, McpServerOptions)` (test-util seams `mcp_server_duplex_for_test`/`mcp_server_duplex_unclamped_for_test`; spine §0.1 SSOT), with every "serve process"/"single-serve"/"multi-serve"/"one serve per workspace" concept noun migrated to "MCP server" language across D14/D27-AD-4/D31/NFR-2/NFR-5/FR-17/FR-20/PRD §8.2 + spine + roadmap + ci-cd + impl-plan + plans-README + crate plans + CLAUDE.md (+per-crate) + `xtask/src/doc_lint.rs` `CANONICAL_VERBS` (serve→mcp) + code + snapshots. `McpServerError` and the rmcp API (`serve_with_ct`, cargo feature `server`) STAY. NO `CONTRACT_HASH`/`CONTRACT_VERSION` bump (terminology/interface only — serialized descriptor bytes unchanged; `contract_suite__schema_bundle.snap` NOT re-blessed). Decision-change checklist: D-range `D1..D31`→`D1..D32` at the 3 sites (`CLAUDE.md`, ci-cd §2.1(a), `xtask/src/doc_lint.rs` comment); the STATUS.md:106 unblock serve change-log token was CI-force-flipped to `unblock mcp` (doc-lint class-c carve-out), the rest of the frozen change-log left untouched (D31/T3.4.1 precedent). STATUS gains the T3.4.2 + T3.4.3 rows; T3.5/T3.6/T3.7 gain a hard `T3.4.2` dep (the T3.5 STATUS-vs-impl-plan dep drift reconciled to agree). **T3.4.3 (a new D-id — the AGENTS.md capabilities table) is a SEPARATE follow-up PR depending on T3.4.2.**

- **2026-07-13 — D33: enrich the `unblock agents` managed AGENTS.md block into a full capabilities table via a NEW pure typed `unblock-mcp` helper `agents_digest()` (Option C); the CLI stays a thin renderer (T3.4.3 spec-first cascade, branch `t3.4.3-agents-capabilities-table`).** Mints **D33** (PRD §4): the `unblock agents` managed block grows from a prose paragraph into a FULL capabilities table — the 7 tools / 5 resources / 3 prompts descriptor tables (← `capabilities()`), per-tool ACTIONS derived STRUCTURALLY from the `oneOf` discriminants of `schema_bundle().<tool>.input` (the per-arm discriminant `const` — NOT description prose, so `create_bulk` is captured), each action's FULL parameter surface — required AND optional params, derived structurally from the `oneOf` arm and resolving an arm-root `$ref` one level for the delegated payload (e.g. `issue create` → `title` + its optional fields) — the error-code → exit-code/retryable map ← `capabilities().error_codes`, and pointers to `unblock://schema` + `unblock://capabilities`. **Mechanism = Option C (Miguel):** a NEW pure typed `unblock_mcp::agents_digest() -> AgentsDigest` next to `schema_bundle()` returns a CLI-friendly typed digest (a DERIVED OFFLINE VIEW over the existing `capabilities()`/`schema_bundle()` bytes); the CLI `commands/agents.rs::managed_block()` becomes a ZERO-ARG THIN markdown renderer over it — NO `serde_json` production dep added to the CLI, and the schema-shape/`create_bulk`/`$ref`-resolution drift risk co-locates with the contract owner. **Rejected:** Option A (walk-the-JSON in the CLI + a `serde_json` prod dep). D27/AF-3 STAYS (`agents` is SEPARATE; `init` does NOT touch AGENTS.md); the idempotent managed-block merge (`<!-- BEGIN unblock -->`/`<!-- END unblock -->`) is preserved. **NO `CONTRACT_HASH`/`CONTRACT_VERSION` bump** — `agents_digest()` is a derived read that does NOT alter the discovery-document bytes; `contract_suite__schema_bundle.snap` is NOT re-blessed (only Rust source + prose + the `init_agents__agents_managed_block.snap` snapshot + the `agents.rs` assertions change). Mandatory drift-guard tests `managed_block_lists_every_tool_action` (asserts `create_bulk` renders for the `issue` tool, proving the structural `oneOf` walk, not prose) and `managed_block_shows_create_title_param` (asserts `issue create` lists `title` as a required param, proving arm-root `$ref` resolution). Decision-change checklist (ONE spec-first commit): the D33 PRD §4 row + spine §5.4 (the `agents_digest()`/`AgentsDigest` derived-view note) + spine §0.1 (the identifier registration) + spine §5b (the agents paragraph) + crate plans (`unblock-mcp.md` public-API + resources row / `unblock-cli.md` agents.rs row) + impl-plan T3.4.3 (Option C RESOLVED — the Open-Decision fork language removed) + the `D1..D32`→`D1..D33` range-string bump at EXACTLY 3 sites (`CLAUDE.md`, the ci-cd §2.1(a) D-range bullet, the `xtask/src/doc_lint.rs` comment — the PRD-§4-data-driven D-set needs no lint logic change); the frozen `D1..D32` history at impl-plan `:92` and STATUS `:88`/`:112` is left untouched. **Implement in progress:** the code (`agents_digest()` + `AgentsDigest` in `unblock-mcp`, the thin CLI renderer, the drift-guard tests, the re-blessed AGENTS.md snapshot) lands after this spec-first cascade. Depends on T3.4.2 + T2.6.
- **2026-07-15 — D35: T3.6 release pipeline + GA graduation — cut unblock at semver 1.0.0 + correct the NFR-17 self-update verify mechanism (T3.6 spec-first cascade, PR-0 branch `t3.6-ga-spec-cascade`).** Mints **D35** (PRD §4), one bundled decision: **(1)** GA = semver **1.0.0**, tag `v1.0.0` (the `dist` single-App default), graduating the pre-1.0 posture (the Stage line → "GA 1.0.0 — semver stability applies from GA"); from GA the MCP contract (`unblock.mcp.v1.4`), the CLI lifecycle surface (D3), and the 0–8 exit-code taxonomy are STABLE — a breaking change → **2.0.0**, additive `contract_version` bumps within 1.x stay non-breaking (FR-12). **(2)** NFR-17 mechanism REFRAME (fact correction vs axoupdater 0.10.0 source): axoupdater does NO attestation/checksum verification itself — it runs the `dist` installer, which verifies each artifact's **SHA256 checksum** (`dist-manifest.json`) before `self_replace` swaps the binary; GitHub artifact **attestations are publish-side provenance** (`actions/attest@v4`, `gh attestation verify`), NOT on the auto-update path; the installer SHA256 check-before-swap is the SOLE client-side integrity gate; self-update works only for a `dist`-installed copy (it reads the install receipt). **(3)** human-gated post-merge cut — the agent never pushes the tag or configures secrets. **Ships as a 3-PR linear stack** (mirrors T3.5): **PR-0 `t3.6-ga-spec-cascade`** (this cascade — the doc reconciliations + D35 mint + NFR-17 reframe + the `Cargo.toml` `0.1.0`→`1.0.0` bump + `Cargo.lock`); **P1 `t3.6-dist-pipeline`** (`dist-workspace.toml` `[dist]` cargo-dist 0.32.0 + `[package.metadata.dist] dist=true` + generated SHA-pinned `release.yml` + a `verify-pins` job); **P2 `t3.6-confinement-verify`** (`no-network` + `feature-matrix` jobs + the G1 `new_for("unblock")`+`load_receipt()` fix + the D7 tampered-download hermetic stand-in test). **Decision-change checklist (this spec-first commit):** the D35 PRD §4 row + PRD Stage + FR-25 + NFR-17 + PRD §12.3 + PRD §13 M3 row + RK-6 + spine §5b self-update seam + spine §6.7 self-update-whitelist parenthetical + roadmap Stage + FR-25 + ci-cd (§3.1 config → `dist-workspace.toml` `[dist]`, cargo-dist `0.32.0`, `checksum="sha256"`, `vX.Y.Z` tag, ci-cd §4 + ci-cd §5 NFR-17 reframe, the `no-network`/`feature-matrix`/`verify-pins` rows + DEFERRED ledger + ci-cd §7 out-of-v1 note + the `AXOUPDATER_GITHUB_TOKEN` client-runtime correction) + impl-plan (T3.1 AC + T3.6 full rewrite + Human-setup) + README CF-K + crate plans (`unblock-cli.md` update.rs/update_verify.rs rows, `unblock-config.md` SF-5) + code doc-comments (`update.rs` module-doc, `exit.rs`/`exit_codes.rs`/`update_verify.rs` mechanism wording, `migrations.rs`/`schema.rs`/`merge.rs` posture premise) + the `D1..D34`→`D1..D35` range-string bump at EXACTLY 3 sites (`CLAUDE.md`, ci-cd §2.1(a), the `xtask/src/doc_lint.rs` comment — the PRD-§4-data-driven D-set needs no lint logic change); the frozen `D1..D33→D1..D34` history at PRD `:117` + impl-plan `:94`, and the T3.1 STATUS:83 "attestation" record (OQ-6), are left untouched. **Implement in progress:** the P1 dist config + P2 confinement/verify code land after this PR-0 cascade. Depends on T3.1 + T3.4.2.

- **2026-07-16 — D36: drop `aarch64-pc-windows-msvc` from the `dist` target set (6→5 triples); keep `install-updater=true` (post-GA config fast-follow off T3.6).** Mints **D36** (PRD §4; supersedes D17's six-triple clause only). v1.0.0-rc.1 (run 29486619269) failed ONLY on the `aarch64-pc-windows-msvc` build — `install-updater=true` is global-only and fetches a standalone `axoupdater` updater per target, and axoupdater ships NO standalone updater for Win-ARM64 in any version (verified vs the 0.10.0 assets + axoupdater's build matrix); `install-updater=false` is DISPROVEN (drops the install receipt → breaks `AxoUpdater::new_for("unblock-cli")` on ALL platforms), so `install-updater=true` is MANDATORY → the one un-shippable target is dropped. Shipped set = 5 triples (`x86_64`/`aarch64` on linux-gnu + macOS, `x86_64` on windows-msvc); Win-ARM64 users run the x86_64-windows build under Win11 emulation, native returns v1.1+ if axoupdater adds it upstream. The ONLY functional change is `dist-workspace.toml` (delete the target line); `release.yml`'s `dist plan`-computed matrix regenerates to 5 and stays byte-identical (`verify-pins` green). Decision-change checklist (ONE spec-first commit): the D36 PRD §4 row + the D17 amendment note + the 6→5 count/cross-product edits (CLAUDE.md, RELEASING.md ×2, README ×2, PRD FR-25, plans-README, ci-cd ×5, impl-plan ×2) + the `D1..D35`→`D1..D36` range-string bump at the 3 sites (`CLAUDE.md`, ci-cd §2.1(a), the `xtask/src/doc_lint.rs` comment incl. the tokenizer alternation) + this change-log entry + the T3.6-row pointer; the frozen prior range-bump histories `D1..D33→D1..D34` (PRD `:117` + impl-plan `:94`) and `D1..D34→D1..D35` (PRD `:118` + STATUS `:116`) are left untouched. **The release must be RE-CUT** with the 5-triple matrix (the `v1.0.0` tag push is human-gated, per D35/RELEASING.md — NOT agent scope).
- **2026-07-16 — D37: pull the FULL comment surface forward from FR-6/v1.1 into v1; comments HOLD the `v1.0.0` GA tag (T3.9 spec-first cascade, branch `t3.9-comments-spec-cascade`).** Mints **D37** (PRD §4). *(Task id = T3.9: T3.8 is the concurrent release-DX-polish task, not this.)* Five coupled sub-decisions (Miguel): **(D-A)** comments ship IN v1.0.0; the GA tag WAITS on them (pre-GA scope). **(D-B)** a DEDICATED `comment` tool — the 8th (`action: add\|list\|update\|delete`), the deliberate "extend before add" exception (RK-3), budget now FULL; SUPERSEDES the "`issue comment` sub-action" sketch. **(D-C)** full CRUD. **(D-D)** update is provenance-preserving — `Comment` gains `updated_at` + a NEW `EventType::CommentEdited` (NOT in-place-replace). **(D-E)** delete is soft-redact — `Comment` gains `redacted_at`, the row is KEPT + body masked, a NEW `EventType::CommentRedacted` (the audit Event retains the original body). Resolved forks: FORK-3 (existence-only guard → reuse `IssueNotFound`, allow CLOSED issues), FORK-E1 (reuse `IssueNotFound` exit 3 — NO `CommentNotFound`, no taxonomy churn), FORK-M1 (Options skip-when-None), FORK-M1b (author = session actor), FORK-M2 (`sync_equals` ignores comment `updated_at`, compares `redacted_at`), FORK-S1 (`issues.updated_at` bumps on add+edit+redact; `content_hash` unaffected → FR-26 intact), FORK-T (FLAT, not threaded), FORK-R1/R2 (plain+markdown+json+robot render; csv comment-free; NO CLI command). Contract: `content_hash` UNCHANGED; the additive `{input,output}` pair + `$defs/Comment` +2 props move `CONTRACT_HASH` → `CONTRACT_VERSION` `unblock.mcp.v1.4`→`v1.5` (additive, FR-12-consistent, NOT 2.0.0) — landing at **T3.9** (the code + version-string flips + goldens re-bless), NOT this docs-only cascade. **Decision-change checklist (this ONE docs-only spec-first commit):** the D37 PRD §4 row + the D35 "D37 amendment" GA-v1.5 note (PRD `:118`) + PRD Stage line + FR-6 (comments carved to v1, `[v1.1]` marker KEPT) + PRD §11 out-of-scope note + PRD §13 M3 row/Gate; spine §1.5 (EventType +2)/§1.6 (`Issue.comments` populated v1)/§1.7 (`Comment` +2 fields)/§1.8 (sync_equals comparator)/§3.2 (4 trait methods)/§3.2.1 (17-named oracle + table + comment method semantics + hydration + FORK-3)/§4.1 (4 Session methods)/§5.1 (8th tool row)/§5.2 (`CommentToolInput`)/§5.3 (`CommentOutput`/`CommentList`)/the T3.9 v1.4→v1.5 gate note/§6.6 (budget FULL); crate plans model/storage/engine/mcp/render/sync (+ the unblock-error.md:29 FORK-E1 note); roadmap §2 FR-6 Lands + the feature-matrix SPLIT (comments→v1 ●, labels/rollups stay v1.1 ●); impl-plan T3.9 (full AC enumerating every code+doc site); STATUS T3.9 row + this entry; the `D1..D36`→`D1..D37` range-string bump at EXACTLY 3 sites (`CLAUDE.md`, ci-cd §2.1(a), the `xtask/src/doc_lint.rs` comment incl. the tokenizer alternation). The frozen prior range-bump histories `D1..D34→D1..D35` (PRD `:118` + STATUS `:116`) and `D1..D35→D1..D36` (PRD `:119` + STATUS `:119`) are left untouched. The FR-6 definition marker `[v1.1]` stays (labels-rename/list-all + epic rollups remain v1.1); NO `FR-6`-adjacent `[v1]` release marker is introduced anywhere (doc-lint class-b safe). **Implement (T3.9) lands after this spec cascade** — it HOLDS the `v1.0.0` GA tag (D-A).

- **2026-07-17 — T3.9 spec-first correction pass: resolve FORK 1 + FORK 2 (Miguel) and make the T3.9 checklist TRUE before any code (branch `t3.9-comments`, DOCS-ONLY — not one `.rs` file).** A 7-scout Understand sweep over the merged D37 cascade found the impl-plan T3.9 checklist (`:99-106`) factually wrong at ~16 sites and surfaced two genuine spec contradictions. Per the PROCESS.md drift policy (spec-first on drift; report AND resolve in the same session), the authoritative docs are corrected FIRST. **FORK 1 — `StorageError::CommentNotFound { id: i64 }` is ADDED to spine §3.1 and MAPPED to the EXISTING `ErrorCode::IssueNotFound` (Miguel).** The spine's §3.1 list is NORMATIVE ("the full v1 variant set") and omitted it, while impl-plan `:101` + `crates/unblock-storage.md:60`/`:74` both prescribed it → a genuine spine GAP the D37 cascade never touched, not a plan bug. **FORK-E1 constrains the `ErrorCode` taxonomy, NOT `StorageError`:** adding an internal variant that maps onto an existing code honours it literally — taxonomy stays 36, no exit-code-table re-bless, no `oneOf`/error-golden movement. Decisive argument: reuse would force `IssueNotFound { id: comment_id.to_string() }`, whose `Display` renders `issue 42 not found` when **comment** 42 was missing — misleading an agent in an agent-first tracker is real harm. (The checklist's "analog of `DependencyNotFound`" was also wrong — that variant is FIELDLESS; the field-bearing analog is `IssueNotFound`.) **FORK 2 — `CommentValidator` gains `validate_body(body)`; `validate_comment(issue_id, author, body)` DELEGATES to it (Miguel).** `crates/unblock-model.md:61` pinned the 3-scalar fn and claimed "the engine calls it before add/**update**", but spine §4.1 gives `update_comment(comment_id, body)` with NO issue_id and spine §3.2 has no `Storage::get_comment(comment_id)` to fetch one — the validator was literally UNCALLABLE on the update path. Spine `:1468`/`:1776` + `crates/unblock-engine.md:82` already scope the mutation check to the BODY ONLY, so a body-only entry point is what those texts already described; delegation keeps the rule set single-homed (spine §1.9). **Landed:** spine §1.9 (`CommentValidator`, both entry points) + §3.1 (the variant + the WHY-it-maps prose + the `context["comment_id"]` key) + §3.2/§3.2.1 (StorageError-level vs ErrorCode-level prose de-conflated; the **MUST-1 SCOPE** rule made explicit — it binds `add_comment` ONLY, so the seed/import INSERT must persist `updated_at`+`redacted_at` or the redacted round-trip is unreachable) + §5.4 (the stale 7-field `SchemaBundle` sketch → 8, with the `comment` field's **hash-visible declaration order** stated) + the tool COUNT 7→8 at the six sites the cascade missed (§5 header, §5.1 heading + the `create_bulk` parenthetical, §5.2 CD-1, §5.4 `$defs` rationale, §5b agents block); crate plans model (both entry points + the "faithful port of bd" reframe — bd validates a whole `Comment` VALUE, so the 3-scalar shape is a deliberate ADAPTATION that correctly drops bd's `id<=0` rule + the `lib.rs` explicit-re-export drift note), storage (the variant in the `error.rs` list + pinned map, the `DependencyNotFound` fix, **12** impl blocks not 11 (`GateStorage` was missing) with a **PER-SITE** stub idiom — the blanket rule is wrong for 3 of 12), engine (which validator per call site), render (`issue_long`, not the nonexistent `format_issue_long`); impl-plan T3.9 AC rewritten against all 16 findings; STATUS T3.9 row reconciled. **The checklist's worst defects (now fixed):** it claimed the D33 AGENTS.md snapshot "auto-gains the 8th tool table" — FALSE (`agents_digest.rs:106` hard-codes `[(&str,&Value);7]` and `unwrap_or_default()` at `:124` swallows the miss SILENTLY → GA would ship an EMPTY `comment` actions table; the `:124` comment claims a test catches it — none exists); it pointed at `tests/capabilities.rs` (does not exist → `src/resources/capabilities.rs:191`), at a spine version flip that no longer exists (`~:1886` is the `{id}` prose; `:1982` is T3.5 history — do NOT touch), at a `table_info(comments)` golden to "re-bless" (it must be CREATED) and a `list_tools` golden (it is live asserts at `contract_suite.rs:115`/`:237`); it MISSED `mappers.rs:254-273` `parse_event_type` (whose `Custom(..)` catch-all makes an omission clippy-clean), `crud.rs:171-182`'s 4-col seed INSERT (the import path — data loss), `server.rs:175`'s live handshake string, `resources/schema.rs` entirely, and the `epic_status` model golden (while `export_report`/`import_report`/`event`/`event_type` were false positives — the resolved set is EXACTLY 4); it asked for phantom work (`session/organization.rs` never existed; `ImportReport.comments` landed at T2.5/D24 and "completing" it would break `bd_contract.rs`); and it miscounted CI (17 jobs, not the M0-era 11). **Most important: the VACUOUS-AC traps.** The sync export golden, the render goldens and the sync round-trip all run on `Issue::default()`/`base()` fixtures with EMPTY comments — "re-bless the snapshots" there is a NO-OP whose green tick certifies ZERO comment coverage. The AC now requires a live AND a redacted comment fixture (sub-second timestamps, so the `canonicalize_ts_in_value` non-vacuity assert bites) BEFORE any re-bless counts, and requires the FORK-M2 comparator test to FAIL against the derived `PartialEq` it replaces. **No code, no `D`-id, no FR-tier change** (FR-6 stays `[v1.1]` per D37 — labels-rename/list-all + epic rollups remain v1.1); no `CONTRACT_VERSION`/`CONTRACT_HASH` movement (the `v1.4`→`v1.5` flip + goldens remain T3.9 code-PR work). `cargo xtask doc-lint` green (19 docs, 6 classes). **T3.9 Implement follows this pass** — it still HOLDS the `v1.0.0` GA tag (D-A).
- **2026-07-17 — T3.9 correction pass, ITERATION 2: the design Review gate returned FAIL (7 must-fixes); all 7 + 10 should-fixes landed (branch `t3.9-comments`, still DOCS-ONLY).** Three iteration-1 statements in the entry above are **SUPERSEDED** by this pass. **(1) FORK 2 composition — `validate_comment` must NOT call `validate_body`.** Miguel's FORK 2 (two entry points; `validate_comment` shares the body rules) is UNCHANGED and `validate_body` stays PUBLIC — but "delegates to `validate_body`" is unimplementable as written: `validate_body` returns an ALREADY-SEALED `Result`, so `validate_body(body)?` is FAIL-FAST and would emit a 1-entry `context["fields"]` where `IssueValidator` emits N — breaking the D-E1 UNIFORM AGGREGATE carrier that `crates/unblock-model/src/validation.rs` implements (private helpers taking `fields: &mut Vec<FieldError>`, one seal at the end). Spec'd instead: a PRIVATE `body_rules(body, &mut fields)` that BOTH public entry points call, each sealing its own aggregate — rule set still single-homed, both errors still multi-fault. Author rules now DELEGATE to the existing `validate_actor` (its single home) + RELABEL the `FieldError` `"actor"`→`"author"`; the wire field names `content`/`author`/`issue_id` are pinned (FR-11) and the chars-vs-bytes adaptation of bd's rule is stated. A multi-fault test (empty body + over-length author → 2 entries, not 1) pins the design — it is exactly what the fail-fast composition would fail. **(2) The tool COUNT stays 7; the spine section-5 header is restored to DUAL framing** ("7 shipped; → 8 at T3.9"). Iteration 1's flat "8 consolidated tools" FALSIFIED `docs/plans/README.md:120` — a claim whose whole job is to be checked against the spine — and was false against shipped code (T3.9 is docs-only; the router does not exist). Dual framing is the spine's own idiom for this fact (its `§6.6` and its `§5.1` row 8 both carry the landing T-id) and is true on BOTH axes continuously. Every count literal now flips 7→8 in the T3.9 **code** PR, atomically with the router that makes 8 true; the AC gained a **GENERIC** sweep (`git grep -n "7 tools\|seven tools"`) because the enumerated list under-counted (`capabilities.rs:3`/`:80`, `contract_suite.rs:104`/`:230`, the mcp crate plan's one-line purpose). The 2026-06-19 consistency report is annotated, NOT rewritten (it is a dated audit record — the same convention as its resequence note). **(3) Fixture timestamp precision is PER-TEST, not a blanket "sub-second".** `serialize_issue_line` renders at second precision and FORK-M2 puts comment `redacted_at` INTO the `sync_equals` compare, so a blanket sub-second demand turns `roundtrip_proptest.rs` **and** `contract.rs:83` RED — and the tempting fix (dropping `redacted_at` from the comparator) is a SILENT FORK-M2 VIOLATION. Split: sub-second ONLY for the `export_insta` canonicalizer non-vacuity golden; second-truncated on every compared field. **Also landed:** the FALSE "auto-gains the 8th tool table" ripple + the drifted anchors (`:115` is the count, `:116` only the message; `src/resources/` prefix; `list_tools` is a live ASSERT, an EDIT not a re-bless) pulled forward into `crates/unblock-mcp.md` — the doc the mcp implementer reads FIRST, which still contradicted the corrected impl-plan; the `schema.rs` row gained the `comment` field + the hash-visible declaration-order rule; a **drift-guard** for the `server.rs:175` handshake string (derive the counts or assert them); ONE literal masked marker `[redacted <ts>]` pinned for BOTH render backends (a GA freeze — plain gave a non-normative "e.g.", markdown gave no form, so they would have been guessed independently); FORK-E1 qualified in the crate plan that OWNS `ErrorCode` (it mints no CommentNotFound **`ErrorCode`**; the StorageError variant maps onto `IssueNotFound`); the two engine line refs marked APPROXIMATE INSERTION POINTS (they point at `remove_dep`/`close_with_suggestions` and `list_dependencies`, i.e. no code to edit). **The AC's worst remaining trap, now closed:** the seed/import clause was **VACUOUS** — NO named test in the workspace reaches the 4-column seed INSERT (`crud.rs:173`, inside `insert_issue_in_tx`, shared by `create_issue` AND `create_issues`): the sync `FakeStorage`s never execute it, the export/proptest suites import no storage, NFR-16 was spec'd only for the 4 new comment methods, and `testkit.rs:322` (the actual seed path) seeds comment-less issues — so the clause could tick GREEN with a redacted comment silently importing back UN-REDACTED. It now requires a libsql-backed `create_issues`→read-back case carrying a live + redacted comment that FAILS against 4 columns. **No code, no `D`-id, no FR-tier change** (FR-6 stays `[v1.1]`). `cargo xtask doc-lint` + `cargo test -p xtask --test doc_lint_corpus` both green.
- **2026-07-17 — T3.9 IMPLEMENTED + both gates PASS (branch `t3.9-comments`, 25 commits: 10 docs + 15 code/test/re-pin).** The vertical slice landed as one compile unit (the 4 `Storage` trait methods break all **12** `impl Storage for` blocks at once, so a per-crate split was impossible): `Comment` +`updated_at`/`redacted_at`, `EventType` +2 (ALL_KNOWN [15]→[17]), `CommentValidator` (private `body_rules` shared by both entry points), the field-wise FORK-M2 `sync_equals` comparator; `StorageError::CommentNotFound`→`IssueNotFound`, NEW `libsql/comments.rs`, the 6-col seed INSERT, hydration on all 7 read paths; 3 engine `Session` mutations under permit+`.write.lock` + `list_comments` (no permit); render plain/markdown sections + `safe_md_multiline`; the 8th MCP tool `comment` + `CommentOutput`/`CommentList` + `SchemaBundle.comment` (declared LAST) + derived handshake counts + `agents_digest` 7→8; sync comment fixtures. **Contract bump `unblock.mcp.v1.4`→`v1.5`; `CONTRACT_HASH` `eca90bb7…`→`ddd02b7b…`** (re-derived live from `capabilities()`+`schema_bundle()`, so the pin checks itself). `content_hash` UNCHANGED by add/edit/redact (FR-26 — `golden_hash__canonical_content_hash` UNMOVED at `dc1b794f…`). **Design Review gate: 7 must-fixes → 1 → closed** (the last, an engine-plan clause contradicting the NORMATIVE spine §1.9 composition, hand-fixed by the orchestrator per PROCESS §4 + Miguel's authorisation at the §5 2-iteration escalation). **Verify gate: 3 must-fixes, ALL closed + proven by revert-and-confirm-RED** — (1) NFR-18 quota preflight on tool #8 (the quota is per-tool-body, unlike the pre-dispatch rate limit, so the new tool could silently omit it — deleting `self.preflight` left the whole suite green); (2) the FR-26 comment-relation exclusion pinned at the MODEL hash layer (leaking `comments[].body` into the hashed title left `cargo test --workspace` green); (3) the live `#[tool(description)]` bytes pinned as `(name, description)` pairs. **Miguel-decided this session:** the redact-disclosure fold (the `delete` description now discloses the audit trail retains the original body — a mask, not erasure — a deliberate hash-moving GA freeze so a user redacting a secret knows to rotate it) and the ratified fuller `claim` WIRE description as canonical (MF-3 surfaced a pre-existing rc.1 wire↔hashed divergence; adopting the wire bytes changes nothing for existing clients). Also: SF-A single-sourced the 4×-spelled comments projection (`COMMENT_COLUMNS` `pub(super)`, a positional-mapping hazard removed), SF-B made the add-guard fail CLOSED. Full real CI green (17 legs incl. the testkit-featured storage contract leg — a bare `-p` subset compiles the NFR-16 cases to nothing; workspace `--all-features` does NOT compile here, `ci.yml:33` forbids the libsql `remote` TLS stack). **Bench-gate PASSES on merit** despite `hydrate_ids` going `1+3·⌈N/900⌉`→`1+4·⌈N/900⌉` (~33% more round-trips): `storage_list/10000` 66.9ms vs the 500ms ceiling — NO ceiling widened. The 4 forbidden golden classes (content_hash/exit-code/csv+15-col/help) all UNMOVED. Non-gating followups: a pre-existing timing flake in the untouched `lock.rs::zero_timeout_fails_fast…` (de-flake separately). **Claude opens the PR; the human merges; only THEN does this row flip to ☑ and the `v1.0.0` tag become cuttable (D-A) — and T3.9 is NOT the only GA blocker: the concurrent T3.2.1 FR-17 signal-exit fix also holds it.**
- **2026-07-17 — D38: FR-17 signal-exit precedence + no-hang on every return path — a GA-blocking T3.2 defect (T3.2.1 spec-first cascade, branch `t3.2.1-fr17-signal-exit-hang`).** Mints **D38** (PRD §4); **amends D27/AD-4** forward (the D24/D29 forward-amendment precedent — only AD-4's *"first signal → `run_mcp_server` returns Ok"* PREMISE was false; its token-cancel + `with_shutdown_flag` wiring, the Windows cfg no-op, and the 2-arg signature stay live). **Verified defect:** `unblock mcp` signalled BEFORE a client completes the MCP `initialize` handshake never exits — rmcp 1.7 `select!`s the whole handshake against the cancellation token → `Err(ServerInitializeError::Cancelled)` → `McpServerError::Transport` → `commands/mcp.rs` early-returns past the Ok-path-only `128+signo` guard → exit 1 → the `#[tokio::main]` runtime drop blocks forever on the parked `tokio::io::stdin()` blocking-pool read. Two ratified clauses (Miguel): **(1) the signal wins, on the two cooperative-shutdown returns** — a recorded signal yields `128+signo` on the `run_mcp_server` return (Ok AND post-cancel Err) and the `session.shutdown()` return; the teardown error is a consequence of the cancellation → a human stderr diagnostic only (NFR-14), never the exit code; a pre-run-loop failure (e.g. `Session::open`) is NOT such a consequence and keeps its 0–8 code; with NO signal a genuine `Err` stays exit 1; both cooperative returns still reach `session.shutdown()`. **(2) no-hang on every return path** — including a **panic unwind** (`ManuallyDrop`; a `shutdown_background()` after `block_on` is dead code on an unwind). (1) alone is insufficient because the ratified mechanism returns the signal exit as a VALUE, so the signal path flows through the runtime drop too — a restore-blocking-drop mutation still hangs the pre-handshake case (4 tests RED, genuine 20s hangs). **CORRECTED at the Verify gate (2026-07-17): the earlier "the no-signal `Err` path hangs too" is FALSE and retracted** — measured 3x independently, that case does not hang and is never RED under that mutation (rmcp returns after a complete message without re-issuing `receive()`, so no stdin read is parked); the no-hang proof is carried by the **pre-handshake signal** case. **Delivery mechanism RATIFIED by the T3.2.1 design gate (2026-07-17):** `main.rs` owns the runtime explicitly + `shutdown_background()` after `block_on`, `mcp::run` returns `Ok(Some(128+signo))` through `run_with`, and the in-command first-signal `std::process::exit` is REMOVED; the second-signal escalation `process::exit` stays NORMATIVE (demoted to a backstop, not superseded). **NOT a semver event (D35):** `128+signo` is the D27/AD-4 signal convention, OUTSIDE the stable 0–8 taxonomy — NO new `ErrorCode`, spine §2.3 exit table UNCHANGED, no `CONTRACT_HASH`/`CONTRACT_VERSION` bump. **Decision-change checklist (this docs-only spec-first commit):** the D38 PRD §4 row + the D27/AD-4 inline D38 amendment (PRD `:110`) + the rewritten FR-17 AC (PRD `:163`, four clauses) + PRD §8.2 Shutdown prose; spine §0.1 (real rmcp handshake-cancel semantics — `Ok(())` OR `Err(Transport{Cancelled})`, both normal), spine §2.1 + §5b error boundary (the signal-precedence qualifier on the `McpServerError`→`InternalError` map), spine §2.3 (a note under the UNCHANGED table: signal exit is a separate axis), spine §4.1 (`shutdown()` reached on BOTH returns), spine §4.2 (the tx-cancel-safety ↔ process-exit-no-hang cross-ref), spine §5b `mcp` (the normative signal-wins + escalation + no-hang bullets, and the retired-D14 topology sentence reconciled to child-per-client/D31); impl-plan T3.2 (partial-AC note) + the NEW T3.2.1 bullet with full AC; STATUS T3.2 row note (stays ☑) + the NEW T3.2.1 row + this entry; `unblock-cli.md` rows (`main.rs` — now runtime-owning, NOT `#[tokio::main]`, `lib.rs`, `shutdown.rs`, `commands/mcp.rs`, `tests/mcp_lifecycle.rs`, + the previously-missing `tests/shutdown_failure_injection.rs` row); **`unblock-mcp.md` `:22`** (the owning crate plan for `run_mcp_server` still asserted the unconditional "`ct.cancel()` … returns cleanly" claim spine §0.1 now corrects — a plan↔spine drift this cascade would otherwise INTRODUCE); the `D1..D37`→`D1..D38` range bump at EXACTLY 3 sites (`CLAUDE.md`, ci-cd §2.1(a), the `xtask/src/doc_lint.rs` comment incl. the tokenizer alternation). Frozen prior range-bump histories are left untouched. **Implement (T3.2.1) lands after this spec cascade** — the fix is BOTH components (precedence AND no-hang); the `6e5b72b` C2 assertion widening is re-tightened, never preserved.
- **2026-07-17 — D39: workspace discovery adopts `CLAUDE_PROJECT_DIR`, bounds the cwd walk-up, and reports the bound dir at startup — an ADDITIVE, non-semver discovery hardening (T3.10, branch `t3.10-workspace-discovery`, STACKED on D38/PR #423).** Mints **D39** (PRD §4). A committed project-scoped MCP client config (`.mcp.json`/`.vscode`/`.cursor`) must NOT carry a machine-specific absolute `--dir` (the repo dogfoods this — `mcp.json` passes none). Three coupled clauses (Miguel, 2026-07-17): **(1) precedence** — a single total order `--db` (derive) > `--dir`/`UNBLOCK_DIR` (`cli.dir`, ONE slot via clap `env`) > `CLAUDE_PROJECT_DIR` (a project ROOT — its `.unblock`/`_unblock` child probed like a `--dir` root, NO walk-up; miss → fall through) > guarded cwd walk-up; `CLAUDE_PROJECT_DIR` (+ `$HOME`/`%USERPROFILE%`) is read via the injectable `EnvSource` seam (NOT `std::env` — testable via `MapEnv`, NFR-16), never into `EnvOverrides`. **(2) walk-up guard** — the ascent is BOUNDED (was unbounded to `/`): probe each ancestor FIRST then STOP INCLUSIVE at a `.git` entry (a `Path::exists` filesystem stat — NOT a git op, NO git library; D13/NFR-6 hold) and/or `$HOME`, so a repo-root or deliberate `$HOME/.unblock` stays usable but a repo-blind `cwd=$HOME` spawn yields an honest `WorkspaceNotFound` (existing variant → `NotInitialized`, exit 2) instead of silently binding an unrelated far-up `.unblock` (a wrong-DB integrity defect). Root markers = `.git` only in v1 (`.hg`/`.svn`/`.jj` are v1.1). Accepted residuals: a `.unblock` above an intermediate `.git` needs explicit `--dir`; `$HOME` is best-effort; an at-boundary stray `.unblock` binds-then-reports. **(3) startup visibility** — `unblock mcp` ALWAYS reports the bound dir + winning tier on stderr at startup (an unconditional line, since `info!` is silent at WARN default; NFR-14, mcp stdout stays MCP-framing only), carried by an ADDITIVE `source: WorkspaceSource` field on `WorkspaceContext`/`ResolvedContext` (internal config/engine type, NOT the MCP wire contract). **Rejected:** MCP `roots` (DEPRECATED SEP-2577, advisory). **NOT a semver event (D35):** NO new `ErrorCode`, spine §2.3 exit table UNCHANGED, no `CONTRACT_HASH`/`CONTRACT_VERSION` bump, `CLAUDE_PROJECT_DIR` is a READ-ONLY external input distinct from the `UNBLOCK_` family (D10 stays true); co-holds the `v1.0.0` tag with T3.9/T3.2.1 (introduces NO new hold). **Decision-change checklist (spec cascade first, then code, then tests — ONE PR, separate conventional commits):** the D39 PRD §4 row + PRD §8.2 client-registration/discovery bullet + PRD §12.7 (Resolved Items) + PRD §14.2 RK-7 (silent-wrong-DB mitigated); spine §4 (the discovery precedence + guard + cwd-start GAP-close block + the additive `source`-field/`WorkspaceSource` struct-block edit + the two pinned delegation-call `&ProcessEnv` edits + the T1.3a walk-up cross-ref); `unblock-config.md` (discovery.rs/env.rs/context.rs/lib.rs rows + the API signature); README (registration block :104-128 rewritten into project-scoped-vs-user-global worlds, keeping the `unblock.mcp.v1.5`/topology paragraph verbatim + the `--dir` global-flag bullet); impl-plan T3.10 + AC; STATUS T3.10 row + this entry; `CLAUDE.md` env-conventions line; the `D1..D38`→`D1..D39` range bump at EXACTLY 3 sites (`CLAUDE.md`, ci-cd §2.1(a), the `xtask/src/doc_lint.rs` comment incl. the tokenizer alternation). Frozen prior range-bump histories are left untouched. **Code (cleanup rider):** the dead `EnvOverrides.dir` is DELETED (zero live readers — `UNBLOCK_DIR` reaches `cli.dir` via clap `env`, not this field). **Implement (T3.10) lands after this spec cascade** — additive, co-holds the `v1.0.0` GA tag (no new hold).

## Known issues / fast-follows *(not blocking v1)*

Surfaced during development, tracked here (the repo's system of record) so they aren't lost; resolved just-in-time.

- **✅ RESOLVED — `cargo build --all-features --all-targets` `E0004` (latent `toon` test gap).**
  `crates/unblock-config/tests/proptest.rs`'s `output_format_wire` match did not cover the model's
  feature-gated `OutputFormat::Toon`, so a whole-workspace `--all-features --all-targets` build broke (the
  compiler confirmed it was the ONLY site — the lib + every other crate compiled clean). **Fixed:** the match
  gains a `#[cfg(feature = "toon")] OutputFormat::Toon => "toon"` arm, and `unblock-config` gains a
  `toon = ["unblock-model/toon"]` passthrough (mirrors `unblock-render` so every `OutputFormat`-re-exporting
  crate is toon-coherent) — `--all-features --all-targets` now compiles, default build unchanged. Was a
  pre-existing gap (files last touched T1.3; surfaced by the T2.4 Verify gate). The default CI matrix still
  uses targeted features (D15/NFR-10 keep the libsql `remote` TLS stack off the gate); a remote-free
  `--features toon` regression guard is a candidate follow-up (not added here — it is a gate change).

- **⏳ PARTIAL — MCP-conformance fixes CD-1..CD-6 (impl follow-ups to the 2026-07-09 spec-first spine
  reconciliation logged above).** A trackable home so the code follow-ups are not lost. **✅ CD-1 + CD-2 LANDED (ONE
  joint `CONTRACT_VERSION` `unblock.mcp.v1.2` → `unblock.mcp.v1.3` bump):** `#[schemars(extend("type" = "object"))]`
  on the six tagged-enum inputs + the `IssueList`/`CountList`/`DepList`/`CycleList` output wrappers + `CONTRACT_HASH`
  re-pin + `schema_bundle`/`capabilities` golden re-bless + the strengthened `every_tool_schema_is_an_object` and a
  new live `tools[*].inputSchema.type == "object"` gate (spine §5.2a/§5.3; crate-plan F-7). **✅ CD-3 + CD-4 + CD-5
  LANDED (pure impl in the `ServerHandler`, no spine edit, no `CONTRACT_HASH` impact):** CD-3 a hand-written
  `list_resources` override advertises the 4 concrete URIs (ready/blocked/capabilities/schema) with `issues/{id}` the
  sole template (re-align to PRD §12.2); CD-4 a `VersionClampingTransport` decorator clamps an UNSUPPORTED inbound
  `initialize` `protocolVersion` (outside rmcp's `KNOWN_VERSIONS`) to `ProtocolVersion::LATEST` BEFORE rmcp's
  serve-loop negotiation (a pure `ServerHandler::initialize` override is empirically insufficient — rmcp 1.7's
  serve-loop re-derives the wire version as a lexical `min(client, handler)` AFTER the handler); CD-5 the read-path
  `mimeType` → `application/json`. **◐ CD-6 PARTIAL (hardening) — assumption pin LANDED; upstream rmcp fix + wrapper
  removal remain:** CD-4's clamp relies on rmcp 1.7's undocumented serve-loop lexical min-negotiation, so an rmcp bump
  could shift semantics. **✅ Assumption pin landed (tests + docs only, no behavior change, no `CONTRACT_HASH` impact):**
  a raw UNCLAMPED MCP-server seam (`mcp_server_duplex_unclamped_for_test`) + two `tests/protocol_version.rs` pins —
  `rmcp_serve_loop_echoes_unsupported_below_latest_version_verbatim` (asserts rmcp still echoes an unsupported
  below-latest version verbatim over the un-clamped path — the exact misbehavior the clamp compensates for) and
  `known_versions_and_latest_match_the_clamp_key_set` (pins the `KNOWN_VERSIONS`/`LATEST` set the clamp keys on) — so a
  future rmcp bump that changes the negotiation or the supported set fails LOUDLY, pointing maintainers straight at the
  `VersionClampingTransport` doc-comment. **⏳ Open remainder:** pursue an UPSTREAM rmcp fix making unsupported-version
  clamping a first-class `ServerHandler` contract, then delete the transport wrapper once it lands. **PRD §4 D-id —
  RESOLVED 2026-07-10 (Miguel): none.** CD-1..CD-6 are spec-conformance fixes (MCP-spec-forced,
  not product decisions with a genuine fork), so no D-id is minted; the rationale lives at the right altitude — the
  interface SSOT spine (§5.2a/§5.3) + crate-plan F-7 + git — and the `v1.2` → `v1.3` bump is governed by the FR-12
  drift gate + F-5 history, not a D-id. (A D-id would duplicate the spine at the wrong altitude and add doc-lint /
  decision-change-checklist overhead reserved for genuine product forks.)

- **✅ T3.5.1 Verify follow-up — `open_in_memory` shared-cache heavy-load boundary hardened (option C,
  Miguel 2026-07-15) — [PR #412](https://github.com/websublime/unblock/pull/412) MERGED (2026-07-15).**
  Test-harness-only hardening — **zero production-code behaviour change**
  (`open_in_memory`/`apply_pragmas`/`memory_open_lock`/`create_issues`/`seed_corpus` bodies unchanged): a new
  un-gated file-backed heavy-corpus parallel stress regression (`crates/unblock-storage/tests/heavy_corpus_stress.rs`
  — 16 tasks × a heavy 902-row `open_local`+migrate+`create_issues`+read-back batch, 0 failures) proving `open_local`
  carries the load the shared-cache `open_in_memory` path cannot, plus boundary rustdoc on `open_in_memory` and
  `testkit::seed_corpus` (test-only / light-tests-only vs heavy/high-concurrency MUST-use-`open_local`, with the
  deliberate no-runtime-guard rationale stated inline) and a new OQ-8 reconciling `docs/plans/crates/unblock-storage.md`
  (Tests columns + Open questions). Rejects an "immunize `open_in_memory`" fix (option A/E — the residual flake is not
  reproducible in a dedicated single-process adversarial harness, so a targeted fix would be unfalsifiable) and a
  single-connection in-memory store (option B — trades away the OQ-5 two-connection fidelity). **Verify gate PASS
  (3 reviewers: CI-equivalent probe green, flakiness 0/5, non-vacuity mutation RED+reverted; 1 doc-accuracy
  must-fix applied). PR #412 merged 2026-07-15 — resolved.**
