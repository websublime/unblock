<!--
TEMPLATE — Wiki run-report. Copy this shape into .knowledge/wiki/runs/<YYYY-MM-DD>-<slug>.md
(do NOT edit this file in place). One report per significant session or team run; it lands in the SAME
commit/PR as the work + the .unblock/issues.jsonl re-export (PROCESS.md §8). DESCRIPTIVE ONLY — nothing
normative may originate here; the repo is PUBLIC — nothing personal or strategic. Issue comments stay the
short per-task spine and LINK here for depth. ALL six H2 sections are mandatory (knowledge-lint enforces
presence + non-empty body); the Glossary defines EVERY session-local id used in this report OR in the
run's issue comments.
-->

---
name: <YYYY-MM-DD>-<slug>
description: <one line — the wiki/index.md one-liner must copy THIS line verbatim>
type: run
date: <YYYY-MM-DD>
branch: <branch or ->
pr: <number or ->
issues: [<ub-id>]
---

# Run — <title>

## Context

<One block, self-contained: task/issue (`ub-*` id + one-clause anchor), lifecycle phase and gate/round
(e.g. "Verify gate, round 2"), branch, PR, team shape (mates + coordinator) or "solo".>

## What & why

<What was attempted and the reason it was needed now. Name the authoritative spec sections read
(PRD §… / spine §… / crate plan §…) — the report never restates them as rules.>

## Outcome

<What landed / what was rejected. Gate verdicts with must-fix counts. Commit/PR refs. If a gate failed:
which phase it looped back to.>

## Gotchas

<Surprises, traps, dead ends, environment quirks hit in THIS run — each one line + where it bit
(file:line / job / tool). Promote any repeat-worthy item to a memory or topic and link it. "None." is
valid but must be written.>

## Glossary

<MANDATORY (approved decision 3). Every session-local id used in this report or in this run's issue
comments — mutant ids (M10), must-fix ids (MF-2), briefing rules (R8), lens indices, facet letters —
gets a row: what it is in words + where it lives. Durable ids (D-id, T-id, FR/NFR, ub-*) need no row —
they resolve in PRD/spine/tracker. If none were coined, write exactly: "No session-local ids were used
in this run.">

| id | what it is (in words) | where it lives (file:line / doc § / issue id) |
|----|-----------------------|-----------------------------------------------|
| <M10> | <…> | <…> |

## Links

<Issue ids (with one-liners), PR, key files touched (absolute repo paths), related wiki topics /
memories, prior related run-reports.>
