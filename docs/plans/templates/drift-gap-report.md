<!--
TEMPLATE — Drift / Gap Report. Copy this shape (do NOT edit this file in place).
Used by the coordinator (and any session) to report drifts & gaps in a consistent form.
Policy: drifts/gaps are reported and, BY DEFAULT, resolved IN THE SAME SESSION — via review → iterate with
Miguel → adjust. Don't defer or let them accumulate. The spine is the interface reference, but a plan↔spine
disagreement is reconciled WITH Miguel (usually the plan is fixed to match the spine; the drift may instead
expose a spine bug) — never overwrite either side silently.
Where it lives: this is a working artifact for the session. Land the fixes in the real docs, log the outcome in
STATUS.md + the commit, and let git be the archive — do NOT keep standalone resolved reports.
-->

# Drift / Gap Report — <scope>  (<YYYY-MM-DD>)

- **Phase:** <understand|decide|spec/plan|review|implement|verify|track>
- **Reviewers (lenses):** <e.g. architect, rust, research>  · **Coordinator:** multi-agent-coordinator
- **Scope reviewed:** <files / area>
- **Default disposition:** resolve this session (review → iterate with Miguel → adjust).

## Findings

| id | kind | severity | location (+ other side if drift) | issue | recommended fix | raised by | status |
|----|------|----------|----------------------------------|-------|-----------------|-----------|--------|
| F-1 | drift\|gap | critical\|major\|minor | `file` §… ( ↔ `file` §… ) | … | … | <lens> | open → resolved (how) |
| F-2 | … | … | … | … | … | … | … |

## Spine ↔ plan disagreements (if any)

| id | spine says | plan says | decided (with Miguel) | which side changed + why |
|----|-----------|-----------|-----------------------|--------------------------|
| F-… | … | … | … | spine / plan |

## Resolution

- **Resolved this session:** <n>/<total>.  **Deferred (with explicit reason + where tracked):** <… or none>.
- **Docs touched:** <…>   ·   **STATUS.md updated:** <rows>   ·   **Commit/PR:** <ref>
- **Verdict:** <corpus consistent / residuals listed above>

## Prevention (only if a NEW recurring class)

- <add a check to the CI doc-lint (`ci-cd-and-distribution.md §2.1`) and/or a rule to `PROCESS.md` so this class
  cannot recur — same spirit as the spine=SSOT + decision-change checklist>
