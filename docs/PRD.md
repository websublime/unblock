# unblock — Product Requirements Document

- **Status:** APPROVED (v1.1) — §12 closed; CI/CD + distribution via `dist` added (D17); plans in `docs/plans/`
- **Date:** 2026-06-19
- **Owner:** Miguel Ramos
- **Repo:** `websublime/unblock`
- **Stage:** Pre-1.0, no external users — breaking changes welcome, no migration or backward-compat burden.

> **unblock** is a ground-up, idiomatic, multi-crate Rust rewrite of the deprecated `beads_rust` tool
> (binary `br`, an "agent-first issue tracker"; source under `temp/beads_rust-main`). It is grounded in a
> code-confirmed discovery of the original (25 functional / 17 non-functional requirements, 11 domain
> entities) and reshaped by explicit, locked product decisions (§4).
>
> **Value proposition:** *unblock is the only local, offline-capable, dependency-aware issue store with
> atomic multi-agent claim, a versioned dependency-aware scheduler, and contention-safe swarm coordination
> at 250k+ issues — no accounts, no internet, with a credible shared-state path via libsql sync.*

---

## 1. Overview & Vision

unblock is a **local-first, agent-first issue tracker** purpose-built so AI coding agents — and the humans
orchestrating swarms of them — can keep **dependency-aware, machine-readable issue state next to the code**,
with no accounts and no external service required.

It inverts the original product's interface: the **Model Context Protocol (MCP) is the primary surface**.
Every domain feature is exposed as MCP tools/resources/prompts over stdio; the command-line binary exists
only for **lifecycle/operations** (mcp, migrate, doctor, version, init, agents, update — D3), not as the feature surface.

Persistence is a **libsql** (Turso SQLite fork) database — the **source of truth** — accessed behind a
`Storage` trait, local-file by default with a native path to remote/replicated sync later. A line-oriented
JSONL export/import is retained as an **optional** portability/audit feature (git-diffable snapshots), **not**
as a synchronization mechanism.

### 1.1 Competitive context

| Alternative | Why unblock instead |
|---|---|
| **GitHub MCP server** (issues as MCP tools) | Requires internet + account; no dependency graph; no atomic claim; no offline. |
| **saga-mcp** (TS, SQLite, hierarchy + deps + audit) | No atomic claim; single-agent assumption; no swarm-coordination diagnostics; no scheduler; no performance validation; TS runtime overhead. |
| **Raw SQLite MCP server** | No domain model; no exit-code/error contract; no typed dependency edges. |

**Defensible wedge:** swarm-scale correctness at 250k+ issues — atomic multi-agent claim, contention-safe
coordination, a versioned agent error contract, and a dependency-aware scheduler. Everything else is table stakes.

## 2. Problem Statement

External trackers (GitHub Issues, Jira, Linear) require internet and accounts, fragment context away from the
code, cost money, and expose weak machine APIs; bare TODO comments carry no status, dependencies, or
queryability. AI coding agents working in swarms need **deterministic, non-interactive, dependency-aware**
issue state that lives with the repo, is offline-capable, and is machine-readable end to end.

The **trust anchor** is the local libsql database (durable, transactional, with a real sync protocol for the
shared case). Issues no longer need git as a transport; a git-diffable JSONL snapshot remains available as an
**optional, secondary** convenience for audit and review, not as the source of truth.

The original `beads_rust` delivered the agent value but accreted into an unmaintainable shape: a single
~50k-LOC binary crate with by-convention-only layering, monster files (`storage/sqlite.rs` ~22.6k LOC,
`sync/mod.rs` ~9.4k, `doctor.rs`), write-orchestration logic duplicated between the CLI and the MCP server,
and a deep dependency on niche single-author sibling crates — most critically the pre-1.0 pure-Rust
**fsqlite** SQLite engine (15 crates) whose error type leaked into the public API and which **hot-spins at
100% CPU on lock contention** (defect 243), forcing `busy_timeout=0` plus a hand-rolled backoff.

unblock delivers the same agent value as a **coherent, acyclic multi-crate workspace** with an embeddable
core, a mainstream storage backend behind a trait, MCP as the first-class interface, and the agent contract
preserved.

## 3. Personas

| Persona | Tier | Needs |
|---|---|---|
| **AI coding agent** | primary | Deterministic, non-interactive, always-valid structured output; structured errors (`code`/`message`/`hint`/`retryable`); the 0–8 exit-code taxonomy (CLI) / matching MCP error codes; atomic claim; a "what should I work on" query (`ready`/`scheduler`); discovery of contracts (`capabilities`/`schema`) versioned by `contract_version`; `discovered-from` dependency for the work flywheel; stale-claim coordination diagnostics; MCP stdio transport. |
| **Swarm orchestrator** | primary | Ranked, explainable ready-work scheduling (deterministic evidence `unblock.scheduler.v1`); read-only coordination status to diagnose hidden/stale `in_progress` claims (`unblock.coordination.v1`); contention resilience (no CPU hot-spin) at 250k–1M issues and thousands of agents. |
| **CI / external system** | primary | Scriptable structured output and exit-code categories; changelog from closed issues; orphan detection; workflow **gate** verdicts (`ci_green`/`min_reviewers`/`security_sign_off`) that block status transitions. |
| **Maintainer / rewrite engineer** | primary | Embeddable, testable core library API; enforced acyclic crate layering; mainstream swappable dependencies behind traits; a `Storage`-trait contract suite. |
| **Human developer** | **secondary in v1 — equal-importance stakeholder at team scale (D28)** | Issue lifecycle and dependency planning. **Note:** under D3 (CLI has no domain features) and D7 (no rich rendering), daily domain work requires a **running MCP client**; a human in a bare terminal cannot create/list/close/dep directly. A domain-CLI shim stays out of scope (would contradict D3). **D28 note (2026-07-07):** the product's end state is **mixed human+agent company teams over one shared logical issue store** (the remote/shared-DB path, `00-roadmap.md` §3), with humans and agents as **equal-importance stakeholders**; humans work **through MCP clients** (Claude Code today; the proposed v1.5 local TUI later — itself an MCP client, `00-roadmap.md` §6), never through a second domain surface. The "secondary in v1" tier therefore describes the v1 local test phase only, not the product's end state. |

## 4. Product Principles & Key Decisions

These decisions are **locked** (confirmed with Miguel) and shape the rest of this document.

| ID | Decision | Rationale |
|---|---|---|
| **D1** | **Storage = libsql** behind an async `Storage` trait. Local file now; remote/embedded-replica/synced available later. | Mainstream, maintained; gives local **and** remote with one async client → no separate "shared service". Replaces the niche fsqlite stack and its hot-spin defect. |
| **D2** | **MCP = rmcp over stdio, and MCP is the PRIMARY interface.** | rmcp is the official async SDK (`rmcp 1.7`, `server`+`transport-io`). Agent-first product → the agent protocol is the product. |
| **D3** | **CLI is reduced to lifecycle/ops only** (`mcp`, `migrate`, `doctor`, `version`, `init`, `agents`, `update` — all lifecycle/ops, no domain features). All **domain features via MCP**. | "All features via MCP." The binary still needs a lifecycle surface; that is not the feature surface. |
| **D4** | **Errors = snafu**, **per-crate** error enums (no god-enum); mapped to MCP error data / exit codes at the boundary. | Idiomatic typed errors with context selectors; no backend error leakage. |
| **D5** | **libsql is source of truth; JSONL is an OPTIONAL light export/import feature.** Drop the heavy git-merge coordination (3-way merge, 4-phase collision detection, distributed locks, data-loss guards). | libsql provides real sharing; git-as-transport is redundant. Keep JSONL only for portable, diffable snapshots. Reversible toward DB-only later. |
| **D6** | **Whole stack is async (tokio).** | Consequence of rmcp + libsql; matches the scaffold (`tokio = full`). |
| **D7** | **MCP-only sheds the rich-rendering stack** (rich_rust, crossterm, indicatif); rendering is the MCP client's job. | Structured content over MCP; large supply-chain reduction. |
| **D8** | **Rename everything to `unblock`** — binary `unblock`, crates `unblock-*`, config dir `.unblock/` (**monorepo alias `_unblock/` also accepted on discovery** — the original beads `.beads`+`_beads` affordance for dot-dir-hostile environments; amended 2026-06-26), resource URIs `unblock://…`, contract ids `unblock.*.v1`. | New product identity. |
| **D9** | **Target stable Rust** (pinned `1.96.0`); drop clap dynamic/unstable features. | Removes the only reason the original needed nightly. |
| **D10** | **Config = TOML** (`.unblock/config.toml`); **single env prefix `UNBLOCK_`** (`UNBLOCK_ACTOR`/`UNBLOCK_DIR`/`UNBLOCK_JSONL`/`UNBLOCK_OUTPUT_FORMAT`). The resolved actor (precedence `--actor` > `UNBLOCK_ACTOR` > `config.toml [actor]` > `$USER` > `"unblock"`) is **bounded via `unblock_model::validation::validate_actor`** (≤ 200 chars + NUL/control-char rejection) before it can reach storage. | Idiomatic; avoids the unmaintained serde_yaml fork on an untrusted-input path; unifies BD_/BEADS_/BR_. |
| **D11** | **Drop** the town/mayor **cross-project routing**; keep single-workspace `.unblock/` discovery. | Elaborate and niche; reintroduce only on a concrete multi-repo need. |
| **D12** | **Keep** TOON output (feature-gated; **v1.1**), **keep** the compaction fields in the model — **rationale: JSONL round-trip fidelity, not Go-bd conformance.** *(In-binary self-update superseded by D17 — now `axoupdater` in `unblock-cli` at v1, no isolated crate; see FR-25.)* | Conservative; not dropped without need, but scoped out of v1 (see D13/§13). |
| **D13** | **No daemon self-install, no automatic git operations, no git library linked, no network on normal command paths.** | Preserves the non-invasive, offline-first stance. (The MCP stdio server is a foreground process launched by the client, not a self-installed daemon.) |
| **D14** | **v1 concurrency = single `unblock mcp` per workspace owns the DB.** The engine single-writer discipline is **in-process only** (a tokio `Semaphore` in `unblock-engine`); agents scale by connecting as MCP clients to that one MCP server. Concurrent external writers (CLI while the MCP server runs, or multiple MCP servers) are **best-effort** via SQLite WAL + `busy_timeout`, **not** the supported path; `migrate`/`doctor` run when the MCP server is inactive. **D31 amendment (2026-07-09):** the "in-process only / connect as MCP clients to **that one MCP server** / multiple MCP servers = **not** the supported path" clause is **SUPERSEDED by D31** — **child-per-client stdio (multiple MCP servers per workspace) is the SUPPORTED topology**, made correct-by-construction by a RESTORED cross-process advisory write lock on a **NEW** file `.unblock/.write.lock` (an L2 storage primitive, distinct from the vestigial `.unblock.lock` detector target). The in-process `Semaphore` is **RETAINED** and now COMPOSES ABOVE that L2 advisory lock; `busy_timeout` stays the WAL-level backstop. "Single MCP server per workspace" remains accurate only in the **v1.2 per-machine embedded-replica** sense. `migrate`/`doctor` "run when the MCP server is inactive" is now TIGHTENED for `migrate`, which takes the exclusive lock (timeout=0 fail-fast) for the whole command, UNCONDITIONALLY (before the version check); the v1-lite read-only `doctor` takes no lock. | Simplest correct model; makes NFR-2/NFR-3 testable; matches libsql's local SQLite backend. |
| **D15** | **libsql ships local-file-only / bundled by default; the remote/replica feature is OFF by default**, behind an explicit non-default Cargo feature, with a warning that enabling it may make network calls. | Preserves D13/NFR-17 ("no network on normal path") and keeps the TLS/HTTP transitive surface out of the default build (NFR-10/NFR-11). |
| **D16** | **v1 ships a one-shot, best-effort `bd`→unblock import** (bd-export → `issues.jsonl` → `import`). | A general bd→unblock migration capability for anyone with an existing `bd` repo. (unblock's own dev tracking is NOT in bd — it lives in `docs/plans/STATUS.md`.) Reuses the FR-7/FR-8 import path. |
| **D17** | **CI/CD & distribution via `dist`** (cargo-dist): 6 target triples (mac/linux/win × x86_64/aarch64), shell+powershell installers, **self-update via `axoupdater` in v1**, GitHub artifact attestations; CI quality gate from M0. | Mainstream release tooling; replaces the hand-rolled `self_update` stack; satisfies NFR-9/NFR-11/NFR-17. See `docs/plans/ci-cd-and-distribution.md`. |
| **D18** | **`blocked` composes the `list` narrowing facets (FR-4 "filters compose").** `blocked_issues` applies the same narrowing facet set as `list` (status-OR, `issue_type`-OR, inclusive `priority` range, `assignee`, `labels_all` AND / `labels_any` OR, `text_contains` title-only) to candidate rows **before** the live blocked-set membership test, making `blocked` conform to the FR-4 "filters compose" AC. Facets only NARROW — the 3-pass blocked detection and the `ORDER BY` are unchanged. `blocked`'s baseline stays **deferred-inclusive** (`status NOT IN ('closed','tombstone')`); it does NOT inherit `list`'s default visibility, so `include_closed`/`include_deferred` are no-ops on `blocked` (spine §3.2.1). | `blocked` was facet-agnostic only by omission (OQ-B), inconsistent with `list`/`search`/`count`/`stale`; honoring facets is agent-ergonomic + spine-consistent. No interface change (`Session::blocked(&filters)` already takes filters); no migration. |
| **D19** | **`detect_cycles` exposes a `blocking_only` parameter (gating-only vs all-dependency-type cycle detection).** The single `detect_cycles(blocking_only: bool)` (storage trait + `Session` forward) unifies the original's two methods: `blocking_only=true` restricts the cycle graph to the 4 gating types (`affects_ready_work`: `blocks`/`parent-child`/`conditional-blocks`/`waits-for`) — the ready-work view (= original `detect_blocking_cycles`); `blocking_only=false` detects cycles over **all** dependency types — the integrity/lint view (= original `detect_all_cycles`). The `dep cycles` MCP action surfaces it (default TRUE = gating-only); results are ordered cycle-path witnesses (shape pinned by spine §3.2.1/§5.3). | Agents need both the gating view (what blocks ready-work, the FR-5 AC) and the all-types view (dependency integrity/lint); the original `bd` split this across `detect_blocking_cycles(true)`/`detect_all_cycles(false)`, unblock unifies it via one param. Faithful to the original; no interface drift beyond the added param; no migration. |
| **D20** | **Live issue restore (un-tombstone) — net-new (no `bd` precedent).** A **dedicated** `Session::restore(id)` / `Storage::restore_issue(id, actor)` (NOT an `update` patch) reverses a SOFT delete (FR-1c "recoverable"). **(1) Target status = best-effort via `closed_at`:** restore lands `Closed` iff `closed_at.is_some()`, else `Open` — the pre-delete status is not preserved (only `original_type` survives), so `closed_at` is the only was-Closed signal; this also self-satisfies the issues-table CHECK constraint on both branches. Open and Closed round-trip; InProgress/Blocked/Deferred collapse to Open (lost, acceptable). **(2) Type fields:** `issue_type` is **UNTOUCHED** and `original_type` is **cleared → `None`** — explicitly **CORRECTING** the earlier "restore `original_type`→`issue_type`" framing in STATUS/impl-plan (that write is a no-op for local deletes and CORRUPTS imported rows whose serde-carried `original_type` diverges from `issue_type`; `into_tombstone` never mutates `issue_type`, so the live value on a tombstone is already correct). **(3) Audit:** emits a single `Event(Restored)` — NEVER `StatusChanged`/`Reopened` (a dedicated path; it does not reuse the generic update terminal→non-terminal rule). **(4) Scalar/non-cascading in v1:** restores ONE issue — no `--cascade`, no ancestor guard (the tombstone records no delete-batch provenance); cascade-restore is a **v1.1 seam**. **Idempotent error model:** already-active → no-op `Ok(issue)` (no event, no `updated_at`); missing/hard-deleted → `IssueNotFound` (no new ErrorCode minted — the unblock-error golden/exit-code/retryable set stays frozen). **TTL-agnostic in v1** (`deletions_retention_days` reserved/unenforced; TTL-refusal is a v1.1 seam). **ORTHOGONAL to the import no-resurrection invariant** (NFR-8): that guarantee is import-path-scoped; restore is the sanctioned, audited LIVE recovery path. | FR-1c's "recoverable" AC needs a real recovery op, but a tombstone cannot be reopened via `update` (the storage tombstone-patch guard fires first) — so restore is structurally a separate, audited engine path. `closed_at`-driven status is the only signal that survives a soft delete; clearing `original_type` (not writing it to `issue_type`) is the only correct inverse of `into_tombstone` and avoids corrupting imported rows. Scalar-only v1 avoids over-reviving independently-tombstoned children; the cascade/TTL seams shape the v1.1 delete-batch identity. |
| **D21** | **Issue-id generation scheme + atomic minting in the engine (FR-1a).** A faithful port of the classic `bd` adaptive-base36 id scheme, minted by the **engine** under the **D14 write permit**. **Prefix is CONFIG-DERIVED (default `ub`):** the issue-id prefix is **not** a hard-coded constant — it is resolved from `unblock-config` (an ADDITIVE config key, default `ub`, faithful to the original `IdConfig::with_prefix`/`issue_prefix`), normalized via the model `normalize_prefix`, and the engine allocator reads it from the `WorkspaceContext`/`ResolvedConfig` it already holds at mint time (so `ub-` in `ub-<hash>`/`ub-<slug>-<hash>` is the *default* rendering, overridable per workspace). **Creator seed:** the engine resolves the actor → `creator` and passes it as the seed's `creator` field at mint (`generate_id_seed(..., creator, ...)`, faithful to the original `IdGenerationInput.creator`). **(1) Root id = `ub-<hash>`:** SHA-256 over a length-prefixed seed (`len:title`, `len:description`, `len:creator/actor`, `len:created_at` nanos, `len:nonce`), first 8 bytes → `u64` → base36-lowercase, truncated to an **adaptive length** (`min=3..max=8`) chosen by the birthday-problem heuristic `P(collision) ≈ 1 − e^(−n²/2·36^len) < 0.25` over the current issue count; the candidate loop tries nonces `0..10` at a length, then **grows the length**, then the original's saturated fallback (12-char hash, then `…{nonce}` desperation), probing each candidate against storage via `get_issue(id).await?.is_some()` (the existence probe reuses the `get_issue` read — there is **no** `Storage::exists` method) — all **under the write permit** so the probe→insert is atomic. **(2) Slug variant `ub-<slug>-<hash>`** (optional user slug): the slug is normalized to lowercase ASCII-alphanumeric + single hyphens (cap 48 chars, trailing-hyphen-stripped) via the model's `normalize_slug`, then made **prefix-budget-aware** via `normalize_slug_for_prefix(slug, prefix)` — which fits `<prefix>-<slug>` within `MAX_ID_PREFIX_LEN` (=64), truncating the slug to the remaining budget (and re-trimming a trailing hyphen) or **dropping the slug entirely** (empty drop-signal) when the prefix alone exhausts the budget; the **hash suffix is always appended** (uniqueness); an empty-after-normalization slug (or a budget-exhausted drop) falls back to the hash-only `ub-<hash>` ladder. **(3) Hierarchical child id = `parent.N`** when a `parent` is supplied: `N` = storage's existing `next_child_number(parent)` (its `child_counters` high-water mark), then `create_issue` bumps the counter in-tx. **(4) Placement = engine, NOT L7:** the mint+probe+insert (and the child-counter read→bump) MUST be serialized under the single write permit so two concurrent creates under the same parent cannot mint the same `parent.N` — this is **why** minting cannot live in the CLI/MCP adapters; FR-9's single-mutation-home keeps MCP and CLI from drifting. The pure, deterministic candidate compute (hash/seed/adaptive-length/slug-normalize) lives in **`unblock-model`** (`id.rs`, alongside the parser); the **stateful** collision-retry loop + the storage existence probe (`get_issue(id).await?.is_some()`, NOT a `Storage::exists`) and the `next_child_number` read live in the **engine** id-allocator. **(5) `Session::create(&Issue)` (caller-supplied id) STAYS** — it is the **import/internal** path (it preserves caller ids so the `content_hash`-keyed import idempotency, FR-26, is byte-stable; it **never mints**). A **new** engine minting path (`create_issue(NewIssue)`) is added for **interactive** create (MCP/CLI), resolving `parent`→`parent.N`, adding `deps` as edges in/after the same tx, and returning the created `Issue`. | FR-1a needs a real id to mint, and the child-counter `next_child_number`→`+1` read-modify-write is a **race** unless serialized — so minting MUST run atomically under the D14 permit (an L7 adapter cannot guarantee that), and FR-9's single mutation home keeps MCP and CLI on one path. The `bd` adaptive scheme keeps ids short while collision-safe as the DB grows; the slug variant keeps ids human-recognizable. Keeping `create(&Issue)` separate preserves the import path's id-preservation (FR-26 idempotency), which a minting path would break. Splitting pure-compute (model) from the stateful probe (engine) keeps `unblock-model` I/O-free while reusing the existing `id.rs` parser home. |
| **D22** | **Bulk-markdown import + create-surface field fidelity (FR-1a).** Two coupled decisions landing the `bd create --file` bulk-markdown feature on the MCP `issue` tool **and** closing the create-surface field gap it exposes. A faithful port of `temp/beads_rust-main/src/util/markdown_import.rs` (the grammar + the 2-phase intra-file resolution). **(1) Bulk surface = a NEW discriminator on the existing `issue` tool, NOT a new tool:** `IssueInput::CreateBulk { markdown: String }` (`{action:"create_bulk", markdown:"..."}`) — keeps the MCP tool count at 7 (≤ 8, spine §6.6 / RK-3). The markdown is the **inline document content** (a `String` on the wire), NOT a file path — the file-read + path-confinement guards of the original `parse_markdown_file` are a CLI/T3.1 concern; the MCP path takes already-read content. **(2) Parser = a pure `parse_bulk_markdown(&str) -> Result<Vec<ParsedIssue>, StructuredError>` helper** (mcp-owned `src/tools/bulk_markdown.rs`), a byte-faithful port of `parse_markdown_content` + `apply_section_to_issue` + `Section::from_header` + `split_dependency_content`/`split_list_content`/`parse_dependency` (the H2-record/H3-section grammar; the case-insensitive section set ID/Parent/Priority/Type/Description/Design/Acceptance-Criteria[/Acceptance]/Assignee/Labels/Dependencies[/Deps]/Agent-Context[/agent-context/agent_context]; the implicit-description quirk — only the first non-empty pre-H3 line; the dep encoding `type:id`/bare/`external:`/`blocked-by`→`blocks`; bulleted/checkbox-stripped list items). Unknown H3 sections are ignored. **(3) TRULY ATOMIC bulk create — all-or-nothing including mint/storage failures (Miguel-ratified option (b); deviation from the original's best-effort per-issue `continue`, FR-1a "rejected pre-mutation"; the PRD wins over the port here):** the batch is atomic at TWO levels. **(3a) Pre-mutation parse+validate:** the WHOLE document is parsed + validated (empty title, unparseable record, unresolvable intra-file ref) before any mint — a single malformed/unresolvable block rejects the entire batch with ONE `StructuredError{code: ValidationFailed, hint, context}` and ZERO writes. **(3b) One-transaction mint+insert:** the validated records are then created through a NEW dedicated bulk primitive — engine `Session::create_bulk(Vec<NewIssue>) -> Result<Vec<Issue>, EngineError>` over a NEW storage `Storage::create_issues(&[Issue], actor) -> Result<(), StorageError>` — that inserts the WHOLE batch in **ONE `BEGIN IMMEDIATE` libsql transaction** (every row + its `Event(Created)` + per-relation events + the resolved dependency edges + child-counter bumps, committed ONCE). **Any failure mid-batch — a mint exhaustion, an id/`external_ref` collision from an out-of-band race, a ref that slipped validation, any backend error on record #k after 1..k-1 were staged — ROLLS BACK the entire tx: ZERO issues persisted, the caller gets ONE `StructuredError`.** This closes the partial-batch hole the single-create loop would leave: because bulk-markdown **MINTS** (non-idempotent, unlike the id-preserving JSONL/bd import), a partial commit followed by a re-run would produce DUPLICATES with no clean recovery — so the batch MUST be one atomic unit, never N independent `create_issue` calls. The original streamed `eprintln!` warnings and created what it could; unblock refuses the batch atomically and never half-applies it. **(4) 2-phase intra-file dep/parent resolution (faithful port), resolved IN MEMORY before the tx:** the engine `create_bulk` acquires the write permit ONCE for the whole batch; under it, Phase 1 MINTS a fresh id for every record — the existing `get_issue` collision probe runs against BOTH committed storage state AND the in-batch already-minted set (intra-batch dedup), so two records cannot mint the same id — registering each `title`→id and stand-in `### ID`→id (case-insensitive). **In-batch per-parent child-counter + topological mint order (the gating correctness fix, T2.3 design review):** for a hierarchical (`parent.N`) id the mint phase keeps an in-memory `HashMap<parent_id, u32>` next-child counter — the FIRST child of a parent SEEDS from `storage.next_child_number(parent)` (committed high-water) and each subsequent SAME-parent batch sibling uses the INCREMENTED in-memory value, so siblings mint DISTINCT `parent.1, parent.2, …`. This is REQUIRED because `next_child_number` reads ONLY committed state (the batch's own in-tx siblings are not yet committed): without it, two same-parent siblings would both mint the same `parent.N` → an in-tx `IdCollision` → guaranteed whole-batch rollback (the bulk would be UNBUILDABLE for any 2+ same-parent children). The committed `child_counters` row is bumped once by the single `storage.create_issues` tx; the in-tx `IdCollision` guard stays the backstop for an out-of-band racer. The mint phase processes records in **parent-before-child TOPOLOGICAL order** over the intra-batch parent edges (a record whose `### Parent` is another batch record can only mint `parent.N` after that parent's id exists; a record whose parent resolves to a pre-existing storage id mints in file order); a **parent CYCLE** among intra-batch records → reject the whole batch (no valid mint order). This restores the original's per-record-commit behaviour, where each `create_issue` committed before the next so `next_child_number` saw the just-committed sibling (`create.rs:855`–`1121` / `:535`–`537`) — the atomic one-tx design loses that commit-per-record, so the in-memory counter + topological order reproduce it. Phase 2 resolves every deferred `### Dependencies` / `### Parent` symbolic ref (stand-in id → title → pre-existing storage id, the `lookup_import_reference` order, case-insensitive) — entirely in memory, against the just-minted ids — then builds the N fully-formed `Issue`s with their resolved dependency edges and hands them to the ONE `storage.create_issues` tx. The `blocked-by`→`blocks` dep-type alias is flipped at THIS engine edge-resolution step (when the edge is built — `create.rs:1190`), NOT in the pure parser, which preserves the reference string verbatim. **WHOLE-BATCH PRE-MUTATION REJECTION SET (all-or-nothing; faithful-but-STRICTER than the original's per-record skip):** the batch is rejected with ONE `ValidationFailed` and ZERO writes on ANY of — an **ambiguous** intra-file ref (matches >1 batch record, `create.rs:1131`/`:1174`), an **unresolved** ref (`:1135`/`:1216`), a **self-dependency** (a record depending on its own minted id, `:1227`), a **self-parent** (`:1144`), a **marker-only / empty** dep ref (a `-`/`*`/`+` token or empty-after-strip, `:1234`/`is_marker_only_dependency`:1376 — most are dropped by the parser, but any surviving to resolution reject the batch), or the parent cycle above. The original SKIPPED each per-record (`eprintln!` + `continue`) and created the rest; unblock refuses the whole batch (NFR-8). The id-collision **probe** runs pre-tx (collision avoidance, faithful to the single-create ladder); the in-tx id-collision **guard** is the atomicity backstop (an out-of-band racer rolls back the whole batch). The bulk primitive lives on the `Session`/`Storage` surface BY DESIGN — it is the only way to guarantee the one-tx atomicity; an L7 loop over single `create_issue` calls CANNOT (each would commit independently). **(5) Create-surface field extension (the gap the port exposes):** the markdown captures `design`/`acceptance_criteria`/`assignee`/`agent_context` — fields the domain `Issue` (§7) already carries but `NewIssue` (spine §4.1) + `IssueInput::Create`/`CreateInput` (spine §5.2) LACK. They are ADDED to `NewIssue` + `CreateInput` + the `create_issue` Issue-build mapping, so BOTH scalar `create` and `create_bulk` are full-fidelity. **`notes` and `owner` are NOT added** — the parser has no `### Notes`/`### Owner` section, so adding them would invent surface the faithful port does not capture (the markdown-captured set is the exact authority). **NO model change** — `Issue` already has all six. **(6) `max_batch` wired into `enforce_quota` (FR-5/NFR-18):** the bulk record count is capped at `Quotas::max_batch` (default 100) at the preflight, BEFORE any mint — closing the T2.2-documented `max_batch` deferral. **(7) CONTRACT_VERSION bumps (the FR-12 gate fires BY DESIGN):** the new `create_bulk` arm + the extended `Create` fields change the `issue` tool's `JsonSchema`, so `CONTRACT_VERSION` (and the capabilities/schema goldens) MUST bump — the drift gate is proven, not bypassed. **(8) Hash-coupled drift gate (hardens FR-12):** a content-hash of the schema bundle is pinned in-source next to `CONTRACT_VERSION`; the `tests/contract_suite.rs` non-vacuity assertion fails if the bundle hash changes without a `CONTRACT_VERSION` bump — so a schema edit FORCES a version bump, not merely a silent golden re-bless. *(Artifact set widened at T2.6 by D25: the pin becomes the two-document `CONTRACT_HASH` over `capabilities()` + `schema_bundle()` — the bundle now carrying the 7 per-tool `{input, output}` schema pairs + the shared in-band error schema; D22's rule itself — edit forces bump, non-vacuous both ways — is unchanged.)* | FR-1a lists "bulk markdown import" as a must; the original is a real, byte-specific grammar that must be ported faithfully (not reduced). Putting the surface on `issue` (a discriminator) over a new tool honours the ≤ 8 budget + the "extend before add" rule (spine §6.6). All-or-nothing beats the original's best-effort because unblock is a data-integrity tool: a partially-applied batch with silently-dropped records is exactly the drift the rewrite exists to prevent (NFR-8). **TRUE atomicity (option (b)) — not merely pre-mutation validation — is REQUIRED because bulk-markdown MINTS ids (non-idempotent):** an N-independent-`create_issue` loop that fails on record #k after committing 1..k-1 leaves a partial batch, and a re-run re-mints the survivors as DUPLICATES with no clean recovery (the import path is idempotent via `content_hash`, but the minting path is not). The only correct design is one `BEGIN IMMEDIATE` tx covering the whole batch (rollback-on-any-failure), which forces a real `Storage::create_issues` + `Session::create_bulk` primitive — an L7 loop physically cannot give one tx. The single `create_issue`/`create` paths stay UNCHANGED (the bulk primitive is additive). The field extension is forced by the port — the markdown can set `design`/`acceptance_criteria`/`assignee`/`agent_context`, so dropping them on the floor at create would be a silent fidelity loss; the domain `Issue` already carries them, so no model change is needed. Excluding `notes`/`owner` keeps the create surface exactly as wide as the markdown authority, no wider. The hash-coupled gate makes FR-12's "bumping a tool's schema bumps `contract_version`" AC machine-enforced rather than reviewer-trusted. |
| **D23** | **JSONL sync fidelity contract (T2.4) — tombstone export, idempotent-Skip import, bounded ingestion, canonical timestamps.** Five coupled decisions landing `unblock-sync`'s light JSONL export/import (D5 model B: no git, no merge). **(1) Export includes tombstones** via a NEW `ListFilters.include_tombstone: bool` (default `false`) on the spine §1.10 CF-C contract type + a widest-visibility `visibility_branch` SQL branch in `unblock-storage` + an INJECTIVE addition to `unblock-policy::filters_fingerprint`; export pulls the full non-ephemeral corpus (`include_closed=true, include_tombstone=true`, exclude ephemeral/`-wisp-`, order `id ASC`) so import-side tombstone-non-resurrection (FR-8) is round-trippable; import-side anti-resurrection reads `get_issue` (which returns tombstoned rows). **(2) Import collision default = `Skip`** — an idempotent no-op via `sync_equals`/`content_hash` (FR-26), faithful to D5 (no merge); `Error`/`OverwriteIfNewer` stay INTERNAL opt-ins on sync's `ImportOptions`; the MCP surface exposes only `dry_run` (spine §4.1/§5.2) — NO `CONTRACT_VERSION` bump. **(3) Bounded ingestion** — `MAX_IMPORT_FILE_BYTES = 2 GiB` (fd-metadata preflight) + `MAX_LINE_BYTES = 4 MiB` (streaming `BufReader`), overridable-later via an additive `ImportOptions` field WITHOUT a v1 signature break (NFR-18). **(4) Canonical export timestamps** — the RFC-3339/`SecondsFormat::Secs`/`Z` formatter is LIFTED into `unblock-model` (`fmt_ts_secs`, L0 single source of truth, D-OQ-B); render's `fmt_ts` rewires to call it, sync's export calls it directly (no render dep) → render↔export byte-coherence by construction. **(5) Import apply is ATOMIC (all-or-nothing):** the production `Skip` path classifies via read-only `get_issue` probes then applies the new-id subset through ONE `Storage::create_issues` transaction (rollback-on-any → zero writes), reusing the T2.3 atomic primitive under the D14 permit — never a per-record loop. Faithful to the original single-`with_write_transaction` import (`mod.rs:4493`); `OverwriteIfNewer` is an internal, non-production opt-in outside this guarantee. The engine gains a cfg-gated `#[snafu(transparent)] Sync { source: SyncError }` source; atomic write includes the parent-dir fsync after rename (NFR-4/5). `import_bd` stays `FeatureNotWired{"sync"}` (T2.5). | A merge-capable, unbounded, tombstone-blind, non-canonical-timestamp, or partial-commit JSONL round-trip. `Skip` is the idempotent-safe default a data-integrity tool needs; `OverwriteIfNewer` is not cleanly buildable via `IssuePatch` in v1 (missing `defer_until`/`created_by`/tombstone/`ephemeral`/`source_*` fields), so surfacing it would over-promise. Tombstone export is required for FR-8 to be testable; the caps bound a pathological file while admitting the NFR-2 250k–1M-issue corpus; the canonical-ts lift makes render + sync agree by construction, not by hand-copied string. An import that half-commits on a mid-batch failure drops a defence layer the original had (`create_issues` proves the atomic primitive already exists in-codebase); a data-integrity tool must not ship a partial-commit window as its default. |
| **D24** | **bd one-shot import fidelity contract (T2.5/FR-26/D16).** Six coupled sub-decisions landing `unblock-sync`'s one-shot best-effort `bd`→unblock import. The unblock `Issue`/`Dependency`/`Comment`/enum serde is already a byte-faithful field-for-field port of bd's, so the struct-level field map is **identity** — the real T2.5 work is the bd import-normalize **repair pass** + dropped-field accounting + id-preservation, NOT a field-rename table. **(1) bd-import-local repair pass (fork F2 = bd_import-only).** Port bd's `normalize_issue` (`temp/beads_rust-main/src/sync/mod.rs:3813-3918`) — the **7 repairs unblock's shared `jsonl::normalize()` lacks** — applied INSIDE `bd_import.rs` BEFORE the `content_hash` recompute and BEFORE the funnel into `import.rs`: (2) dep-type legacy-underscore repair — a **GENERAL** rule (faithful to bd `normalize_issue`, `temp/beads_rust-main/src/sync/mod.rs:3822-3832`): for any `Custom` `dep_type`, apply `replace('_','-')` and ADOPT the result iff it parses to a known (non-`Custom`) `DependencyType` variant. unblock's `DependencyType::parse_lowercased` (`crates/unblock-model/src/enums/dependency.rs:83-96`) recognizes ONLY kebab forms, so EVERY legacy underscore form lands in `Custom` and needs the repair — `parent_child`/`conditional_blocks`/`waits_for`/`discovered_from`/`replies_to`/`relates_to`/`caused_by` → their kebab variants; `parent_child`→`ParentChild` is the canonical **EXAMPLE**, NOT the whole rule (special-casing only `parent_child` silently breaks FR-26 idempotency for the other six — each moves `dep_type`, a `sync_equals`/ready-work input); (3) dependency dedup keep-latest per `(issue_id, depends_on_id, dep_type)`; (4) terminal-status text aliases `done`/`complete`/`completed`/`finished`/`resolved` (arriving as `Status::Custom`)→`Closed`; (5) wisp id (`id.contains("-wisp-")`)→`ephemeral=true`; (6) `closed_at`=`updated_at` when terminal & `closed_at.is_none()`; (7) `closed_at` cleared when non-terminal; (8) `external_ref` blank/whitespace→`None` else trimmed. The SHARED `jsonl::normalize()` (`crates/unblock-sync/src/jsonl.rs:97`) is **UNCHANGED**: the generic `import_jsonl` path only ever imports unblock's own canonical exports (never legacy bd forms), so widening spine §1.8 for it is unwarranted. **Correctness rationale (why the repairs are REQUIRED, not cosmetic):** all 7 repairs move an input to `content_hash` (status, `external_ref` are hashed — spine §1.8) or to `sync_equals` (`dep_type`, `closed_at`, `ephemeral`, dependency count). bd computes and stores its `content_hash` AFTER these repairs, so unblock MUST apply the same repairs BEFORE its recompute for the recomputed hash to equal bd's stored hash — byte-identical via the frozen 17-field Go-bd padding tail (`crates/unblock-model/src/issue/hash.rs:100-119`). **Repair ORDER + hash composition (SF-2/SF-4):** the repairs run in bd's SOURCE ORDER — specifically the status-alias repair (`mod.rs:3873-3881`) BEFORE the `closed_at` set/clear repair (`mod.rs:3888-3896`), which tests `is_terminal()`; and AFTER the 7 repairs `bd_import` calls the SHARED `jsonl::normalize()` (labels sort/dedup + `updated_at>=created_at` clamp + the `content_hash` recompute) — faithful to bd folding those 3 shared steps into the SAME `normalize_issue` pass (`mod.rs:3816-3820`/`:3908-3917`) — so the recompute includes the shared steps and matches bd's stored hash; `bd_import` does NOT recompute standalone. Skipping the repairs silently corrupts the FR-26 dedup key and the idempotency-Skip predicate; the "field map is near-identity" headline must NOT tempt that simplification. **(2) Preserve bd ids verbatim (fork F6 = preserve).** No prefix remap/reject on the default path. `content_hash` EXCLUDES `id` (`hash.rs:39-56`) so preserving is idempotency-safe; dependency edges stay coherent by construction; aligns with the id-preserving `Session::create(&Issue)` import path (D21 clause 5 — never mints). `SyncError::PrefixMismatch` (`crates/unblock-sync/src/error.rs`, exit-6, "used at T2.5") stays a **RESERVED v1 seam** (defined, NOT exercised on the default path). Prefix **REMAP is DEFERRED to v1.1**; if built it must be a PURE function of the source record (else rerun re-mints different ids → breaks FR-26 idempotency). Tombstones are exempt from bd's prefix check regardless. **(3) Extend `ImportReport` (fork F1 = extend).** Add `dependencies: usize` + `comments: usize` (additive, default `0` on the generic `import_jsonl` path) — faithful to bd's `ImportResult` relation/comment counts (`temp/beads_rust-main/src/sync/mod.rs:4563`), satisfying the FR-26 AC's "issues/deps/comments" count literally. **NO `CONTRACT_VERSION` bump — VERIFIED:** `schema_bundle()` (`crates/unblock-mcp/src/resources/schema.rs:43-54`) hashes ONLY the 7 tools' INPUT schemas (`IssueInput`/`ClaimInput`/`DeferInput`/`QueryInput`/`DepToolInput`/`SyncInput`/`DiagnosticsInput`); `ImportReport` rides in the mcp-owned `SyncOutput` wrapper (`crates/unblock-mcp/src/tools/output.rs:23-28`), which is NOT in the bundle — so the field addition cannot move the hash-coupled drift gate (FR-12/D22). **Implement change-surface (F1 ripple — `ImportReport` has NO `Default`, `results.rs:54`, so no `..Default::default()` escape):** (i) re-bless the JsonSchema golden `crates/unblock-model/tests/snapshots/schema_snapshots__import_report.snap` (currently 3 properties/3 required → WILL diff to 5); (ii) update the 3-field struct literals at `crates/unblock-sync/src/import.rs:167` & `:180` (folded into `apply_records`), `crates/unblock-model/src/results.rs:157-161`, `crates/unblock-mcp/src/tools/output.rs:57-61`, + the new `bd_import.rs` constructor — defaulting the two counts to `0` on the generic path. The golden re-bless is the model snapshot, NOT the `schema_bundle` hash — the no-`CONTRACT_VERSION`-bump claim holds. **(4) Synthesized byte-faithful fixture (fork F3).** Tests use a hand-authored `bd_export.jsonl` (byte-faithful to bd's serializer semantics) exercising ALL 7 repairs + dropped fields + deps/comments counts; NO dev-dependency on the deprecated `temp/beads_rust-main`. **(5) `dropped_fields` = unknown-top-level-keys only (fork F4).** `map_bd_record(serde_json::Value)` diffs raw top-level JSON keys against the known `Issue`/`Dependency`/`Comment` key set; the residue is `dropped_fields`. The Value key-diff MUST run BEFORE `from_value::<Issue>` (serde silently discards unknowns — neither struct sets `deny_unknown_fields`). Repair/coercion notes are NOT drops (optionally tracing-logged, not in `dropped_fields`). **(6) `apply_records` reuse seam (fork F5).** Refactor `crates/unblock-sync/src/import.rs`: extract the classify + atomic-`create_issues` tail into a `pub(crate) apply_records(storage, records, dropped, actor, opts) -> Result<ImportReport, SyncError>` helper that BOTH `import_jsonl` and `import_bd` call — reusing the EXACT tombstone-guard-first / `sync_equals`-Skip / one-atomic-tx code (D23). **The `apply_records` seam is classify+tx ONLY (steps 5a-5c) — it does NOT carry the FR-8 preflight.** Since `import_bd` reads records via `map_bd_record(Value)` (not `validate_records`), it MUST re-run every FR-8 guard on its `Value`-per-line path BEFORE `apply_records` — path confinement, conflict-marker rejection, the bounded read (`MAX_LINE_BYTES`/`MAX_IMPORT_FILE_BYTES`), `IssueValidator::validate` on each mapped `Issue`, and in-file dup-id — preferably via a **shared `preflight_source` confinement helper both paths call**, so FR-8 cannot be bypassed (this closes FR-26's "reuses the FR-7/FR-8 import path" guarantee). **Count plumbing:** `apply_records` returns `imported`/`skipped`/`dropped_fields` with `dependencies:0, comments:0`, and each caller finalizes the two relation counts on the returned report (`import_jsonl` leaves them 0; `import_bd` sets its POST-repair/POST-dedup tallies, faithful to bd's `record_imported_relation_counts`, `mod.rs:4611-4614`). `import_bd` takes NO `opts` (Skip-only production semantics: synthesizes `ImportOptions{dry_run:false, allow_external:false, on_collision:Skip}` at the call site; the `dry_run` early-return stays in `import_jsonl`). **Forward amendment (2026-07-02, T2.6 — superseded by D25):** clause (3)'s forward rule — "NO `CONTRACT_VERSION` bump […] `ImportReport` rides in the mcp-owned `SyncOutput` wrapper (`crates/unblock-mcp/src/tools/output.rs:23-28`), which is NOT in the bundle — so the field addition cannot move the hash-coupled drift gate" — is SUPERSEDED going forward by D25/FORK-1B: from T2.6 the schema bundle carries the per-tool `{input, output}` schemas (the `sync` output = `SyncOutput`), so `ImportReport` and every other output DTO ARE in the hashed contract surface, and any future output-DTO change moves `CONTRACT_HASH` and forces a `CONTRACT_VERSION` bump. The T2.5 no-bump ruling stays historically valid under the then-current inputs-only gate; nothing else in D24 changes. | A bd import that skips the repairs (silently breaking the FR-26 `content_hash` dedup key + cross-tool dedup), or rejects/remaps `bd-*` ids on the default path (failing the AC "a representative bd repo imports" or pulling minting into L3 sync). Preserving ids is byte-faithful and idempotency-safe because `content_hash` excludes `id`; the repairs are the exact reason bd's own stored hash matches, so porting them is fidelity, not scope creep. Extending `ImportReport` is the only reading that meets the AC's deps/comments-count clause literally and is additive (no `CONTRACT_VERSION` bump — the DTO is not in the schema bundle). A synthesized fixture keeps the deprecated bd crate out of the workspace graph (D13/NFR-6) while exercising every repair (non-vacuity gate for the M2 dogfood DoD). Unknown-keys-only `dropped_fields` matches the AC wording ("dropped/unmapped fields") and stays forward-compatible with newer bd builds; conflating coercions with drops would make the insta snapshot noisy. The `apply_records` seam reuses the landed D23 atomic apply path instead of a parallel copy, and is where `dropped_fields` + counts are threaded. |
| **D25** | **Self-describing-contract widening (T2.6/FR-12): tool outputs + the capabilities document enter the machine gate; static hint-shape taxonomy; resource not-found fidelity.** Four coupled sub-decisions closing the T2.6 Understand forks (FORK-1B/2B/3A/4B — Miguel-ratified 2026-07-02). **(1) SchemaBundle recomposed to per-tool `{input, output}` schema pairs (FORK-1B):** the spine §5.3 output surface is MATERIALIZED as mcp-owned per-tool success unions (`IssueOutput`/`QueryOutput`/`DepOutput`; `claim`/`defer` output = `Issue`, `sync` = `SyncOutput`, `diagnostics` = `DiagnosticReport`) plus the typed `DeletePlanOutput`/`DepAdded`/`DepRemoved` replacing the landed ad-hoc JSON values — all `#[serde(untagged)]`/wire-identical, CONSTRUCTED at every tool success path (never schema-only) — and `SchemaBundle` becomes `contract_version` + 7 × `ToolSchemas { input, output }` + ONE bundle-level shared `error` schema (`StructuredError`, the FR-11 in-band shape published once; the rmcp `is_error` flag is the channel discriminator). This supersedes the spine §5.3 single-`ToolOutput`-union sketch (which also MISSED the landed delete/dep-add/dep-remove output shapes — a pre-existing spine↔code drift closed here; the name `ToolOutput` survives only in earlier decision rows/task histories); spine §5.4's "JsonSchema per tool I/O" becomes TRUE as written, per tool. **This SUPERSEDES D24 clause (3)'s forward rule** (see the explicit amendment note on the D24 row): from T2.6 on, ANY output-DTO change moves the machine gate and forces a `CONTRACT_VERSION` bump; the T2.5 `ImportReport` no-bump ruling stays historically valid under the then-current inputs-only gate. **(2) Gate widened to the capabilities document (FORK-2B) — ONE pin:** the in-source pin becomes **`CONTRACT_HASH`** (superseding `SCHEMA_BUNDLE_HASH`; unblock-mcp `options.rs`, in lockstep with `CONTRACT_VERSION`) = SHA-256 over `serde_json::to_vec(&(capabilities(), schema_bundle()))` — the ordered two-document tuple. **Version-coupled artifact set = exactly the two discovery documents** (tool/resource/prompt descriptors, the `code → exit_code/retryable/hint_shape` error map, the 7 tool input schemas, the 7 tool output schemas + the shared error schema, both `contract_version` stamps, and everything transitively in their JsonSchema `$defs`); accepted consequence: `unblock-error` `ErrorCode`-set/exit/retryable/hint-shape changes and edits to the capabilities-document descriptor copies now force a bump. **Residual (explicit):** the live served rmcp-attr strings (`claim.rs:33`, `prompts/mod.rs:19-43`, `server.rs:145-169`) already diverge from the document copies today and stay OUTSIDE the hash — taxonomy parity pins their name/URI SETS only (`tests/contract_suite.rs`); extending that parity to description EQUALITY would force reconciling those 7 divergent strings — a content fork routed to Miguel, NOT part of this task. **Golden-only set (re-blessable, NEVER version-coupled): the 3 prompt rendered-message snapshots** (prompt CONTENT is guidance, not a machine contract; prompt NAMES ARE version-coupled — live-vs-builder parity + hash; tool/resource/prompt DESCRIPTION strings are version-coupled only in their capabilities-document copies) plus the redundant per-crate schema/exit-table goldens. The gate stays **non-vacuous BOTH directions** (like D22 clause 8): a byte change in either document moves the digest (edit without bump → the contract test fails), and `contract_version` is a stamped field of BOTH documents, so a bump alone also moves the digest (bump without re-pin → fails). **(3) `unblock://issues/{id}` not-found folds fuzzy `similar_ids` (FORK-3A):** faithful to the original `issue_not_found_resource` (`temp/beads_rust-main/src/mcp/resources.rs:60-87` over `error/structured.rs:336-359`): candidate corpus = the FULL id set (the original `get_all_ids` semantics — every `issues` row, closed/tombstoned included, `sqlite.rs:6962`), cap **3**, via the already-public `unblock_error::find_similar_ids`; a FAILED corpus scan surfaces the scan error (not the not-found), matching the original; hint family "Did you mean …?" / list-discovery fallback when nothing is close; `context{searched_id, similar_ids}`; surfaced at the `read_resource` boundary as `ErrorData::resource_not_found` (-32002 — landing with the T2.6 not-found-mapping fix) carrying the `StructuredError` as data; the scan PATTERN is faithful (the original's per-not-found full-corpus scan) but the COST is heavier — full `Issue` rows hydrated via `Session::list` where the original scanned ids only (`SELECT id`, `sqlite.rs:6963`), and `read_resource` has no quota preflight; still read-only, error-path-only (FR-10); an id-only-scan storage read is the recorded v1.1 optimization seam (logging the accepted trade-off's exit — the cost point sits explicitly on the T2.6 Verify-gate checklist). **(4) Static hint-shape taxonomy (FORK-4B):** `unblock-error` (L0, zero new deps) gains `HintShape { None, StaticText, ContextualText, SimilarIds }` + `pub const fn ErrorCode::hint_shape(self) -> HintShape` + `pub const fn ErrorCode::static_hint(self) -> Option<&'static str>` (spine §2.2; the `hint_shape() == StaticText ⟺ static_hint().is_some()` invariant holds by construction, and `ModelError::hint()` delegates to `static_hint` — one source of truth), and `ErrorCodeDescriptor` gains a `hint_shape` field — satisfying spine §5.4's "code → exit_code, retryable, hint" AS WRITTEN. The map is HONEST, grounded in the surveyed production hint sites only: `IssueNotFound → SimilarIds` (clause 3); `InvalidPriority`/`InvalidStatus`/`InvalidType` → `StaticText` (`ModelError::hint()` → the pinned `hints.rs` constants); `ValidationFailed → ContextualText` (the mcp over-quota + bulk-markdown-parse site hints); all remaining 30 codes → `None`. A code moves off `None` only when a real production hint site ships (no invented hints); the map is golden-pinned in unblock-error alongside the exit-code table (the quadruple golden) and re-surfaced (version-coupled) in the mcp capabilities error map. **(5) One `CONTRACT_VERSION` bump `unblock.mcp.v1.1 → unblock.mcp.v1.2`** covers the whole T2.6 contract delta (recomposed bundle + `hint_shape` on all 35 descriptors), with `CONTRACT_HASH` pinned and both discovery goldens re-blessed — the FR-12 gate fires by design, proving itself again after T2.3. **Ripple (decision-change checklist):** the D24 row carries the explicit forward-amendment note (never silent); D22 clause (8) gains a widened-at-T2.6 cross-ref (its RULE is unchanged; its artifact set is superseded); every D-id-range citation moves D1..D24 → **D1..D25** with zero stale hits (`CLAUDE.md`, `ci-cd-and-distribution.md` §2.1(a), the `xtask/src/doc_lint.rs` comments); FR-12 + spine §2.2/§5/§5.3/§5.4/§1.10 + the unblock-mcp/unblock-error/unblock-sync crate plans amended in the same PR. | The rejected wording-narrowing (FORK-1A/2A: amend "I/O" → "inputs", narrow the bump-trigger claims to the enforced gate) would have KEPT client-visible surface — output payload shapes, the error map, descriptors — invisible to the version signal, and T2.5 proved that hole in practice: `ImportReport`, a wire DTO, changed with NO bump under the inputs-only gate. FR-12's own text promises "payload shapes, and error/exit codes", so the honest gate must cover them; the bump noise on capabilities-document descriptor/error-map edits is deliberately accepted (a `.M` re-pin is cheap, silent client-visible drift is the exact failure class this product exists to prevent). ONE pin beats two: the two insta goldens already localize WHICH document moved, so a second pin would only double the re-pin ritual and admit half-re-pinned states. PER-TOOL `{input, output}` pairs beat a single pooled output union: the pooled union under-describes (a client cannot tell which shapes a given tool can return), leaves "JsonSchema per tool I/O" only half-true (the exact F-3 drift class), and the single-union sketch was already incomplete against the landed outputs; publishing the in-band error shape ONCE (bundle-level) models the real channel (`is_error` discriminates) instead of duplicating one shape 7 times. Implementing `similar_ids` (vs amending the plan row down to the landed bare `IssueNotFound`) honours never-simplify and the plan-promised original behaviour with an already-public helper. The static `HintShape` (vs dropping "hint" from the spine error-map line) is the only reading that satisfies the spine as written while staying honest: an enum of the 4 OBSERVED shapes, golden-pinned — NOT per-code hint TEXT, which does not exist statically and would have to be invented; `SimilarIds` as its own shape is machine-actionable (the client knows to read `context["similar_ids"]`). |
| **D26** | **T2.7 diagnostics FR-15 — pure-DB v1 scope ratified (five OQ resolutions).** Closes the T2.7 diagnostics taxonomy as a faithful, pure-DB v1 build — every derivation from libsql state only, **no git read or link on any path** (NFR-6/D13; the `cargo deny` git-crate ban stays the static gate). Five ratified resolutions define the scope: **(OQ-1 — changelog `since` threading)** the engine read gains a bare method parameter — `Session::diagnostics(kind, since: Option<DateTime<Utc>>)` — with the default living ONLY on the MCP wire (`DiagnosticsInput::Changelog{ #[serde(default)] since }`), the EXACT `detect_cycles(blocking_only: bool)` precedent (D19; the spine §3.2.1 witness note already draws this analogy). Storage `closed_since(Option<since>)` already matches; the mcp adapter STOPS dropping `since`. **(OQ-2 — changelog output shape)** KEEP the THIN `id`=`title` `DiagnosticFinding{label,detail}` rows — NO grouped/nested DTO (a richer output DTO would change the mcp schema bundle and force a `CONTRACT_VERSION` bump). **(OQ-3 — lint)** PORT bd's template-section lint NOW (a faithful port of `temp/beads_rust-main/src/cli/commands/lint.rs`: Bug ⇒ `## Steps to Reproduce` + `## Acceptance Criteria`; Task/Feature ⇒ `## Acceptance Criteria`; Epic ⇒ `## Success Criteria`; other types ⇒ none; case-insensitive heading-substring test over `description` only, `## `/`# ` prefix stripped; template/terminal rows excluded; one finding per missing section — REPLACING the current `blocked=<n>`-lite finding, which bd's `lint` never computes), REVERSING the `blocked=<n>`-lite deferral — a scope GROW, NOT a simplification. **(OQ-4 — orphans)** KEEP the status-agnostic `external_ref`-matches-commit-pattern reading (the faithful FR-15 wording), NOT bd's `status IN ('open','in_progress')` narrowing; add a non-empty-corpus (non-vacuity) test. **(OQ-5 — stats)** PORT bd's rich `StatsSummary` (`temp/beads_rust-main/src/cli/commands/stats.rs:376-499`: total [tombstone-EXCLUDED], open/in_progress/closed/deferred/draft/tombstone counts, pinned [`pinned` col OR `Pinned` status], blocked [`Status::Blocked` ∪ dependency-blocked active ids, deduped], ready, epics_eligible_for_closure, average_lead_time_hours [emitted only when `Some`]) **MINUS any git-activity block** (bd's `RecentActivity`/`git_recent_activity` is git-derived — EXCLUDED per NFR-6), following bd's tombstone treatment faithfully. **Tombstone visibility (not over-count):** the CURRENT unblock `stats` already excludes tombstones from `total` (it counts via `count_issues(include_tombstone:false)`, whose `visibility_branch` `else if include_closed` arm emits `AND status != 'tombstone'`, `crates/unblock-storage/src/libsql/query.rs`) AND emits NO `tombstone` bucket — so the gap is the ABSENT tombstone COUNTER, not an over-counting `total`. T2.7 ADDS tombstone VISIBILITY: the per-status tally comes from `count_issues` with `include_tombstone:true` (a live read-path flag since T2.6/D25), from which the distinct `tombstone` finding is surfaced and `total` is the non-tombstone sum (`total` needs no change). **Emission order (NFR-14 insta stability):** the stats findings emit in the fixed bd-parity order `open, in_progress, blocked, closed, ready, deferred, draft, tombstone, pinned, epics_eligible, [avg_lead_time_hours], total` (bd's Summary print order, `stats.rs:1225-1244`, with `draft` folded in and `total` last); `avg_lead_time_hours` is an ABSENT row when `None` (bd `skip_serializing_if`) — two documented snapshot shapes (has-closed vs empty corpus). **Expression constraint:** OQ-3 and OQ-5 enrichment stays entirely within generic `DiagnosticFinding{label,detail}` rows (more findings / richer `detail` strings) — the mcp schema bytes are UNCHANGED, so **NO `CONTRACT_VERSION` bump** (v1.2 stays). **Storage-surface note (OQ-5 seam — design-Review RATIFIED the NARROW primitive):** changelog/lint/orphans compose from the existing trait (`list_issues`/`ready_issues`/`blocked_issues`/`count_issues`/`closed_since`/`orphan_candidates`), NO new method. Bd-faithful `stats` needs ONLY the per-epic `parent-child` child rollup added as a primitive; `pinned` and the per-status tally are composed IN-MEMORY / from `count_issues`. The ONE additive pure-DB method is **`async fn epic_child_rollup(&self) -> Result<Vec<(String, (usize, usize))>, StorageError>`** — per-epic `(child_total, child_closed_or_tombstone)`, the Vec sorted by epic id in SQL (`ORDER BY`, NOT HashMap iteration — bd's `get_epic_counts` returns a non-deterministic `HashMap`, `sqlite.rs:6978`; unblock sorts for NFR-14). It is bd's `get_epic_counts` ported 1:1 (`JOIN dependencies d JOIN issues i ON d.issue_id = i.id WHERE d.type='parent-child' AND the CHILD is non-template (`i.is_template = 0 OR IS NULL`); child-closed = `status IN ('closed','tombstone')`). The epic-side active + non-template filter (`issue_type==Epic ∧ ¬terminal ∧ ¬template`, `stats.rs:441-446`) is applied IN-MEMORY in the engine — keep BOTH filters at their respective sites. `pinned` is DROPPED from the primitive: computed in-memory over the widest-visibility `list_issues` pass as `issue.pinned || issue.status == Status::Pinned` (`stats.rs:436`; `Status::Pinned` exists). The per-status tally, tombstone count and tombstone-excluded total come from the EXISTING `count_issues` (see the tombstone-visibility note above) — NO `StatsRollup` model DTO is needed (the bare `Vec<(String,(usize,usize))>` suffices), so the spine §1.10 surface does NOT grow a new type. `epic_child_rollup` is an internal aggregate, NOT `JsonSchema`-derived, NOT in `schema_bundle()`/`capabilities()` — zero `CONTRACT_HASH`/version impact (no bump — still `unblock.mcp.v1.2`). Dropping `epics_eligible`/`pinned` to avoid the primitive would thin the faithful port (forbidden — never-simplify hard rule). **Seam notes (documented, NOT silent bd divergences):** `ready`'s external-blocker exclusion is a **vacuously-empty v1.1 config seam** (unblock v1 has no external-project layer, so bd's `external_blockers` term is empty) and bd's wisp filter is spine-DROPPED (Miguel) — a future reader must NOT read the v1 `ready` count as a bd divergence. | FR-15 lists stats/lint/changelog/orphans as MUST and the original is a real, byte-specific behaviour that a faithful rewrite ports, not thins (never-simplify hard rule). The `since` bare-param + wire-default asymmetry is the pinned D19 shape (spine §3.2.1), so OQ-1 has zero contract impact. Keeping findings as `{label,detail}` (OQ-2) is what lets the lint/stats grow WITHOUT touching the hash-coupled FR-12 gate — richer detail is client-visible without a schema change. OQ-3 REPLACES the `blocked=<n>` lint-deferral in `docs/plans/crates/unblock-engine.md` (`src/diagnostics.rs` row) with bd's template-section rules and OQ-5 GROWS stats to bd-faithful; both are faithful scope-grows MINUS git (NFR-6), not new outward surface. Status-agnostic orphans (OQ-4) is the literal FR-15 reading ("issues whose `external_ref` matches a commit pattern") — bd's status narrowing is a UI-scan optimization, not the requirement. The `epic_child_rollup` primitive is the minimal pure-DB addition that keeps the port faithful without a wire change (`pinned` and the per-status/tombstone tallies are composable in-memory / from `count_issues`, so they do NOT enter the trait); it is additive (no existing signature moves), returns a bare id-sorted `Vec<(String,(usize,usize))>` (no new model DTO), and stays outside the hashed contract surface. |
| **D27** | **T3.1 `unblock-cli` lifecycle surface ratified (four forks + adopted defaults).** Closes the T3.1 Understand/Decide forks as the faithful, no-network, MCP-first lifecycle binary (D3 command set `mcp, migrate, doctor, version, init, agents, update`), reconciling three HIGH plan↔code drifts (the `run_mcp_server` signature, no `Session::migrate()`, `doctor()` seamed to health) spine-first against the live surface on `main` @ b384103. Seven ratified clauses: **(AF-1 — doctor-lite = diagnostics-composed + integrity).** `unblock doctor` at T3.1 composes a READ-ONLY report from the BUILD-now `Session::diagnostics(kind, since)` path (kinds `Stats` + `Lint` + `Info`) PLUS `PRAGMA integrity_check` — reachability CONFIRMED: `integrity_check` IS a first-class `Storage` trait method (`trait_def.rs:59`, libsql `diagnostics.rs:19`), so AF-1's "add integrity IFF reachable through the engine/Storage trait" clause is SATISFIED and the fuller build INCLUDES it via a NEW thin engine read `Session::integrity_check() -> Result<Vec<String>, EngineError>` (a pure read, FR-10, no permit — surfaces the existing `Storage::integrity_check`; NOT testkit-only). A doctor-lite `DiagnosticReport` REUSES an existing `DiagnosticKind` (`Info`) — NO `unblock-model` spine §1.10 variant added now. **Non-zero exit only on detected corruption:** a non-empty `integrity_check` result maps to the existing `ErrorCode::DatabaseError` (exit 2; the db bucket has NO dedicated corruption code — spine §2.3 exit table UNCHANGED, NO new ErrorCode), Lint/orphan findings are ADVISORY and do NOT flip the exit; otherwise exit 0. The FULL Healthy/Drifted/Recoverable/Unsafe taxonomy + `--repair` + `.unblock/.recovery/` evidence land at **v1.1** over the `Session::doctor()`/`recover()` seam; **T3.3 (v1) wires only `doctor()` with the lite body** (integrity + file-state) and the cli `doctor` then ROUTES through the wired `doctor()` — a forward-refinement of this AF-1 clause ratified as **D29** (the D24 forward-amendment precedent, NOT a reversal of D27), while `recover()` stays `FeatureNotWired{"health"}` until v1.1. **(AF-2 — migrate = engine `Session::migrate()` passthrough with real schema data.)** Add a NEW additive engine method `Session::migrate(&self) -> Result<MigrateOutcome, EngineError>` (spine §4.1) that, under the write permit (D14), reads the on-disk schema version, runs the idempotent `Storage::migrate()` (a no-op on a current DB), re-reads, and returns `MigrateOutcome { from: i64, to: i64, applied: bool }` (`applied = from != to`). This needs a NEW additive `Storage` trait read `async fn schema_version(&self) -> Result<i64, StorageError>` (spine §3.2, lifecycle block after `integrity_check`; libsql delegates to the existing `pub(crate)` `current_user_version` widened to `i64`; every `Storage` impl — 1 real + 9 test stubs — gains it, the accepted additive-primitive cost). NOT opaque success; a newer-than-build DB surfaces the transparent `StorageError::SchemaMismatch` (exit 2), never a fake. Because the config open facade already migrates on open (FR-9 no-drift, single open path), `applied` is normally `false` post-open — an honest idempotent signal. **(AF-3 — init scaffold = `config.toml` + migrated empty `unblock.db` ONLY.)** `unblock init` creates `.unblock/config.toml` (hand-written TOML — `ProjectConfig` is `Deserialize`-only, so the CLI writes the text itself; seeds `id_prefix` with the `normalize_prefix`-NORMALIZED value → the CLI takes a direct `unblock-model` dep) + a migrated empty `unblock.db` opened through the config facade `open_with_storage_with_cli` (FR-9 no-drift, NOT a direct `LibsqlStorage` open). `agents`/AGENTS.md stays a SEPARATE command (not folded into init). **Clobber guard:** refuse if `config.toml` OR `unblock.db` is already present under the target `.unblock/` without `--force` → a CLI-local `CliError::AlreadyInitialized` mapping to the existing `ErrorCode::AlreadyInitialized` (exit 2; `ConfigError` has no such variant). `--prefix` honored. NO `.gitignore`, NO `metadata.json`, NO seeded `issues.jsonl` (D13/NFR-6/model-B). **(AF-4 — error boundary = `impl CodedError for RenderError` + explicit McpServerError map + the render 4th variant.)** Additively `impl unblock_error::CodedError for RenderError` in `unblock-render` (delegating to the inherent `code()`), so the uniform `(&err).into()` L7 bridge covers it. `McpServerError` (`Transport`/`RunLoop`, `#[non_exhaustive]`) is mapped EXPLICITLY in the CLI `exit.rs` to `ErrorCode::InternalError` (exit 1) — an MCP-server run-loop/transport failure is an INTERNAL condition, NOT a user IoError (Transport → exit 1, not 8; no `impl CodedError for McpServerError`). Also land the verified-faithful render 4th variant `RenderError::UnknownFormat { name: String } → ErrorCode::ValidationFailed`: `parse_format`'s `_ =>` arm carries the raw name, `UnsupportedFormat` reverts to its true kind-not-supported meaning, and render's "exactly three variants" doc/doctest becomes "four". Purely additive, NO new ErrorCode, exit table (spine §2.3) UNCHANGED, spine §6.5 (one error → one code) untouched. **(AD-1 — no mint.)** NO v1 lifecycle command calls `Session::create_issue` (create is MCP-only, D3; `init` scaffolds an empty DB; `agents` is a pure file op) — the STATUS "CLI create-ingestion consumes T1.8" framing is DROPPED (the render `parse_format` hand-off stays; that IS in scope, AF-4). **(AD-2 — render via `DiagnosticReport`.)** `version`/`migrate`/`doctor`/`init` reports are CLI-LOCAL private structs (`VersionReport`/`MigrateReport`/`DoctorReport`/`InitReport`, deriving `serde::Serialize`; NOT spine §1.10 contract types — the T2.1 render private-type precedent) that MAP onto a `DiagnosticReport` (kind + `{label,detail}` findings) and call `Renderer::diagnostics` — one supported shape, all five formats, the NFR-14 stdout/stderr split + FR-11 always-valid-JSON uniform. `output.rs` gets a to-`DiagnosticReport` adapter, NOT a generic `render<T>` (the `Renderer` trait has no such method). **(AD-3 — FR-13 clap `env` single point.)** The CLI binds `--dir`→`UNBLOCK_DIR` and `--actor`→`UNBLOCK_ACTOR` via clap `env` so `--flag > UNBLOCK_*` precedence is free and clap is the SINGLE wiring point into `CliOverrides`; config's env layer owns the strict parse of `UNBLOCK_OUTPUT_FORMAT` (the one PARSED env var) so the `-o` value never double-parses across an exit-code split. The CLI does NOT re-implement layering (config owns it — crate-plan Q5 CONFIRMED). **(AD-4 — MCP-server reconcile, spine-first.)** The LIVE `run_mcp_server(session: Arc<Session>, opts: McpServerOptions) -> Result<(), McpServerError>` (transport bound internally to `stdio()`, shutdown = a `tokio_util::sync::CancellationToken` in `McpServerOptions.cancel`) is the shipped reality (T2.2/PR #387) — spine §0.1 and the cli plan describe THIS 2-arg signature; the 3-arg `run_mcp_server(session, transport, shutdown)` / bespoke `ShutdownToken` never shipped. The CLI `mcp` command builds a `CancellationToken`, installs the FR-17 OS signal handlers (signal-hook, unix) that BOTH cancel the token AND set the engine `AtomicBool` via `Session::with_shutdown_flag`; on EOF/first signal → token cancels → `run_mcp_server` returns Ok → `session.shutdown()`; a second signal → `std::process::exit(128+signo)` (no unsafe); Windows is a cfg no-op; the exit carries `128+signo`. `tokio-util` is added to the cli deps. **(AD-5 — build.rs metadata, NFR-6.)** `version` metadata (version/profile/rustc/target/features + OPTIONAL git sha) comes from build-time env only — NO `git` invocation, NO git crate; `build.rs` synthesizes `UNBLOCK_BUILD_*` from `CARGO_PKG_VERSION`/`PROFILE`/`TARGET` + rustc, the git sha via `option_env!("VERGEN_*")`/a CI-injected env (absent = `None`, not an error; no new `vergen` build-dep). `version` does NOT do a GitHub update-check (D13/NFR-17 — the update-check lives only in `unblock update`). | The three HIGH drifts change command bodies, not just wiring, so they are resolved spine-first before spec (never let code and docs drift silently). doctor-lite INCLUDING integrity is the never-simplify reading of AF-1 (the signal IS reachable through the trait); reusing `DiagnosticKind::Info` + `ErrorCode::DatabaseError` keeps the §1.10 and §2.3 surfaces untouched while giving `doctor` real read-only value at T3.1, additive over `Session::doctor()` at T3.3. `Session::migrate()` + `Storage::schema_version()` give a real from→to/applied report through the SINGLE open path (FR-9 no-drift) rather than opaque success or a phantom `applied: Vec<String>` with no data source; the schema-version read is the minimal additive primitive (one `PRAGMA user_version`), and its stub cost across the 10 `Storage` impls is the accepted price of a new primitive (the D26/`epic_child_rollup` precedent), not a simplification. `impl CodedError for RenderError` (render already computes the code) tidies the boundary to one uniform bridge, while the explicit `McpServerError → InternalError` map is honest: an MCP-server failure is internal, not a user IoError, so it does NOT belong in exit 8. The render 4th variant preserves the raw offending format name for the agent instead of the mislabeled `UnsupportedFormat{Json}` placeholder that LOST it. init scaffolding exactly `config.toml` + a migrated empty DB (no git/bv-oriented `.gitignore`/`metadata.json`/`issues.jsonl`) is the model-B, no-git reading (D5/D13/NFR-6); the clobber guard reuses the existing `AlreadyInitialized` code. Mapping every lifecycle report onto `DiagnosticReport` reuses the ONE live lifecycle-render path (all five formats, FR-11 uniform) instead of fragmenting to json-only. Clap `env` as the single FR-13 wiring point (config owns layering) keeps ONE source of truth. The 2-arg `run_mcp_server` is the shipped code; the plan/spine are the bug on this interface (spine-first reconcile). build.rs pure-`option_env!` (no vergen, no `rustc -V`/git subprocess) keeps the no-network/no-git invariant trivially true. |
| **D28** | **Audience ratified (2026-07-07): unblock's real audience is mixed human+agent company teams sharing one logical issue store — with MCP kept as the single primary domain surface.** The product's end state is **teams at Miguel's company**: human and agent actors with **EQUAL stakeholder importance** working over ONE shared logical issue store; the local-only single-workspace deployment is the **initial test phase**, not the end state — the remote/shared-DB path (`00-roadmap.md` §3, v1.2) is **crucial, not optional**. Equal stakeholder importance does NOT mean equal domain surfaces: **MCP stays the single primary domain surface** (D2), the CLI stays lifecycle/ops-only (D3), and the engine stays the single mutation home (FR-9) — humans work **through MCP clients** (Claude Code today; the proposed v1.5 local TUI later — itself an MCP client, `00-roadmap.md` §6), never through a second domain surface. D2/D3/FR-9 are **preserved, not relaxed**. Consequences live in the roadmap (the 2026-07-07 v1.2–v1.5 resequence, `00-roadmap.md` §3–§7): shared state (v1.2), the planning layer (v1.3), the streamable-HTTP transport (v1.4), the local TUI (v1.5) — each stays PROPOSED/`[NEW]` until its version locks (the existing roadmap convention); **no FR is added or re-tiered by this decision**. The PRD §3 persona table carries the matching note (the human developer's "secondary in v1" tier describes the v1 test phase only). **PRO note (2026-07-08):** the audience may in future extend beyond internal teams via a **commercial web dashboard** — a **v2+ direction** (`00-roadmap.md` §7): read-only, reading a **client-minted Turso token**, with the **model/policy** domain logic (ready-sort + scheduler explanations) **compiled to WASM** and the storage-layer reads (blocked / rollups) **re-issued as SQL against the replica** — still **not** a second domain surface (FR-9 preserved), **gated on a future go-to-market decision** — which is when any PRO D-id would be minted, not now. | The persona table's "human developer = secondary/future" framing understated the audience: the real buyer is a mixed human+agent team, and remote/shared state is the value proposition's core, not a stretch goal (D1 chose libsql precisely for the one-client local-AND-remote path). Ratifying this as a D-id makes the product direction explicit while PRESERVING the locked surface decisions: serving humans via clients keeps one contract, one mutation home, and zero drift between surfaces — the alternative (a parallel human-facing domain surface) would reverse locked decisions (D2/D3) and reintroduce the exact drift class FR-9 exists to prevent. |
| **D29** | **T3.3 `unblock-health` scoped to HEALTH-LITE in v1 (Miguel, 2026-07-08) — five coupled sub-decisions.** T3.3 builds the v1 modules of the currently-empty `unblock-health` crate (libsql `integrity_check` passthrough + a PURE file-state classifier + `run_doctor` aggregation + the stable 4-variant `HealthLevel` enum, of which v1 only ever PRODUCES 3 — `Healthy`/`Recoverable`/`Unsafe`; `Drifted` is unreachable until v1.1 JSONL-drift) and WIRES `Session::doctor()` off the `FeatureNotWired{"health"}` seam as a read-only lite report. The ACTIVE 4-state severity taxonomy (`WorkspaceClassification`/full `AnomalyClass`/`from_anomalies`/`is_operable`/`needs_recovery`/`recovery_possible`), `--repair`, the `Session::recover()` body, the `.unblock/.recovery/` evidence writer, `classify_workspace` drift-detection, `audit.rs`, and the CF-E `Storage::diagnostic_probe(s)` seams all STAY **v1.1** (FR-16 full; roadmap §2 / §8 feature-matrix "● lite \| ● full"; RK-5). **(F1 — `recover()` stays unwired.)** `Session::doctor()` is wired at T3.3; `Session::recover()` STAYS `FeatureNotWired{"health"}` through v1 — its body is `--repair` + the `.unblock/.recovery/` evidence writer, both v1.1; wiring it to a hollow "nothing repaired" report would be the faked success the `FeatureNotWired` contract forbids. **(F2 — reuse `DiagnosticKind::Info`, contract-neutral.)** doctor()-lite maps its `DoctorReport` onto a `DiagnosticReport` REUSING the existing `DiagnosticKind::Info` (the landed T3.1/D27-AF-1 doctor-lite precedent) — NO new `DiagnosticKind` variant, NO spine §1.10 change, NO `CONTRACT_VERSION`/`CONTRACT_HASH` bump (D22/D25). **(F3 — `run_doctor` is storage-free.)** `fn run_doctor(integrity_rows: &[String], paths: &WorkspacePaths) -> Result<DoctorReport, HealthError>` is PURE and NON-async: the engine calls `Session::integrity_check()` itself and passes the `Vec<String>` rows in; `unblock-health` gains NO `unblock-storage` dependency (its landed `Cargo.toml` deps stay model/error/sync — `sync` is an unused edge in lite, kept for the v1.1 `classify.rs`). **(F4 — the cli `doctor` routes through the wired `Session::doctor()`.)** At T3.3 the `unblock doctor` command renders `Session::doctor()`'s `DiagnosticReport` (integrity + file-state anomalies), preserving the D27/AF-1 exit-2-on-corruption derivation (`ErrorCode::DatabaseError`) — a FORWARD-REFINEMENT of D27/AF-1 (the D24 forward-amendment precedent), NOT a reversal: D27's T3.1 `doctor` was a self-contained diagnostics+integrity composition; from T3.3 it routes through the wired seam. `doctor --repair` does NOT land at T3.3 (needs `recover()`/v1.1). **(F5 — FileAnomaly severity map + faithful `OrphanedLockFile`.)** The v1 file-state severity map: `JsonlConflictMarkers`→**Unsafe** (the only v1 `Unsafe` producer); `DatabaseMissing`/`DatabaseNotSqlite`/`TruncatedWal`/`SidecarMismatch`/`JournalSidecarPresent`/`OrphanedLockFile`→**Recoverable**; the original `Degraded`→`Drifted` rename (v1 never produces `Drifted`). `OrphanedLockFile` (lock path `.unblock.lock`, D8) is ported FAITHFULLY (defensive/vestigial today — writes serialize via the D14 in-process Semaphore, so no on-disk lock is written in normal operation — but dropping it would be a forbidden simplification). **(F5-A — `TruncatedWal` predicate; ratified D29 addendum, Miguel 2026-07-08.)** `TruncatedWal` fires on a `-wal` of `0 < len < 32` bytes, NOT the original's literal `len < 32`: a 0-byte live `-wal` is a VALID `SQLite` state under unblock's PERSISTENT-OPEN `Session`. beads' offline doctor CLOSED the DB (checkpointing the WAL away) before classifying, so its literal `len < 32` never saw a live WAL; the literal port instead false-positives `truncated_wal` on every healthy workspace (empirically reproduced). A genuine partial 1–31-byte header (a torn write) is still caught. This is a ratified faithful-adaptation — an ADDENDUM to D29, NOT a new D-id (the D1..D29 range is unchanged). | LITE is the spec-locked v1 scope (PRD §12.4; roadmap §2 / §8; RK-5), NOT a simplification — the full taxonomy was always a v1.1 design item. Wiring only `doctor()` (never `recover()`) keeps every v1 seam honest: no faked success (F1). Reusing `DiagnosticKind::Info` keeps doctor()-lite contract-neutral — `doctor()` is not MCP-exposed, so a new `DiagnosticKind` variant would move `CONTRACT_HASH` (forcing a D22/D25 contract event + golden re-bless) for zero external benefit (F2). The storage-free `run_doctor` matches the landed manifest and makes NFR-15 backend-opacity trivially true (integrity rows cross as `Vec<String>`, never a libsql type) (F3). Routing the cli `doctor` through the now-wired `Session::doctor()` is the single-composition, no-drift reading of FR-9 (one report shape; MCP and cli identical) and surfaces the new file-state signal to users; framing it as a forward-refinement (not a reversal) preserves D27's mutation-proven exit derivation (F4). The severity map keeps every anomaly's signal (never dropping `OrphanedLockFile`) while honouring the libsql-authoritative model where `Drifted` means JSONL-drift only, unreachable until v1.1 (F5). |
| **D31** | **v1 concurrency AMENDMENT to D14 (Miguel, 2026-07-09) — child-per-client is the SUPPORTED topology, and beads' cross-process advisory write lock is RESTORED as `.unblock/.write.lock`.** **(1) Topology reconciled DOWN to §8.2.** The supported v1 topology is **child-per-client stdio**: each MCP client (Claude Code today; the v1.5 TUI later) spawns its **own** `unblock mcp`, so **multiple MCP servers per workspace is the NORMAL case**. The D14 clause "single `unblock mcp` per workspace … agents scale by connecting as MCP clients to **that one MCP server** … multiple MCP servers = best-effort, **not** the supported path" is RETIRED as physically unrealizable over stdio (rmcp stdio is the process's OWN stdin/stdout; a second client is a second process — a shared MCP server would need the deferred v1.4 socket/streamable-HTTP transport, not shipped in v1). "Single MCP server per workspace" stays accurate only in the **v1.2 per-machine embedded-replica** sense (one local MCP server per machine; cross-machine writes serialize at the shared Turso primary — D15). **(2) HARD-RULE trade surfaced + un-done.** D14 silently replaced beads' cross-process `.beads/.write.lock` (an OS advisory `File::try_lock`, taken per write, that serialized ALL mutating ops across processes) with an **in-process-only** tokio `Semaphore`. Under the now-supported child-per-client topology two MCP servers' Semaphores guard nothing across processes, so cross-client write serialization fell to WAL + `busy_timeout` alone — best-effort liveness, **not** a simplification of integrity (universal `BEGIN IMMEDIATE` keeps multi-MCP-server atomic and lossless; proven by the T0.8 contention lab). This is the simplify-for-progress trade the HARD RULE requires be UN-done, not relabeled. **(3) D-lock guarantee.** RESTORE an OS advisory write lock on a **NEW** sibling file **`.unblock/.write.lock`** — a **distinct** file from the vestigial `.unblock.lock` (D8, the `OrphanedLockFile` detector target, which STAYS never-written; `unblock-health`, its F5 severity map, and all its snapshots are UNCHANGED). It is an **L2 storage primitive** (`std::fs::File::try_lock`; stable 1.89, available on the pinned 1.96 with NO external crate, `#![forbid(unsafe_code)]` holds), acquired at the **WHOLE-MUTATION scope** — the SAME span as the engine `Semaphore(1)` permit (spine §4.2), covering the `next_child_number` allocation READ **and** the write transaction (faithful to beads `with_mutation`, closing the cross-process `parent.N` id-collision hole a tx-scoped lock would leave). It **COMPOSES BELOW** the RETAINED engine `Semaphore` (L5, in-process) and the storage write `Mutex` (L2, in-process): acquire order permit → `.write.lock` → write `Mutex` → `BEGIN IMMEDIATE`, release inner-first — deadlock-free (the Semaphore admits one in-process writer, so `.write.lock` is contended by at most one writer per process; it is the single cross-process resource). A **single EXCLUSIVE lock, two hold-scopes** (short per-mutation; whole-command for `migrate`); **reads take NO lock** (WAL MVCC, FR-10). `migrate` takes the exclusive lock **explicitly** with a **timeout=0 fail-fast** for the WHOLE command, UNCONDITIONALLY (acquired before the version check + migration run; it bypasses the per-mutation chokepoint) — taking it unconditionally rather than only when the schema advances removes the lock-free pre-read TOCTOU, and an already-current DB migrate ALSO acquires the lock and fail-fasts under contention; the v1-lite read-only `doctor` takes **NO lock** (reads are WAL-safe). The native `busy_timeout = 5000 ms` + `BEGIN IMMEDIATE` STAY as the WAL-level integrity floor and the networked-fs residual; the beads `busy_timeout=0` + hand-rolled backoff-poll stays REJECTED. **(4) Non-hot-spin (NFR-3).** Acquisition is one native `try_lock` fast-path then a bounded, SLEEPING async poll (`tokio::time::sleep`, 25 ms) to `write_lock_timeout_ms` — never `File::lock()` (blocking) on a worker, never a busy-spin; a timeout surfaces the retryable `StorageError::DatabaseLocked` → `ErrorCode::DatabaseLocked` (**no new ErrorCode**, no `CONTRACT_HASH`/exit-table/golden churn). **(5) Config seam.** The timeout is an ADDITIVE `unblock-config` (L4) key **`write_lock_timeout_ms`** (default 30000; faithful to beads `--lock-timeout`), resolved in L4 and threaded DOWN to L2 at open (the `open_local` precedent; **no** L2→L4 back-edge — the lock path is derived from the db-file parent, like the health file-state paths). **(6) Rejected alternatives.** **D-wal** (relabel WAL+busy_timeout best-effort as "supported") — a relabel, not a fix; the forbidden simplify-to-progress move. **B / C shared-daemon** (attach-to-one-MCP-server via socket) — pull forward the deferred v1.4 socket/streamable-HTTP transport + the NFR-18 security surface, disproportionate since integrity is not at risk. **(7) Residual limits ACCEPTED.** Voids on NFS/SMB/9p (advisory locks AND WAL `-shm` coherence break where POSIX locks are not honoured; unblock cannot detect the fs type without unsafe/dep-budget cost — **documented-only**, no runtime fs-type detection); the lock is a **machine-wide non-FIFO write bottleneck** (a TUI+agent burst can see multi-second p99 stalls, bounded by `write_lock_timeout_ms` → a starved writer errors retryable, never hangs); it does **NOT** close **version-skew** across two concurrently-live post-`update` binaries (each applies its own logic — a single owner/handshake is a v1.2/v1.4 concern; the existing future-`user_version` → `SchemaMismatch` guard still self-limits the dangerous case). | Child-per-client is the ONLY stdio-realizable topology and the MCP-ecosystem norm (Claude Code spawns its own child); §8.2 was always the accurate half and the D14 headline clause was an internal PRD contradiction (physically impossible over stdio). Restoring the exact mechanism beads shipped — a separate `.write.lock` (crash-safe advisory, never orphans) kept distinct from the vestigial `.unblock.lock` detector so `unblock-health`/F5 and its snapshots stay UNCHANGED — is the faithful un-simplification the HARD RULE demands: it ships entirely under v1 stdio (no socket, no daemon, no auth surface) and repays the precise debt of D14's silent lock drop. Whole-mutation scope (not tx-scoped) is required because the engine mints `parent.N` from a pre-tx `next_child_number` read; a narrower lock would leave a cross-process id-collision hole. Pre-1.0, no external users: an additive doc+code change, not a versioned break. Framed as a D14 forward-amendment (D24/D29 precedent), preserving D14's correctness+testability spirit while inverting only its "second writer unsupported / one serialization point" literal clause (already a fiction). |
| **D32** | **Rename the primary MCP subcommand `serve` → `mcp`; the CLI command formerly invoked as unblock serve is now `unblock mcp`, and the bare verb `serve` is RESERVED for the future v2+ web/dashboard HTTP server (amends D3, clarifies D27/AD-4).** The v1 lifecycle command set becomes `mcp, migrate, doctor, version, init, agents, update`. The library entry (the shipped `unblock_mcp` server fn + its options struct) is renamed to `run_mcp_server` / `McpServerOptions` (see spine §0.1 for the ratified identifiers), and every "serve process"/"single-serve"/"multiple serve"/"multi-serve"/"one serve per workspace" concept noun migrates to **"MCP server"** language across D14/D27-AD-4/D31/NFR-2/NFR-5/FR-17/FR-20/PRD §8.2 + the spine + roadmap. The `agents` command and `McpServerError` are unchanged. | "MCP server" is the accurate register for a stdio MCP endpoint; a bare `serve` verb would collide with the planned PRO web-app HTTP server (v2+), so reserving it now keeps the command surface unambiguous. Pre-1.0, no external users → a clean rename, no compat shim, no migration. This is terminology/interface only: `capabilities()`/`schema_bundle()` bytes are unchanged, so `CONTRACT_HASH`/`CONTRACT_VERSION` do NOT move (verified: no `serve` verb appears inside any serialized descriptor string). |
| **D33** | **Enrich the `unblock agents` managed `AGENTS.md` block into a FULL capabilities table, rendered via a NEW pure typed `unblock-mcp` helper `agents_digest()` (Option C); the CLI stays a thin markdown renderer (T3.4.3).** The `unblock agents` managed block grows from a short prose paragraph into a full capabilities table: the descriptor tables for the 7 tools / 5 resources / 3 prompts (from `capabilities()`); each tool's ACTIONS derived STRUCTURALLY from the `oneOf` discriminants of `schema_bundle().<tool>.input` (the per-arm discriminant `const` — NOT the tool description prose, so `create_bulk` is captured); each action's FULL parameter surface — its required AND optional params — derived structurally from the `oneOf` arm (resolving an arm-root `$ref` one level for the delegated payload, e.g. `issue create` → `title` + its optional fields); the error-code → exit-code/retryable map (from `capabilities().error_codes`); and pointers to `unblock://schema` + `unblock://capabilities`. **MECHANISM = Option C:** a NEW pure, typed helper `unblock_mcp::agents_digest() -> AgentsDigest` lives next to `schema_bundle()` in the crate that OWNS the schema shape and returns a CLI-friendly typed digest (tools with their actions + required AND optional params, resources, prompts, error codes) — a DERIVED OFFLINE VIEW over the existing `capabilities()`/`schema_bundle()` bytes. The CLI `commands/agents.rs::managed_block()` becomes a THIN markdown renderer over `AgentsDigest`: NO `serde_json` production dep is added to `unblock-cli`, and the schema-shape knowledge (incl. the `create_bulk` drift risk and the `issue create` arm-root `$ref` resolution) co-locates with the contract owner. **D27/AF-3 STAYS:** `agents` is a SEPARATE CLI command; `init` does NOT touch `AGENTS.md`; the idempotent managed-block merge (markers `<!-- BEGIN unblock -->` / `<!-- END unblock -->`; a re-run replaces ONLY the block) is preserved. **NO `CONTRACT_HASH`/`CONTRACT_VERSION` bump:** `agents_digest()` derives from — and does NOT alter — the two discovery documents, so `contract_suite__schema_bundle.snap` is NOT re-blessed; only Rust source, prose, the `init_agents__agents_managed_block.snap` snapshot, and the `agents.rs` assertions change. Mandatory drift-guard tests `managed_block_lists_every_tool_action` (asserts the `create_bulk` action renders for the `issue` tool, proving the structural `oneOf` walk, not prose) and `managed_block_shows_create_title_param` (asserts the `issue create` row lists `title` as a required param, proving arm-root `$ref` resolution) both hold. | Co-locating schema-shape knowledge with the contract owner (`unblock-mcp` already defines `capabilities()`/`schema_bundle()`) is the no-drift reading: the `oneOf` walk and its `create_bulk` capture live next to the schema they derive from, so a future action added to any tool's input surfaces in the digest automatically. A typed `AgentsDigest` keeps the CLI a thin renderer with NO new `serde_json` production dependency and no untyped JSON walk in L7. Surfacing the FULL required+optional param surface (not just `required[]`) is Miguel's ruling: an agent reading AGENTS.md must know everything supported, not just the minimum. **Rejected — Option A** (walk-the-JSON in the CLI + a `serde_json` prod dep): it would scatter schema-shape knowledge across the `cli → mcp` boundary and add a production dep exactly where the contract owner should own the walk. Because the digest is a pure derived view (no serialized descriptor byte moves), the enrichment ships with NO contract event — faithful to the FR-12 drift-gate rule that only a real `capabilities()`/`schema_bundle()` byte change bumps `CONTRACT_HASH`/`CONTRACT_VERSION`. |
| **D30** | **T3.4 reliability gates (NFR-4/NFR-5/NFR-13) — v1 LITE posture + a forward override-reason seam.** **(1) NFR-13 field standardization:** every `unblock.reliability` emission carries the four fields `operation`/`path`/`result`/`reason`, routed through the `unblock-sync` SSOT emitter trio — `reliability_guard!` (INFO) / `reliability_warn!` (WARN) / `reliability_detail!` (DEBUG) — so a non-conforming emitter cannot exist by construction (the WARN arm is REQUIRED so the live unix-perms warn at `atomic.rs:73` routes through the SSOT without re-leveling, and the non-unix parent-fsync-skip DEBUG at `atomic.rs:97` routes through `reliability_detail!`; re-leveling either would be a forbidden silent simplification). The single tracing-target const `RELIABILITY_TARGET` is hoisted to the L0 crate `unblock-error` (sync imports it, engine re-exports it) so `init_tracing`'s `EnvFilter` directive can never diverge from the emit-target. INFO at **every** guard site — external-path use / force-override (ONE emission: in v1 the ONLY external-path mechanism IS the reason-gated `allow_external`, so the two categories COINCIDE and a single `result="external-path-force-override"` INFO covers both, never double-firing) and conflict-marker rejection — WARN for the best-effort perms guard, DEBUG per-file/per-issue. A `tracing-capture` dev-dep + the named test `tracing_capture_reliability` (in **`unblock-sync`**, where the emissions live) drive the REACHABLE conflict-marker-rejection guard and assert exactly one INFO event on `unblock.reliability` carrying all four field VALUES (the external-path/force-override guard is v1-unreachable, covered STRUCTURALLY by the SSOT + reason-gate unit test); fixes the engine `logging.rs:3-4` doc-vs-code drift and `unblock-sync` `conflict.rs` (emits nothing today). **(2) emergency-override-written-reason = a v1 SEAM only:** the reason gates `allow_external` on the sync-internal `ExportOptions`/`ImportOptions` (an `allow_external` override WITHOUT a written reason is an error at the sync boundary — `SyncError::ExternalOverrideWithoutReason`, mapped to the existing `ErrorCode::PathTraversal`/exit-6, no new code) and emits the NFR-13 force-override/external-path INFO carrying that reason. `allow_external` is UNREACHABLE in v1 (engine forces it `false` at `session/interchange.rs:37,71`; the MCP `sync` tool does not expose it; the CLI has no export/import command) → a FORWARD seam with **no** `CONTRACT_VERSION`/`CONTRACT_HASH` bump and **no** public-spine `ImportOptions` change (the public engine `ImportOptions` stays `dry_run`-only; the reason lives on the sync-internal opts). **(3) fault-injection mechanism:** a feature-gated seam in `unblock-sync/src/atomic.rs` with deterministic per-stage fault points (write, flush, `sync_all`, rename, parent-dir fsync) behind a non-default cargo feature (testkit precedent; process-global fault plan, serial tests) that MUST NOT touch the shipped binary. The per-stage fs seam is **EXPORT-side** (export owns the atomic temp→fsync→rename write); **IMPORT has no atomic fs write path** (it applies via one `Storage::create_issues` transaction), so import failure-injection rides the DB-transaction rollback→zero-rows proof PLUS an out-of-process test (SIGKILL / ENOSPC on a small tmpfs) for the real killed-export/import case. **(4) CI split:** T3.4 owns ONLY the NFR-5 stress + interleaving half at a MODEST corpus, INTEGRITY-only assertions (no corruption / no partial write / linearizable); the NFR-18 single-MCP-server rate-limit assertions and the NFR-1/NFR-2 perf bench-gate/scale + 250k corpus go to T3.5. The four gates T3.4 proves: (a) failure-replay against the LITE doctor posture (`classify_file_state` + `Session::integrity_check()`), (b) export/import failure-injection, (c) long-lived single-workspace stress, (d) interleaved concurrent command-family integrity. | The full policy-bypass with a DB-persisted `bypass_reason` (beads `close_policy.rs` parity) STAYS **v1.1** — already stipulated in `crates/unblock-policy.md` `src/gates.rs` (`PolicyDocument.allow_bypass`, `ClosePolicy.require_close_reason`, `GateOutcome.bypassed`), and no v1 write path can reach `allow_external`, so v1 lands only the reason-gate seam, not the persisted bypass — building the policy gates now would be forbidden scope-creep in the other direction. **Rejected:** porting the beads `emit_tracing` vocabulary (`source`/`phase`/`outcome` on `br::reliability`) — that is the v1.1 CLASSIFICATION surface; PRD's `operation`/`path`/`result`/`reason` on `unblock.reliability` wins (do NOT port beads field names). **Rejected:** folding rate-limit/perf into T3.4 — keeps the reliability gate integrity-only and separable. **Rejected:** the preflight-only `atomic_failure.rs` tests as sufficient for NFR-4 — they reject before any temp is created, so they never exercise the write→flush→sync→rename→fsync stages. **Rejected:** a per-stage fs fault "in import" — import has no atomic fs write, so such a test is vacuous; import failure-injection uses the DB-tx rollback + out-of-process path. |
| **D34** | **T3.5 performance / scale / rate-limit ratifications — the M3 GA perf gate (Miguel, 2026-07-13): a HYBRID perf gate, a storage-direct validated-non-minted 250k corpus, a minted `ErrorCode::RateLimited` (contract `unblock.mcp.v1.3`→`v1.4`), and a record-only round-trip baseline.** **(1) NFR-1 perf-gate SHAPE = HYBRID (F-1).** The per-PR **hard** gate is a set of **generous absolute-ms budgets** asserted on a **pinned ≥2-vCPU runner** (the `storage-testkit` precedent); the **relative-10% regression delta** is DEMOTED to an **advisory / nightly** (`workflow_dispatch`/`#[ignore]`, the `fuzz-smoke.yml` precedent) **report-only** comparison against a committed libsql baseline — it **never fails a PR**. The generous absolute budgets are CALIBRATED + confirmed on the pinned runner at T3.5 Implement (the "(Re-baseline on libsql)" caveat, now folded into the gate; any budget found tight is widened in the same commit, replicated identically across PRD/ci-cd/roadmap — no lint enforces number-equality). The `create <1ms` budget applies to the **storage insert path** (`Storage::create`/`create_issues`), NOT the engine mint: `Session::create_issue` recomputes `issue_count()` per create (O(N) at scale — a known **v1.4 cached-count** limit, not an NFR-1 miss; the scale job exposes it). Mirrors the T0.8 "core-independent hard-assert + informative diagnostic" split. **(2) 250k `scale` corpus = STORAGE-DIRECT VALIDATED NON-MINTED (F-2 — HARD-RULE-sanctioned, not a simplification).** The NFR-2 corpus is seeded via batched `Storage::create_issues` (one `BEGIN IMMEDIATE` tx per ~1k-row chunk; passive checkpoint every 50 mutations) with synthetic ids + a per-issue `IssueValidator::validate` — NOT the engine mint (whose `issue_count()`-per-create is O(N²) at 250k AND is separately tested). Miguel sanctioned this bypass EXPLICITLY under "never simplify": it is FAITHFUL (validated + integrity-checked), not a shortcut. It runs over a **file-backed DB** with the live D31 `.unblock/.write.lock` on the write path, asserting the NFR-1 **read** budgets (list/ready/count) + a clean `integrity_check()` at 250k, as a **per-PR** job with an explicit timeout + an `#[ignore]`-gated soak variant. K-process contention-at-250k stays OUT of this lane (that is NFR-3, proven by T0.8 + the T3.4.1 D31 extension). **(3) rate-limit over-limit = a MINTED `ErrorCode::RateLimited` → contract bump `unblock.mcp.v1.3`→`v1.4` (F-3 — the explicit contract clause).** The over-limit reject surfaces a NEW `ErrorCode::RateLimited` (serialized `RATE_LIMITED`) — chosen over reusing `DatabaseLocked` (which mislabels an in-process cap as a DB lock) or `ValidationFailed` (wrong retry semantics) for honesty under the never-simplify rule. It mirrors its sibling `DatabaseLocked`: **exit 2** (the only 1..8 bucket carrying "resource busy, retry" — a 9th exit code would break the pinned {1..8} coverage), **retryable = true**, **hint_shape = None**, placed at the END of the exit-2 group (the `AlreadyClaimed`-for-FR-2 precedent). Emitted at the L7 MCP handler only (never by any `EngineError`): **in-band** as a `CallToolResult` structured error for a tool call, and as an `ErrorData` (JSON-RPC error) for the resource surface, which has no in-band channel (FR-11). The taxonomy grows **35 → 36** variants; BOTH discovery documents' bytes move (`capabilities().error_codes` gains a descriptor; `schema_bundle()`'s shared `StructuredError`→`ErrorCode` `oneOf` gains a `const`), so **`CONTRACT_HASH` re-pins + `CONTRACT_VERSION` bumps `unblock.mcp.v1.3`→`unblock.mcp.v1.4`**; re-blessed goldens = `contract_suite__capabilities.snap`, `contract_suite__schema_bundle.snap`, `exit_code_table__golden_exit_code_table.snap`, and (a D33 ripple) `init_agents__agents_managed_block.snap`; `contract_suite.rs` + `exit_code_table.rs` counts go 35→36; the engine error golden is UNCHANGED. This is the heaviest, cross-cutting clause — its CODE lands in its own sub-PR (P2). **(4) round-trip latency = RECORD-ONLY (F-4).** The `query{ready} → claim → close` p50/p99 is measured over a REAL spawned `unblock mcp` child (the D31 `.write.lock` world; the `write_lock_race` `[[bin]]` + rmcp stdio-client precedent; the `initialize` handshake is the readiness barrier) as an integration test with timing — NOT a criterion micro-bench (process startup would pollute the sample). PUBLISHED as the v1 baseline; **NO hard gate in v1 — a hard target → v1.1**. **(5) enforcement mechanism (F-5, orchestrator default).** A shared `Arc<Semaphore>(max_concurrent_requests)` field on `UnblockServer` (built ONCE in `new`; the type derives `Clone`, so the field MUST be `Arc`), the permit acquired around the tool-router dispatch AND the `read_resource` path — gating the **tool AND resource** request surface (+ any future tool by construction), `try_acquire` → **reject** in-band (fast-fail, deterministic for the CI assertion). It composes STRICTLY ABOVE the engine write `Semaphore(1)` + `.write.lock` (no deadlock vs D14/D31). Prompts are excluded (pure builders, no `Session`). **(6) one D34 (F-6).** These ratifications land as ONE decision with clause (3) the explicit contract-bump clause; the D-range bumps `D1..D33`→`D1..D34` at the 3 string sites (`CLAUDE.md`, ci-cd §2.1(a), `xtask/src/doc_lint.rs` incl. the tokenizer-comment alternation, load-bearing/same-commit). **(7) gate scope (F-7, orchestrator default).** The EXISTING policy/render criterion benches are WIRED into the SAME hybrid gate mechanism — NOT demoted to informational (which would silently retract a promised NFR-1 gate). This decision also reconciles the NFR-2 SCALE topology framing DOWN to **D14 as amended by D31 (child-per-client)**. | Honours both NFR-1 clauses without shipping a flaky mandatory wall-time gate (the T0.8 core-count lesson: a real hot-spin gave R≈28 on 14 cores but R≈4.42 on a 4-vCPU runner — a generous absolute budget is MORE portable than a tight relative delta). The storage-direct 250k corpus is faithful because the mint is O(N²) AND separately tested — bypassing it validates the same rows without the quadratic build cost (the contention lab + `common/mod.rs` fixtures already seed storage-direct). Minting `RateLimited` is the honest surface for retry-after backpressure; pre-1.0 with no external users, the `v1.3`→`v1.4` bump is a clean additive event the FR-12 drift gate is designed to absorb (the D25 `v1.1`→`v1.2` / CD-1/CD-2 `v1.2`→`v1.3` precedent). Record-only round-trip defers a hard latency target to v1.1 with real baseline data — impl-plan T3.5 already says "record". |

> The locked §4 set was reviewed by three independent lenses; none is a blocking technical flaw. NFR-3 framing
> (CF-3) and FR-9 scope (CF-1) were clarified — see §6 and §8.2 — without reversing any decision.

## 5. Functional Requirements

> **Delivery (D2/D3):** FR-1…FR-23 are surfaced as **MCP tools/resources/prompts** (§9). CLI command names
> are the canonical vocabulary for tool/action naming and for the few lifecycle commands.
> **v1 tier** = the walking-skeleton thin slice (D14/§13). Items marked **[v1.1]** are deferred.
> Acceptance criteria (AC) below are the objective "done" gate for each v1 must-FR.

### Core issue management
> **FR-1 umbrella:** "FR-1" denotes the core CRUD requirement as a whole and is realized by the three
> sub-requirements **FR-1a** (Create), **FR-1b** (Read/Update), and **FR-1c** (Delete). Other docs may cite
> either the umbrella "FR-1" or a specific sub-id; both resolve here.
- **FR-1a [must] — Create.** create (type, priority, labels, parent, deps, due/defer, estimate, slug, **design, acceptance_criteria, assignee, agent_context**, bulk markdown import, `ephemeral`, attribution fields) and quick-create (returns id only). **The id is MINTED by the engine under the D14 write permit (D21):** a root `ub-<hash>` (faithful `bd` adaptive-base36 scheme) or, with a slug, `ub-<slug>-<hash>`; with a `parent`, the hierarchical `parent.N` via storage's `next_child_number`. The minting create path (`Session::create_issue(NewIssue)`) is **distinct** from the id-preserving `Session::create(&Issue)`, which the import/internal path uses (it never mints — preserves caller ids for FR-26 idempotency). **Bulk markdown import (D22)** is the `issue` tool's `create_bulk { markdown }` discriminator (a NEW arm on the existing tool, NOT a new tool — ≤ 8, spine §6.6): the whole document is parsed + validated **all-or-nothing PRE-MUTATION** (a single malformed/unresolvable block rejects the entire batch with one `code`+`hint` and ZERO writes — a deviation from the original's best-effort `continue`, the PRD's safe-import discipline winning, NFR-8), then the validated records are created **TRULY ATOMICALLY** through the NEW engine `Session::create_bulk(Vec<NewIssue>)` over the NEW `Storage::create_issues(&[Issue], actor)` ONE-`BEGIN IMMEDIATE`-tx primitive: the permit is acquired once, every id minted under it (the `get_issue` probe consulting committed state AND the in-batch minted set), the 2-phase intra-file dep/parent resolution (stand-in `### ID` / title → minted id) done in memory, then all N rows + events + edges inserted in one tx — **any failure (mint exhaustion, an out-of-band id/`external_ref` collision, a storage error) ROLLS BACK the whole tx, ZERO issues persisted** (no partial commit, no duplicate-on-re-run hazard — the mint is non-idempotent, so atomicity is mandatory, not just pre-mutation validation). The create-surface fields `design`/`acceptance_criteria`/`assignee`/`agent_context` (which the markdown captures, faithful to `markdown_import.rs::apply_section_to_issue`) are added to `NewIssue`/`CreateInput` so scalar create AND bulk are full-fidelity (`notes`/`owner` are NOT added — the markdown has no such section). *AC: every option round-trips through libsql and a `show` of the new id; quick-create returns only the id; invalid enum/priority yields a structured error with `code` + `hint`; bulk-markdown parse splits into N records created in ONE atomic tx via `Session::create_bulk`/`Storage::create_issues`, a malformed/unresolvable block is rejected pre-mutation (zero writes) with `code` + `hint`, and a mid-batch storage/mint failure rolls back the WHOLE tx (zero issues persisted — fault-injection test); a `create_bulk` exceeding `max_batch` is rejected at the quota preflight before any mint.*
- **FR-1b [must] — Read/Update.** show; update (multi-id; label add/remove/set; reparent). *AC: multi-id update is atomic per id; reparent rejects cycles; `updated_at` advances; no-op update is detectable.*
- **FR-1c [must] — Delete + restore semantics.** tombstone-based delete with cascade / hard / dry-run, plus a dedicated **restore** (un-tombstone, D20). *AC: default delete tombstones (recoverable); `--cascade` tombstones children; `--hard` removes rows; `--dry-run` mutates nothing and reports the plan; tombstones never resurrect **on import**.* **"Recoverable" AC (D20):** a dedicated, **idempotent** `restore` round-trips a SOFT-deleted issue back to active — status best-effort via `closed_at` (`Closed` iff `closed_at` set, else `Open`), `issue_type` untouched + `original_type` cleared, a single `Restored` audit event — and **re-enters the issue into the dependency graph with its surviving edges** (it may be immediately blocked). *Restore of an already-active issue is a no-op `Ok`; restore of a **hard-deleted** target is rejected (`IssueNotFound`). The live `restore` is the sanctioned recovery path and is DISJOINT from the import "tombstones never resurrect" clause (which is import-scoped).*
- **FR-2 [must] — Atomic claim.** A single mutation sets assignee **and** `in_progress` with no race window. *AC: under the contention lab, N agents claiming the same issue yield exactly one winner; losers get a deterministic "already claimed" structured error.*
- **FR-3 [must] — Scheduling / defer.** `defer`/`undefer` move issues out of and back into the ready set until a date. *AC: a deferred issue never appears in `ready` before `defer_until`; `undefer` restores it immediately.*

### Querying
- **FR-4 [must] — Query surface.** `list` (deep filters: status/type/assignee/label AND+OR/priority ranges/text-contains; structured + CSV), `ready` (unblocked + undeferred + hybrid-sorted; the agent entrypoint; **default-complete/unlimited**), `blocked`, `search` (full-text, default cap 50), `count` (group-by), `stale`. *AC: `ready` excludes blocked/deferred/closed; filters compose (on `list`, `count`, `search`, `stale`, **and `blocked`** — D18); `search` cap is overridable; outputs are snapshot-stable (NFR-14).* *(v1 "full-text" = case-insensitive `instr()` substring over title + description + id; ranked FTS5 deferred to v1.4 per spine §3.2.1.)*
- **FR-5 [must] — Dependencies & graph.** Typed edges (`blocks`, `parent-child`, `conditional-blocks`, `waits-for`, `related`, `discovered-from`, `replies-to`, `relates-to`, `duplicates`, `supersedes`, `caused-by`, `Custom`); dep add/remove/list/tree/cycles + graph view. Only `blocks`/`parent-child`/`conditional-blocks`/`waits-for` affect ready-work. Graph traversal/cycle detection via `petgraph`. The cycles view reports either gating-only or all-dependency-type cycles (`blocking_only`, D19). *AC: adding an edge that creates a `blocks` cycle is rejected with the actual ordered cycle path (naming every node in the cycle, e.g. `a -> b -> c -> a`, not endpoints-only); `ready` reflects edge changes immediately (an `add_dep` blocks an issue out of `ready`; a `remove_dep` re-admits it).*
- **FR-6 [v1.1] — Organization.** Labels (add/remove/list/list-all/rename), threaded comments (add/list), epic rollups with auto-close-eligibility.
- **FR-21 [v1.1] — Saved queries.** Named, reusable `list`-style filter sets. *(Demoted; re-evaluate whether agents have a use case post-D2/D3.)*

### Persistence & interchange (model B)
- **FR-7 [must] — Persistence + optional JSONL interchange.** libsql is the source of truth. Provide **optional** `export`/`import` to/from a line-oriented `issues.jsonl` for portability/audit/git-diffable snapshots. Export is atomic (temp in same dir → flush + `sync_all` → atomic rename; on error remove temp, leave original intact). **No** automatic three-way merge, **no** git operations. *AC: export is byte-deterministic for a fixed DB state; a killed export never corrupts the existing file (NFR-4).*
- **FR-8 [must] — Safe import.** Import validates each line, **rejects git conflict markers and malformed JSON before any DB mutation**, and confines the JSONL path (canonicalize; reject symlink escapes / `.git` / `..` / disallowed extensions). Tombstones never resurrected. *AC: a file with a conflict marker is rejected with zero DB writes; a symlink-escaping path is refused at preflight.*
- **FR-26 [must] — One-shot `bd` import (D16/D24).** Best-effort import of any existing `bd`/beads repo's data via `bd-export` → `issues.jsonl` → `import`, mapping bd fields to the unblock domain model. The struct/serde field map is **identity** (unblock's `Issue`/`Dependency`/`Comment`/enums are a byte-faithful port of bd's); the real work is the **bd import-normalize repair pass** (7 faithful-port repairs applied before the `content_hash` recompute — D24/F2) so a rerun's recomputed hash equals bd's stored hash. **bd ids are preserved verbatim** (no remap/reject on the default path — D24/F6; remap is a v1.1 seam). *AC: a representative `bd` repo imports with ids preserved, a reported count of **issues/deps/comments** migrated (via the extended `ImportReport{dependencies,comments}` — D24/F1), and a list of any dropped/unmapped top-level fields (unknown keys — D24/F4); rerunning is idempotent (dedup by `content_hash` = bd's); every repair is exercised by the synthesized byte-faithful fixture via explicit per-repair POST-import field assertions (status `done`→`Closed`; a non-`parent_child` underscore dep→its kebab variant; dup-deps→N−1; `-wisp-`→`ephemeral`; terminal `closed_at:None`→`updated_at`; non-terminal→`None`; `external_ref` blank→`None` AND whitespace-padded→trimmed) — each MUST fail under a single-repair mutation; idempotency-alone (round-trip Skip) does NOT satisfy this (`content_hash` is `#[serde(skip)]`, so a repair skipped-in-both is still idempotent) (non-vacuity — D24/F3).*

### Engine, contract & lifecycle
- **FR-9 [must] — Single shared engine/session.** One implementation of `open → (optional import) → mutate → (optional export) → recover`, consumed by **both** the MCP server and the CLI, so behaviour cannot drift. **Mutation serialization is in-process** (a tokio `Semaphore`), per D14; reads use a fast path. *AC: a property test shows interleaved mutations through the engine are linearizable; MCP and CLI produce identical results for the same operation.*
- **FR-10 [must] — Read fast path.** Read operations bypass the write semaphore. *AC: reads proceed while a write holds the semaphore (WAL readers).* 
- **FR-11 [must] — Agent contract surface.** Always-valid structured output; structured errors carrying `code`/`message`/`hint`/`retryable`; the 0–8 exit-code taxonomy (CLI) and matching MCP error codes; `close --suggest-next` returns newly unblocked issues; `ready` is the canonical discovery query. *AC: a golden snapshot pins every exit code and MCP error code; output is always valid JSON even on error.*
- **FR-12 [must] — Self-describing contracts.** `capabilities` and `schema` emit a versioned (`contract_version`) machine-readable description of tools, payload shapes (tool **inputs AND outputs** — 7 per-tool `{input, output}` schema pairs + the shared in-band `StructuredError` schema, D25), and error/exit codes (the error map carries `code → exit_code/retryable/hint_shape` — D25). **The "bumping the contract surface bumps `contract_version`" guarantee is machine-enforced by a hash-coupled drift gate (D22, widened by D25):** a content-hash of BOTH discovery documents (`CONTRACT_HASH` — over `capabilities()` incl. descriptors + the error-code map, AND `schema_bundle()` incl. the per-tool input/output schemas + the shared error schema) is pinned in-source next to `CONTRACT_VERSION`, and the contract test fails if either document changes without a `CONTRACT_VERSION` bump (a schema, capabilities-document descriptor, or error-map edit FORCES a version bump, not a silent golden re-bless; a bump without a document change is equally caught). Prompt rendered-message content stays golden-only, never version-coupled (prompt NAMES are version-coupled — live-vs-builder parity + hash; description strings only via their capabilities-document copies — D25). *AC: bumping a tool's input or output schema bumps `contract_version`; a capabilities-document change (tool/resource/prompt descriptors or the error-code map) bumps `contract_version`; a client can detect drift by reading either discovery resource and comparing `contract_version`; the contract test fails if either discovery document changes without a `contract_version` bump.*
- **FR-13 [must (subset) / v1.1 (full)] — Layered configuration.** v1 ships **CLI > env (`UNBLOCK_*`) > project `.unblock/config.toml` > defaults**; the DB config table and user-config layers are **[v1.1]**. Startup-vs-runtime key partitioning. **CLI half (D27/AD-3, T3.1):** the CLI binds `--dir`→`UNBLOCK_DIR` and `--actor`→`UNBLOCK_ACTOR` via clap `env` (the SINGLE FR-13 wiring point into `CliOverrides`, so `--flag > UNBLOCK_*` comes free); `unblock-config` owns ALL layering for the four workspace commands (the CLI is a pure `CliOverrides` forwarder — it does NOT re-implement precedence) and owns the strict parse of `UNBLOCK_OUTPUT_FORMAT`. **No-workspace format path (SF-1):** the no-workspace commands (`version`, top-level parse errors) never open config, so the CLI's own resolver `output::pick_cli_format` consults `--output > UNBLOCK_OUTPUT_FORMAT (parsed via `unblock_render::parse_env_value` / `FormatName::from_str`) > Json default` — so FR-13 precedence holds UNIFORMLY (`unblock version` with `UNBLOCK_OUTPUT_FORMAT=robot` and no `--output` renders robot, NOT Json). Config still owns the single strict parse for workspace commands; the CLI merely REUSES render's lenient env parser for the one path config cannot cover (no double-parse for workspace commands). *AC: precedence is unit-tested across all v1 layers, including that `UNBLOCK_OUTPUT_FORMAT` reaches the no-workspace `version` path (env > default when no `--output`).*
- **FR-14 [must] — Workspace bootstrap.** `init [--prefix] [--force]` creates `.unblock/`; `agents` injects/maintains `AGENTS.md`. **Scaffold (D27/AF-3, T3.1):** `init` writes exactly `.unblock/config.toml` (hand-written TOML seeded with the `normalize_prefix`-normalized `id_prefix`) + a migrated empty `unblock.db` opened through the config facade (FR-9 no-drift) — NO `.gitignore`, NO `metadata.json`, NO seeded `issues.jsonl` (D13/NFR-6/model-B). `agents` is a SEPARATE command (a pure file op) that merges an idempotent managed AGENTS.md block describing the MCP wiring. *AC: `init` is idempotent; refuses to clobber an existing `.unblock/` already carrying `config.toml` or `unblock.db` without `--force` (→ `AlreadyInitialized`, exit 2).*

### Health, reliability & shutdown
- **FR-16 [must (lite) / v1.1 (full taxonomy)] — Workspace health.** v1: `doctor` + libsql `integrity_check` + basic diagnostics. **v1 doctor-lite (D27/AF-1, T3.1):** `unblock doctor` composes the BUILD-now `Session::diagnostics(kind)` reads (`Stats` + `Lint` + `Info`) PLUS `PRAGMA integrity_check` (surfaced through the NEW pure engine read `Session::integrity_check()` — the ONE corruption signal reachable at T3.1; `Session::doctor()` stays the `FeatureNotWired{"health"}` seam until T3.3). The report REUSES `DiagnosticKind::Info` (no spine §1.10 change). **Non-zero exit only on detected corruption:** a non-empty `integrity_check` → `ErrorCode::DatabaseError` (exit 2); Lint/orphan findings are advisory (no exit flip); otherwise exit 0. **[v1.1]:** the full Healthy/Drifted/Recoverable/Unsafe taxonomy with composite severity, redefined for a libsql-authoritative world (Recoverable → libsql integrity/WAL recovery, not JSONL; Drifted meaningful only when JSONL export is enabled), + `--repair`. Recovery preserves evidence under `.unblock/.recovery/`. These land ADDITIVELY over the `Session::doctor()`/`recover()` seam at **v1.1** (D29). **T3.3 (v1, HEALTH-LITE) wires only `doctor()`** with the lite body (integrity `integrity_check` + pure file-state classification via `unblock-health`'s `run_doctor`), mapping its `DoctorReport` onto a `DiagnosticReport` that REUSES `DiagnosticKind::Info` (no spine §1.10 change); the cli `doctor` command ROUTES through the wired `doctor()`; `recover()` stays `FeatureNotWired{"health"}` (its `--repair`/evidence body is v1.1).
- **FR-17 [must] — Cooperative shutdown.** Translate SIGINT/SIGTERM/SIGHUP into an atomic shutdown flag so the MCP-server process unwinds cleanly and flushes/closes libsql; a second signal escalates to async-signal-safe exit; Windows no-op. *AC: SIGTERM mid-write commits-or-rolls-back cleanly and closes the DB with no WAL corruption (failure-injection test).*

### Coordination, gates, audit
- **FR-18 [v1.1] — Swarm coordination diagnostics.** `scheduler` ranks ready work with explainable deterministic evidence (`unblock.scheduler.v1`); `coordination status` (`unblock.coordination.v1`) read-only diagnoses hidden/stale `in_progress` claims. Pure versioned contracts. **Coordination is purely DB-state-derived; the upstream "Agent Mail" dependency is dropped** (§12).
- **FR-19 [v1.1] — Workflow gates.** Policy-driven (`.unblock/policy.toml`) transition gates (CI/reviewers report pass/fail; transition blocked until required gates pass). Project-local, not exported.
- **FR-22 [v1.1] — Audit / flight recorder.** Append-only `interactions.jsonl` with capture-only Tier-1 attribution (`agent_name`/`harness`/`model`), never enforced.

### MCP surface (primary) & diagnostics
- **FR-20 [must] — MCP stdio server (PRIMARY).** `unblock mcp` exposes **tools** (issue lifecycle, claim, defer, query, dependencies, sync export/import; v1.1 adds labels/comments, scheduler, coordination, gates), **resources** (`unblock://issues/ready`, `unblock://issues/{id}`, `unblock://capabilities`, `unblock://schema`; v1.1 adds coordination/status), and **prompts** (triage, plan_next_work). Built on **rmcp** in the isolated `unblock-mcp` crate. *AC: an MCP client can complete ready → claim → close end to end; tool input is schema-validated (`schemars`) and rejects oversized/invalid args.*
- **FR-15 [must] — Diagnostics & info (pure-DB).** stats/status, info, where, version, lint; **changelog and orphans derive purely from DB state** — `changelog` from closed-issue metadata over an optional `since` window, `orphans` from issues whose `external_ref` matches a commit pattern. **No git is read or linked** (resolves the NFR-6 contradiction). v1 ships the FAITHFUL taxonomy (D26): `stats` = bd's rich `StatsSummary` (per-status counts, blocked/ready, pinned, tombstone-excluded total, epics-eligible-for-closure, average lead time) **minus** any git-activity block (NFR-6) — `total` ALREADY excludes tombstones today (no change there); T2.7 ADDS tombstone VISIBILITY (a distinct `tombstone` finding) via `count_issues(include_tombstone:true)`, from which the `tombstone` bucket is surfaced and `total` is the non-tombstone sum; `lint` = bd's template-section lint (missing recommended `## …` sections per issue type — Bug/Task/Feature/Epic); `changelog` = closed **non-template** issues filtered by an optional `since` (the engine `changelog()` composition filters out `is_template` rows after `closed_since(since)`, faithful to bd's `list_changelog_issues` template exclusion, `sqlite.rs:4014`; `closed_since` stays shared/unchanged); `orphans` = **status-agnostic** `external_ref`-matches-commit-pattern. All kinds surface as generic `DiagnosticFinding{label,detail}` rows, so the taxonomy enrichment stays within the existing mcp schema and does **NOT** bump `CONTRACT_VERSION` (v1.2 unchanged, D26). Emission order is PINNED for NFR-14 insta stability: stats findings in `open, in_progress, blocked, closed, ready, deferred, draft, tombstone, pinned, epics_eligible, [avg_lead_time_hours], total` (`avg_lead_time_hours` ABSENT when `None`); lint findings outer = issue id ASC, inner = missing sections in the fixed declaration order (Bug: `## Steps to Reproduce` THEN `## Acceptance Criteria`); the epic rollup is `ORDER BY` epic id in SQL (not HashMap-iterated). *AC: the NFR-6 static gate passes with FR-15 present; `changelog{since}` returns only non-template issues closed at/after `since` (and the full non-template set when omitted); `lint` flags an issue missing a required template section for its type and is silent when present (case-insensitive); `stats` reports the bd-faithful counters in the pinned order, `total` excludes tombstones while a distinct `tombstone` finding carries the count, and the git-activity block is absent; `orphans` matches every `external_ref` with a commit-hash shape regardless of status; each pure-DB kind is proven non-vacuous over a non-empty corpus, and no `Command::new("git")`/git crate links.*
- **FR-23 [v1.1] — Shell completions** (static, bash/zsh/fish/powershell/elvish).

### Distribution & self-update (D17)
- **FR-25 [must] — Self-update via `dist`/`axoupdater` (v1).** `unblock update` (a lifecycle command, D3) embeds `axoupdater`; updates are verified against GitHub artifact attestations before execution (NFR-17); never invoked on normal command paths. Distribution: shell + powershell installers across 6 target triples. The command lands in `unblock-cli` behind the `self-update` Cargo feature (the feature name enables the `unblock update` command; default-on, dropped under `--no-default-features` per CF-K). *(See `docs/plans/ci-cd-and-distribution.md`.)*

### Dropped (D11)
- **FR-24 [wont] — Cross-project town/mayor routing.** Single-workspace discovery only.

## 6. Non-Functional Requirements

- **NFR-1 [performance]** Storage/engine perf budgets with a **HYBRID `criterion` gate (D34)** — two tiers:
  **(i) reference / advisory baseline** = the per-op targets create <1ms; list 1k <10ms / 10k <100ms; ready 1k
  <5ms / 10k <50ms; export 10k <500ms; import 10k <1s — the **relative-10% regression delta** compares against
  this baseline in an **advisory/nightly** posture (`workflow_dispatch`/`#[ignore]`, report-only, never fails a
  PR); **(ii) hard per-PR gate** = a **generous absolute-ms ceiling** (a generous multiple of the baseline —
  T3.5.1-CALIBRATED reads: create ≤15ms; list 1k ≤50ms / 10k ≤500ms; ready 1k ≤50ms / 10k ≤500ms; export 10k ≤2500ms;
  import 10k ≤5000ms; count/search have no hard ceiling in v1) asserted on a **pinned ≥2-vCPU runner**, calibrated +
  confirmed against the first real libsql `criterion` run at T3.5 Implement (the "(Re-baseline on libsql)" step, now
  folded into the gate). **The read (list/ready) ceilings were widened at T3.5-P1 (to list/ready ≤100/≤1000) because
  the then per-row-hydrated read path (`collect_hydrated`, N+1×3 queries per result set) put `ready`/`list` above the
  original 5× ceilings (ready 1k ≈29ms, ready 10k ≈284ms) — a known read-path cost — and were RE-TIGHTENED at T3.5.1
  (list/ready 1k ≤50 / 10k ≤500) once **batch hydration** (T3.5.1, minted from this finding, Miguel 2026-07-14)
  replaced the N+1 read path, dropping the fixed read cost to ~5ms (1k) / ~56ms (10k) — a PRE-GA fix, NOT a v1.4
  deferral. The tier-i advisory relative-10% drift-catcher **LANDED at T3.5.1** (a committed `xtask/bench-baseline.json`
  captured from the fixed read path + the `cargo xtask bench-compare` report-only comparator + the nightly
  `fuzz-smoke` `perf-advisory` leg — D34/F-1), re-baselined against the fixed read cost; the earlier ~3.5×
  read-regression blind window (ready 10k ≈284ms vs the old ≤1000ms ceiling) is therefore closed PRE-GA — the
  re-tightened ≤500ms ceiling plus the advisory delta now bound read drift.** Any ceiling found
  tight is widened in the same commit, replicated identically across the number-equality sites (SF-2). The generous
  ceiling trips only on a gross regression (an O(N)→O(N²) blowup or a missing index) — the T0.8 core-count lesson (a
  real hot-spin gave R≈28 on 14 cores but R≈4.42 on a 4-vCPU runner: a generous absolute budget is MORE portable than
  a tight relative delta). The create budget applies to the **storage insert path**
  (`Storage::create`/`create_issues`); the engine-mint `Session::create_issue` recomputes `issue_count()` per create
  (O(N) at scale — a known v1.4 cached-count limit, not an NFR-1 miss).
- **NFR-2 [performance]** Swarm scale **under the child-per-client topology (D14 as amended by D31)** —
  multiple `unblock mcp` servers per workspace is the SUPPORTED case, cross-process writes serialized by the
  `.unblock/.write.lock` advisory lock (D31); cross-machine/multi-dev sharing is the v1.2 libsql-remote path
  (writes serialized at the primary), with a read-only fast path. **v1 commits to 250k issues validated in
  CI** (ci-cd `scale` job, storage/engine; owner impl-plan T3.5): the corpus is seeded **storage-direct,
  validated but non-minted** (batched `Storage::create_issues` ~1k/tx + per-issue `IssueValidator::validate`,
  NOT the O(N²) engine mint — D34) over a **file-backed DB with the live `.write.lock`** on the write path,
  asserting the NFR-1 **read** budgets (list/ready/count) + a clean `integrity_check()` at 250k. The
  **1M-issue / 10k-agent corpus is a v1.4 CI gate** (not a v1 acceptance gate).
- **NFR-3 [reliability/perf]** **The primary non-spin guarantee is libsql/SQLite WAL + the native `busy_timeout` (>0) handler + the in-process serialized writer — this resolves the fsqlite-243 hot-spin by construction.** App-level jittered backoff is a **secondary** fallback for the remote/replica path only; the chosen retry crate MUST pass `cargo-deny` (do **not** use the archived `backoff 0.4` — use `backon` or `tokio-retry`). A contention-replay lab proves no 100% CPU hot-spin. *(Validate libsql busy/lock semantics EARLY — see Risk Register.)*
- **NFR-4 [reliability]** Atomic JSONL export (temp + `sync_all` + atomic rename; remove temp, leave original intact on error), verified by failure-injection e2e tests: a feature-gated per-stage fault-injection seam (write / flush / `sync_all` / rename / parent-dir fsync) on the EXPORT atomic-write path, plus an out-of-process killed-write case (SIGKILL / ENOSPC on a small tmpfs) for both directions — the seam is behind a non-default cargo feature and never links into the shipped binary; IMPORT (no atomic fs write — it applies via one `Storage::create_issues` transaction) is failure-injected via the DB-transaction rollback→zero-rows proof (D30, T3.4).
- **NFR-5 [reliability]** Release reliability gates must pass (T3.4, INTEGRITY-only at a modest corpus — no corruption / no partial write / linearizable): failure-replay against the LITE doctor posture (`classify_file_state` + `Session::integrity_check()`, NOT the v1.1 audit-record/full-taxonomy posture); e2e export/import failure-injection (the NFR-4 seam export-side; import via DB-transaction rollback); long-lived single-workspace stress; interleaved concurrent command-family integrity (create/update/close vs export/import vs read). **Emergency override requires a written reason (D30):** in v1 this is a FORWARD seam — the reason gates `allow_external` at the sync boundary (an override without a written reason is an error) and rides the NFR-13 force-override/external-path INFO; `allow_external` is unreachable on every v1 write path, so the full policy-bypass with a DB-persisted `bypass_reason` is v1.1 (`crates/unblock-policy.md`). The NFR-18 single-MCP-server rate-limit half and the NFR-1/NFR-2 perf/scale gates are owned by T3.5.
- **NFR-6 [security]** unblock runs **zero git operations** and links **no git library**, enforced by a static gate (no `Command::new("git")`, no git crate). FR-15 is pure-DB and compatible with this gate.
- **NFR-7 [security]** JSONL writes confined to `.unblock/` by default; external paths require explicit opt-in with canonicalization and rejection of symlink escapes / `.git` / `..` / disallowed extensions; preflight before opening/parsing.
- **NFR-8 [security]** Import rejects git conflict markers and malformed JSON before any DB mutation; tombstones never resurrected; force flags mutually exclusive and never bypass syntax/conflict-marker validation. **The no-resurrection guarantee is import-path-scoped** — the live, audited `restore` op (D20) is the sanctioned exception (it un-tombstones a soft-deleted issue through the engine, not through import).
- **NFR-9 [supply-chain]** **`forbid(unsafe_code)`** in every crate (stronger than `deny`; update the scaffold); clippy pedantic; commit `Cargo.lock`; `cargo-audit`/`cargo-deny` in CI; pin every GitHub Action to a 40-char SHA (incl. the `dist`-generated release workflow). Release/distribution via `dist` (D17, `docs/plans/ci-cd-and-distribution.md`).
- **NFR-10 [supply-chain]** Minimize transitive surface: prefer mainstream multi-maintainer crates; eliminate the 15-crate fsqlite stack; keep network/TLS (`reqwest` is **transitive-only** behind libsql's `remote` feature + the cli `self-update`/`axoupdater` surface — never a direct dep; libsql remote) **behind non-default features** so it never appears on the normal path.
- **NFR-11 [portability]** Single self-contained `unblock` binary, no runtime system deps on Linux/macOS/Windows; path normalization; cfg-guarded unix signal handling.
- **NFR-12 [portability]** Build on **stable Rust** (`1.96.0`); nightly only for an explicit, documented reason (none currently). **Gate:** a CI job pins `rust-toolchain` to `1.96.0` stable and builds `--locked`; a green stable build is the NFR-12 acceptance gate (see ci-cd §2).
- **NFR-13 [observability]** Structured `tracing` on an `unblock.reliability` target: **every** reliability emission carries the four fields `operation`/`path`/`result`/`reason` (standardized — D30), routed through the `unblock-sync` SSOT emitter macros `reliability_guard!` (INFO) / `reliability_warn!` (WARN) / `reliability_detail!` (DEBUG) so a non-conforming emitter cannot exist (the WARN arm is required so the best-effort unix-perms guard also carries the four fields — no emitter re-leveled). INFO at every guard site — external-path use / force-override (ONE event: the two categories coincide in v1) and conflict-marker rejection — WARN for the best-effort perms guard, and DEBUG for per-file/per-issue events. The guard emissions live in `unblock-sync` (L3, `src/reliability.rs`); the engine `logging.rs` owns only subscriber init (the tracing-target const `RELIABILITY_TARGET` is hoisted to the L0 crate `unblock-error` so sync/engine/cli share one). Named test `tracing_capture_reliability` (in `unblock-sync`, via a `tracing-capture` capture layer) drives the REACHABLE conflict-marker-rejection guard and asserts exactly one INFO event on `unblock.reliability` carrying all four field values (T3.4).
- **NFR-14 [observability]** Clean structured output strictly on stdout; diagnostics strictly on stderr; snapshot-guarded stable output shapes (`insta`).
- **NFR-15 [architecture]** Enforced **acyclic** crate layering: pure leaf domain/policy crates depend only on model+error; storage hides its backend behind a trait with a backend-agnostic error; **no crate reaches into another's internals** (`unblock-storage` depends only on model+error — see §8.1).
- **NFR-16 [testability]** Per-crate unit tests; `proptest` over lifecycle/content-hash/import round-trip; `insta` snapshots with a CI check gate; `cargo-fuzz` over the ingestion surface; a `Storage`-trait **contract suite** validating each backend independently; `wiremock` for any remote/network path.
- **NFR-17 [reliability/security]** Distribution/self-update via `dist`/`axoupdater` (v1): artifacts carry **GitHub artifact attestations** and are verified before execution; **no network calls on any normal command path** — only on explicit `unblock update` (offline-first; libsql remote behind a non-default feature, D15). See `docs/plans/ci-cd-and-distribution.md`.
- **NFR-18 [security — threat model]** The MCP tool surface is an untrusted-input boundary: all tool args are `schemars`-validated with **size limits** (per-request bytes/array/string/batch caps, rejected pre-mutation), and the **request rate is bounded by a shared `Arc<Semaphore>(max_concurrent_requests)` chokepoint at the MCP handler gating the tool AND resource request surface** (`try_acquire` → `ErrorCode::RateLimited`, retryable — **in-band `CallToolResult` for tools, an `ErrorData` for resources** — D34); a malicious/buggy agent is bounded to its own workspace's data (no path escape, no host command execution); libsql remote credentials (when used) are **never** stored in `config.toml` — only via `UNBLOCK_*` env or OS keychain. Tie safe drift to `contract_version` (FR-12).

## 7. Domain Model

| Entity | Key fields | Notes |
|---|---|---|
| **Issue** | id (prefix + optional slug + hash), content_hash (canonical dedup, not serialized), title, description, design, acceptance_criteria, notes, status, priority (0–4), issue_type, assignee, owner, estimated_minutes, created_at/by, updated_at, closed_at, close_reason, closed_by_session, due_at, defer_until, external_ref, source_system/repo/repo_path, agent_context, tombstone fields, compaction fields, sender, ephemeral, pinned, is_template; relations: labels, dependencies, comments | Core work item. `content_hash` + `sync_equals` drive dedup/equality. Compaction fields **kept for JSONL round-trip fidelity** (D12), not Go-bd conformance. |
| **Status** | open, in_progress, blocked, deferred, draft, closed, tombstone, pinned, `Custom(String)` | Open enum; only some dep types gate ready-work. |
| **Priority** | newtype i32 in 0..=4 (0=Critical … 4=Backlog); parses `P0`/`0` | Surfaced in hybrid ready sorting. |
| **IssueType** | task, bug, feature, epic, chore, docs, question, `Custom(String)` | Open enum; epic participates in rollups. |
| **Dependency** | issue_id → depends_on_id, dep_type, created_at/by, metadata (JSON), thread_id | `discovered-from` is central to the agent flywheel. |
| **Comment** | id, issue_id, author, body, created_at | Threaded. [v1.1] |
| **Event** | id, issue_id, event_type, actor, old_value, new_value, comment, created_at; Tier-1 attribution (agent_name/harness/model) | Append-only audit; attribution capture-only; written transactionally inside mutate(). |
| **EpicStatus** | epic id, total_children, closed_children, eligible_for_close | Derived rollup. [v1.1] |
| **WorkspaceHealth / AnomalyClass** | classification, anomaly code+severity, composite severity = max | Full taxonomy [v1.1]; v1 ships libsql integrity + doctor. |
| **GateResult / PolicyDocument** | gate name, provider, status, required gates per transition | Project-local (`.unblock/policy.toml`), not exported. [v1.1] |
| **On-disk artifacts** (`.unblock/`) | `unblock.db` (libsql, source of truth), `config.toml`, `.write.lock` (D31 — the cross-process advisory write lock; a pure `File::try_lock` target, NO content written, sibling of `unblock.db`, kernel-released so it never orphans), `policy.toml` [v1.1], `interactions.jsonl` [v1.1], `issues.jsonl` (optional export) | libsql is authoritative; `issues.jsonl` is an optional snapshot (D5). `.write.lock` is DISTINCT from the vestigial `.unblock.lock` (the never-written `OrphanedLockFile` detector target, D8). **No separate `metadata.json`** — startup-path keys (db/jsonl filenames, retention, backend) fold into `config.toml` (Q2, 2026-06-26; names §12.5). |

## 8. Architecture

### 8.1 Crate decomposition (acyclic, bottom-up)

| Crate | Layer | Responsibility | Depends on |
|---|---|---|---|
| `unblock-model` | L0 | Pure domain types; content-hash / sync-equality / tombstone logic; validation; shared contract types (e.g. cache-key). No I/O. | error (single sanctioned L0 edge — CF-G: `FromStr::Err` / `IssueValidator` return `unblock_error::ModelError`; `error` has no in-workspace deps so L0 stays acyclic) |
| `unblock-error` | L0 | **snafu** error taxonomy; structured error payloads; 0–8 exit-code table. Backend-agnostic. | — |
| `unblock-policy` | L1 | Pure versioned decision contracts: scheduler, coordination, gates/close-policy, inheritance, cache. Side-effect-free. | model, error |
| `unblock-storage` | L2 | `Storage` trait + **libsql** implementation (schema/migrations, queries, transactions, WAL + `busy_timeout`). Only crate aware of the backend. **Depends on model+error only** (CF-11 fix). | model, error |
| `unblock-sync` | L3 | **Light** JSONL export/import + atomic write + path-confinement + conflict-marker scan. *(Shrunk per D5.)* | storage, model, error |
| `unblock-health` | L3 | v1: libsql integrity + diagnostics. [v1.1]: full Workspace Health Contract. | model, error, sync |
| `unblock-config` | L4 | Layered TOML config resolution, `.unblock/` discovery, open-a-workspace facade. | storage, sync, health, model, error |
| `unblock-engine` | L5 | Shared session API (open → import? → mutate → export? → recover); **in-process write Semaphore (D14)**; shutdown/logging. Composes storage **+ policy**. Embeddable surface for MCP/CLI. | config, sync, storage, policy, health, model, error |
| `unblock-render` | L6 | Output/format (json/robot/plain/csv/markdown; **TOON feature-gated, v1.1**) behind a trait. Reduced under D7. | model, error |
| `unblock-mcp` | L7 | **Primary** rmcp stdio server (tools/resources/prompts) over the engine. Feature-isolated. | engine, render, policy, model, error |
| `unblock-cli` | L7 | Reduced binary: lifecycle/ops (mcp/migrate/doctor/version) + thin routing; owns cooperative-shutdown signal install (FR-17, OQ-4). | engine, render, policy, mcp, error |
| `unblock-fuzz` | — | Unpublished member; `cargo-fuzz` targets over model/sync/storage. | model, sync, storage, error |

> CF-11 fix: `unblock-storage` no longer depends on `unblock-policy`; the engine (L5) composes storage + policy.
> Any shared contract type policy/storage both need lives in `unblock-model`.
>
> **"Depends on" convention:** L0 crates (`model`, `error`) are listed **explicitly** on every row that depends on
> them directly (not left transitive), so each row is a complete dependency declaration. The only sanctioned
> intra-L0 edge is `model → error` (CF-G).

### 8.2 Concurrency, async & data flow

- **Async everywhere** (tokio). The `Storage` trait is async (`async-trait`); libsql is an async client.
- **Supported v1 topology (D14 + D31):** each MCP client spawns its **own** `unblock mcp` (stdio = one server per
  client), so **multiple MCP servers per workspace is the NORMAL, SUPPORTED case** (D31 retires the D14 "one MCP-server
  process / multiple MCP servers unsupported" clause). v1 is **local single-workspace**: for one client, the engine
  serializes writes with an **in-process tokio `Semaphore`** (which now **composes above** the L2 advisory lock,
  D31) and reads use a fast path (WAL readers). **Cross-process** write serialization across the multiple MCP-server
  processes on one `unblock.db` (two editor windows, or a CLI `migrate`/`doctor` while the MCP server runs) is the RESTORED
  **`.unblock/.write.lock` advisory lock** (D31, an L2 storage primitive acquired at the whole-mutation scope),
  with `BEGIN IMMEDIATE` + native `busy_timeout` as the WAL-level backstop; `migrate` takes the
  exclusive lock (timeout=0 fail-fast) for the whole command, UNCONDITIONALLY (before the version check), the read-only `doctor` takes no lock.
- **Multi-dev / cross-machine sharing is v1.2, not v1 (D15):** there, each dev keeps their own local `unblock
  mcp` pointed at a **shared libsql/Turso primary** (embedded replica: local reads, **writes delegated to the
  primary** → atomic ops like `claim` serialize there; reads are eventually-consistent per sync interval). That is
  how the swarm/multi-dev persona scales — **not** thousands of clients on one local MCP server. See `00-roadmap.md` §3.
- **Non-spin guarantee (NFR-3):** WAL + native `busy_timeout` (>0) + the in-process serialized writer + the
  **cross-process advisory `.unblock/.write.lock`** (D31) — no app-level spin: the native `busy_timeout` sleeps AND
  the `.write.lock` poll sleeps (`tokio::time::sleep`, 25 ms; never `File::lock()` blocking, never a busy-spin);
  defect-243 cannot recur. `failsafe`/retry only guard the optional remote/replica path.
- **Write flow (model B):** acquire engine write permit → mutate libsql (issue rows + transactional audit
  events) → optionally export to `issues.jsonl` (atomic temp+fsync+rename) if export is enabled. No git, no merge.
- **Read flow:** fast-path query against libsql (in-memory graph traversal via `petgraph` where needed) → render.
- **Shutdown (FR-17):** a cooperative flag flips on signal; the engine flushes/closes libsql cleanly.

## 9. MCP Surface Design (primary interface)

Designed deliberately rather than one-tool-per-CLI-command (to keep the client's tool list small — token cost
+ selection accuracy):

- **Tools** — actions, consolidated where natural (e.g. an `issue` tool with an `action` enum for
  create/update/close/reopen/delete; dedicated `claim`, `ready`, `dep`, `search`, `export`/`import`). Schemas via `schemars`.
- **Resources** — queryable state: `unblock://issues/{id}`, `unblock://issues/ready`, `unblock://issues/blocked`, `unblock://capabilities`, `unblock://schema`.
- **Prompts** — guided workflows: `triage`, `plan_next_work`, `close_with_suggestions`.
- **Errors** — domain errors map to MCP error data carrying `code`/`message`/`hint`/`retryable`, parallel to the CLI 0–8 exit codes.
- **Discovery** — `capabilities`/`schema` versioned by `contract_version` and pinned by the two-document `CONTRACT_HASH` drift gate (D22/D25).
- **Target:** keep v1 tool count small (favour consolidated tools + resources over a per-command explosion); record the count as a success metric (§14).

## 10. Technology Stack

Confirmed against the workspace scaffold (`Cargo.toml`):

- **Runtime/async:** `tokio` (full). **Storage:** **libsql** *(in `workspace.dependencies` as `default-features = false, features = ["core"]` — local; SQLite is statically bundled by `core` (there is **no** separate `bundled` Cargo feature); remote/replica behind the non-default `remote` feature — D15)* behind `async-trait` `Storage`. **Graph:** `petgraph`. **MCP:** `rmcp 1.7` (`server`, `transport-io`). **Schemas:** `schemars`. **Errors:** `snafu`. **Time:** `chrono`. **Serialization:** `serde`/`serde_json`; **config:** TOML. **Resilience (remote path only):** `failsafe` + a maintained retry crate (`backon`/`tokio-retry`, **not** archived `backoff 0.4`). **HTTP (non-default, transitive-only):** `reqwest` — pulled **only** via libsql's `remote` feature and the cli `self-update`/`axoupdater` surface; **not** a direct workspace dependency; **mocked by** `wiremock`. **Logging:** `tracing`(+subscriber). **CLI (lifecycle):** a lightweight `clap` (stable features only) *(add)*. **Testing:** `proptest`, `criterion` (async_tokio), `insta`, `cargo-fuzz`. **Toolchain:** stable `1.96.0`; lints **`unsafe_code = forbid`**, `missing_docs = warn`, clippy pedantic.

**Scaffold (done at T0.1+T0.2):** added `libsql` (`default-features = false, features = ["core"]`), `clap` (`derive`/`env`), `axoupdater` (cli `self-update`), `backon` (remote retry); swapped archived `backoff` → `backon`; **dropped direct `reqwest`** (transitive-only); `unsafe_code` `deny` → `forbid`; `default-members` excludes `unblock-fuzz`; `rmcp` pinned to `1.7` via the committed `Cargo.lock`. *(This list is the dependency-stack SSOT alongside the per-crate plans — there is no separate spine dependency section.)*

**Removed vs original:** fsqlite (15 crates) → libsql; fastmcp-rust → rmcp; rich_rust/crossterm/indicatif → dropped (D7); serde_yml → TOML; anyhow/thiserror → snafu; clap unstable/nightly → stable; **`self_update` → `dist`/`axoupdater`** (D17). **Release tooling:** `dist` (cargo-dist) for the CI release pipeline + installers + attestations.

**Crate publishing:** the `unblock-*` library crates are **workspace-internal — not published to crates.io**; only the `unblock` binary is distributed (via `dist`).

## 11. Out of Scope (v1)

- Town/mayor cross-project routing (D11) — `wont`.
- A hosted/networked multi-user service (collaboration via libsql sync or optional JSONL snapshot, not a bespoke server).
- Automatic git operations, hook installation, git-history reading, or any self-installed daemon (D13/NFR-6).
- Backward compatibility with classic Go-bd's Dolt architecture or with existing on-disk DBs. *(Note: a one-shot best-effort `bd` data import IS in scope — FR-26/D16 — distinct from maintaining compatibility.)*
- v1.1-deferred features: FR-6, FR-13(full), FR-16(full taxonomy), FR-18, FR-19, FR-21, FR-22, FR-23, TOON. *(FR-25 self-update moved to v1 via `dist`/`axoupdater` — D17.)*

## 12. Resolved Items

### 12.1 Agent Mail — DROPPED
It belonged to the upstream beads author's ecosystem. unblock has no "Agent Mail" dependency; swarm coordination (FR-18) is **purely DB-state-derived**.

### 12.2 MCP tool taxonomy — DEFINED
The concrete v1 consolidated tool/resource/prompt set and tool-count target are specified in the implementation plan (`docs/plans/implementation-plan.md`, MCP surface section). **Taxonomy conformance is proven at T2.3** by the live `list_tools`/`list_resources`/`list_prompts` golden (7 tools ≤ 8, 5 resources, 3 prompts) + builder-vs-router parity — closing this item. The `create_bulk` discriminator (D22) lands as a NEW arm on the `issue` tool, so the tool count stays 7 (spine §6.6 "extend before add").

### 12.3 Self-update (FR-25) — LANDS IN v1
Self-update **lands in v1 via `axoupdater`** (D17/CF-K); signing = **GitHub artifact attestations** verified before execution (NFR-17); **no isolated crate** — the command lives in `unblock-cli`. The earlier minisign-vs-embedded-key question is closed (attestations chosen). Command name is canonical `unblock update` (see §12.6).

### 12.4 Health taxonomy (FR-16) — DEFERRED to v1.1
v1 ships `doctor` + libsql `integrity_check`; the Recoverable/Drifted/Unsafe redefinition for a libsql-authoritative world is a v1.1 design item.

### 12.5 Names — LOCKED
Config dir `.unblock/` (monorepo alias `_unblock/` also accepted on discovery — D8, FORK-2/2026-06-26); DB `unblock.db`; optional export `issues.jsonl`; config `config.toml`; v1.1 artifacts `policy.toml`, `interactions.jsonl`.

### 12.6 Self-update command name — `unblock update`
The single v1 self-update command is spelled **`unblock update`** everywhere (PRD FR-25/NFR-17, ci-cd, roadmap, README, and `unblock-cli` — `Command::Update`/`UpdateArgs`/`commands/update.rs`/help snapshots). The Cargo feature stays named **`self-update`**; the feature enables the `unblock update` command (G-2/G-18).

## 13. Phasing & Milestones

A vertical walking skeleton (D14, thin-slice scope). Each milestone is independently shippable/testable.

| Milestone | Crates | FRs | Gate |
|---|---|---|---|
| **M0 — Foundation** | unblock-model, unblock-error, unblock-storage (Storage trait + libsql impl) | — | `Storage` contract suite (NFR-16) green; **contention lab confirms no hot-spin (NFR-3) BEFORE other crates depend on storage** |
| **M1 — Engine + core domain** | unblock-engine (Semaphore, lifecycle), unblock-policy, unblock-config (subset) | FR-1a/1b/1c, FR-2, FR-3, FR-4 (incl. ready), FR-5, FR-9, FR-10, FR-13 (config-resolution subset; engine/env/file precedence) | Property test: engine mutations linearizable; CRUD/ready/dep via internal API |
| **M2 — MCP surface** | unblock-mcp, unblock-render (reduced), unblock-sync | FR-20, FR-11, FR-12, FR-7, FR-8, FR-26 (bd import), FR-15 | MCP client completes ready → claim → close; a representative bd repo imports |
| **M3 — Reliability + ops + GA** | unblock-cli (T3.1 — D27: mcp/migrate/doctor-lite/version/init/agents/update), unblock-health (lite) | FR-14 (init/agents bootstrap), FR-13 (CLI flag-forwarding half), FR-16 (lite doctor — diagnostics + integrity, D27), FR-17 (cooperative shutdown wired in the MCP server, D27/AD-4), FR-20 (mcp, 2-arg live signature), FR-25 (dist/axoupdater) | Shutdown/failure-injection (NFR-4/5/13) + perf budgets (NFR-1 hybrid gate / NFR-2 250k read-budgets) green + NFR-18 rate-limit enforced (`RateLimited`, contract v1.4) + `ready→claim→close` round-trip baseline recorded (D34); `dist` release pipeline + attestations |
| **v1.1+** | — | FR-6, FR-13(full), FR-16(full), FR-18, FR-19, FR-21, FR-22, FR-23, TOON | per-feature |

## 14. Success Metrics & Risk Register

### 14.1 Success metrics (ship-gates)
- **Functional parity (v1 slice):** every v1-tier FR meets its AC.
- **Performance:** NFR-1 budgets pass as a **hybrid gate** (hard generous-absolute per-PR on a pinned ≥2-vCPU runner + advisory nightly relative-delta — D34) and NFR-2 (250k read-budgets + clean `integrity_check` in CI) passes as a hard gate.
- **Agent experience:** end-to-end `ready → claim → close` round-trip latency **recorded as a v1 baseline** (p50/p99 over a real spawned `unblock mcp` child, the D31 `.write.lock` world — D34/F-4); a **hard target lands in v1.1** with real data. MCP tool count ≤ target (§9).
- **Dev tracking:** unblock's v1 development is tracked in `docs/plans/STATUS.md` (Markdown registry); once v1 works, unblock can take over tracking its own v1.1+ work (the dogfood milestone).

### 14.2 Risk register
| ID | Risk | L | I | Mitigation | Trigger/owner |
|---|---|---|---|---|---|
| RK-1 | libsql busy/lock semantics don't actually avoid hot-spin under load | M | H | **Contention lab in M0, before any crate depends on storage** (NFR-3); **fallback = `rusqlite` behind the same `Storage` trait** if it fails (a swap, not a rewrite) | M0 / storage owner |
| RK-2 | rmcp 1.7 API churn | M | M | Pin version; isolate in `unblock-mcp`; thin adapter over engine | M2 / mcp owner |
| RK-3 | MCP tool count hurts client selection accuracy | M | M | Consolidate tools + resources (§9); measure count (§14.1) | M2 |
| RK-4 | libsql remote feature leaks TLS/HTTP into default build | L | M | Remote behind non-default feature (D15); cargo-deny on tree | M0 |
| RK-5 | Health-contract scope creep | M | M | v1 ships lite; full taxonomy deferred to v1.1 (FR-16) | v1.1 |
| RK-6 | Self-update supply-chain (unsigned) | L | H | v1: verify GitHub artifact attestations before execution (FR-25/NFR-17); `axoupdater` in `unblock-cli`, no isolated crate (D17/CF-K) | M3 / cli owner |

## Appendix A — Traceability

Derived from a 3-analyst + coordinator discovery workflow over `temp/beads_rust-main` and a subsequent
3-lens + coordinator review (technical / product / PM). FR/NFR ids map to the original feature inventory;
deviations are annotated inline and consolidated in §4 (Key Decisions). Review verdict that produced v0.2:
"excellent foundation; needs a concurrency decision, a delivery wrapper, a tightened v1 thin-slice, and
contradiction fixes" — all addressed in this revision.
